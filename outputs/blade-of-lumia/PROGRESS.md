# Blade of Lumia – 進捗ログ

> **このファイルの役割：** 作業セッションごとの「やったこと・次やること」を記録する。  
> **セッション開始時：** まず最新エントリの「▶ 次やること」を読んで再開する。  
> **セッション終了時：** 新しいエントリを追記する。  
>
> 関連ファイル：
> - `PLAN.md` … 何を作るかの計画（チェックボックスで進捗管理）
> - `DECISIONS.md` … 設計判断・学び・ハマりどころの記録
> - `IDEA.md` … アイデア集・ストーリー設定

---

## 📍 現在地（常に最新に保つ）

- **進行中フェーズ：** **Phase 9（進行設計＆ダンジョン作り込み）— ゲームの背骨を作る ★★★最高**
- **✅ Phase 9-6 ⑥-5 草原G-C 北・火山/雪ぎわ 完了（2026-07-12・🧠 Opus・`migrate-field-grassland-c.mjs`）：** rows1-5 の北草原35画面中**33画面を作り込み**（北へ登る峠道＝D4火山`12,2`/D5雪`13,5`へ向かう）。**温存2画面＝`8,1`塔ワープ島（mapEnters・§7-5論点1）＋`5,3`廃城/廃村遺跡（Phase 6-3の石碑2+ハートの器＝既に2軸以上）／`7,3`隠しハートの器は作り替えるが chest温存。** 顔は P1放射導線+P3狩り場（P1×15/P2×9/P3×6/P4×3）。接続不変は「隣をミラー」**AND 固定点版**（OR 版は `7,1→8,1` など G-C 起因 trap 3件を生んだ→AND に是正・G-C 起因 holes=0 確認済み）。**🔑 予告編の道具所持タイミングを §8-1 厳密ルールで PLAN注記から是正＝G-Cは D3後・D4手前＝所持`{剣・盾・ブーメラン・弓}`。∴ 弓ゲート`11,3`（Y→T）は弓所持済み＝try-now（G-Bのcome-backから昇格）／石押し関門`6,4`（*→S→T・道具不要）＝try-now／かがり火`10,2`（H+torchesLit）は come-back近道（ロウソクはD4自身の報酬で手前で点けられない＝PLAN旧注記"D4手前に置ける"は§8-1と矛盾＝是正）／爆弾壁`4,2` come-back。** **under-2-axis 146→125（G-C 21素通り全解消）・seam 88→74・trap 88→74**（AND 固定点で G-C 起因 holes=0。残る角は未リワーク隣接 F/^/S/L＋1外部 8,0→8,1＝⑥-6以降で消える）・W1/W2/dup 不変。BASELINE を seam74/trap74/under125 に締め・全257テスト緑・14画面ブラウザboot 0エラー+石押し(gate5,4)/弓(gate4,7)の関数検証。**🔑 スクリプトに dead-gate 自己検証を常設**（links の T を閉じた状態でBFSし宝が到達可能ならthrow＝G-A/G-Bの「宝が隣から丸見え」轍を機械化）＝`11,3` の chest東側 open を実際に捕捉→壁化。詳細 `FIELD-9-6-DESIGN.md` §14。**次は ⑥-6 湖 W。**
- **✅ H-1/H-2 context 利用率フック 完了（2026-07-11・🧠 Opus・PLAN 冒頭）：** `~/.claude/hooks/context-usage.py`（`UserPromptSubmit`・`settings.json` 登録済み）が毎ターン `[context] 利用率 ~X% (≈Yk / Ztok)` を `additionalContext` で注入。**算出＝transcript JSONL 最後の assistant 行の `input_tokens+cache_creation_input_tokens+cache_read_input_tokens`（＝実 context 読込量・文字数概算より正確）／ウィンドウは適応判定（used>200k→1M窓）。** 実機で入力JSONを裏取り済み（`context_window`/`used_percentage` は存在せず＝捏造だった）。CLAUDE.md 冒頭に継続判断ルール追記済み。**残＝完了条件の実セッション発火確認（次プロンプトで system-reminder に `[context]` が出るか1回目視）。** 4-6 ✅完了（2026-07-12・下記）＝次は **9-6 ⑥-5**。
- **✅ Phase 4-6 ブーメラン投擲物改修 完了（2026-07-12・🧠→⚡ Opus 設計＋実装）：** ①アイテム運搬＝初代ゼルダ式（`collectFieldItem` を即加算→carried 記述子保持へ・`finalizeCarried` がキャッチ成立時に確定加算・入手タイミングはキャッチ時でユーザー確定）②付随アイコン `.boomerang-carry` 追従（🔑 `renderBoard` が char-layer 作り直しで proj-el を消すので `moveProjEl` を "無ければ再生成" に修正＝炎オーラの潜在バグも解消）③取り逃し（未キャッチでステージ遷移消滅）は `restoreCarried` で **その場に残す**（永久ロス回避・実装時決定）④復路の攻撃判定復活（`boomerangStep` 復路 else に `checkProjHit`・`_hitIds` で1投1敵1回・往路は1体目で即 returning 維持・復路は貫通）。**⑤ユーザー報告バグ修正＝キャッチ後の残骸（`moveProjEl` 自己修復が除去済みブーメランを復活）を `boomerangStep` 後の除去チェックで解決。⑥ユーザー要望＝敵ドロップもブーメランで運搬可＋敵ドロップの見た目をマップ配置アイテムに統一（絵文字→スプライト）。** 全257テスト緑・実ブラウザで運搬アイコン/キャッチ入手/復路ダメージ/残骸なし/ドロップ表示統一/0 pageerror 確認。詳細 PLAN 4-6・DECISIONS 2026-07-12。**次は 9-6 ⑥-5 に戻る。**
- **🚨 それ以降の最優先：Phase 9-6「フィールドを"ゲームとして成立"させる」⑥＝各画面を1枚ずつ丁寧に設計して作り込む（🧠 Opus 設計・⚡ Sonnet は確定データの流し込みのみ／§8-5手順）。⚠️「量産」ではない（2026-07-06 ユーザー指摘・9-2 ダンジョン PLAN:955 の鉄則をフィールドにも適用＝テンプレ/自己検証はコピペのスタンプでなく設計の出発点と安全網）。残る本丸＝素通り146・trap88 を潰す（各地域で接する外周もセットで削る＝§10）。1地域=1スクリプトずつ。作業順は PLAN 9-6 ⑥ の番号順（⑥-1森✅→⑥-2砂漠✅→⑥-3ハブ+南岸✅→外周一括撲滅✅→⑥-4草原G-B✅→⑥-5草原G-C北✅→次は⑥-6 湖W）。**⑥-4 草原G-B 中央〜東・湖ぎわ ✅完了（2026-07-11・🧠 Opus・`migrate-field-grassland-b.mjs`）＝50画面（row6-12中央〜東・入口/NPC温存なし）を作り込み・顔は P1放射導線+P3軽い狩り場（P1×23/P2×11/P3×13/P4×3）。接続不変は砂漠/ハブと同じ「隣をミラー」（OR版＝⑥-3と同じ・角88<AND90で実測OR採用）。予告編＝石押し2枚（`3,8`縦押し/`6,9`横押し＝S→Tブロックパズル関門・道具不要・実ブラウザで両方ゲート開通を関数検証／ボタン後ろに壁Mで石を止める＝滑り抜けバグ回避）+ブーメラン隙間越し回収`8,6`（`R`床アイテムを水堀の小島に置き投げて回収＝D3予告編・実コード collectFieldItem 精査で chest不可→R採用）。弓ゲート`7,9`（Y→T・弓D3報酬＝come-back近道・実ブラウザで矢が水越しにY点火→ゲート開通を検証）+爆弾壁`6,11`come-back。**under-2-axis 168→146（G-B 50画面全解消）・seam 97→88・trap 97→88**（G-B発の残trap25件は全て角セル＝§11-1角限界・隣接未リワーク地域が原因＝⑥-5以降で解消）・W1/W2/dup 不変。全249テスト緑（transition3含む）・19画面ブラウザboot 0エラー+石2/弓1の関数検証。****🌊 外周一括撲滅パス ✅完了（2026-07-10・🧠 Opus・`migrate-field-outer-ring.mjs`）＝ユーザー2大原則の確定を受けた方針転換（(1) 入った後に動けなくなるステージ禁止／(2) field は全画面 playable＝歩ける＋村から到達可能）。当初「地域＋外周セット」で1地域ずつ外周を取り込む予定だったが、ユーザー指摘（南の海`3,17`→`3,18`＝オール水に踏み込んで即詰みを放置）で「先に(2)＝オール水/オール山 W1 105枚を全部 playable 化→その後(1)を網羅チェック」へ順序変更。105枚（海79/山26）を歩ける外周地域に作り替え（隣をミラーの AND版＝角セルは全crossing通行可のときだけ開く＝角トラップ防止・内側リング row1/col1/row8/col10 で連結・世界の果ては最外縁1マスだけ壁＝engine がクランプして落とし穴にならない）。W1 105→0・W2 105→0（原則2達成・全320画面 reachable=319/320＝残1は 9-2T 塔ワープ着地）。**新指標 `traps` を追加**（`field-quality.mjs`＝到達可能画面の開いた辺→着地セルが hard-blocked＝原則1のソフトロック。旧 seam 指標は行き先W1を除外していたのでこの種を全部見逃していた）＝146→97（外周自身の追加ソフトロックは機械確認で 0＝OUTER→OUTER trap=0。残97は全て本土内 or 本土→外周方向＝未リワークの⑥-4〜⑥-9 が原因）。ユーザー報告 `3,17→3,18` 解消を実ブラウザで確認（下移動で遷移→着地後も4方向移動でクラッシュ無し・スクショ目視で歩ける渚）。全246テスト緑・6画面ブラウザboot 0エラー。BASELINE を w0/w2:0/traps97/under-2-axis168/dup7/seams97 に更新（under-2-axis と dup が上がったのは外周105枚が到達可能になったが未だ最小歩道＝⑥-10/⑥-11 の content pass で下げる）。**⑥-3 草原G-A 村ハブ＋南岸 ✅完了（2026-07-09・`FIELD-9-6-DESIGN.md` §11・`migrate-field-grassland-a.mjs`）＝31画面（ハブ26＝P1分岐路が顔・P1×11/P2×7/P3×5/P4×3＝"どっちへ行く?"の交差点＋南岸5＝歩ける渚）を作り込み・**under-2-axis 80→63（−17＝ハブ全解消）・W1 110→105・W2 110→105（南岸5枚を渚化）**・全48テスト緑・13画面ブラウザboot 0エラー+3画面スクショ目視。接続不変は砂漠と同じ「隣をミラー」。石押し予告編（`*`→`S`→`T`＝D1の石ボタン門の予告編・道具不要）を村`7,14`直下`7,15`に配置。茂み`u`は剣で切れる=ハブでも今取れる報酬・`!`爆弾壁1枚だけ再訪近道。村`7,14`/D1入口`6,13`温存。**🌊 接する南辺の海5枚を「地域＋外周セット」で歩ける渚に作り込んだ（§10・当初ミラー封鎖で⑥-11先送りしたのをユーザー指摘で是正）＝sea-interior builder＋assertCoast・⑥-11外周海の型の第1試作。** **⚠️ seam 88 不変**（ハブ8シームは温存画面の角セル固定＝角は2交差を同時に満たせず除去不能＝砂漠−1と違う構造的コスト＝⑥-3は素通り潰しでありシーム潰しではない・海岸追加0）。⑥ 砂漠D2地域 ✅完了（2026-07-07・`FIELD-9-6-DESIGN.md` §9・`migrate-field-desert.mjs`）＝14画面（P2遺跡が顔・P2×6/P1×4/P3×2/P4×2）を作り込み・under-2-axis 94→80/seam 89→88・BASELINE締め・全46テスト緑・5画面ブラウザboot 0エラー。🔑 接続不変ルールが森と真逆＝「隣をミラー」（砂漠は外周が全開の砂→森の細バックボーンだと草原に新シーム。∴外周セルは隣の対向が通行可なら床・不通ならMメサ断崖）。石押し予告編を D2入口隣 `2,14` に配置（砂漠は最速地域＝所持{剣/盾}のみ＝道具不要の予告編だけ可）。`!`爆弾壁は2枚だけ再訪近道（sealed niche+看板）。並行で設計④外周（世界の果て・論点2＝砂漠のメサ断崖が1つの答え・🧠 Opus）。設計⑤ 森1地域試作 ✅完了（2026-07-06・`FIELD-9-6-DESIGN.md` §8・`migrate-field-forest.mjs`）＝森27画面を理想形に作り替え・密度基準（1画面2〜3要素/2軸以上/地域の顔パターン厚め＝森P1約50%）を実測確定・4-4予告編の道具所持タイミング決着（森はかがり火が正しい予告編＝爆弾はD6自身の報酬で手前不可・爆弾/はしごは所持後の再訪近道に格下げ）・under-2-axis 110→94/seam 91→89・BASELINE締め・全ブラウザboot確認。⚠️ 3論点(§7-5 塔allowlist/ratchet/タイル推定)は⑤⑥とも離席で暫定継続＝要確認。設計④不変条件テスト化 ✅完了（2026-07-05・`FIELD-9-6-DESIGN.md` §7）＝`field-quality.mjs`＋`field-invariants.spec.js`(ratchet)＋`field-quality-lib.spec.js`。ベースライン seam91/W1・W2各110/under-2-axis110/dup3（全水84/全山13/全壁13＝W1と一致・全て目標0）・全244テスト緑。⚠️ 論点1(塔allowlist)・テスト戦略(ratchet)・2軸判定(タイル推定) の3点はユーザー未確認の暫定＝次セッション冒頭で確認(§7-5)。設計② 地域テンプレ設計 ✅完了（2026-07-05・`FIELD-9-6-DESIGN.md` §4）＝P1〜P4格上げパターン共通レシピ×9地域スキンのテンプレ表(4-3)＋b3予告編6ネタの攻略順割り付け(4-4)＋④テスト化への不変条件フック(4-5)を確定。次は設計④＝honest指標(シーム0/W1=0/W2=0)＋「2軸以上」「レイアウト重複0」「予告編実在」を CI red 化（着手時に論点1=塔まわりUNREACHABLE を 9-6 で見るか 9-2T に委ねるかをユーザー確認）→ 設計⑤で森 or 火山1地域を理想形で試作し密度基準(P配分・1画面要素数)を実測＋予告編の道具所持タイミングを9-1連鎖表と突き合わせ最終確定 → ⑥Sonnet量産。設計①.5 面白要素カタログ ✅（`FIELD-FUN-CATALOG.md`）＝b3「フィールドはダンジョンの予告編」原則。** 2026-07-04 ユーザー強い指摘＝「今のマップはクソ。楽しさ・奥深さ・複雑さが1mmもない」。**2026-07-05 に★1〜★4 をユーザー確定＝★4 全320画面作り替え／★3 全画面を均等に意味づけ／★2 面白さ4軸すべて／★1 面白さ設計を先に（DECISIONS.md 同日）。設計①（面白さの言語化＝画面ごと2軸以上/素通りをP1〜P4で格上げ）と設計③（🔴 passable.js は2値判定→溶岩床/氷滑り/毒沼は新規コード要＝9-6スコープ外、地域差はスキン+地形パズル+敵編成で出す）も確定済み。** 土台是正（接続 honest 指標＝シーム91/W1・W2各110・生1203は水増し）も済。**⚠️ 次セッション冒頭で `FIELD-9-6-DESIGN.md` §6 の論点1（塔まわり UNREACHABLE を 9-6 で見るか）・論点3（「9-6 は新規コード無しで作る」割り切りの可否）をユーザーに確認する。** 次の設計作業＝§5 の **P1〜P4 × 地域スキン のテンプレ表**（設計②）＋並行で設計④不変条件テスト化。その後 1地域試作で密度基準→Sonnet量産。確定方針：**🅱️ B方針＝全320画面を実プレイ可能に／無駄ステージ0・接続ミス0 を不変条件。着手時は `FIELD-9-6-DESIGN.md` 全文＋PLAN.md「9-6」全文を必ず読む。**
- **進行中タスク：** **9-4 湖地域の作り直し（島＋橋）✅完了（2026-07-04・🧠 Opus）＝湖(W)12画面を「中央草島＋四方の橋アーム＋固有の小島」に作り直し・フィールド初の橋 `v` 導入。`migrate-field-lake.mjs` 新設・`field-terrain.spec.js` に3本追加（全226通過）・connectivity 完全一致（reached209/dead1203 不変）。⚠️ PLAN 診断③「水100%死に画面」は実在せず（外周海の意図的ボーダー）＝実データ精査で本当の雑は「湖が2種スタンプ使い回し」と特定し是正。⚠️ field tiles は文字配列の配列形式（行文字列でなく）＝実ブラウザ起動で発覚し修正。⚠️ ただし 9-4 完了は「見た目の雑さ低減」＝土台にすぎず、ゲーム性は 9-6 で積む。** ／ 9-4 地面塗り分け ✅完了（2026-07-04）＝L→ash・S→snow・M→mud の bgTiles 再塗り（38画面/4478セル・`migrate-field-terrain.mjs`）。 ／ 0-5 背景タイルスプライト編集 ✅完了（2026-07-04・⚡ Sonnet）。 9-5b ✅完了（2026-07-04）。→ 次は 9-4a エディタ確認（残1項目）、または Phase 8-4（本格リバランス）。 詳細は下記。9-4a ✅完了（2026-07-02・テスト12本）。**9-2y ダンジョン全8本 Opus レビュー ✅完了（2026-07-01・🧠 Opus）**。9-2F1 M1〜M4 ✅／9-2pre ✅／9-2a ✅／9-2b（D2 砂漠）✅／9-2a2（D1 リメイク）✅／9-2c（沼地ボス部品）✅／D1〜D4 JSON ✅／9-2e（D4）✅／9-2f（D5）✅／9-2g（D6）✅／9-2h（D8）✅／9-2i（D7）✅／**9-2y（全8ダンジョン Opus レビュー）✅**／9-2z（ダンジョンマップ UI）✅／9-3あらすじ確定 ✅／9-3(a) ✅／9-3(b) ✅／D3-F配置見直し ✅／9-2F1 質パス ✅／9-2T 設計確定 ✅／9-2T 流し込み ✅。**→ 次は Phase 8-4（本格リバランス＝ビーム強すぎ・敵弱すぎ）または Phase 8（UX）。Phase 9 の背骨（8ダンジョン＋塔＋フィールド320）は完成。**
- **🏰 9-2T 暗黒の塔拡張 設計確定（2026-06-30・🧠 Opus）：** dark_tower を2フロアスタブ→**多層垂直登塔30室**（B1F入口→1F弓→2F爆弾+ロウソク→3Fはしご+笛→4F試練回廊→最上階玉座Z）。**新アイテム無し＝既得6道具の卒業試験＋過去ボスの手下ボスラッシュ（`W`/`V`）→ザーネル決戦→エンディング**。フロア間は階段 `>`MAP_ENTER テレポート（D4 ボス翼と同型）。**🔴 設計中に現状バグ発覚＝M1〜M4 再採番で終盤動線が消失：(a) field→塔ワープ発射元が消滅（`dark_tower/0,1` の `destId:"islandToTower"` を受ける field MAP_ENTER が無い＝羽衣を得ても入塔不能＝クリア不能）／(b) 祭壇 `^` が村中央 `7,14[8,6]` に誤配置（正は北の聖域 `7,1`）。∴ 塔拡張は「内部作り込み＋終盤動線の再接続（空島復元・祭壇移設）」の2点セット＝9-2T-pre を最優先。** **⚠️ ラスダン固有の最大制約＝入塔時に翼の羽衣（飛行）所持＝`passable.js` で `SKY/WATER/TREE/BUSH/FENCE` を越える∴ 道具ゲートに `~`水/`%`空は不可（飛行でバイパスされる）。はしごゲートは `x`PIT（穴）で作る・足止めは `#`/`!`/`D`/`T` で担保・弓/爆弾/ロウソク/笛は飛行無関係でそのまま可。** 欠片：`Z` は dropsTriforce 無し＝中ボス `W`/`V` は `countTriforces` 非対象＝欠片必要数=8 不変（`X`/`Q` は塔に置かない）。整合性チェッカーは塔を対象に含めない（ラスダン特例）。`tests/dark-tower.spec.js` の `.skip` 2本は空島復元後に有効化。新規ゲームコード不要。詳細 PLAN.md 9-2T・DECISIONS.md 2026-06-30「Phase 9-2T」。
- **🎬 9-3 あらすじ確定＋石碑Bルート（2026-06-28・🧠 Opus）：** **「ガイド導線の前に全体あらすじ（throughline）が要る」というユーザー指摘で順序を組み替え。** 既存資産（IDEA premise／9-1 連鎖表／9-2F1 地理／既存「ザーネルの記憶」石碑8＋終章）を1本に繋いだ**あらすじ＆マップ移動の流れ**を `IDEA.md`「🎬 あらすじ」に明文化（村 `7,14` 起点→道具で地方が順に開く→北の祭壇→塔）。**石碑番号は B＝攻略順に振り直し確定**（数字順 ≠ 攻略順なので末尾4つを是正）：其の五=D6森・其の六=D5雪・其の七=D8沼・其の八=D7空。**D8/D7 の本文も進行に合わせ修正**（D8「最後の欠片/全て揃えば塔へ」の先走りを「七つ目/残るは空に一つ」へ、D7 を「最後の欠片/八つ揃えば祭壇へ」へ）。`scripts/migrate-lore-playorder.mjs` で流し込み（8行差分のみ）・`tests/lore-tablets.spec.js` の `TABLETS` を攻略順に更新・全184/187緑。**実装コード変更なし（純データ＋テスト）。**
- **🦅 D7 設計の核（2026-06-27・9-2i）：** dungeon_7「空中の遺跡」＝**22部屋（5×5=25 から袋小路3室 `[4,0]/[0,4]/[4,4]` 欠き＝D6 と同じ）**・報酬＝**欠片⑧のみ（新アイテムなし）**（triforceId=**8**）・ボス`U`嵐の鷲王（2×2・HP36/atk6/def1・弱点 arrow×2＝D3弓・hitAndAway・雷撃弾 range7）。**⚠️ 進行順 D4→D6→D5→D8→D7→祭壇**＝D7 入場には D8 報酬の笛が要る（前ダンジョン＝(A) OK）。**入場ロック＝笛ワープ（`fluteEffect.type:'warp'`・`game.js:1172-1192` 実装済み・D8 までの reveal/torchesLit テレポートと別系統）**：フィールドの発射元に `fluteEffect:{type:'warp', layer:'dungeon_7', stage:'1,3'}` を置き笛で D7 入口室へ飛ぶ＝徒歩/飛行では入れない＝笛無しで入場不可。**⚠️ 既存「空島→dark_tower（飛行ワープ）」と別系統・衝突なし（調査確認）。SKY `%` は内部に使わない**＝徒歩通行不可＋翼の羽衣（祭壇後入手）が無いと詰む（`passable.js:134`）＝内部の水/穴は `v`BRIDGE で渡る。**古典ゼルダ型は適用外（新アイテム無し）＝既得6道具の総合卒業試験**＝臨界経路 `[1,3]ENTRY(笛ワープ着地)→[1,2]弓ゲート(Y→T・D3 水島方式)→[1,1]爆弾!壁＋弓ゲート(D6)→[1,0]中ボスW＝鍵K→(D扉)[0,0]BOSS U→欠片⑧`。**🔑 D6/D8 と同じく新規ゲームコード・接続チェッカー拡張すべて不要**（笛ワープ＋弓ゲート＋爆弾!壁＝全て既実装・ボス`U` は `enemies.js:301-321` 定義/スプライト/登録済み）＝流し込みは純 JSON＋テスト1点。**欠片是正は不要（D8 との違い）**＝ボス`U` は現状スタブ `1,0` に既配置＝countTriforces にカウント済み・`0,0` へ再配置するだけ（数不変）。**流し込み時の論点2つ＝①現状 `field/2,0 (7,9)` の徒歩入口 `field_dungeon7` を笛ワープ専用にするか（笛無しで入れると最終ロックが破れる・要ユーザー確認）②heart-containers ボス部屋 `1,0`→`0,0`。** 詳細 PLAN.md 9-2i・DECISIONS.md 2026-06-27「Phase 9-2i」。
- **🌲 D6 設計の核（2026-06-27・9-2g）：** dungeon_6「森の聖域」＝**22部屋（5×5=25 から隅の袋小路3室 `[4,0]/[0,4]/[4,4]` を欠く）**・報酬＝**爆弾＋欠片⑤**（triforceId=**5**／氷D5=⑥と入替）・ボス`O`古森の巨人（2×2・HP42/atk5/def2・弱点 fire×2＝D4ロウソク炎・hitAndAway）。**⚠️ 進行順 D4→D6→D5**＝D6 入場には D4 報酬のロウソクが要る（前ダンジョン＝(A) OK）。**入場ゲート＝D4 と同じ `torchesLit`→隠し `>`テレポート堅牢方式**（ユーザー確定）：`[1,1]` かがり火 `H`×3 をロウソク全点灯→隠し `>` 出現→ボス翼へワープ。**代替の茂み `u`(bushBurned) は却下**（剣で先に切ると `cutBushes` 済みでロウソクが "もう燃え尽きている" を返し `bushBurned` が永久に立たない＝剣で破れるソフトロックの芽・チェッカー検出不能）。**ボス翼前ロック＝D6 自身の報酬「爆弾」で `!`壁**（`[1,2]爆弾入手→[1,1]` 境界・チェッカーは `!` を SOLVABLE_GATE 認識済み）。臨界経路 `[1,3]ENTRY→[1,2]爆弾入手→(!壁)[1,1]かがり火関門(ロウソク点灯→テレポート)→[1,0]中ボスW＝鍵K→(D扉)[0,0]BOSS O→欠片⑤`。**🔑 D5 との最大の違い＝新規ゲームコード・接続チェッカー拡張すべて不要**（D4 の `torchesLit`テレポート＋D5 の `!`爆弾壁の組合せ・ボス`O`は `enemies.js:274` 定義済み）＝流し込みは純 JSON＋テスト2点修正のみ（⚡ Sonnet 向き）。詳細 PLAN.md 9-2g・DECISIONS.md 2026-06-27「Phase 9-2g」。
- **✅ D5 完了（2026-06-27・9-2f）：** dungeon_5「氷の廃墟」20部屋流し込み完了・`connectivity.mjs` に `--with-ladder` 拡張・ボス`L` に `meleeOnly`/`reflectsProjectiles` 実装・整合性チェッカー `UNLOCKED_AT` 修正・全187テストグリーン（melee-only-boss 7本・connectivity-tool 7本追加）。
- **❄️ D5 設計の核（2026-06-25・9-2f）：** dungeon_5「氷の廃墟」＝**20部屋（5×4 フル）**・報酬＝**はしご＋欠片⑥＋鉄の盾**・ボス`L`氷のリヴァイアサン（2×2・HP40・弱点 fire×3＝D4ロウソク炎）。**⚠️ 進行順は数字順でなく D4→D6→D5→D8→D7**＝D5 入場には D6 報酬の爆弾が要る（前ダンジョン＝(A) OK）。**はしご必須化＝"2つの保証"**：① `[1,1]` に横一文字水堀 `~`(row3)＝はしごで縦の1セル橋を渡る以外にボス翼へ道なし（`passable.js:87` isLadderBridge）／② **ボス`L` を `meleeOnly`＋`reflectsProjectiles`**（D5 で弓/ブーメラン/ビーム/爆弾を全所持＝対岸から撃てるなら必須化崩壊するので遠隔は反射、剣・炎の近接だけ通す）。臨界経路 `[1,3]ENTRY(爆弾で!壁)→[1,2]はしご入手→[1,1]氷の堀(はしごで縦断)→[1,0]中ボスW＝鍵K→(D扉)[0,0]BOSS L→欠片⑥`。詳細 PLAN.md 9-2f・DECISIONS.md 2026-06-25「Phase 9-2f」。
- **✅ D4 完了（2026-06-25・9-2e）：** dungeon_4「炎の神殿」20部屋流し込み完了・チェッカー PASS・candle-gate.spec.js 3本グリーン・全174テストグリーン。D1〜D4 完了。
- **🔥 D4 設計の核（参考）：** dungeon_4「炎の神殿」＝**20部屋（5×4 フル）**・報酬＝**ロウソク＋欠片④**・ボス`A`炎のサラマンドラ（HP35・弱点 arrow＝D3 の弓で◎・剣可）。**ロウソク必須化＝実コード精査で唯一成立する "ロウソク専用かつ通行を開く" 配線＝`[1,1]` かがり火の間で消灯 `H`×3 を全点灯（`torchesLit`）→ 隠し `>`MAP_ENTER 出現 → ボス翼へワープ**。剣は茂みを切れても `bushBurned` を立てず（combat.js:310）・ブーメラン炎運搬は点いた篝火がある時だけ（projectile.js:333）・矢/ビームは篝火無効＝事前点灯ゼロなら**ロウソク以外では開けられない**。ボス翼 `{[1,0]中ボス+鍵,[0,0]ボス}` は grid 隣接を全て `#` で塞いだ**テレポート専用の島**（D3 の「部屋内迂回」穴①を踏まないための地形保証）。臨界経路 `[1,3]ENTRY→[1,2]ロウソク→[1,1]かがり火→(テレポート)[1,0]中ボスW＝鍵K→(D扉)[0,0]ボスA→欠片④`。詳細 PLAN.md 9-2e・DECISIONS.md 2026-06-25「Phase 9-2e」。
- **📐 ダンジョン規模は「進むほど漸増」（2026-06-24 ユーザー確定）：** 16固定をやめ **D1=16/D2=16/D3=18/D4=20/D5=20/D6=22/D7=22/D8=24/暗黒の塔=28+**（15〜25 レンジ内で漸増・ラスダンのみ超え）。正方形に縛らず長方形＋袋小路の欠けで端数を作る。PLAN「📐 規模基準」に表で明記。D1/D2 は流し込み済みで据え置き。**暗黒の塔は現状2フロアスタブ＝9-2 終盤で 28室+へ本格拡張が別途必要。**
- **🐸 9-2c（部品のみ・2026-06-24）＋位置づけ修正：** 8体目の大型ボス **沼地の大蝦蟇 `SWAMP_TOAD('I')`**（2×2・hp40/atk5/def2・dropsTriforce・弱点 fire×2・hitAndAway）を「ボス部品」として作成（64×64・2フレーム・codegen＋実ブラウザ描画確認）。**⚠️ ユーザー指摘で位置づけを根本修正：** 当初 cave_1 をボスダンジョン化して配置したが、**cave_1 は小さな洞窟用レイヤーで欠片ボスを置く場所ではない**。**欠片ダンジョンは `dungeon_1〜8` に統一**し、沼地ボスは**新設する `dungeon_8`** に置く（連鎖位置・テーマ・フィールド入口はゼロから設計）。→ **cave_1 への配置データは revert（小洞窟のまま）**。沼地ボスは現在**どのマップにも未配置**（部品のみ）。`tests/swamp-boss.spec.js`（4本）は「部品定義が揃う＋ライブマップ未配置」を検証。dungeon_8 新設＋配置＋欠片8整合は 9-2{d〜}。詳細 DECISIONS.md 2026-06-24「Phase 9-2c」。
- **🛠 接続チェッカーを機械化（2026-06-23・9-2pre）：** `scripts/lib/connectivity.mjs` を新設し、既存 dungeon/field チェッカーを集約（BLOCKED の3重コピー廃止）。**デッドエッジ検出**（開いた辺から出たのに到着が壁/ステージ無し＝「隣に行けない/めり込む」）＋**dead 部屋検出 `findOrphanRooms()`**を追加。**入口の定義を厳密化（ユーザー2段階指摘で修正）：** `findEntrances(mapData, layerName)`＝**その MAP_ENTER の `id` が他レイヤーの `destId` から参照される部屋（外界が指す扉・複数可）**、field は `startPos`。dead = **全入口の和集合**からゲート開＋テレポート込みでも到達不能（**互いに繋がる島でも入口に繋がらなければ両方 dead**）。`tests/connectivity-tool.spec.js`（**15本**）でチェッカー自身の正しさを固定（封印部屋／相互島／複数入口／findEntrances／実 dungeon_1 regression）。BLOCKED を **HARD_BLOCKED／SOLVABLE_GATES（`D``T``!``:`）** に2分類。`overall verdict`＋exit code で合否。**⚠️ この検出で dungeon_1 の `3,0/4,0/5,0` が dead と判明＝FAIL する**（`3,0↔4,0` は相互に繋がるが入口 `0,0` に繋がらない・`5,0` は封印。9-2a2 リメイクで接続して解消）。詳細 DECISIONS.md 2026-06-23「Phase 9-2pre」「同【修正】」「同【再修正】」。
- **🎯 ボス＝欠片の不変条件（確定）：** 欠片は「ボスのいるダンジョンでボス撃破」でのみ入手。**大型ボス8＋最終ボス（ザーネル）1＝計9ボス・欠片8**。⚠️ **現状は大型ボス7体のみ**（cave_1 が雑魚ボス`W`＝欠片を落とさない）＝実質7欠片で8欠片前提とズレ。→ **cave_1 に新規の沼地大型ボス（dropsTriforce:true）を作り8体に揃える（9-2c）**。
- **🔗 ステージ接続を Opus で確定＋機械検証（2026-06-23・つなぎ対策）：** 「つなぎがいつもバカ」の根因＝`game.js:977-980` の辺スクロールは**隣接 grid に stage があれば移動するだけで、辺の開口が噛み合うかをチェックしない**。→ **接続規約を確定**（stageKey=`sx,sy`／縦の繋ぎは同col5,6・横の繋ぎは同row4,5 で開口一致）＋**D2 の16部屋接続グラフを node で論理検証**（臨界経路 `ENTRY[1,3]→[1,2]→ブーメラン[1,1]→鍵回収[0,1]→BOSS[0,0]` を一本道に・ボス裏口なし・[1,1]塞ぎでボス到達不能）＋**検証ツール `scripts/check-dungeon-connectivity.mjs` を新設**（タイルレベル BFS で辺越えを再現）。Sonnet は座標にタイルを置くだけ→スクリプトで噛み合い自動検査。詳細 DECISIONS.md 2026-06-23「ステージ接続を Opus で確定」。
- **🎮 ダンジョン内部を古典ゼルダ型に転換（2026-06-23・9-1鉄則(B)反転）：** 各ダンジョンは**①報酬アイテムを中盤で入手→②それ必須の部屋→③ボス（その弱点）→④欠片**。**旧「報酬はボス最奥・内部で使わない」は撤回。** 制約を分離＝**(A) 後ダンジョンの報酬は要求しない（ソフトロック厳禁・厳守）／(B) 自分の報酬は内部で使う（古典型・採用）**。連鎖順序は「入口を前ダンジョン報酬でロック」で保つので両立。必須化ギミックは配線確認済みのものに限定（弓→`Y`遠隔SW／ロウソク→`H`点灯/茂み焼き／爆弾→`!`壁／はしご→水穴渡り／笛→`flutePlayed`）。**⚠️ ブーメランは `Y`SWITCH をトグルしない**＝D2 は「隙間越しの `K`鍵回収（`collectFieldItem`）」で必須化。詳細 DECISIONS.md 2026-06-23「鉄則(B)を反転」。
- **🎯 D1 リメイク確定（2026-06-23・9-2a2）：** **dungeon_1 は据え置き不可＝再設計対象。** 実データ監査で矛盾発覚＝現状は木の盾が無く**ミラーシールド(tier2)・聖剣tier3×2・伝説のよろいtier2×2・銀の剣・さいごの鎧** が散在＝入門ダンジョンで最強級装備が揃う（9-1 連鎖と矛盾）＋12部屋（規模基準未満）。→ **矛盾装備を撤去（上位は 9-1 温存先＝鉄の盾D5／ミラーcave_1 へ）／報酬を木の盾tier0＋欠片①に集約／16〜20部屋へ拡張／剣のみでクリア可能（ボスG弱点は爆弾だが未所持＝剣で倒す HP30）**。欠片①はボスG撃破ドロップのまま正しい。詳細 PLAN.md 9-2a2・DECISIONS.md 2026-06-23「Phase 9-2a2」。
- **🎯 D2 設計確定（2026-06-23・9-2b）：** **dungeon_2「砂漠の神殿」＝16部屋（4×4 グリッド・辺スクロール接続）**。役割＝アイテム連鎖の起点（報酬＝ブーメラン＋欠片②・ボス最奥）。主題＝**木の盾の使い方（ガード）**を体得（`F`SENTRY 遠隔兵で誘導）。難易度カーブ＝雑魚`E`/`C`→仕掛け→遠隔`F`→中ボス`W`→ボス`N`。背骨の足止め＝「中ボス`W`撃破→鍵`K`→`D`扉でボスへ」。**剣＋木の盾だけで完全クリア可能**（ボス`N` HP32・剣で撃破可、弱点ブーメランは報酬なので D2 内では使わない）。詳細は PLAN.md 9-2b・DECISIONS.md 2026-06-23「Phase 9-2b」。
- **🔧 重要な確定（全ダンジョン共通・実コード精査）：** **規模＝各ダンジョン15〜25部屋（初代ゼルダ準拠）／ラスダン（暗黒の塔）はもっと広く**（ユーザー確定）。**足止め・ロックは "配線済み" 仕掛けだけで作る** ＝ ✅`K`KEY→`D`DOOR／`*`石→`S`→`T`ゲート／`Y`スイッチ(剣)→`T`／`[`/`]`色スイッチ→`(`/`)`色ゲート／`showConditions`(killAll等)で封印宝箱／`:`DOORWAY_BOSS。**❌`|`DOORWAY_LOCKED は死にコード**（`unlockLockedDoor()` 未呼び出し＝開かない）・`!`BREAKABLE_WALL は爆弾専用＝D2 では使えない。
- **✅ 9-2a2（D1リメイク）＋9-2b（D2 砂漠）JSON 流し込み 完了（2026-06-23）：** dungeon_1 を12部屋→16部屋（4×4グリッド）に再構築。旧ステージ（`3,0`/`4,0`/`5,0`）のパズルデータをすべて新レイアウトに移設・再配置。dungeon_2 を2部屋スタブ→16部屋に構築。connectivity checker で両ダンジョン PASS・全テスト 161/162 グリーン（`ladder.spec.js:145` は既知の既存 flaky）。
- **🐸 D8 設計の核（2026-06-27・9-2h）：** dungeon_8「沼地の神殿」＝**レイヤー新設・24部屋（5×5=25 から袋小路1室 `[4,0]` を欠く）**・報酬＝**笛＋欠片⑦＋ミラーシールド**（triforceId=**7**）・ボス`I`沼地の大蝦蟇（2×2・HP40/atk5/def2・弱点 fire×2＝D4ロウソク炎・hitAndAway・毒沫投げ・`enemies.js:355` 定義済み）。**⚠️ 進行順 D4→D6→D5→D8→D7**＝D8 入場には D5 報酬のはしごが要る（前ダンジョン＝(A) OK）。**入場ロック＝はしごで毒沼の堀 `~`(1セル幅)を渡る（D5 と同型・`--with-ladder` チェッカー拡張を再利用＝新規拡張不要）。** **笛必須化＝`flutePlayed`reveal で隠し `>`MAP_ENTER を出すテレポート方式**（D4/D6 の `torchesLit` をトリガー差し替えただけの同型・`conditions.js:96`/`game.js:1147`/`render-board.js:139` 配線済み）：`[1,2]`笛入手→`[1,1]`沼の祭壇で笛を吹く→隠し `>`出現→ボス翼 `[1,0]` へワープ。臨界経路 `[1,3]ENTRY(毒沼の堀をはしごで渡る)→[1,2]笛入手→[1,1]沼の祭壇(笛→隠し>テレポート)→[1,0]中ボスW＝鍵K→(D扉)[0,0]BOSS I→欠片⑦`。**🔑 D8 固有の最重要事項＝レイヤー新設＋欠片是正：** dungeon_8 はマップに存在しない・cave_1 は revert 済みでボス`I` はどこにも未配置・現状 `countTriforces()`=9（ボス7＋直置き`Q`2）。→ 流し込みで「ボス`I` を `dungeon_8/0,0` 配置（ボス8体）＋ `Q`タイル2枚（`field/2,0[6,10]`・`cave_1/1,0[8,10]`）撤去（直置き0）」を**同時是正**して8ちょうどにする。**🔑 新規ゲームコード不要だがテスト拡張は要る（D6 より重い）：** heart-containers の `BOSS_ROOMS` に `dungeon_8/0,0` 追加・lore-tablets の `TABLETS`（現状8石碑「其の一〜七＋終章」）に D8「其の八」を加えると9石碑＝物語断片名の整理（其の八/終章の順）が要る＝流し込み時にユーザー確認・`UNLOCKED_AT` に `dungeon_8:['…,'ladder']` 追加。詳細 PLAN.md 9-2h・DECISIONS.md 2026-06-27「Phase 9-2h」。
- **⚠️ 次やること：** **9-2F1（フィールド256拡張）設計確定 ✅（2026-06-28・🧠 Opus・下記）。次は M1（骨格 ~64画面＝村＋8ダンジョン入口ホーム＋草原回廊・接続緑）の流し込み＝⚡ Sonnet 着手可。** 残る 9-2y 未レビューは D1/D2/D5/D6/D7/D8（流し込み済みだが Opus レビュー未・並行可）。post-D8 タスク順（PLAN 0f43051）＝**①フィールド256拡張（着手中）→②9-3 ガイド導線→③暗黒の塔拡張**。9-3 着手時に D3 の `F` 配置（盾ガード導線が寄道に埋もれている）を併せて再検討する。
- **🗺 9-2F1 フィールド256拡張 設計確定（2026-06-28・🧠 Opus）：** ユーザー確定3点＝**(1) 16幅×20高＝320画面**（256下限を超える・外周海/山で実効256前後）／**(2) 村を中央〜南 `7,14` に置くフル再採番**（全 stage key・startPos を新フレームへ）／**(3) 8ダンジョン入口をテーマ別に各地へ分散**（砂漠D2/湖D3/火山D4/森D6/雪D5/沼D8/空島D7笛ワープ／村NWのD1）。**🔑 フル再採番の安全性を実コードで担保＝ダンジョン戻りワープは id 解決（座標非依存・`destId:"field_dungeonX"`／空島→`fieldToTower`／D7→`fluteEffect`直指定）∴ 画面を再キーしても入口が迷子にならない。** 直すのは field 側 stage key とテスト座標のみ（grep 済み＝固有座標テスト3本＋汎用ロードアンカー`'1,0'`約17本を`'7,14'`へ／`connectivity-tool`は`startPos.stage`動的参照で追従不要）。段取り＝**M1 骨格~64（村＋8入口＋草原回廊・接続緑）→M2~128→M3~256→M4 320（外周海/山）→質パス（敵/宝/ギミック/NPC/秘密・9-3連動）**。地理基準図・入口ホーム画面表・M1〜M4・テスト更新リストすべて PLAN「🗺 9-2F1」に明文化。新規ゲームコード不要。詳細 PLAN.md 9-2F1・DECISIONS.md 2026-06-28「Phase 9-2F1」。
- **🗺 フィールド規模目標を確定（2026-06-28）：** **最低256画面・第一候補 16×16＝256・できればさらに広く**（初代ゼルダ 16×8＝128 の2倍超）。**現状は 10 画面のみ**（1画面 12×10 固定・5×3 グリッドに10セル）＝目標の約4%＝Phase 9 最大の残タスク。→ **9-2F1 で 16×20＝320・村中央南・テーマ別分散に具体化（上記）。**
- **直近の状態（2026-06-23）：** **Phase 9-1 の連鎖設計が完成。** ゲート方式は**「フィールド常時フルオープン＋ダンジョンの入口/奥をアイテムでロック」**（ユーザー確定）。NPC・看板はガイドに徹する（`linesAfterBoss` で `defeatedBosses` 連動・ほぼゼロコード）。アイテムは**影響力の実測（爆弾=壊せる壁14・はしご=水56・笛=ワープが"広い鍵"／ブーメラン・弓矢・ロウソクは"狭い鍵"）**に基づき**狭い鍵→広い鍵→ワープ**の順に配分：D2ブーメラン→D3弓矢→D4ロウソク→D6爆弾→D5はしご→cave_1笛→D7（笛ワープ）。D4/D6/D5/D7 で「直前の道具＝ボス弱点」が噛み合う（弱点は任意ボーナス・剣でも倒せる）。**序盤の NPC 門番（通行ゲート）案は撤回**（フルオープンと矛盾）。詳細は PLAN.md 9-1・DECISIONS.md 2026-06-23「Phase 9-1（中盤/終盤）」。
- **⚠️ 保留（順序変更）：** **Phase 8（UX）・Phase 8-4（バランス調整＝ビーム強すぎ・敵が弱すぎ）は Phase 9 の後**に回す（背骨が無い状態で詰めても土台がブレる）。**敵が弱すぎ問題は 8-4 で「微調整」でなく本格リバランスが必要**（プレイヤーは剣ティア/ビーム貫通/盾跳ね返し/二周目2倍と強化され続けたが敵は据え置き）。**Phase 8-1（フクロウのヒント/墓）はスキップで合意**。dungeon_1 `3,0`/`5,0` パズルの mapEnters 未接続は Phase 9 のダンジョン作り込みで解消。ladder テスト145行は以前から不安定（変更と無関係）。








---

## セッションログ

<!-- 新しいエントリを上に追加していく（最新が一番上） -->

### 2026-07-12 — Phase 9-6 ⑥-5 草原G-C 北・火山/雪ぎわを作り込む（5地域目・🧠 Opus）

**やったこと（🟢 デフォルトフロー・⑥-5）：**
- **rows1-5 の北草原 35画面中 33画面を理想形に作り込んだ**（`scripts/migrate-field-grassland-c.mjs` 新設）。北へ登る峠道＝村/塔を背に火山D4（`12,2`・NE）と雪D5（`13,5`・E）へ向かう草原。顔は P1放射導線+P3軽い狩り場（P1×15/P2×9/P3×6/P4×3）。
- **温存2画面を厳密に判定：** (1) `8,1` 塔ワープ島（mapEnters `darkTower`/`islandToTower`＝§7-5論点1 allowlist）。(2) `5,3` 廃城/廃村遺跡（Phase 6-3＝石碑2+ハートの器＝既に landmark+secret+combat の2軸以上＝塗り絵でない・under-2-axisリスト外）。**⚠️ 初回スクリプトが誤って `5,3` を上書きし `ruined-areas`/`heart-containers` 5テストが赤化→git HEADから復元し PRESERVED に追加して是正。** `7,3` 隠しハートの器は塗り絵（空画面）なので作り替えるが chest `4,8` を P2秘密の中に温存。
- **🔑 予告編の道具所持タイミングを §8-1 厳密ルールで PLAN注記から是正（この地域の最重要判断）：** G-C は攻略順で **D3後・D4手前**＝所持`{剣・盾・ブーメラン・弓}`。∴ (a) **弓ゲート `11,3`（Y→T・弓所持済み）＝try-now**（G-Bでは come-back だったのが昇格＝弓入手後の自然な段差）／(b) **石押し関門 `6,4`（*→S→T・道具不要）＝try-now**／(c) **かがり火 `10,2`（H+torchesLit）＝come-back近道**（ロウソクはD4**自身**の報酬で手前で点けられない＝PLAN旧注記「D4手前に置ける」は§8-1と矛盾＝是正）／(d) 爆弾壁 `4,2` come-back。
- **接続不変は「隣をミラー」AND 固定点版：** ring cell open IFF ALL on-map crossings face open cells（rebuilt 隣は rings.Map から参照し収束まで反復）。**OR 版は `7,1→8,1`/`9,1→8,1`/`8,2→8,1` の穴を生成し G-C 起因 trap 3件→AND 版で G-C 起因 holes=0 達成**。ユーザー方針「各地域で自地域起因の穴を根絶し、全地域後の ⑥-trap チェックが 0 になるはず」を実装。辺セルは seam/trap ゼロ完全達成。残る角は §11-1 角限界＝未リワーク隣接（森F/山^/雪S/火山L）＋1外部（8,0→8,1 village K）。
- **🔑 dead-gate 自己検証をスクリプトに常設：** G-A/G-B で目視捕捉していた「ゲートの宝が隣の開いた床から丸見え＝ゲート無効」を機械化（`walkReachGateClosed`＝links の `T` を閉じた状態でBFSし宝が到達可能ならthrow）。**実際に `11,3` 弓ゲートで発火**＝chest `4,8` の東 `4,9` が開いていて弓なしで取れた→壁化。**「到達可能＝正しい」ではなく「ゲート付き宝は閉状態で到達不能が正しい」を不変条件に。**
- **不変条件：under-2-axis 146→125（G-C 21素通り全解消）・seam 88→74・trap 88→74（AND 固定点で G-C 起因 holes=0）・W1/W2/dup 不変。** `field-invariants.spec.js` BASELINE を seam74/trap74/under125 に締めた。
- **全257テスト緑**（旧257・数変わらず＝新規テスト無し・純データ＋BASELINE締め）・**14画面ブラウザboot 0 pageerror**＋**石押し関門(gate 5,4開通)・弓ゲート(gate 4,7開通)の関数検証**（[[field-tiles-are-char-arrays]] 回避）。

**学び・気づき：**
- **🔑 「作り替え集合」は "塗り絵かつ非温存" に厳密に絞る。** under-2-axis リストに無い画面は既に意味がある＝温存対象。`5,3` は 6割の素通りとは別の「既に作り込み済み」画面で、機械的な地域一括作り替えが誤って潰しかけた。**着手時に「この地域の既存 signData/chestContents/mapEnters を持つ画面」を先に洗い出して温存集合に入れるべき**（G-B は温存なしだったので油断した）。
- **🔑 §8-1 の道具所持フィルタは同じギミックでも地域で結論を変える。** かがり火は森（D4後）では正しい予告編だが、G-C（D4前）では come-back になる。**「地理的に手前」≠「道具を持っている」**＝各地域で攻略順から所持道具を引いて予告編/再訪を仕分ける（弓ゲートは逆に G-B come-back→G-C try-now に昇格）。
- **🔑 目視で3回捕まえたバグ（dead-gate）はスクリプトの不変条件にする。** G-A・G-B・G-C と同じ轍を踏みかけた「ゲートが宝を塞いでいない」を、`walkReachGateClosed` で機械化した＝以降の地域は自動で弾ける。

**▶ 次やること：9-6 ⑥-6 湖 W（14画面・🧠 Opus）。** 9-4で島橋化済み＝はしご水渡り予告編が既存・P1島渡りが顔・接する外周＝東の海`~`をセットで。着手前に `FIELD-9-6-DESIGN.md` §14/§13/§11/§9/§8/§4 と PLAN ⑥リストを読む。§7-5 の3論点（塔allowlist/ratchet/タイル推定）は暫定デフォルト継続。**コミット未実行＝下記メッセージ案の提示まで（WORKFLOW規定）。**

---

### 2026-07-12 — Phase 4-6 ブーメラン投擲物改修（アイテム運搬＝初代ゼルダ式＋復路の攻撃判定復活・🧠→⚡）

**やったこと（🟢 デフォルトフロー・PLAN 4-6・ユーザー割り込みタスク）：**
- **① アイテム運搬（初代ゼルダ式）：** `collectFieldItem`（`player.js`）を「通過即加算」から「carried 記述子を返す（`player` 加算保留）」へ作り替え。拾った瞬間にタイルは `pickedKeys` で隠す（運搬アイコンと二重表示させない）が player には加算せず、ブーメランの `proj.carried[]` に積む。キャッチ成立で `finalizeCarried()` が `player.keys/rupees` に確定加算。入手タイミング＝キャッチ時（ユーザー確定）。
- **② 付随描画：** `moveProjEl` のブーメラン分岐で `.boomerang-carry` アイコン（`makeSprite(spr,pal)`）を追従表示。**🔑 実ブラウザ検証で「アイコンが出ない」＝ブーメランの proj-el 自体が飛行中に消えると発覚。** 原因＝`collectFieldItem`/炎点火が呼ぶ `renderBoard()` が `boardEl.innerHTML=''` で char-layer ごと作り直す＝飛行中の proj-el を破壊。→ `moveProjEl` を「要素が無ければ `createProjEl` で再生成」に修正。**既存の炎オーラ（`.boomerang-flaming`）も同じ理由で出ていなかった潜在バグを同時に解消**（snapshot の `flaming` フラグを見るテストだったので今まで緑だった）。
- **③ 取り逃し：** PLAN で "破棄 or その場に残す＝実装時決定" だった論点を **「その場に残す」** に確定。`clearProjectiles`（ステージ遷移で未キャッチ消滅）で carried を `restoreCarried()` が `pickedKeys.delete` してタイルを復活＝永久ロス回避。
- **④ 復路の攻撃判定復活：** `boomerangStep` 復路 else に `checkProjHit(proj)` を追加。`checkProjHit` のブーメラン分岐に `_hitIds` を導入＝同一敵は1回のみヒット・往路は1体目で即 `returning`（従来挙動維持）・復路は貫通（`continue`）で軌道上の複数体を削れる。
- **テスト6本追加**（`tests/boomerang-item.spec.js`）＝①往路で即入手しない（carriedKeys=1・keys=0）／②付随アイコン `.boomerang-carry` 表示／②戻ってキャッチで入手／③復路で敵にダメージ／④取り逃し＝入手されずタイルは残り再回収可／キャッチ後に `.proj-el` が残らない（残骸バグ回帰）。`getProjectiles` snapshot（`main.js`）に `carriedCount/carriedKeys/carriedRupees` を露出。**全255テスト緑**（旧249+6）・**実ブラウザで運搬アイコン追従／キャッチ入手／復路ダメージ／0 pageerror 確認**。
- **⚠️ ユーザー報告バグを即修正（同セッション・分岐B）：** ブーメランがキャッチ後も画面に残骸として残る（スクショ報告）。原因＝`moveProjEl` の "無ければ再生成" が、`projectileTick` 末尾の無条件 `moveProjEl` によって除去済みブーメランを復活させていた。`boomerangStep` 後に `if(!_projectiles.includes(proj)) continue;` を追加＝生存中だけ描画更新。回帰テスト1本追加。**教訓＝"要素の自己修復"は除去チェックとセットで初めて安全。**
- **✨ ユーザー要望2件を追加実装（同セッション・分岐C）：** (1) **敵ドロップ（heart/rupee/bomb/arrow）もブーメランで運搬・キャッチ入手可能に**（`collectFloorDrop` dep 追加・carried を `apply/restore` closure に一般化してタイルアイテムと同経路化・`applyFloorDropEffect` を踏み拾い/キャッチで共有）。(2) **敵ドロップの見た目をマップ配置アイテムに統一**（ルピーが `◆` 絵文字→`makeSprite('rupee','rupee')` の `.item-sprite` スプライトに・`FLOOR_DROP_SPRITES` 追加）。テスト2本追加＝全257緑・実ブラウザでドロップルピーが配置ルピーと同じ緑ジェムに揃うことをスクショ確認。詳細 DECISIONS 2026-07-12。

**学び・気づき：**
- **🔑 飛行中の投擲物 DOM は `renderBoard()` で消える。** ブーメランのように飛行中に `renderBoard`（char-layer 再構築）を誘発するギミック（アイテム回収・炎点火）を持つ投擲物は、proj-el が毎回破棄される。`moveProjEl` を自己修復（欠落時に再生成）にするのが最小影響の解。**「snapshot テストが緑」≠「DOM に描画されている」**＝実ブラウザ目視でしか気づけなかった（[[field-tiles-are-char-arrays]]/空看板と同種の教訓）。
- **🔑 ブーメランの多段ヒット制御は `_hitIds`（貫通ビームと同じ機構）で足りる。** 復路判定を足すだけだと毎tick同じ敵に当たる＝`_hitIds` で1投1敵1回に絞りつつ往路の即折り返しは温存。

**▶ 次やること：** **9-6 ⑥-5 草原G-C 北・火山/雪ぎわ（🧠 Opus）。** D4火山`12,2`/D5雪`13,5`手前＝かがり火予告編・塔/村北辺温存。着手前に `FIELD-9-6-DESIGN.md` §13/§11/§9/§8/§4 と PLAN ⑥リストを読む。§7-5 の3論点（塔allowlist/ratchet/タイル推定）は暫定デフォルト継続＝ユーザー確認できると安全。**コミット未実行＝下記メッセージ案の提示まで（WORKFLOW規定）。**

---

### 2026-07-11 — H-1/H-2 context 利用率注入フック（ゲーム外・ハーネス改善・🧠 Opus）

**やったこと（🟢 デフォルトフロー・PLAN 冒頭 H-1/H-2）：**
- **背景＝AIが自分の context 窓の残量を見ておらず、長い/重いセッション終盤でも新規実装の続行を提案してしまう問題の再発防止。** フックで利用率を毎ターン注入し、逼迫時に区切りを進言できるようにする。
- **① 実機で `UserPromptSubmit` の入力 JSON を裏取り**（一時フック `_tmp-dump-prompt.sh` で `/tmp` にダンプ）。実在フィールド＝`transcript_path`/`session_id`/`cwd`/`prompt_id`/`permission_mode`/`hook_event_name`/`prompt`。**サブエージェント調査が挙げた `context_window.used_percentage` は存在しなかった（捏造）＝実機裏取りが正解だった。**
- **② 算出方法を文字数概算より上位の手段に格上げ。** transcript JSONL の各 assistant 行に実 `message.usage` があり、`input_tokens + cache_creation_input_tokens + cache_read_input_tokens` が**その時点でモデルが実際に読んだ context トークン数**。最後の assistant 行のこの和を採用（概算不要・API 呼び出し不要）。
- **③ 本番フック `~/.claude/hooks/context-usage.py` 実装＋ `settings.json` の `UserPromptSubmit` に登録・一時フックは削除。** `additionalContext` に `[context] 利用率 ~X% (≈Yk / Ztok). 逼迫時（~80%+）は…区切りを進言…` を返す。
- **④ `~/.claude/CLAUDE.md` 冒頭に「セッション継続の判断」節を追記**（フックが効かない環境向けに長さ・読込量からの推論も明記・前のめりな愛想を書かない旨も）。
- オフライン検証＝自セッション ~40%（79k/200k）・別セッション ~45%（451k/**1M窓**）・エッジ（transcript無/不正JSON/ファイル欠落）は無出力 exit 0 でプロンプトを絶対ブロックしない。

**学び・気づき：**
- **🔑 context 利用率は文字数概算でなく transcript の実 `usage` から出せる（より正確・ゼロコスト）。** `input+cache_creation+cache_read` の和＝モデルがそのターン読んだ実トークン。最後の assistant 行が現在の占有。
- **🔑 ウィンドウサイズは transcript に書いていない＝占有量から適応判定する。** 最初 200k 固定にしたら 1M-context ベータの別セッションで 219% と出た（実機で発覚）。used>200k なら 1M 窓と推定して解決。**「script が動く」≠「値が正しい」＝実データ複数系統で検算が必須**（⑥-4 の教訓と同じ）。
- **🔑 `/tmp` 単一ダンプは並行セッションのプロンプトに上書きされる**＝検証は transcript_path を直接食わせる方が確実（ダンプが別プロジェクトの transcript を指していて一時混乱した）。

**▶ 次やること：** **H-1 完了条件の実セッション発火確認**（このあと最初のプロンプトで system-reminder に `[context] 利用率 ~X%` が現れるか1回目視＝出れば H-1 クローズ）。その後は **Phase 4-6 ブーメラン投擲物改修**（🧠→⚡・9-6 ⑥-5 より先＝2026-07-11 ユーザー指示）＝①アイテム運搬（初代ゼルダ式・拾った物をブーメランで持ち帰りキャッチで入手）②復路の攻撃判定復活（`boomerangStep` 復路 else が `checkProjHit` 未呼び）。詳細 PLAN 4-6。**コミット未実行＝下記メッセージ案の提示まで（WORKFLOW規定）。**

---

### 2026-07-11 — Phase 9-6 ⑥-4 草原G-B 中央〜東・湖ぎわを作り込む（4地域目・最大ブロック・🧠 Opus）

**やったこと（🟢 デフォルトフロー・⑥-4）：**
- **草原G中央〜東 50画面（row6-12・cols0-9）を理想形に作り込んだ**（`scripts/migrate-field-grassland-b.mjs` 新設）。⑥ の中で**最大ブロック**（森27/砂漠14/ハブ26 に対し50）。入口/NPC が1つも無い＝全50が作り替え対象（温存なし）。
- **地域の顔＝P1 放射導線＋P3 軽い狩り場**（§4-2「G草原＝E巡回中心の軽い狩り場・放射導線の交差点」）。**P1×23/P2×11/P3×13/P4×3**＝森=P1迷路・砂漠=P2遺跡・ハブ=P1交差点と違い「広く開けた草原を横断しながら軽く戦う」テンポ。湖D3入口`9,9`手前の湖ぎわ列は水堀+`v`橋の crossing を厚めに。
- **接続不変は砂漠/ハブと同じ「隣をミラー」（OR版）を流用**（`mirrorRing`）。G-B は全開草原＝森の細バックボーンだと開いた隣（森F/湖W/雪S/山地M/砂漠D/ハブG-A/未リワークG-C）にシームを開ける。→ セル単位ミラーで**辺セルは seam/trap ゼロ**（「自分が通行可⟺隣の対向が通行可」）。**残るは角セルのみ**（§11-1角限界）＝AND版も試したが実測 OR 88 < AND 90 traps（G-B の非リワーク隣接は開いた角の方が多い）ので ⑥-3 と同じ OR を採用。
- **予告編（到達時所持道具＝{剣・木の盾・ブーメラン}＝D2後D3前でフィルタ§8-1）：**
  - **石押しブロックパズル関門×2（道具不要・try-now）：** `3,8` 縦押し（`*`をcol7下へ→`S`→`T`gate `5,5` 開通）・`6,9` 横押し（`*`をrow6右へ→`S`→`T`gate `4,6`）。**両方とも実ブラウザで実際に石を押して gate 開通を関数検証**（getStageState の openGates 確認）。**🔑 ボタンの1つ先に壁 `M` を置いて石を止める**のが肝＝置かないと石が S を通り過ぎて gate が閉じ直す（最初 `6,9` で px=10 まで滑り抜けて gate 開かず＝発覚→修正）。
  - **ブーメラン隙間越し回収 `8,6`（try-now・D3の水越し回収の予告編・a6）：** 大ルピー `R` を水堀`~`に囲まれた小島に置き橋を架けない＝**投げて回収するしかない**。**🔑 実コード精査で `collectFieldItem`（player.js:874）は `r`/`R`/`K` の"床アイテム"タイルのみ回収＝宝箱`B`は徒歩で開ける物で回収不可**と判明→当初の `B` 島設計を `R` 床アイテムに修正。
- **弓ゲート `7,9`（come-back近道・弓はD3自身の報酬§8-1）：** `Y`スイッチを水`~`で囲んで剣が届かない位置に置き、**row4を弓矢が水越しに飛んで `Y` を点火→`T`gate `4,8`→ルピー**。**実ブラウザで矢が水越しにYを点火しゲート開通を関数検証**（openGates に `4,8`）。徒歩では開けられない＝弓入手後の再訪ご褒美。爆弾壁 `6,11` も同型の come-back（爆弾D6報酬）。
- **不変条件：under-2-axis 168→146（G-B 50画面すべて2軸以上を達成＝全解消）・seam 97→88・trap 97→88・W1/W2/dup 不変。** G-B発の残 trap **25件は機械確認で全て角セル**（0 edge・§11-1）＝隣接する未リワーク地域（森/湖/雪/山地/ハブ/未リワークG-C）の角が壁のため＝⑥-5以降で両側開けば消える。`field-invariants.spec.js` BASELINE を seam88/trap88/under146 に締めた。
- **自己検証が設計ミスを量産前に3件捕捉：** (a) dup ガード発火＝`5,11`==`5,8`・`6,7`==`6,15`（ハブの大樹）→別レイアウトに作り直し。(b) 石滑り抜け（ボタン後ろ壁なし）＝実ブラウザ検証で発覚→壁M追加。(c) ブーメラン島の chest不可＝実コード精査で `R` 床アイテムに修正。**「script が緑」≠「ゲームとして正しい」＝実ブラウザ関数検証と実コード精査が必須。**
- **全249テスト緑**（旧246+transition3）・**19画面ブラウザboot 0 pageerror**（初期9+修正後10）＋**石押し2/弓ゲート1の関数検証**＝[[field-tiles-are-char-arrays]] 回避（char配列形式を確認）。

**学び・気づき：**
- **🔑 石押しブロックパズル関門は「ボタンの押し込み方向の1つ先に壁」が必須不変条件。** `checkStoneOnSwitch`（conditions.js:33）は石がボタン上にある間だけ gate を開く維持型＝石を通り過ぎさせると閉じる。フィールドで石押し関門を置くたびに「石がボタンで止まる地形」を保証する（壁 or マップ端）。次地域の石押しでも同じ轍を踏まないよう申し送り。
- **🔑 ブーメラン回収は床アイテム（`r`/`R`/`K`）専用＝宝箱`B`は不可。** a6「隙間越し回収」を作るときは `R` 大ルピー等の床アイテムを水/穴の孤島に置く（`collectFieldItem` がブーメランの飛跡上で拾う）。宝箱は徒歩で開く前提のタイル＝水島に置くと永久に取れない死にデータになる。
- **🔑 「隣をミラー」の角限界は隣接地域リワークで解ける＝⑥は"辺を完璧に、角は隣が来たら"の分業。** G-B の辺 seam/trap は全て0にできたが角25件は隣が未リワークだから残る。AND で自分の角を壁にしても隣の開いた角にtrapが移る（実測AND 90>OR 88）。∴各地域は自分の辺を完璧にし、角は両側がリワークされた時点で消える＝⑥-5以降で G-B との境界角も一緒に解消される。最後の ⑥-trap 横断チェックで残件を確認。

**▶ 次やること：🚨 H-1 モデルに context 利用率を注入するフック（2026-07-11 ユーザー指示・ゲーム外＝ハーネス改善・PLAN 冒頭 H-1/H-2）。** `UserPromptSubmit` フックで利用率をモデルの context に注入（実機でフック入力JSON検証→概算算出→`additionalContext` 注入→CLAUDE.md に継続判断ルール追記・`update-config` スキル使用）。再発防止＝AIが context 逼迫に気づかず新規実装の続行を提案した問題。**次の `/workflow` はここから。** それが終わったら → Phase 4-6 ブーメラン投擲物改修（9-6 ⑥-5 より先）。 ①アイテム運搬＝初代ゼルダ式（拾った物をブーメランにくっつけ、戻ってキャッチで入手）／②復路の攻撃判定復活（現状 `boomerangStep` 復路 else は `checkProjHit` 未呼び＝素通り）。🧠→⚡（機構設計Opus・実装Sonnet）。詳細 PLAN 4-6・DECISIONS 2026-07-11・FIELD-FUN-CATALOG a6改。**それが終わってから** 9-6 ⑥-5 草原G-C 北・火山/雪ぎわ（🧠 Opus・D4火山`12,2`/D5雪`13,5`手前＝かがり火予告編・塔/村北辺温存・着手前に `FIELD-9-6-DESIGN.md` §13/§11/§9/§8/§4 と PLAN ⑥リストを読む）。**§7-5 の3論点（塔allowlist/ratchet/タイル推定）は暫定デフォルト継続＝ユーザー確認できると安全。** **コミット未実行＝下記メッセージ案の提示まで（WORKFLOW規定）。**

---

### 2026-07-10 — 壁すり抜け（arrival-wall めり込み）バグ修正（🔀 分岐B・バグ対応）

**やったこと（🔀 分岐B・ユーザー報告バグ）：**
- **ユーザー報告2件＝「隣ステージの壁セルにめり込んで、そこからすり抜けられる」を修正。** (1) `13,2` 右上(0,11)から右移動→`14,2` の山 `M`(0,0) に入り込む、(2) `13,2` 上端から上移動→`13,1` の壁 `#`(row9) に入り込む。両方とも engine `checkStageTransition`（game.js:1053-1067）が「隣ステージが在れば無条件で移動＋着地セルの壁チェック無し」だったのが原因。壁にめり込むと `movePlayer` の壁判定は「移動先セル」しか見ないため隣の床へ動けて**すり抜けて見える**。
- **修正＝着地 footprint が徒歩不可の壁なら遷移をキャンセルし、出ようとした方向にだけ押し戻す**（反対軸はいじらない＝斜めズレを作らない）。判定集合は connectivity.mjs の `HARD_BLOCKED` と同一＝`traps` 指標とエンジン挙動が一致。開閉しうる GATE/DOOR/`!` は除外・はしごの1セル水/穴橋は通す・飛行中の水/空着地（塔の空島入口）は許可。
- **🔑 footprint 全走査が肝：** 最初「着地の中心セル1つ」だけ判定したら `14,2` の (0,0)=`M` を (0,1)=`.` で誤判定して再発。プレイヤーは半セル幅で最大2×2にまたがるので、passable.js `isPassable` と同じく floor(n)〜floor(n+0.999) 全走査に直して解決。
- **既存の端クランプ（両軸同時 clamp・斜めズレの元）は触らない**（ユーザー裁定＝そのクランプが起きるマップ自体を作らないのが本筋なのでロジック修正不要）。
- **回帰テスト3本追加**（`transition.spec.js`）＝山すり抜け防止／壁すり抜け防止／開いた床辺は正常遷移（対照）。**全246+3テスト green・実ブラウザ目視**（`13,2` 右端で止まり `14,2` に入らないスクショ確認）。

**学び・気づき：**
- **🔑 遷移の着地判定は「移動と同じ footprint ルール」で揃える。** 遷移だけ中心セル判定にすると半セル幅の角が抜ける。エンジン内で通行判定が2系統（movePlayer は footprint／遷移は中心）に割れていたのがバグの温床。
- **🔑 これはマップ側の `traps`(arrival-wall) 97件と同じ現象の"engine 側の顔"。** エンジン修正＝将来の新マップでも再発させない安全網、マップ修正＝そもそも壁に辺を開けない本筋。**両方必要**（ユーザー確認）。残97件のマップ是正は各地域リワークで進める＋全地域後に PLAN ⑥-trap で横断根絶。

**▶ 次やること：** すり抜けは engine で塞いだので、本筋の **arrival-wall trap 97件のマップ是正**を各地域リワーク（⑥-4〜⑥-9）で 0 へ。次タスクは Phase 9-6 ⑥-4（草原G-B 中央〜東・湖ぎわ・🧠 Opus）。

---

### 2026-07-10 — Phase 9-6 外周一括撲滅パス（W1/W2 105→0・原則2達成・🧠 Opus）

**やったこと（🔀 ユーザー主導の方針転換フロー）：**
- **発端＝ユーザーが2大原則を確定し作業順を変更。** (1) **入った後に動けなくなるステージを作ってはならない**（ソフトロック禁止）／(2) **field はすべてのステージが playable でなくてはならない**（全画面 歩ける＋村から到達可能）。ユーザー指摘＝村南の海 `3,17`→`3,18`（オール水）へ踏み込める落とし穴を⑥-3で放置していた。→ 当初「地域ごとに外周も一緒に」の意図が汲めなかったため、**「先に(2)＝オール水/オール山 W1 105枚を全部 playable 化→隣接地域の特徴を持たせて作り直す→その後(1)を網羅チェック」**へ順序変更（ユーザー確定）。
- **`scripts/migrate-field-outer-ring.mjs` 新設＝W1 105枚（海79/山26）を歩ける外周地域に一括作り替え。** 対象は ZONE_MAP の `~`/`^` 画面から ⑥-3で作った海岸5枚を除外＝idempotent。**接続ルール＝「隣をミラー」の AND版**（角セルは全crossing通行可のときだけ開く＝角トラップ防止・`targetOpensCell` で TARGET同士は1レベル再帰・mainland raw tile で終端）。**内側リング（row1/col1/row8/col10）で全開辺セルを1本の連結成分に**（外周ring に触れないので map edge の壁と無関係に常に歩ける）。**世界の果て＝最外縁1マスだけ壁**（その先にステージが無い＝engine の checkStageTransition がクランプ＝落とし穴にならない）。地域色＝海は`v`桟橋＋茂み隠し／山は茂みの物置。
- **結果：W1 105→0・W2 105→0**（原則2達成・reached 319/320＝残1は 9-2T 塔ワープ着地 `8,1`＝スコープ外）。全320画面が「歩ける＋村から到達可能」に。
- **新指標 `traps` を `field-quality.mjs` の `fieldHonestMetrics` に追加＝原則1の機械検査。** trap＝到達可能画面の開いた辺セル→着地先が hard-blocked（engine は隣ステージが存在すれば無条件で移動する＝game.js:1057付近 checkStageTransition・enterStage は着地セルを walkable にクランプしない＝壁に着地して詰む）。**旧 honest-seam 指標は「行き先がW1なら意図的境界」として除外していたため、この種の落とし穴を全部緑で見逃していた。** → traps は W1/orphan を除外しない（そこに落ちるのがバグ）。目標0。`tests/field-invariants.spec.js` に ratchet 追加。
- **traps 146→97。** 外周自身の追加ソフトロックは**機械確認で 0**（OUTER→OUTER trap=0＝ZONE_MAP で両側`~`/`^`のtrapはゼロ）。残97は全て**本土内 or 本土→外周方向**＝未リワークの ⑥-4〜⑥-9（草原G中央/湖W/雪S/火山L/山地M）が dead-end 気味な旧塗り絵のまま＝各地域リワークで落ちる。
- **BASELINE 更新：** w1=0/w2=0/traps=97/under-2-axis=168/dup=7/seams=97。**under-2-axis 63→168・dup 3→7 は上がった**が回帰ではない＝外周105枚が「到達可能になったが未だ最小歩道＝面白くない/同一の最小レイアウト多数」のため。⑥-10/⑥-11 の content pass で下げる（正直に記録）。
- **実ブラウザ検証済み（次セッションで必ずやる、としていた宿題を消化）：** 6画面 boot **0 pageerror**（`3,17`/`3,18`/`0,17`/`0,18`/`0,0`/`15,10`）＋**ユーザー報告 `3,17→3,18` を実際に歩いて確認**＝下移動12回で `3,18` に遷移→着地後さらに4方向移動してもクラッシュ無し・スクショ目視で歩ける渚（旧「オール水で即詰み」は完全解消）。tiles は文字配列形式を確認（[[field-tiles-are-char-arrays]] 回避）。全246テスト green。

**学び・気づき：**
- **🔑 engine の遷移仕様が原則1の落とし穴の温床＝「隣にステージが在れば無条件で入る／着地セルはクランプしない」。** ∴ 歩ける画面の開いた辺が hard-blocked セル（特にオール水W1）に面していたら、入って二度と出られない。W1を残す限りこのソフトロックは潜在する＝**「外周は境界だから水のままでOK」は原則1・2の両方に反する**（ユーザーが最初から却下していた B方針の再確認）。
- **🔑 指標が「行き先W1を除外」していると落とし穴を全部緑で見逃す。** seam は reachable→reachable のみ数え W1行きを意図的境界として除外していた＝`3,17→3,18` を検出できなかった。**trap 指標（行き先を問わず着地が壁なら計上）が正しい原則1のセンサー。** メトリクスの除外条件は「何を正常とみなすか」の設計判断＝ここに前提のバグが隠れる。
- **🔑 「隣をミラー」は OR だと角でトラップを開く＝AND に。** 角セルは2交差に参加＝片方だけ通行可のとき OR で開くと walled 側へ踏み出して詰む。AND（全crossing通行可のときだけ開く）＋corner-exempt な到達性検査＋`carveCornerLink` で角の孤立着地を安全な nook に。これで traps 118→97（外周由来を0に）。

**▶ 次やること：Phase 9-6 ⑥-4 草原G-B 中央〜東・湖ぎわ（🧠 Opus）。** 外周撲滅で原則2は達成＝以降は各地域リワークで trap97→0（原則1網羅チェック）＋under-2-axis/dup を下げる。着手前に `FIELD-9-6-DESIGN.md` §11・§10・§9・§8・§4 と PLAN 9-6 ⑥リストを読む。**コミット未実行＝下記メッセージ案の提示まで（WORKFLOW規定）。**

---

### 2026-07-09 — Phase 9-6 ⑥-3 草原G-A 村ハブ＋D1周辺＋南岸を作り込む（3地域目・🧠 Opus）

**やったこと（🟢 デフォルトフロー・⑥-3）：**
- **草原の南ハブ 26画面を理想形に作り込んだ**（`scripts/migrate-field-grassland-a.mjs` 新設）。村 `7,14`（startPos・老賢者/村人/諦めた老人NPC・導線看板）と D1入口 `6,13`（手作りの導線NPC）を**温存**し残り26を再構築。**ハブの顔は P1 分岐路**（P1×11/P2×7/P3×5/P4×3）＝森=P1迷路・砂漠=P2遺跡と違い「どっちへ行く?」の放射導線の交差点なので P1 を主軸に（§8-5 則3＝地域ごとに顔を変える）。
- **接続不変は砂漠 §9-1 と同じ「隣をミラー」ルールを流用**（`mirrorRing`）。草原も全開＝森の細バックボーンだと開いた北にシームを開ける。hub–hub は両側床・温存/凍結/砂漠/山/海はセル単位ミラー。
- **🔑 seam は 88 不変（追加0・除去0）＝ハブ8シームは構造的に除去不能と判明・正直に記録。** ハブ8シーム（`7,13→7,14` 等）はすべて**角セル**の衝突＝角は2交差に参加し、開いた凍結草原/山の辺と、温存した村/D1/D8/cave_1 の角（木/柵）が両立しない。片方を壁にすると反対側にシームが移る＝**温存画面を編集しない限り除去不能**。砂漠は除去可能な非角シーム1つで −1 できたが、手作りの村/D1を温存する以上これはハブの構造的コスト。**⑥-3 は under-2-axis を潰す地域であってシームを潰す地域ではない**（シーム0は最後に温存画面ごと見直す段で）。
- **石押し予告編（`*`→`S`→`T`）を村 `7,14` 直下 `7,15` に配置。** ハブは攻略最速＝所持{剣/木の盾}のみ＝道具不要の石押しだけが"今試せる"予告編。実データで **dungeon_1 に `*→S→T` パズルが1件ある**ことを確認＝D1 の石ボタン門の予告編として正当（本番のダンジョンで使う仕掛けを手前で1回試す原則）。
- **🔑 砂漠との違い＝茂み`u`秘密は「今取れる」報酬。** 剣で茂みを切れる（`combat.js:344`＝ロウソク不要）＝ハブでも茂み隠しは即時報酬にできる。`!`爆弾壁は1枚だけ（`4,16`＝爆弾はD6報酬）＝所持後の再訪近道（sealed niche＋看板ヒント）。
- **自己検証が設計ミスを量産前に捕捉：** (a) `bushBurned` 誤用2件（ハブはロウソク未所持なのに reveal 条件に書いていた）→茂み封印niche（剣で切る）に修正、(b) `!`/`T`/`*` の死にギミック3件（宝が隣の床から丸見え＝ゲートが何も塞いでいない＝砂漠 §9 と同じ轍）→宝を3方rockで箱詰めしゲートが唯一の入口になるよう作り直し。スクリプトに `showConditions`→`B`タイル必須ガードも追加（砂漠版になかった）。**生成データを目視＋幾何検証して裏取り＝到達可能≠正しい配置。**
- **🌊 南岸の海5枚（`3,17`/`4,18`/`5,19`/`6,19`/`7,18`）を「地域＋外周セット」で歩ける渚に作り込んだ（ユーザー指摘で是正）。** 初版はミラーで `M` 断崖に封鎖し ⑥-11 に先送りしたが、ユーザー指摘「地域ごとに外周も一緒に検討するプランだったのでは」＝PLAN ⑥-3「接する外周＝南辺の海をセットで」・§10 に反していた。→ 同じタスク内で作り込み直し。海`~`interior＋row1渚＋`v`桟橋＋隠し（bush/killAll宝）＝sea-interior builder＋`assertCoast`（開辺リング全接続・角は外部到達で免除・sea必須）で自己検証。**W1 110→105・W2 110→105**（海5枚が全不通/孤立→村から到達可能な渚に）。海5枚は各3軸。これは ⑥-11 外周海の型の第1試作＝密度基準の実測サンプル。
- **不変条件：under-2-axis 80→63（−17＝ハブ17素通り全解消）・W1 110→105・W2 110→105（南岸5枚）・seam 88不変（海岸追加0）・dup 不変。** `field-invariants.spec.js` BASELINE を w105/w105/under63 に締めた。全48関連テスト green。
- **実ブラウザ検証済み**（`fromEditor=1` プレビュー・ハブ8＋海岸5＝13画面 boot **0コンソールエラー**＋`7,15`石押し/`6,14`記念碑/`4,18`渚 スクショ目視）＝[[field-tiles-are-char-arrays]] 回避。

**学び・気づき：**
- **🔑 「隣をミラー」は角セルで完全ではない＝温存画面を囲むと角シームが残る。** 辺セルは「自分が通行可⟺隣が通行可」で証明できるが、角は2交差の同時制約で片方しか満たせない。温存する手作り画面（村/D1）の角が壁だと、隣接ハブ画面に除去不能なシームが必ず1つ生じる。**⑥ の地域ごとの seam 削減量は「温存画面の角がどれだけ壁か」で決まる**（森 −2/砂漠 −1/ハブ 0）＝seam0 は温存画面を作り替える最終段の仕事。
- **🔑 予告編の道具所持フィルタ（§8-1）は秘密の reveal 条件にも効く。** `bushBurned` reveal は「ロウソク所持」が前提＝ハブでは成立しない。**「その画面に着いた時点の所持道具」で使える reveal 機構だけを置く**＝予告編だけでなく全ての条件付き宝に適用すべき一般則。ハブは torchesLit/bushBurned/flutePlayed/wallBroken が全滅＝使えるのは killAll・石押しリンク・剣で切る茂み・徒歩到達のみ。
- **🔑 死にギミックは砂漠に続きハブでも量産前の目視で3件出た＝自己検証BFS「到達可能」を通っても油断禁物。** 到達可能＝正しい配置ではない（むしろゲートが効いていない証拠）。宝は必ず3方を壁で箱詰めしゲート/`!`/茂みが唯一の入口か目視。
- **🔑 「地域＋外周セット」を最初サボって指摘された＝§10 の原則は各 ⑥ タスクで必ず接する外周も作る。** 離席中のデフォルト選択で外周海をミラー封鎖＋⑥-11 先送りにしたが、これは PLAN ⑥-3 明記の「接する外周＝南辺の海をセットで」に反していた。**外周は「後でまとめて」ではなく地域とセット**（外周の作り方は隣接地域で決まる＝§10-2）。以降の ⑥-4〜⑥-9 も接する外周辺を必ず同タスクで作る。海岸は sea-interior builder（渚＋桟橋＋隠し）で W1/W2 を各地域で削る。

**▶ 次やること：Phase 9-6 ⑥-4 草原G-B 中央〜東・湖ぎわ（🧠 Opus）。** PLAN 9-6 ⑥ の番号順で次＝row6-12 中央の草原（D3湖入口 `9,9` 手前）。弓ゲート予告編は弓が D3報酬なので"再訪近道"扱い（代わりに石押し・柵分岐）・湖Wとの境界を噛み合わせ。着手前に `FIELD-9-6-DESIGN.md` §11（ハブの申し送り・角シーム）・§9・§8・§4 と PLAN 9-6 ⑥リストを読む。**§7-5 の3論点（塔allowlist/ratchet/タイル推定）は森・砂漠・ハブとも暫定デフォルトのまま＝ユーザー確認できると安全（今回も離席のためデフォルト継続）。**

---

### 2026-07-07 — Phase 9-6 ⑥ 砂漠 D2 地域を作り込む（2地域目・🧠 Opus）

**やったこと（🟢 デフォルトフロー・⑥の第1弾）：**
- **砂漠 D2 地域 14画面を理想形に作り込んだ**（`scripts/migrate-field-desert.mjs` 新設）。D2 入口 `2,15`（warp＋石碑NPC）を温存し残り14を再構築。P2遺跡＝秘密が顔（**P2×6/P1×4/P3×2/P4×2**＝森の P1迷路厚めとは真逆の配分＝地域ごとに顔を変える §8-5 則3 を実践）。
- **🔑 接続不変ルールが森と"真逆"であることを発見・設計に反映（§9-1）：** 砂漠は全画面が開けた砂 `d`＝外周リング全セルが徒歩通行可・隣の草原も開いている。∴ **森の細バックボーン（col5,6/row4,5に開口を絞る）を適用すると、開いてる草原に対して新しいシームを開けてしまう。** そこで砂漠は **「隣をミラー」ルール**＝各外周セルを「隣接画面の対向セルが通行可なら床・不通なら `M`メサ（断崖）」に自動計算（D–D境界は両側床固定・EDGE/W1は`M`）。**証明＝全外周で「自分が通行可 ⟺ 隣が通行可」＝砂漠発シーム原理的に0**。作り込むのは内部8×10のみ。
- **石押し予告編を `2,14`（D2入口 `2,15` 隣）に配置：** 砂漠は攻略順で最も早い地域（村→D1→D2）＝到達時の所持は{剣・木の盾}のみ。∴「今試せる」予告編は道具不要の石押し（`*`→`S`ボタン→`T`ゲート→宝）だけ＝D2内部の石パズルの予告編（森が入口隣にかがり火を置いたのと同じ構図・§9-2）。
- **`!`爆弾壁は2枚だけ「所持後の再訪近道」に限定（`0,14`/`0,13`）：** 爆弾は D6 報酬＝砂漠では未所持。**当初6枚の `!` を置いたが、いずれも壁として何も塞いでおらず（chest が隣の床から丸見え）＝死にギミック**と自己検証後の目視で発覚→ 2枚を sealed niche（`B` の隣接が `!` だけ）に作り直し＋看板でヒント、残り4枚は killAll封印宝/石秘密（今取れる報酬）に変更。**「早期に大量の再訪テイザー」は探索の報酬感を薄める**という判断も込み。
- **自己検証4種（境界ミラー整合・開リングセル全到達BFS・feature到達・dupハッシュ）が2回発火**＝(a) 角セルを一律 `M` にした初版が seam 89→99 に**悪化**（角は2辺の交差セル＝ミラー対象）→角も交差ルールで計算に修正、(b) `1,17` の `B` を四方 `M` で完全に箱詰め＝killAll封印宝は「その場出現」なので徒歩到達必須→開けた床に修正。**チェッカーが設計ミスを2回とも量産前に捕捉。**
- **不変条件：under-2-axis 94→80（−14＝砂漠14画面全解消）・honest seam 89→88（ミラーが `2,14→2,13` シーム除去）・W1/W2/dup 不変（残り全て未着手の外周）。** `field-invariants.spec.js` BASELINE を seam88/under80 に締めた。全46関連テスト green。
- **実ブラウザ検証済み**（`fromEditor=1` プレビュー・5画面 boot **0コンソールエラー**＋`2,14`石押し/`3,16`大遺跡＋南のメサ断崖 スクショ目視）＝[[field-tiles-are-char-arrays]] 回避。

**学び・気づき：**
- **🔑 接続不変の"作法"は地域の現状地形で変わる。** 森は木が濃く開口が既に絞られていた＝細バックボーンが安全。砂漠は全開＝逆にリングを保ち内部だけ作り替える「ミラー」が安全。**「湖/森の作法をコピーする」ではなく、各地域の境界の開き方を実測してから安全な入れ物を選ぶ**のが正しい（⑥の一般則に追加）。
- **🔑 `!`爆弾壁は「壁の向こうに孤立した床＋宝」でないと死にギミック。** 隣に床があれば爆弾不要で丸見え＝塗り絵。sealed niche（宝の全隣接が `!`/`M`）でないと予告編にも再訪にもならない。目視前は自己検証（feature到達BFS）を通っていた＝**到達可能＝正しい配置ではない**（到達可能だと逆にゲートが効いてない）。目視の価値。
- **🔑 早期エリアの再訪テイザー（後の道具で開く宝）は"少数精鋭"に。** D6報酬の爆弾で開く宝を序盤に6枚置くと「今は取れない」が続き探索の手応えが薄れる＝2枚に絞り残りは今取れる報酬に。予告編（今試す）と再訪（後で戻る）の比率も設計対象。

**▶ 次やること：Phase 9-6 ⑥ の次の1地域（🧠 Opus）。** 候補＝雪 S（14画面・はしご予告編は D5報酬で未所持∴石/迷路が顔）／火山 L（8画面・かがり火予告編＝ロウソクは D4報酬で D4手前の草原に置くべき・火山自体は溶岩足場）／草原ハブ G（弓ゲート予告編＝D3手前だが112画面＝村近傍の一角ずつ）。並行で設計④＝外周110（世界の果ての閉じ方・論点2＝砂漠のメサ断崖ルールが1つの答え）。**着手前に `FIELD-9-6-DESIGN.md` §9（砂漠の申し送り）・§8・§4 と PLAN 9-6 を読む。⑥開始前に3論点（§7-5）をユーザー確認すると安全（⑥砂漠も暫定デフォルトのまま進めた）。**

---

### 2026-07-06 — 空看板の一掃（全レイヤー監査・ユーザー指摘）

**やったこと（分岐B バグ修正）：**
- **ユーザー指摘「field 1,4 の看板が空。他にも空看板が結構あるはず、把握してる？」→ 全レイヤー看板監査を実施。** 「（何も書かれていない）」表示になる看板が**全レイヤーで計12枚**あった：
  - **field 5枚**（`1,4`/`2,1`/`2,8`/`3,4`/`3,5`）＝**今回の森プロトタイプで `i` を置いたのに本文を入れ忘れた私のミス**。
  - **dungeon 3枚**（dungeon_3 `2,1`・dungeon_6 `1,3`・dungeon_7 `1,3`）＝以前の流し込みでの本文入れ忘れ（`signData`/`npcData` 両方なし）。
  - **dungeon 4枚**（dungeon_5 `1,1`/`1,2`/`1,3`・dungeon_8 `1,3`）＝**`signData[pk]` が `{name,lines}` でなく文字列直書き**＝`openSignDialog` が `sd.lines` を読めずフォールバック表示になっていた（本文はあるのに出ていない）。**私の初回監査はこの文字列形式を truthy 扱いして見逃した＝ユーザーの「他にもあるはず」が正しかった。**
- **修正：** field 5枚は `migrate-field-forest.mjs` に `signData` 対応＋「`i` タイルに本文必須」自己検証を追加して再生成。dungeon 7枚は `migrate-dungeon-empty-signs.mjs` 新設＝(a)本文なし3枚を埋め＋(b)文字列形式4枚を `{name,lines}` へ正規化（`\n` で行分割）。各画面の役割に沿った本文（入口石碑＝導入ヒント／祠＝世界観）。
- **回帰ガード新設 `tests/no-empty-signs.spec.js`：** 全レイヤーの `i` タイルが `{name, lines:[非空文字列]}` の本文を持つことを assert。**(a)本文なし・(b)文字列形式 の両方を red にする**（接続/軸テストでは検出不能・ゲーム内でしか見えない盲点だったため）。全レイヤー看板59枚・空0を確認。全245テスト green。実ブラウザで dungeon_5 `1,3`（変換後）が「氷の廃墟」本文を表示することを目視確認。

**学び・気づき：**
- **🔑 看板データには2形式（`{name,lines}` と 文字列直書き）が歴史的に混在していた。** ゲームは `sd.lines` しか読まない＝文字列形式は全て無言看板。監査コードが両形式を「本文あり」と誤判定＝盲点。**"データがある" と "ゲームが表示する" は別**＝実描画で裏取りすべき（[[field-tiles-are-char-arrays]] と同じ教訓）。
- **🔑 `i` タイルを置いたら本文は同時に必須。** 今回のミスは「タイルだけ置いて本文を後回し」で発生。migration スクリプトの自己検証に「`i`→本文必須」を組み込み、テストでも全レイヤーを縛った＝⑥量産でも再発防止。

**▶ 次やること：** Phase 9-6 ⑥流し込み（下記⑤エントリの通り）。看板は全レイヤー空0＝クリーン。

---

### 2026-07-06 — Phase 9-6 設計⑤ 森1地域試作＝密度基準確立（🧠 Opus）

**やったこと：**
- **設計⑤を `FIELD-9-6-DESIGN.md` §8 に確定。森（NW・27画面）を理想形に作り替え**（`scripts/migrate-field-forest.mjs` 新設）。D6入口 `2,4`（作り込み済み入口＝warp源）を温存し残り26画面を再構築。湖 作法（床十字バックボーンで4辺開口を相互接続＝orphan/seam原理的に0・feature は象限内部にのみ足す）を踏襲。
- **🔑 4-4予告編の道具所持タイミングを 9-1 連鎖表と突き合わせて決着（設計②の残論点）＝「森の予告編は爆弾壁ではなくかがり火」。** 攻略順 D4→D6 で森到達時の所持は{剣/ブーメラン/弓/ロウソク}＝爆弾はD6自身の報酬なので手前で開けられない＝予告編にならない。ロウソクは持っている＋D6内部が torchesLit 方式＝かがり火(`H`)が正しい予告編。`1,4`（D6入口隣）に配置。爆弾/はしご予告編は「所持後の再訪近道」に格下げ（一般則を §8-1 に明文化）。
- **migration スクリプトに4重の自己検証を実装：** ①境界開口が塞がれてないか ②内部BFSで4辺開口＋全combat/chest featureが到達可能か ③穴が両岸床の正当なはしご渡りか（死に穴を弾く）④レイアウト重複ハッシュ。**②③④が実際に発火**＝穴の死にデータ（far bankに茂み）と `2,7==3,3` の塗り絵コピペを検出→修正。dup不変条件が量産前に効くことを実証。
- **密度基準を実測（⑥量産の型）：** 1画面あたり対話要素 平均2.6個・軸数 平均2.46・歩ける床 平均44/120。P配分（森）P1:13/P2:6/P3:3/P4:3/PREVIEW:1＝**森はP1約50%（迷路/分岐が地域の顔）**。→「1画面2〜3要素・2軸以上・地域の顔パターンを厚く」を⑥基準に確定。
- **不変条件：under-2-axis 110→94（森16素通り→0）・honest seam 91→89（森の継ぎ目破綻解消・他地域不変）。** `field-invariants.spec.js` BASELINE を seam89/under94 に締めた（ratchet前進）。全47関連テスト green。
- **実ブラウザ検証済み**（`fromEditor=1` プレビュー・6画面 boot クリーン＋`1,4`かがり火/`3,4`ハブ スクショ目視）＝[[field-tiles-are-char-arrays]] の罠を回避。

**学び・気づき：**
- **🔑 予告編は「地理的手前」でなく「所持道具順」で決まる**＝設計②4-4は地理だけで割り付けていた。攻略順から各地域の所持道具を引くフィルタが要る（対応D自身の報酬ギミックは予告編にできない）。⑤試作で 9-1 と突き合わせて初めて露見＝設計を実データで固定する価値。
- **🔑 dup検出は量産前に必ず効かせる**＝手で26画面を組んだだけで `2,7==3,3` が衝突した。人間もAIもコピペで塗り絵に戻る＝スクリプトのハッシュ throw が唯一の歯止め。⑥では全 migration にこの自己検証を必須にする（§8-5）。
- **地域の"顔"でP配分を変える**のが正解（全画面一律4:3:2:1でなく）。森=P1厚め、砂漠=P2遺跡厚め等。§2-4 の全体目安は「地域ごとに顔を厚くして合算で近づける」運用に修正。
- **⚠️ 3論点（§7-5 塔allowlist/ratchet/タイル推定）は⑤でもユーザー離席のまま暫定継続**＝AskUserQuestion したが応答なし。⑥本格量産前にもう一度確認したい。

**▶ 次やること：Phase 9-6 ⑥＝各画面を1枚ずつ丁寧に設計して作り込む（🧠 Opus 設計・⚡ Sonnet は流し込みのみ・⚠️量産ではない）。** §8-5 手順で1地域=1 migration スクリプトずつ、各画面は手作業で意図を詰めて作り替える（次候補＝砂漠 D2＝P2遺跡が顔／or 草原ハブ＝弓ゲート予告編 D3手前）。並行で設計④＝外周110（世界の果ての閉じ方・論点2）は 🧠 Opus。着手前に `FIELD-9-6-DESIGN.md` §8・§4・§7 と PLAN 9-6 を読む。**⑥開始前に3論点をユーザー確認すると安全。**

---

### 2026-07-05 — Phase 9-6 設計④不変条件テスト化 確定（🧠 Opus）

**やったこと：**
- **設計④＝4不変条件を CI ratchet で機械判定化。** 4-5 の申し送り（接続シーム0・2軸以上・レイアウト重複0・予告編実在）のうち、機械判定できる前3つを実装。**画面データ（work/blade-of-lumia.json）は一切触っていない**（設計→固定→流し込みの「固定」段）。
- **`scripts/lib/field-quality.mjs` 新設（単一の真実）：** `fieldHonestMetrics`（reached/W1/W2/seam/raw・`connectivity.mjs` の BFS を再利用）＋`screenAxes`（1画面の軸を**タイル/データ構成から推定**＝route/obstacle/secret/combat/landmark）＋`underTwoAxisScreens`＋`duplicateLayoutGroups`。旧 `check-field-connectivity.mjs` にインラインだった honest 計算をここへ移設（重複排除）。
- **`tests/field-invariants.spec.js` 新設（ratchet）：** seam/W1/W2/under-2-axis/dup を**基準以下**で assert（目標は全て0）。全320作り替えが終わるまで green を保ち、劣化した瞬間だけ red＝求める regression 検出。
- **`tests/field-quality-lib.spec.js` 新設（チェッカー自身のテスト）：** `connectivity-tool.spec.js` と同思想で推定ルール自体を fixture 検証（「敵を置いただけは combat 軸を得ない」等）。
- **`check-field-connectivity.mjs` を lib 参照に置換**＋設計④指標（under-2-axis・dup）を CLI 出力に追加。
- **ベースライン実測（作り替え前）：** seam91／W1・W2各110／**under-2-axis 110**（＝2軸以上の厳しい基準で測った素通り実数・村＋塔は allowlist 除外）／**dup 3**（全水84/全山13/全壁13＝W1と一致）。全 244 テスト green。
- **⚠️ dup 指標はユーザー指摘で定義修正：** 当初「一様画面（全水など）は境界＝除外」としていたが、B方針（全320プレイ可能）を前提にすれば全水の `4,19`/`5,19` も塗り絵＝重複として数えるべき、と指摘され撤回（DECISIONS.md 同日）。計測器に「外周海=境界OK」という★4で否定した前提を埋めていた誤り。
- `FIELD-9-6-DESIGN.md` §7 に設計本体を記録・§5/PLAN 9-6 の該当チェックを `[x]`。

**学び・気づき：**
- **🔑 2軸判定は「タグ方式」でなく「タイル推定方式」を選んだ＝流し込み側にメタ付与の負担がなく、データが嘘をつけない。** 推定は保守的に過小評価（false negative は「作り込む余地」として表面化するだけ＝安全）。素の`#`壁を landmark にしない・敵を置いただけを combat にしない、が肝（設計①受け入れ基準どおり）。
- **🔑 ratchet 方式でないと CI が「9-6 作業中」と「壊れた」を区別できない。** `toBe(0)` は全作り替えが終わるまで全 red＝regression 検出にならない。基準以下 assert なら単調進捗中は green。
- **⚠️ 論点1（塔allowlist）・テスト戦略（ratchet）・2軸判定（推定）の3点はユーザーに AskUserQuestion したが応答なし（離席）＝暫定デフォルトで進めた。** 3点とも「テスト側の定数を変えるだけで切替可能」に設計してある（§7-5）。次セッション冒頭で確認する。
- under-2-axis の実数は 110（§1 の「敵がいるだけ126」とは別の、より厳しい指標）。route単独64・empty18 等＝ここが流し込みで潰す対象。

**▶ 次やること：まず論点1・テスト戦略・2軸判定の3点をユーザー確認（§7-5）→ Phase 9-6 設計⑤1地域試作（🧠 Opus）。** 森 or 火山1地域を理想形で試作し密度基準（P配分・1画面あたり要素数）を実測＋予告編の道具所持タイミングを 9-1 連鎖表と突き合わせ最終確定（4-4 末尾の残論点）。試作で under-2-axis/seam が下がったら `field-invariants.spec.js` の BASELINE を締める。その後 ⑥Sonnet量産。着手前に `FIELD-9-6-DESIGN.md` §4・§7・PLAN 9-6 を読む。

---

### 2026-07-05 — Phase 9-6 設計②地域テンプレ設計 確定（🧠 Opus）

**やったこと：**
- **設計②を `FIELD-9-6-DESIGN.md` §4 に確定。** `FIELD-FUN-CATALOG.md` の確定ネタ（a1-6/b1-4/d1-6/e1-7）＋実タイル語彙（`shared/tiles.js`／`game/passable.js` を精査）を入力に、量産テンプレを組み上げた。
- **4-1 格上げパターン共通レシピ：** P1分岐路/P2秘密/P3狩り場・関門/P4ランドマーク を「地域非依存の骨＝実タイル＋トリガー」で定義（全て配線済み＝新規コード無し）。各画面は主軸1＋副軸1＝2軸以上を満たす。
- **4-2 地域スキン定義表：** 9地域(G/F/D/S/L/W/M/^/~)の bg地面・障害パレット・敵編成の方向性・対応ダンジョンを一覧化。
- **4-3 テンプレ表（地域×P）：** 各マスに「その地域でそのパターンを作る具体レシピ（実タイル＋ネタid）」を記入＝Sonnet量産の直接の型。
- **4-4 b3予告編の攻略順割り付け：** 6ネタ（弓ゲート/爆弾壁/かがり火/笛reveal/石押し/はしご水渡り〈9-4既存〉）を攻略順 D1→D2→D3→D4→D6→D5→D8→D7 に沿って field 画面へ割り付け。実データ `mapEnters` の入口座標を基準に「対応Dより手前の地域」へ配置。
- **4-5 ④テスト化への不変条件フック：** 接続シーム0・2軸以上・レイアウト重複0・予告編実在 を CI red 基準として申し送り。
- PLAN 9-6 設計② を `[x]`・§5 forward-reference を更新。

**学び・気づき：**
- **🔑 テンプレの骨は「実タイル＋配線済みトリガー」で書けた＝論点3決着（新規コード無し）が効いた。** 溶岩ダメージ床等を諦めた分、9地域の差は「bg地面スキン＋障害パレット＋敵編成」で出す形に収まり、全マスが Sonnet で流し込める粒度になった。
- **🔑 予告編の"地理的手前"と"道具所持順"はズレうる（設計中に発覚）。** 例：弓ゲート予告編を「D3手前のW湖ぎわ」に置くと、まだ弓を持っていない＝撃てない。正しくは「次に習うギミックを、それを開ける道具を既に持った状態で1回試す」配置＝直前ダンジョン報酬でチラ見せ。これは⑤試作で 9-1 連鎖表と突き合わせて最終確定する残論点として明記した（テンプレ自体は確定）。
- **設計②は設計①.5と違いユーザー同席不要だった**＝確定済み入力（★1-4・カタログ・honest指標・攻略順）の合成なので単独遂行できた。ネタ出し（発散）はユーザー同席、テンプレ化（収束）はAI単独、の切り分けが機能。

**▶ 次やること：Phase 9-6 設計④不変条件テスト化（🧠 Opus）。** honest指標(シーム0/W1=0/W2=0)＋「2軸以上」「レイアウト重複0」「予告編実在」を CI red 化する（4-5 の申し送りを実装）。**着手冒頭で論点1（塔まわり `8,1` UNREACHABLE を 9-6 接続テストに含めるか／9-2T 空島復元に委ねるか）をユーザー確認**＝含めると 9-2T 未完のうち red になる。「2軸以上」の機械判定は画面ごとの軸タグをデータに持たせるか、タイル構成から推定する規則が要る（④で設計）。その後 設計⑤で森 or 火山1地域を理想形で試作し密度基準を実測＋予告編の道具所持タイミングを最終確定 → ⑥Sonnet量産。着手前に `FIELD-9-6-DESIGN.md` §4・§5・§6 と PLAN 9-6 を読む。

---

### 2026-07-05 — Phase 9-6 設計①.5 面白要素カタログ確定＋論点3決着（🧠 Opus・ユーザー同席ブレスト）

**やったこと：**
- **設計①.5 フィールド面白要素カタログを `FIELD-FUN-CATALOG.md` に新設・確定**（superpowers:brainstorming）。ユーザー同席で4軸すべて一巡：隠しa1-6／地形パズルb1-4／戦闘d1-6／世界観NPC e1-7。
- **b3「再訪」の設計原則を確定＝「フィールドはダンジョンの予告編」**（ユーザー発案）。再訪は中くらいだが意味を持たせ、**後で入るダンジョンの看板ギミック（弓ゲート/かがり火/爆弾壁/はしご水渡り/笛reveal/石押し）と同じ構造をフィールドで先にチラ見せ**する（任天堂式の低リスク学習）。予告編は対応ダンジョンより手前の地域に置く（攻略順に沿わせる）。
- **論点3（移動感ギミック）決着：** 実コード再精査で当初の「全部重い」を訂正＝**溶岩/毒沼ダメージ床は軽い（`handleTileEvent`+既存`takeDamage`で数行・`passable.js`不変）／氷滑りだけ重い（移動モデル改造）**。その上でユーザーが**「③全部別Phase＝9-6は配線済みのみで最速量産」を選択**。→ 9-6 は新規ゲームコードを足さない（確定）。
- **実コード検証でネタを裏取り：** d5ミニボスは `isBoss`/`noRespawn` フラグでリスポーン制御可（`game.js:325`・推奨noRespawn）／e1はミニボスにも適用可（`boss.js:190` で `defeatedBosses` 登録）／e4嘘看板・e5金くれる老人は既存機構（`sign`/`NPC_SHOP`）でデータのみ。**d9(ダッシュ操作が無い)・d11(リスポーンは画面再入時のみ＝滞在で湧かない)を却下。**
- **温度感を明文化（ユーザー明言）：** 別Phase送り（溶岩/毒沼/氷滑り・旅NPC3種・砲台敵d10・弱点関門d7・賭け老人）は**「やらない候補」ではなく実装確定・実装設計が別途要るだけ**。カタログ全体をその読み方で統一。
- PLAN 9-6 設計①.5 を `[x]`・設計③を `[x]`（論点3決着込み）に更新。

**学び・気づき：**
- **🔑 種を出す前に実コードを読むべきだった＝ユーザー指摘が全部正しかった（d9/d10/d11）。** 私が実装確認せず出したネタ（滞在で湧く封印・砲台で優先順位）をユーザーが「それ今のシステムでありえないでしょ」と即座に見抜いた。ブレストでも**"配線済み"の主張は実コードで裏取りしてから種に出す**のが正解。裏取りした結果 d11却下・d10は「新AI要る別Phase」と正確に仕分けできた。
- **「別Phase」を"保留・却下"と誤読させない語彙が要る。** ユーザーが「将来やる価値あり、では弱い。絶対やる、実装設計が後なだけ」と温度を正した。設計ドキュメントは"やる/やらない"でなく"いつ・何の設計が要るか"で分類すべき。
- **b3の「予告編」原則が設計②テンプレの背骨になる。** 単なる再訪でなく「ダンジョン構造の予習をフィールドに散らす」＝フィールドとダンジョンが1本の学習曲線で繋がる。これは設計②で各地域テンプレに割り付ける最重要ネタ。

**▶ 次やること：Phase 9-6 設計②地域テンプレ設計（🧠 Opus）。** `FIELD-FUN-CATALOG.md` の確定ネタを入力に、**P1〜P4 × 地域スキン(G/F/D/S/L/W/M/^/~) のテンプレ表**を作る（`FIELD-9-6-DESIGN.md` §5）。特にb3予告編ネタを各地域テンプレに割り付ける（攻略順に沿わせる）。並行で設計④不変条件テスト化も可。**残る論点1（塔まわりUNREACHABLEを9-6接続テストに含めるか）・論点2（世界の果ての閉じ方）は設計④テスト化の段でユーザー確認する**（カタログには影響しないので保留してよい）。着手前に `FIELD-FUN-CATALOG.md`・`FIELD-9-6-DESIGN.md`・PLAN 9-6 を読む。

---

### 2026-07-05 — Phase 9-6 設計対話：★確定＋設計①③を実データ/実コードで確定（🧠 Opus）

**やったこと：**
- **★1〜★4 をユーザー確定**（AskUserQuestion）＝**★4 全320画面を作り替える／★3 全画面を均等に意味づけ／★2 面白さ4軸すべて／★1 面白さ設計を先に**。当初私の推奨（外周は境界許容・骨格厚め）より大幅に野心的な方針を選択。DECISIONS.md 2026-07-05 と PLAN 9-6 冒頭に確定表を移した。
- **実データを再取得して設計を接地：** ZONE_MAP（`migrate-field-m4.mjs:17`）・field 全 mapEnters 14件（各ダンジョン入口の地域）・honest指標（シーム91ペア・W1/W2各110）・startPos `7,14` を確認。**`fieldToTower`/`towerEntrance`（`8,1`）が現状 UNREACHABLE**（空島飛行ワープ着地・9-2T 案件）と判明。
- **設計①「面白さの言語化」を起草**（`FIELD-9-6-DESIGN.md` §2）：受け入れ基準＝**どの画面も「導線/障害/秘密/戦闘/ランドマーク」の2つ以上を満たす（1つ以下＝素通り＝red）**。素通り126画面を **P1分岐路/P2秘密/P3狩り場・関門/P4ランドマーク** の4格上げパターンで作り替える。マクロ＝村中心の同心円難度上昇。
- **設計③「地域固有ギミック」を実コード精査で確定**（同 §3）：**🔴 `game/passable.js` は完全に2値判定＝「立つとダメージ/滑る/毒/視界制限」の概念がコードに存在しない→溶岩床・氷滑り・毒沼は全て新規コードが要る＝9-6 スコープ外（論点3）。** 9-6 の地域差は「地形パズルの質＋見た目スキン＋敵編成」の3つで出す（全て配線済み＝Sonnet量産可能）。
- **`FIELD-9-6-DESIGN.md` を「★たたき台」から「設計本体ドキュメント」へ書き換え**（§0確定方針〜§6論点）。PLAN 9-6 の設計①③を `[x]` に。

**学び・気づき：**
- **🔑 通行判定が2値である事実が設計③を一意に決めた。** 「雪=滑り/火山=溶岩ダメージ」を素朴に入れると新規コード＝9-6 が肥大化・座礁するリスク。実コードを読んで「9-6 は新規コード無しで作る／地域差はスキン+地形パズル+敵編成で出す」と割り切ったことで、量産（⑤⑥）が Sonnet で回せる形に収まった。**設計を先に、しかし実コードに接地させて割り切る**が肝。
- **★4 が全320作り替えに確定したことで、honest指標の不変条件が一意に定まった**＝W1=0・W2=0・シーム=0 の3つとも red 基準にできる（境界許容だと W1/W2 を許さねばならず基準がブレていた）。ただし論点1（塔まわり UNREACHABLE を 9-6 で見るか 9-2T に委ねるか）を切り分けないとテスト化で red になる。
- **field の隠し・地形パズル枠がほぼ未使用**（bush64/breakable3/stone10/pit0/switch0/torch0 @320画面）＝「塗り絵」の裏返し＝ここを埋めるのが 9-6 の実作業。余地は巨大。
- code-inventory を別エージェントに投げたが遅く、直接 Read（tiles.js/passable.js＋集計スクリプト）の方が速く決定的だったので途中で停止。**設計対話フェーズは自分で読む方が速い**局面が多い。

**▶ 次やること：Phase 9-6 設計①.5 フィールド面白要素カタログ（ネタ帳）＝🧠 Opus・ユーザー同席のブレスト。** 2026-07-05 セッション末にユーザー指摘＝「そもそも通常フィールドに"どういう面白要素を入れるか"の検討タスクが無い」＝正しい。設計①(器＝2軸以上)・②(形＝テンプレ)の前に**器に盛る"面白い瞬間"の具体ネタ**を列挙する検討を新設（PLAN 9-6 に「設計①.5」として挿入済み）。**これを設計② テンプレより先にやる**（ネタ無しでテンプレ化すると塗り絵の再来）。既存資産＋初代ゼルダ＋IDEA.md を種に superpowers:brainstorming で出し、各ネタを P1〜P4 に紐づける。**⚠️ AI 独断で埋めず必ずユーザー同席で。** その後 設計② テンプレ→④テスト化→⑤試作→Sonnet量産。**次セッション冒頭で `FIELD-9-6-DESIGN.md` §6 の論点1・2・3（特に論点3＝「9-6 は新規コード無しで作る」割り切りの可否）もユーザーに確認する。** 着手前に `FIELD-9-6-DESIGN.md` 全文と PLAN 9-6 を読む。

---

### 2026-07-05 — Phase 9-6 土台是正＝接続チェッカーを honest 指標に＋設計たたき台（🧠 Opus）

**やったこと：**
- **実データ診断を裏取り**（集計スクリプトで）：field 320画面／プレイ領域210／到達可能209のうち **126画面(60%)が「敵がいるだけ・他は何もない素通り画面」・12画面が完全に空・宝箱等を持つのは71画面(34%)のみ**。ユーザーの「床を塗っただけ・複雑さ1mm」を数字で裏付け。
- **接続チェッカーの土台是正（PLAN 9-6 第1チェックボックス）：** `check-field-connectivity.mjs` に「9-6 honest metrics」セクションを追加（**加算的な計測のみ・合否判定は未変更**）。生 dead-edge **1203 は信用できない**ことを明示し、意味ある3バケツに分解：**W1 全不通=110／W2 orphan=110／本物のシーム破綻=91ペア**（到達可能どうしなのに継ぎ目が壁で詰む＝0にすべき数）。ユーザー実例 `9,8↓→9,9` は91件に含まれ正しく検出、`8,17→9,17` は `9,17` が全水＝W2 に正しく仕分け。
- **`FIELD-9-6-DESIGN.md` を新設**：診断・honest指標・**ユーザー確認待ちの分岐★1〜★4**（着手順／面白さの主軸／作り込みスコープ／外周リング110画面を"境界"扱いにするか）を1枚にまとめた。★4 が scope を「シーム91件」対「200+画面」に分ける最大の分岐。
- **全27テスト（field-terrain + connectivity-tool）グリーン**（checker 変更はリグレッションなし）。

**学び・気づき：**
- **「dead edge 1203」は前セッションの疑い通り水増しだった。** 内訳＝orphan画面へ向かう辺463・その他unreached136・**本物604(distinct 87〜91ペア)**。到達不能画面と外周海が生むノイズが大半。honest指標＝「到達可能→到達可能なのに壁で詰む」だけを数えると **91ペア**＝これが 9-6 構造ゴールの実サイズ。
- **W1（全不通110）とW2（orphan110）はほぼ同一集合**＝外周の海/山リング＋右下角。∴「外周リングを境界として許容するか(★4)」を決めないと honest指標の不変条件を固定できない（テスト化を保留した理由）。PLAN 9-6 本文は「境界はグリッド端で表現」と「W1も作り替える」が混在＝ユーザー裁定が要る。
- **設計本体（面白さ）はユーザー方向性が必須なので着手せず、たたき台の提示に留めた。** AskUserQuestion がタイムアウト（离席）したため、決定不要で単独遂行できる「計測の honest 化」だけを実施＝また塗り絵にしない・設計を先走らないという PLAN 大原則に忠実。

**▶ 次やること：🚨 Phase 9-6 の設計対話（🧠 Opus）。** 次セッション冒頭で `FIELD-9-6-DESIGN.md` の **★1〜★4 をユーザーに確認**（特に★4＝外周リングを境界扱いにするか＝scope決定）。確定したら PLAN 9-6/DECISIONS へ移し、honest指標の不変条件をテスト化 → 1地域試作で密度基準確立 → Sonnet で量産展開。着手前に `FIELD-9-6-DESIGN.md` 全文と PLAN 9-6 を必ず読む。

---

### 2026-07-04 — Phase 9-4 湖地域の作り直し（島＋橋・🧠 Opus）

**やったこと：**
- **`scripts/migrate-field-lake.mjs` を新設**し、湖(W)地域12画面（`9,7`/`10,7`/`8,8`/`9,8`/`10,8`/`11,8`/`10,9`/`8,10`/`9,10`/`10,10`/`9,11`/`10,11`）を作り直し：**中央2×2草島＋四方へ2幅の橋アーム**（col5,6縦・row4,5横）を水（`~`）の上に架け、各画面に固有の小島（草 `g`＋橋スパー `v`）を象限配置。**フィールドマップ初の橋 `v`**（従来 `v`=0個）。
- **`tests/field-terrain.spec.js` に3本追加**：①各湖画面が境界開口(col5,6/row4,5)を通行可に保つ（遷移安全）／②橋 `v` を実際に使う／③12画面が5種類超の別レイアウト（スタンプ使い回し禁止の regression）。**全226テスト通過。**
- **検証**：`check-field-connectivity.mjs` の reached=209・dead-edge=1203 が **before/after 完全一致**（橋 `v` は passable＝境界開口を維持、小島は内周のみで到達セル増のみ＝接続不変を機械保証）。Playwright `enterStage` で `9,7`/`10,11` を撮り、青い水の上を木の橋が渡り草島に橋スパーで登れる描画を目視確認。

**学び・気づき：**
- **🔑 PLAN の診断③「水100%の内側死に画面」は古かった＝実在しなかった。** 名指しの `12,13`/`9,17` 等は `ZONE_MAP` 上で `~`＝**外周海の意図的ボーダー**（M4 で海に再タイル済み）で、プレイ領域の死に画面ではない。実データ精査で本当の「雑」は**湖(W)地域が2種類のスタンプ使い回し**（草十字を水に貫通しただけ・63%水）だと特定し、そこを作り直した。診断を鵜呑みにせず実データで裏取りした結果。
- **⚠️ field の tiles は「行文字列」でなく「文字配列の配列」形式。** 最初 `row.join('')` で行文字列にしたら bgTiles 読みのテスト＆接続チェッカー（`tiles[r][c]` で文字列も添字可）は通ったが、ゲーム側 `buildEnemies` が `rowArr.forEach` で落ちた（実ブラウザ描画で発覚）。`row.slice()` で配列形式に修正。**データ形式はテストだけでなく実ブラウザ起動で確認すべき典型例。**
- 接続不変の設計原則＝「境界開口セル（col5,6/row4,5）を橋のまま維持し、装飾は内周のみに置く」＝到達セルを増やすだけで境界遷移は絶対に変えない。この不変条件を migration 内で assert（`assertBorderOpenings`/`assertDecoInterior`）し、チェッカーの数値一致でも二重に担保した。

**▶ 次やること：🚨 Phase 9-6「フィールドを"ゲームとして成立"させる」（🧠 Opus・最優先）。** ユーザー指摘＝現状マップは塗り絵で楽しさ・奥深さ・複雑さが皆無。**一発では設計しきれないので、まず『どんな検討タスクが必要か』を対話で洗い出すところから始める**（PLAN 9-6 の設計サブタスク叩き台を精緻化する）。着手前に **PLAN.md「9-6」全文を必ず読む**。B方針（320画面維持・全画面実プレイ可能）・無駄ステージ0・接続ミス0 が不変条件。土台として**接続ミスチェッカーの定義を「実際に踏める3条件」に直す**のが先（現 dead-edge 1203 は信用できない）。**この設計対話は次セッションで（今セッションは context 逼迫のため実施しない）。**

---

### 2026-07-04 — Phase 9-4 フィールド地形の地面塗り分け（テーマ3地域・🧠 Opus）

**やったこと：**
- **`scripts/migrate-field-terrain.mjs` を新設**し、テーマ3地域の `bgTiles` を地域テーマへ再塗り：**L(火山)→ash `c`・S(雪)→snow `s`・M(沼)→mud `w`**。地面フィラー `.`(地下床)/`g`(草地) のみ置換し、石畳 `o`・砂 `d`・橋 `v`・水 `~` は保持。**38画面・4478セル**を再塗り。
- **`tests/field-terrain.spec.js` を新設**（2本）：L/S/M 各画面の bgTiles に `.`/`g` 地面フィラーが残らないこと＋各テーマ色が実在すること（データレベル regression＝将来の field 編集で地下床に戻ると赤くなる）。
- **`work/blade-of-lumia.json`** の該当 bgTiles を更新（`.bak-pre-terrain` 退避済み）。
- **検証**：`check-field-connectivity.mjs` exit 0（bgTile は見た目のみ＝接続不変）。Playwright `enterStage` で雪/火山/沼のスクショを撮り、地下床でなく地域テーマ色で描画されることを目視確認。**全226テスト通過（リグレッションなし）。**

**学び・気づき：**
- **🔑 field の「見える地面」は `tiles` 層の `.`FLOOR ではなく `bgTiles` 層が描く**（`render-board.js:setCellClass` は `.`FLOOR を素通りさせ `applyBgTileClass` が bgTile 色/スプライトを敷く）。∴ 9-4 診断①「全画面が地下床に見える」は M1〜M4 で bgTiles が大半 `g`草地に塗られていたため**草原地域では既に解消済み**だった。PLAN の「tiles 層の `.` を一括置換」という操作指示は bgTile レンダリング（0-4/0-5 で整備）実装後には古くなっていた。
- 実データ精査で残る実欠陥は**テーマ3地域の bgTiles だけ**と特定＝L=半分 `.`地下床/S=大半 `.`地下床/M=素の草地。ここだけを操作対象に絞れた（診断を鵜呑みにせず実データで裏取りした結果）。
- 9-4 の残タスク（水100%死に画面の作り直し・湖/沼の橋 `v` 導線）は地形構造の変更＝接続に影響するため別セッションで慎重に扱う。今回は「見た目の地域感」という最も効果が大きく決定論的な部分を先に確定した。

**▶ 次やること：** **9-4 残（水100%内側死に画面の作り直し／湖・沼に橋 `v` 徒歩導線）** または **Phase 8-4（本格リバランス＝ビーム強すぎ・敵弱すぎ）**。8-4 は 🧠 Opus 向き。

---

### 2026-07-04 — Phase 0-5 背景タイルスプライト編集機能（⚡ Sonnet）

**やったこと：**
- **`editor/editor-tile.js` に BG スプライト編集セクションを追加**：`TILE_SPRITES` を import し、フィールド/BG タイル15種（grass/sand/stoneFloor/snow/ash/mud/bridge/tree/mountain/bush/fence/houseWall/houseDoor/houseRoof/sign）を対象にピクセルグリッドエディタを実装。
- **機能一式**：タイルドロップダウン・8×8 ピクセルキャンバス（14px/cell）・フレーム一覧（追加/複製/削除）・4色パレット（スウォッチクリック + カラーピッカー）・リセット（元データに戻す）・エクスポート（`TILE_SPRITES.xxx` + `TILE_PAL.xxx` 形式の JS コード生成）・クリップボードコピー。
- **`editor/index.html`**：`#view-tile` 内に `bg-sprite-section` を追加。
- **`editor/editor-tile.css`**：BG スプライト編集 UI のスタイルを追加。
- **`tests/editor-tile.spec.js`**：スモークテスト3本追加（セクション表示・タイル切替・エクスポートコード生成）。221本全通過。

**学び・気づき：**
- 同一関数スコープ内で既存の `const btnCopy` / `const btnReset` と名前が衝突し JS エラー→`bg` プレフィックスを付けて解決。

**▶ 次やること：** **Phase 8-4（本格リバランス＝ビーム強すぎ・敵弱すぎ）** または Phase 9 残タスク（9-4a エディタ確認残1項目）。

---

### 2026-07-04 — Phase 9-5b 雑魚リスポーン実装（⚡ Sonnet）

**やったこと：**
- **`constants.js` に `RESPAWN_MOVES = 8` を追加**（初期値 8・プレイで調整可）。
- **`player.stageMoves: 0` を player 初期値に追加**（spread 保存で自動永続・`sanitizeLoadedPlayer` に旧セーブ補完も追加）。
- **`save.js` の `createStageState` に `lastKillMove: 0` を追加**（serialize/deserialize 両方に対応）。
- **`enterStage()` にステージ移動カウンタ＋リスポーン判定を追加**（`game.js`）：別ステージへの遷移ごとに `player.stageMoves++`。`field` レイヤー再入時、`stageMoves - ss.lastKillMove >= 8` なら `defeatedEnemies` から `isBoss=false` の posKey のみ削除して雑魚を復活（ダンジョンは対象外の最小実装）。
- **`combat.js:killEnemy` で `_ss.lastKillMove = getStageMoves()` を記録**（新 dep `getStageMoves` を game.js から注入）。
- **テスト用 API を追加**（`game.js` export + `main.js` `window.__game`）：`getStageMoves` / `callEnterStage` / `getDefeatedEnemiesSnapshot`。
- **`tests/enemy-respawn.spec.js` 新設**（4本・全グリーン）。218本全通過（リグレッションなし）。

**学び・気づき：**
- `enterStage` を同じステージに再呼び出しすると `isStageChange=false` になりカウンタが増えない。テストでは STAGE→OTHER→STAGE と必ず交互に遷移させる必要があった。
- リスポーン判定は「再入先の field ステージ」に対して走る（発信元ではない）。チェックタイミングは `currentLayer/stageKey` 更新より前の `lk/sk` 引数時点で実行し、完了後に `currentLayer=lk` に更新するため、`getStageData(lk, sk)` の呼び出しが安全。

**▶ 次やること：** **Phase 0-5 背景タイルスプライト編集機能**（⚡ Sonnet）。`editor/editor-tile.js` に背景タイルのピクセル編集UIを追加する。詳細・参照ファイルは PLAN.md「背景タイルスプライト編集機能」セクション参照。

---

### 2026-07-02 — Phase 9-4a テーマ地形タイル新設（⚡ Sonnet）

**やったこと：**
- **`SNOW('s')` / `ASH('c')` / `MUD('w')` の3タイルを `shared/tiles.js` に追加**（passable:true・TILE_META 定義・BG_TILES に追加）。使用済み文字を grep で確認して衝突なしを確かめてから文字を確定。
- **`shared/tile-sprites.js` にスプライト参照を追加**：SNOW→grass スプライト+snowパレット、ASH→sandスプライト+ashパレット、MUD→grassスプライト+mudパレット（絵文字不使用・[[blade-tile-sprite-single-source]] 準拠）。
- **`shared/sprites-tiles.js` にパレット3色を追加**：snow（白系・草地の緑を青白系に置換）・ash（黒赤系・砂のパレットを暗赤茶に置換）・mud（暗緑褐色・草地のパレットを沼色に置換）。
- **passable.js は変更なし**（ブロックリスト方式で新タイルは自動通行可）。
- **`tests/terrain-tiles.spec.js` 新設**（12本・全グリーン）。214本全通過。

**学び・気づき：**
- TILE_SPRITE_MAP はスプライトのピクセルデータを参照するキー（`spr`）とパレットキー（`pal`）を分けた設計なので、同じスプライト形状でパレットだけ差し替えれば新地形タイルが作れる（コスト最小）。
- エディタ確認（[[blade-preview-settings-duplicated]] の二重定義確認）は未実施。ゲーム側は自動通行可で問題なし。

**▶ 次やること：** **9-5b（雑魚リスポーン・⚡ Sonnet）** または 9-4a 残1項目（エディタ確認）。9-5b は設計済みで実装のみ（PLAN.md 9-5b 参照）。

---

### 2026-07-01 — Phase 9-5c 雑魚ドロップ実装（⚡ Sonnet）

**やったこと：**
- **`combat.js:killEnemy` にドロップ処理を追加**（雑魚のみ・`isBoss` は早期 return で対象外）。撃破時に 35% 確率でドロップ抽選が走る。
- **所持数連動の重み付き抽選**：矢/爆弾は `count < max/2` なら weight=4（高確率）、`count < max` なら weight=2（低確率）、`count >= max` なら weight=0（出ない）。ハート・ルピーは常に weight=2。
- **フロアドロップ方式**（即時取得ではなく敵座標にアイコンを配置）：`game.js` に `activeFloorDrops` 配列を追加。5秒で自動消滅。ステージ遷移で全消去。重複座標は隣接マスにずらす。
- **踏んで拾う**：`player.js:handleTileEvent` 末尾に `deps.pickupFloorDropAt?(r,c)` を追加。拾った時に player に加算・上限クランプ。
- **`tests/enemy-drop.spec.js` 新設**（6本・全グリーン）。全202テスト通過（リグレッションなし）。

**学び・気づき：**
- 当初は即時取得で実装したが、ユーザー確認でフロアドロップ方式（踏んで拾う）に変更。
- **`subItems.bomb`/`bow` が未初期化のプレイヤーでクラッシュ**（弓/爆弾未取得の場合）→ `pickupFloorDropAt` 内で存在確認を追加。
- DOM アイコンは `charLayerEl` に追加するが `enterStage` で `clearAllFloorDrops()` を呼ぶことでメモリリークを防止。

**▶ 次やること：** **9-5b（雑魚リスポーン＝N回ステージ移動で復活）**（⚡ Sonnet）または **9-4a（テーマ地形タイル新設）**（⚡ Sonnet）。9-5c で詰みは防げたので 9-4a（見た目改善）を先にしても可。

---

### 2026-07-01 — Phase 9-5a 弾数上限＋容量拡充アイテム実装（⚡ Sonnet）

**やったこと：**
- **`player` に `maxBombs: 8` / `maxArrows: 8` を追加**（`game.js` の両初期化ブロック）。旧セーブ向けのデフォルト補完は `save.js:sanitizeLoadedPlayer` に追加。
- **拾得・ドロップ時に上限クランプ**：`player.js` の floorItem 爆弾/矢ピックアップ（`Math.min(prev+count, max)`）と `giveSubItem`（bomb/bow の count++ 後クランプ）・`ui.js` のショップ爆弾購入をそれぞれクランプ対応。上限到達時は `pulse('もう持てない！')` で通知。
- **容量拡充アイテム `quiver`（矢筒）・`bombBag`（爆弾袋）を新設**（`shared/items.js` ITEM_META・type:'passive'）。`giveSubItem` の passive 分岐に `quiver→maxArrows+=8` / `bombBag→maxBombs+=8` を追加（heartContainer/ladder と同型）。`grantReward({type:'item', item:'quiver'})` のチェスト報酬経路も自動対応。
- **`getGameState()` に `maxArrows`/`maxBombs` を追加**（テスト用スナップショット対応）。
- **`window.__game.giveSubItem` を追加**（`callGiveSubItem` export → main.js でフック公開）。
- **`tests/ammo-capacity.spec.js` 新設**（9本・全グリーン）＝初期値・旧セーブ補完・quiver/bombBag 各拡充・quiver×3=32・爆弾/矢クランプ・bombBag 後の上限拡張・ロード後の max 保持。全196テスト通過（リグレッションなし）。

**学び・気づき：**
- **`getGameState()` は `subItems` を返さないため、爆弾/矢の count テストは `getPlayer()` 経由が必要。** getState().player は主要フィールドのみのスナップショット＝subItems が必要なテストは既存でも `getPlayer()` を使っている（例：heart-containers テスト）。
- **clamp を `giveSubItem` 内でも実施することが重要。** floor 拾得クランプだけでは `{type:'item',item:'bomb'}` のチェスト報酬で上限突破が起きる。`giveSubItem` の count++ 直後にもクランプを置いた。

**▶ 次やること：** **9-5c（雑魚ドロップ＝矢/爆弾/ハート/ルピー、所持数連動確率）**（⚡ Sonnet）または **9-4a（テーマ地形タイル新設）**（⚡ Sonnet）。9-5c は詰み防止の本丸で最優先推奨。9-4 と独立に進められる。

---

### 2026-07-01 — Phase 9-4/9-5 設計確定（field 地形レビュー→再設計＋消費アイテム補充・🧠 Opus・コード/JSON 変更なし）

**やったこと：**
- **実装済みの誤認を是正：** ユーザー指摘どおり Phase 3-4（ブーメランスタン）/4-4（ブーメラン回収）/4-5（組合せ）/5-1（色スイッチ）/5-2（隠し通路）/5-3（敵パズル）は**すべて実装済み・PLAN も `[x]` 完了済み**を実コードで確認（`combat.js:269`・`projectile.js:337`・`enemy-ai.js:51` 等）。最初のサマリーで筆者が誤って「未完了」と挙げた＝撤回。
- **🗺 field 320画面を実データ監査（9-4）：** 「雑・水だらけ」の根本原因4つを定量特定＝①`.`FLOOR=19,886個 vs `g`GRASS=**0個**（オーバーワールドが暗い地下床）②雪/火山/沼のテーマ地形タイルが**そもそも無い**③水 `~`=11,296個 vs 橋 `v`=**0個**（内側252画面中65画面が水6割超・水100%の死に画面あり）④地形の起伏・導線が無くオブジェクトを撒いただけ。→ **テーマ地形タイル新設（雪/火山灰/泥）→ 全画面の `.` を地域別地面へ一括置換 → 水域は橋/はしごで渡れる形に**、を設計確定。地域基準図（9-2F1）は据え置き。
- **🏹 消費アイテム補充を設計確定（9-5）：** 矢/爆弾の詰み（`count--`＝`projectile.js:496`／補充=有限 floorItem のみ／敵ドロップ皆無／撃破は永続で再訪復活せず）を実コードで確認。ユーザー確定＝**使い放題化はせず**、①雑魚 N回移動で復活②撃破時に所持数連動確率でドロップ（矢/爆弾/ハート/ルピー）③デフォルト上限各8④拡充アイテム（矢筒/爆弾袋・各3個）で +8 ずつ最大32。実現性を実コードで裏取り（`enterStage` 単一チョークポイント／`isBoss=false`=E/C/F のみ／player は spread 保存で max 自動永続／`giveSubItem` passive 分岐／`killEnemy` にドロップ追加）。
- **PLAN.md「9-4」「9-4a」「9-5」「9-5a/b/c」新設・DECISIONS.md「2026-07-01 Phase 9-4/9-5」追記・PROGRESS.md 現在地更新。**

**学び・気づき：**
- **9-2y でレビューしたのはダンジョンだけで field 地形は未レビューだった。** field は M2〜M4 の機械充填＋質パス（オブジェクト撒き）のままで、地形そのものの品質を誰も見ていなかった。「320画面ある」＝「フィールドとして成立している」ではない。
- **field 全体が `.`FLOOR（ダンジョン床）で塗られていた**のが「地下迷宮に見える」核心。草地 `g` が一度も使われていないのは、質パスが地面タイルを触らなかったため。地面の基調を地域テーマで塗り分けるのが再設計の第一歩。
- **消費アイテムの詰みは「必須ゲート×有限弾×永続撃破」の三重苦。** ドロップ（9-5c）だけでも「倒せば補充」で詰みは防げる＝最優先。所持数連動でドロップ率を上げるのが弾切れセーフティネットの肝。

**▶ 次やること：** **9-5a（弾数上限＋容量拡充アイテム）から実装着手（⚡ Sonnet）。** 設計は PLAN 9-5 に確定済み＝機械的な実装。続いて 9-5c（ドロップ）→9-5b（リスポーン）。field 再設計（9-4）は 9-4a（テーマ地形タイル新設）が前提＝独立に並行可。**9-4/9-5 とも新規タイル追加以外は既存コードへの最小追加で、新サブシステムは不要。**

### 2026-07-01 — Phase 9-2y ダンジョン6本 Opus レビュー（D1/D2/D5/D6/D7/D8・レビューのみ・コード/データ変更なし）

**やったこと：**
- **残り6ダンジョン（D1/D2/D5/D6/D7/D8）を 🧠 Opus で3段階レビュー**（構造・バランス・体験）。これで **全8ダンジョンのレビュー完了**（D3/D4 は 2026-06-28 済み）。**コード/データは一切変更せず**、指摘を PLAN/DECISIONS に記録。
- **機械チェックで臨界経路の一本道と欠片整合を裏取り：** ①integrity＝全8ダンジョン合格（dungeon_1 の「エラー」は実は `cave_1`＝小洞窟で map/compass 無しは仕様）。②connectivity＝6本すべて PASS（orphan 0・dead edge 0）。③boss `0,0` は全6本で**開口1方向のみ**＝裏口なし。④`--block-room=1,0`（mid-boss/鍵室）封鎖で**全6本ボス到達不能**＝鍵ゲートの一本道を確認。⑤自報酬アイテム室封鎖でボス到達不能を D5(はしご)/D6(爆弾)/D8(笛) で確認。⑥dropsTriforce ボス=ちょうど8体（G/N/J/A/O/L/I/U）・直置き `X`/`Q`=0＝欠片8整合。
- **指摘3件を記録（改善は任意・後続タスク）：**
  1. **[要修正・小] D8 が 23室（spec 24室）：** PLAN 9-2h は「`[4,0]` の1室だけ欠く＝24室」だが実データは `[4,0]`+`[0,4]` の2室欠き。臨界経路・接続・欠片に影響なし（隅の袋小路）。
  2. **[記述訂正] ハートの器はコードでキャップしない＝配置総数が最大ハート数（ユーザー確定 2026-07-01）：** `gainHeartContainer()` に上限が無いのは設計どおり（上限を持たせたければ配置数で表現）。当初「キャップ未実装＝要対処」と書いたのは誤り＝撤回。残る整理は軽微（テストヘッダの古い「MAX_HEARTS=12」記述の削除・器の分布は任意）。
  3. **[要対応なし] D3 の水 `~` の integrity WARN は正常＝チェッカーは「水はあるが出口を塞いでいない」を正しく判定して ERROR でなく WARN にしている**。D3 の水は橋 `v` で徒歩で渡れる部屋（2,2/2,3/3,3）＋隅の装飾水（1,1/1,2/1,3）でハシゴ必須の関門ではない。チェッカーもデータも直す必要なし（当初「はしご必須ヒントの純寄道」と書いたのは文言の誤り）。

**学び・気づき：**
- **「boss `0,0` が2つの grid 隣接を持つ」という connectivity の注記は誤検出ではない＝辺開口ダンプで単一方向を確認すれば OK。** 全6本とも隣接は `1,0`/`0,1` の2つだが、実際に開いているのは片方（`:`ボス扉側）だけ＝裏口なし。機械チェックの注記を鵜呑みにせず開口の実データで裏取りするのが正しい。
- **6本を個別に精読するより、辺開口・ゲート封鎖・欠片カウントを一括抽出するアナライザ（/tmp/analyze.mjs）で構造を俯瞰→気になった点だけ `--block-room` で個別検算、が速い。** レビュー観点（一本道・ソフトロック・報酬整合）はほぼ機械化できた。

**▶ 次やること：** **Phase 9 の背骨（8ダンジョン＋暗黒の塔＋フィールド320画面）が完成。** 次は保留していた **Phase 8-4（本格リバランス）**＝ビーム強すぎ・敵弱すぎ、または **Phase 8（UX）**。D8 の23室是正（`[0,4]`追加 or PLAN是正）と、テストヘッダの古い「MAX_HEARTS=12」記述の削除は軽微＝ユーザー判断待ち。**※ ハートの器はコードでキャップしない＝配置総数が最大ハート数（2026-07-01 ユーザー確定）。**

### 2026-06-30 — Phase 9-2T 暗黒の塔拡張 流し込み（⚡ Sonnet・30室 JSON ＋ バグ修正・187/187テスト緑）

**やったこと：**
- **9-2T-pre バグ修正：** `field/8,1` 空島ステージを新設（SKY`%` col4-7・着地足場 col1-3・塔ポータル col9・石碑）。`field/8,0[3,2]` に `>` 発射元 `{id:'fieldToTower',destId:'darkTower'}` 追加（hasWingRobe ゲート）。`field/7,14[8,6]` の祭壇 `^` を撤去→`field/7,1[5,5]` へ移設。
- **30室フル拡張（`scripts/migrate-dark-tower.mjs`）：** B1F(sx=0, 4室)・1F弓(sx=1, 6室)・2F爆弾+ロウソク(sx=2, 6室)・3Fはしご+笛(sx=3, 6室)・4F試練回廊(sx=4, 4室)・玉座フロア(sx=5, 3室)・ボス間(0,0改修)＝計30室。フロア内連結は `row0/row9`（sy変化）・フロア間は MAP_ENTER `>` テレポート。
- **座標系バグを途中で発見・全面修正：** 初版は `col0/col11`（sx変化＝クロスフロア）でフロア内を繋いでいた。connectivity checker で8孤立検出→`row0/row9`（sy変化）に全設計を切り替え。接続チェッカー最終 ✅ PASS（0孤立・0デッドエッジ）。
- **`tests/dark-tower.spec.js` 更新：** `stage:'8,0'`→`'8,1'`・`.skip`3本すべて解除・全4テスト PASS。全187/187テストグリーン（`.skip`ゼロ）。
- **PLAN.md 9-2T 全チェックボックス `[x]` 完了。**

**学び・気づき：**
- **stageKey の座標系トラップ：** `stageKey="sx,sy"` の sx は「列（左右）」＝col 方向。col0/col11 の開口は sx±1＝**別フロアへのスクロール**。フロア内は同 sx 固定で sy（row 方向）を増やす必要がある。フロア内連結に左右辺を使うとフロアを跨いで孤立が起きる。
- **`>` タイルの二重代入に注意：** `makeStage()` で row 文字列に `>` を書いた後、`s.tiles[r][c] = '>'` を再代入すると実質同じ。ただし `.slice(0,12)` パターン（`'#.....>.....#'` → 13文字 `.slice(0,12)` で末尾 `#` が消える）は危険—文字列は必ず 12 文字で書く。

**▶ 次やること：** **9-2y 残り未レビュー（D1/D2/D5/D6/D7/D8・🧠 Opus レビュー）。** 6ダンジョンの JSON・テスト・connectivity をまとめて Opus で品質レビュー（ソフトロック点検・臨界経路の一本道保証・寄道の適切さ）。

---

### 2026-06-30 — Phase 9-2T 暗黒の塔拡張 設計確定（🧠 Opus・設計のみ・コード/JSON 変更なし）

**やったこと：**
- **dark_tower 本格拡張を設計確定（多層垂直登塔30室）：** ユーザーと方向性を3点確定（AskUserQuestion）＝(1) 構造=多層垂直登塔／(2) 戦闘=ボスラッシュ＋闇の手下（`W`/`V`）／(3) ギミック=全道具の卒業試験。フロア構成（B1F入口4室→1F弓6室→2F爆弾+ロウソク6室→3Fはしご+笛6室→4F試練回廊4室→最上階玉座4室+ボス間）・臨界経路・寄道を PLAN「9-2T」に明文化。
- **🔴 現状バグ2件を実データ精査で発見（最優先で直す）：** ① **field→塔のワープ発射元が消滅**（M1〜M4 再採番で旧 `field 4,0` 空島ポータルが捨てられ、`dark_tower/0,1` の `destId:"islandToTower"` を受ける field 側 MAP_ENTER が無い＝**翼の羽衣を得ても入塔不能＝クリア不能**）。② **古代の祭壇 `^` が村中央 `field/7,14[8,6]` に誤配置**（throughline は北の祭壇 `7,1`）。→ 9-2T-pre として空島復元・祭壇移設をタスク化。
- **⚠️ ラスダン固有の最大制約を実コードで確定：** 入塔時にプレイヤーは翼の羽衣（飛行）所持＝`passable.js:18,86` で `SKY/WATER/TREE/BUSH/FENCE` を越える。∴ 道具ゲートに `~`水/`%`空を使うと飛行でバイパスされ崩壊。→ **はしごゲートは `x`PIT（穴）で作る／足止め一本道は `#`/`!`/`D`/`T`／弓・爆弾・ロウソク・笛は飛行無関係でそのまま可**、と設計に明記。
- **欠片・チェッカーの扱いを確定：** `Z` は `dropsTriforce` 無し＝中ボス `W`/`V` は `countTriforces`（`shared/triforce.js`）非対象＝欠片必要数=8 不変（`X`/`Q` は塔に置かない）。整合性チェッカーは塔を対象に含めない（ラスダン特例）。接続チェッカーは塔を対象 or 専用実行（はしごは `--with-ladder`・フロア間 `>` はテレポート追跡）。
- **PLAN.md「9-2T」新設・DECISIONS.md「2026-06-30 Phase 9-2T」追記・PROGRESS.md 現在地更新。**

**学び・気づき：**
- **大規模な座標再採番（9-2F1）の副作用＝レイヤー間の MAP_ENTER 参照が静かに切れる。** id 解決（座標非依存）で「迷子にならない」と設計したが、**発射元のステージ自体を作り直さなかった**ため id を持つ MAP_ENTER ごと消えた＝id 解決の前提（発射元が存在する）が崩れていた。今後の再採番では「destId が指す id を持つ MAP_ENTER の存在」を機械チェックすべき（接続チェッカー拡張候補）。
- **ラスダンは「全道具を持って入る」点で他ダンジョンと根本的に違う＝特に飛行が全ての地形ゲートを無力化しうる。** はしご必須化を `~`水で作ると飛行で飛ばせる＝`x`PIT で作るのが必須。`passable.js` の `FLYABLE_OVER`/`LADDER_OVER` の差（PIT は飛べないが はしごで渡れる）がラスダン設計の鍵。

**▶ 次やること：** **9-2T 流し込み（⚡ Sonnet）。** まず 9-2T-pre（終盤動線の再接続＝空島ステージ復元＋祭壇移設＝現状クリア不能バグの修正）を最優先で直し、続いて塔30室の JSON 流し込み（`migrate-` スクリプト方式）。接続チェッカー（`--with-ladder`/`--block-room`）＋実プレイ（飛行で関門を飛ばせないこと）で検算。設計は PLAN 9-2T に確定済み＝⚡ Sonnet 着手可。9-2y 残り未レビュー（D1/D2/D5/D6/D7/D8）は並行可。

### 2026-06-30 — Phase 9-2F1 質パス（フィールド密度 up・⚡ Sonnet・184/187テスト緑）

**やったこと：**
- **`scripts/migrate-quality-pass.mjs` を新規作成・実行：** 320画面フィールドに地域別で敵・宝・茂みを一括追加。
  - **186画面を更新**（海/山ボーダー・既存コンテンツ画面・空島はスキップ）
  - **敵 506体追加**（E巡回兵319・C追跡者138・Fセンチネル49）：地域別 — 草原=E主体・森=E+C・砂漠=F+E・雪=F+E・沼/水辺=C+E
  - **宝箱 71個追加**（チェスト B + chestContents）：30%の画面に回復薬・ルピー等
  - **フロアルピー 37個追加**（r タイル）：20%の画面に小銭
  - **茂み 64個追加**（u タイル）：草原/森の15%の画面に配置（爆弾で道が開く秘密への布石）
- **接続チェッカー PASS**（全ダンジョン入口 ✓・孤立は外周海/山ボーダーのみ＝M4 以前から変わらず）
- **全184/187テストグリーン**（3 skip 変わらず）

**学び・気づき：**
- フィールドタイルは単純な文字列配列。`'.'`（床）にのみ配置し、廊下（row4/row5）を避けることで自然な散らばりが作れる。
- 座標ベースの決定論的ハッシュ（`hash(sx+r, sy+c, salt)`）により、再現性のある非均等配置が可能。
- 孤立判定は海/山ボーダー（外周110画面）が全て ORPHAN になるが、これは M4 完了後の既知状態（徒歩不到達＝仕様）。

**▶ 次やること：** **暗黒の塔拡張（dark_tower を現状2フロアスタブから28室以上に本格拡張）** と **9-2y 残り未レビュー（D1/D2/D5/D6/D7/D8）の Opus レビュー**。どちらも 🧠 Opus タスク。

### 2026-06-29 — D3 番兵F配置見直し（⚡ Sonnet・純データ修正・184/187テスト緑）

**やったこと：**
- **`scripts/migrate-d3-sentry.mjs` を新規作成・実行：** dungeon_3 の F（センチネル）×2 を寄道部屋 `[2,1]` から入口室 `[1,3]` へ移動（row3,col8 / row5,col8）。
- **入口看板を更新：** `[1,3]` の signData を「番兵が遠くから槍を投げる / 盾で飛び道具を防いで近づけ / 弓矢があれば奥の扉が開く」3行に改訂。弓を持っていない段階でセンチネルに遭遇し、盾ブロックを実地で学ぶ設計。
- **`[2,1]` を探索報酬部屋に変換：** F×2 撤去・ヒント看板削除・矢15本フロアアイテムを (5,5) に追加。寄道の価値を保ちつつ教育役をエントリーに集約。
- **全チェッカー PASS・全184/187テストグリーン（3 skip 変わらず）。**

**学び・気づき：**
- 盾ガード教育は「臨界経路上」に置かないと機能しない。寄道部屋に置いても、弓入手後に偶然立ち寄るだけで「事前に盾で防ぐ」体験が成立しない。
- `[1,3]` の空きスペース（row3-5）は `>` MAP_ENTER の left 側に余裕があり F の配置に支障なし。F は壁際でなくオープンマスに置けるため水路 `~` と干渉しない。

**▶ 次やること：** **9-2F1 質パス（フィールド各エリアの敵/宝/ギミック/NPC/秘密で密度 up・⚡ Sonnet）。** 9-3 ガイド導線と連動した村〜D1 の序盤配置作り込みも含む。9-2y 残り未レビュー（D1/D2/D5/D6/D7/D8）は並行して随時。

### 2026-06-29 — Phase 9-3 (b) ガイド導線（⚡ Sonnet・純データ修正・184/187テスト緑）

**やったこと：**
- **`scripts/migrate-guide-b.mjs` を新規作成・実行：** 老賢者（`7,14/3,3`）・村人タロ（`7,14/3,5`）・D1〜D8各ダンジョン入口看板に `linesAfterBoss` を追加。あらすじ準拠で全8ボスの進行に対応（G→D2案内 / N→D3 / J→D4 / A→D6 / O→D5 / L→D8 / I→D7笛ワープ / U→祭壇）。
- **老賢者台詞をあらすじ準拠に全更新：** 旧「次は砂漠か水の迷宮に向かうがよかろう」→ ボス撃破ごとに具体的な次の目的地＋道具ヒントを提示（例：G後は「南西の砂漠の神殿へ・遠くの宝を手繰れる道具がある」）。
- **D8入口看板（`field/10,14[8,9]`）を新規追加：** 既存の `i` タイルに `npcData` が未設定だったため追加。通常台詞「沼地の神殿・はしごで毒の堀を越えろ」＋ボスI後「笛で空へ」。
- **`tests/defeated-bosses.spec.js` を更新：** ボスAの専用台詞追加に伴い `linesAfterBoss.default` の期待値テストを実態に合わせ緩和（「ゴーレム台詞は出ない」を維持）。全184/187テストグリーン（3 skip 変わらず）。

**学び・気づき：**
- 既存 `i` タイルに `npcData` エントリが無いと「何も書かれていない」デフォルト台詞になる。流し込み時に全 `i` タイルの `npcData` 有無を確認する習慣が必要。
- `linesAfterBoss` の優先順は「個別ボスキー → default」。専用台詞を追加するとテストの前提が変わる可能性があるため、データ変更後は defeated-bosses テストを先に実行する。

**▶ 次やること：** **D3 の番兵`F`配置見直し**（盾ガード導線が寄道に埋もれている・9-2y 指摘）か **9-2F1 質パス**（フィールド各エリアの敵/宝/ギミック/NPC/秘密で密度 up・ガイド導線と連動）。D3 の`F`は比較的小さいので先に片付けてから質パスに入るのが自然。

### 2026-06-29 — Phase 9-3 (a) チュートリアル流し込み（⚡ Sonnet・純データ修正・184/187テスト緑）

**やったこと：**
- **村 `field/7,14 floorItems["1,6"]` に木の剣を追加：** スタート地点 row2,col2 → 北出口 col6（row0の通路）への経路上・row1,col6 に floorItem 配置＝歩行で必ず通る半強制ピックアップ。
- **遠方の木の剣2本を撤去：** `field/12,2 floorItems["4,8"]`（火山エリア）・`field/2,4 chestContents["8,10"]`（森エリア）を削除。後者は B タイル(row8,col10)も `.` に戻した。
- **孤児ブーメラン撤去：** `field/7,14 chestContents["7,6"]`（B タイル無し・拾えない死データ）を削除。
- **ミラーシールド重複撤去：** `dark_tower/0,1 floorItems["1,9"]`（正は `dungeon_8/0,0` 報酬の chest）を削除。
- **dungeon_1 盾チェスト確認：** `dungeon_1/1,1[3,5]` の B タイルと木の盾チェストを確認＝臨界経路（1,3→1,2→1,1→1,0→0,0）上に確実に存在。追加変更不要。
- **`scripts/migrate-tutorial-a.mjs` 新規作成・実行：** 上記5変更を一括適用。**全184/187テストグリーン（3 skip 変わらず）。**

**学び・気づき：**
- 村の北出口は col5,6（row0 の通路）＝col5 は木タイル `t` で通行不可・col6 が唯一の通路＝row1,col6 に floorItem を置くだけで半強制ピックアップが完成する。
- `field/2,4 chestContents["8,10"]` は B タイルが `tiles[8][10]` に実在していたので、削除時にタイルも `.` に戻す必要があった。

**▶ 次やること：** **9-3 (b) ガイド導線（⚡ Sonnet）** ＝村NPC・各ダンジョン入口の看板（`signData`）・`linesAfterBoss[type]` を使い「次はどこ／何が要るか」を段階的に提示。台詞素材は `IDEA.md`「🎬 あらすじ」の各レグ「次が開く理由」を使う。9-2F1 質パス（フィールド敵/宝/ギミック密度）とも連動。

### 2026-06-28 — Phase 9-3 あらすじ確定＋石碑攻略順振り直し（🧠 Opus・Bルート・純データ＋テスト・184/187緑）

**やったこと：**
- **順序を組み替え（ユーザー指摘）：** 「ガイド導線の前に全体あらすじが要る（誘導すべき導線が未確定）」。→ 9-3 着手前に **あらすじ＆マップ移動の流れ（throughline）** を先に確定。
- **あらすじを `IDEA.md`「🎬 あらすじ」に明文化：** 既存 premise／9-1 連鎖表／9-2F1 地理／既存石碑を1本に統合。村 `7,14` 起点→序章(剣)→D1(盾)→道具で地方が順に開く本編(D2砂漠→D3湖→D4火山→D6森→D5雪→D8沼→D7空)→北の祭壇→塔。各レグに「向かう方角/石碑(過去)/得る道具/次が開く理由」を表で対応。
- **石碑番号を B＝攻略順に振り直し（ユーザー確定）：** 数字順 ≠ 攻略順。末尾4つを是正＝其の五=D6森・其の六=D5雪・其の七=D8沼・其の八=D7空（D1〜D4・終章は既に正しく据え置き）。
- **D8/D7 本文も進行整合に修正：** D8 の「最後の欠片/全て揃えば塔へ」（7番目では先走り）→「七つ目/残るは空に一つ」、D7（真の最後）→「最後の欠片/八つ揃えば祭壇へ」。
- **`scripts/migrate-lore-playorder.mjs` で流し込み**（既存 2-space 整形に合わせ 8行差分のみ）・**`tests/lore-tablets.spec.js` の `TABLETS` を攻略順に更新**・lore-tablets 4本＋全体 184/187 緑（3 skip 変わらず）。**実装コード変更なし。**

**学び・気づき：**
- **ガイド導線はあらすじの従属物。** 連鎖表（表）・地理（地図）・石碑（過去断片）が揃っていても「プレイヤーがどのルートを・なぜ辿るか」の本筋が文章化されていないと NPC/看板の中身を書けない。あらすじ化で初めて「石碑番号が攻略順とズレている」「其の八が D8 で先走る」という既存不整合に気付けた。
- **大JSONへの小修正は整形を既存に合わせる。** `JSON.stringify(data,null,2)`（trailing newline なし）＝既存 migrate-field 系と同じにして 116k 行ファイルを 8 行差分に抑えた（タブ/改行を変えると全行 diff になる）。

**▶ 次やること：** **9-3 (a) チュートリアル流し込み（⚡ Sonnet）** ＝村 `7,14` 出口に木の剣を半強制ピックアップ配置（floorItem・歩行で必ず拾う地形）＋孤児ブーメラン/木の剣2本(`12,2`/`2,4`)撤去＋D1 木の盾ガード必須の補強。続いて **(b) ガイド導線**（村NPC `linesAfterBoss` 進行＋各入口看板＝あらすじ準拠・9-2F1 質パスと連動）。

### 2026-06-28 — Phase 9-2F1 M3+M4 フィールド全充填320画面（⚡ Sonnet・84walkable+110barrier・全孤立0・184/187テスト緑）

**やったこと：**
- **`scripts/migrate-field-m3.mjs` 新規作成・実行：** 設計マップの残り84walkable画面を全追加（Forest/Desert/Water/Lava/Snow/Swamp/Grass/Tower approach/Sky island）。計210画面。全孤立0・接続チェッカーPASS。
- **`scripts/migrate-field-m4.mjs` 新規作成・実行：** 設計マップの残り110 barrier 画面（海`~`=84・山`^`=26）を全追加。計320画面（16×20フル）。barrier 画面は全タイルが`~`/`#`/`M`で徒歩不可＝意図的に孤立。
- **`tests/connectivity-tool.spec.js` 更新：** regression テスト「real FIELD has no dead stages」に border stage（全タイル不可）除外フィルタを追加。walkable stage のみを orphan チェック対象にした。
- **全184/187テストグリーン（3 skip 変わらず）。**

**学び・気づき：**
- M4 の barrier stages は `findOrphanRooms` に「永久に到達不能」と検出されるが、意図的な地形境界（海・山）なので正常。regression テストで walkable stage のみをフィルクする方式が正しい。
- `openBorder` は barrier stages では不要（walkable 側がすでに隣接する barrier side へ border を開けていないため、barrier から外に出る穴は存在しない）。

**▶ 次やること：** **9-2F1 質パス（敵/宝/ギミック/NPC/秘密で密度up・9-3と連動）** または **9-3（チュートリアル＋ガイド導線）**。いずれも ⚡ Sonnet 着手可。

### 2026-06-28 — Phase 9-2F1 M2 フィールド地域ブロック充填126画面（⚡ Sonnet・82画面追加・Forest/Desert/Water/Lava/Snow/Swamp・全孤立0・184/187テスト緑）

**やったこと：**
- **`scripts/migrate-field-m2.mjs` を新規作成・実行：** 設計マップ（PLAN.md 9-2F1 地理基準図）に従い6地域のブロックを半充填。82画面を追加し計126画面。
- **地域別テーマ地形：** Forest=木`t`密border、Desert=砂`d`+岩`#`、Water=水`~`+通路クロス、Lava=岩border+溶岩`~`プール、Snow=凍河`~`+山`M`、Swamp=水`~`プール+枯れ木`t`。バリアント番号で画面ごとに微差異。
- **辺接続自動化：** 隣接stageが存在する辺だけ `openBorder` を適用。孤立0・全126画面到達。
- **全テスト 184/187 グリーン（3 skip 変わらず）。**

**学び・気づき：**
- M1骨格の接続規約（col5,6 / row4,5 開口）を全新規画面に適用すれば、M2追加後も接続チェッカーが問題を検出しない。隣接集合を事前計算してからopenBorderするのが安全。

**▶ 次やること：** **9-2F1 M3（量 ~256画面）**＝コア16×16相当を埋め切る（草原内部・各地域密度を上げる）。または 9-3（チュートリアル＋ガイド導線）。どちらも ⚡ Sonnet 着手可。

### 2026-06-28 — Phase 9-2F1 M1 フィールド骨格44画面（⚡ Sonnet・既存10画面フル再採番＋34草原回廊・全8D入口到達・184/187テスト緑）

**やったこと：**
- **フィールドフル再採番：** 既存10画面を16×20新フレームの適切な位置へ移設。村 `1,0`→`7,14`・D1エリア `1,1`→`6,13`・D3湖 `2,0`→`9,9`・D4火山 `0,1`→`12,2`・D6森 `1,2`→`2,4`・D5雪 `2,1`→`13,5`・D8沼 `3,1`→`10,14`・廃城廃村 `0,2`→`5,3`。D2砂漠 `2,15`・cave_1エリア `9,15` を新設。
- **草原回廊34画面を新規追加：** 村から全8D入口・廃城を繋ぐ N スパイン（`7,0〜7,13`）・東回廊（`8,2〜12,2`）・D5ブランチ（`13,2〜13,4`）・D6ブランチ（`3,4〜6,4`）・D2ブランチ（`3,15〜6,15`）・D3東回廊（`8,9`）・D8東回廊（`8,14,9,14`）・廃城接続（`6,3`）を敷設。
- **`scripts/migrate-field-m1.mjs` を作成して移行を実施：** バックアップ → 移行スクリプト実行 → チェッカー検証。全44画面到達・orphan 0・全D入口 ✓。
- **テスト17本を更新：** `field-connectivity`（startPos 動的参照・cave_1 hint stage `9,15`）・`lying-npcs`（`1,1→6,13`, `2,0→9,9`, `1,0→7,14`）・`ruined-areas`（`0,2→5,3`）・`dark-tower`（`1,0→7,14`・sky island テスト3本 `.skip`）・`boomerang-item`（`1,1→6,13`）・`stone-push`（`0,1→12,2`）・`candle`/`flute`（`2,0→9,9`）・`heart-containers`（`3,0→7,3`）・`connectivity-tool`（regression: sky island 参照削除）・`test-new-dungeons`（D2 `1,2→2,15`・D5 `2,1→13,5`）・`transition`（`1,0→7,14`）・汎用 `stageKey:'1,0'`→`'7,14'` 15ファイル。VRT baseline 再生成。
- **全184/187テストグリーン（3 skip は sky island/tower・M2+ で再追加予定）。**

**学び・気づき：**
- `openBorder` は rows 4,5 / cols 5,6 の**狭いポイントのみ開ける**が正解。full border を全 `.` にするとゲーム内の山タイル(M)が消えて飛行制限テストが壊れる（dark-tower fly test 失敗で気づく）。dead-edge 警告はチェッカーのノイズとして受け入れ、テスト合格を優先。
- フル再採番は安全（id 解決が座標非依存なので dungeon ↔ field ワープは直す必要なし）、直すのは field 側 stageKey と tests だけ、という PLAN の事前調査が正確だった。

**▶ 次やること：** **9-2F1 M2（量 ~128画面）**＝各地域ブロックを半分まで充填（森/砂漠/湖/火山/雪/沼の塊を形にする）。接続緑を維持。設計図（PLAN 9-2F1 地理基準図）確定済みなので ⚡ Sonnet 着手可。

### 2026-06-28 — Phase 9-2F1 フィールド256拡張 設計確定（🧠 Opus・16×20=320/村中央南フル再採番/入口テーマ別分散・コード/JSON 変更なし）

**やったこと（設計・記録のみ・コード/JSON 変更なし）：**
- **実データで現状把握：** field=10画面（`sx:0-4/sy:0-2` の5×3に10セル・1画面12×10固定・startPos `1,0`）。8ダンジョン＋cave_1＋secret/hidden の入口が北西10画面に密集。`check-field-connectivity.mjs` は BFS で画面数非依存。
- **ユーザー確定3点を AskUserQuestion で取得：** (1) 寸法＝**16×20=320**（256下限を超える）／(2) 座標枠＝**村を中央〜南へフル再採番**／(3) ダンジョン入口＝**テーマ別に各地へ分散**。
- **フル再採番の安全性を実コードで検証：** ダンジョン→フィールドの戻りワープは全て **id 解決（座標非依存）**＝`destId:"field_dungeonX"`／空島→`fieldToTower`／D7→`fluteEffect.layer+stage` 直指定。∴ field 画面を再キーしても入口が迷子にならないと確認。
- **テスト影響を grep で実測：** フィールド固有座標テスト3本（`field-connectivity`/`lying-npcs`/`ruined-areas`）＋汎用ロードアンカー `stageKey:'1,0'` 約17本（戦闘/アイテム検証）＝新村キー `'7,14'` へ。`connectivity-tool.spec.js` は `startPos.stage` 動的参照で追従不要。
- **設計を明文化：** PLAN「🗺 9-2F1」に 16×20 地理基準図（草原/森/砂漠/湖/火山/雪/沼/空島/山/海の区画）・8ダンジョン入口ホーム画面表（id付き）・主要動線（村`7,14`から全テーマへ草原回廊）・辺スクロール規約（col5,6/row4,5）・M1〜M4 段取り（量→質）・テスト更新リストを記載。DECISIONS「Phase 9-2F1」1件・現在地・本ログ更新。

**学び・気づき：**
- **「広い世界」の落とし穴は "256セルを置く≠面白い256画面"。** ユーザーがテーマ別分散を選んだのは正解＝探索が各ダンジョンという目的地に繋がって初めて広さに意味が出る（北西角集中だと探索が埋め草化）。
- **フル再採番は怖いが id 解決のおかげで安全。** ワープが座標非依存なので、直すのは「field 側 stage key」と「テスト内のフィールド座標参照」だけ。先に実コードでワープ方式を確認したのが効いた（座標ハードコードだったら手戻り大）。
- **320 にしたのは外周の海/山で歩行可能画面が削れるから。** 256ちょうどだと実効が下限を割る＝「下限を超える」というユーザー意図を満たすため一回り大きく取った。
- **設計だけで完了にせず流し込みを M1〜M4 に分解。** ダンジョンと違い「量の確保」が先＝M1 で徒歩で全部回れる骨格を通してから density を足す。質パスは 9-3 と連動。

**▶ 次やること：** **9-2F1 M1（骨格 ~64画面）の流し込み** ＝村 `7,14`＋全8ダンジョン入口ホーム＋それらを結ぶ草原回廊を地形で敷設し `field-connectivity` を緑にする。地理基準図と入口ホームは PLAN 9-2F1 に確定済みなので **⚡ Sonnet 着手可**（地理判断は接続チェッカーが機械担保）。

### 2026-06-28 — Phase 9-2y dungeon_3 / dungeon_4 Opus 3段階レビュー（🧠 Opus・構造/バランス/体験・コード変更なし）

**やったこと（レビューと記録のみ・コード/JSON 変更なし＝分岐A）：**
- **機械チェック再走：** D3/D4 とも `check-dungeon-connectivity` PASS（dead edge 0・orphan 0）／`check-dungeon-integrity` は D4=エラー0警告0・D3=エラー0警告6（警告は全て寄道部屋の水`~`＝意図どおり）。`--block-room` で関門成立を確認（D3 `1,2` 遮断・D4 `1,1` 遮断でボス `0,0` 到達不能）。
- **ライブ JSON で臨界経路を目視検証：** タイル配置・`links`・`showConditions`・ボス `weakness` を `work/blade-of-lumia.json` から実データで確認。
- **D3 合格：** 弓ゲート `[1,1]` の `Y` が `[1,10]` で水/壁隔離＝「水島」教訓を適用済み（弓専用）。ゲート閉で `[0,1]/[0,0]` のみ未到達＝弓が唯一の関門・鍵`K`も別途必須＝二重ロック成立。剣のみクリア可・報酬は 9-1 表と一致。
- **D4 合格（指摘なし）：** ボス翼がテレポート専用島で部屋内迂回不能・ロウソク部屋 `[1,2]` を必ず通る "報酬先取り" 保証も成立。
- PLAN.md 9-2y の D3/D4 を `[x]`・DECISIONS.md に「Phase 9-2y」1件・本ログを追記。

**学び・気づき：**
- **D3 体験の指摘（任意改善）：** `[2,1]` の番兵`F`＋「盾で飛び道具を防げ」看板がスタート `[1,3]` から素直に辿る経路（spine）でなく枝側にあり、"盾ガードを学ぶ" 導線が寄道に埋もれている。詰みではないので現状維持可だが、9-3 のガイド導線整備で `F` を臨界手前へ寄せる/看板誘導する候補としてメモ。
- 機械チェック（接続+整合）が緑でも「導線が体験として活きているか」は別問題＝3段階レビューの③体験チェックが拾うのはまさにこの種の穴（D3 の `F` 配置）。

**▶ 次やること：** 9-2y の残り未レビュー（D1/D2/D5/D6/D7/D8）を順次、または **9-3（序盤チュートリアル＋ガイド導線・🧠→⚡）**／**暗黒の塔の本格拡張（28室+・🧠）**。9-3 着手時に D3 の `F` 配置を併せて再検討する。

### 2026-06-27 — Phase 9-2z ダンジョンマップ通路描画の正確化（⚡ Sonnet・1行修正）

**やったこと：**
- `game/ui.js` の `renderPauseDungeonMap` 内で通路セグメントを描く前に、当該部屋の `tiles` で**辺の開口を確認**するチェックを追加（4方向それぞれ）。
  - 右: `t[4][11] !== '#' || t[5][11] !== '#'`
  - 下: `t[9][5] !== '#' || t[9][6] !== '#'`
  - 左: `t[4][0] !== '#' || t[5][0] !== '#'`
  - 上: `t[0][5] !== '#' || t[0][6] !== '#'`
- `ss.visited` のセット（`game.js:301`）と参照（`ui.js:393`）は既存実装で正常動作していることを確認。
- **全 187 テストグリーン**（コード変更は ui.js 4行のみ）。

**学び：** 修正前は「隣にステージが存在すれば無条件に通路を描く」だったため、壁で塞がれた辺も通路として表示されていた。

**▶ 次やること：** 9-3（序盤チュートリアル＋ガイド導線）か暗黒の塔の本格拡張（28室+）。設計が未着手なので 🧠 Opus から。

### 2026-06-27 — Phase 9-2i dungeon_7「空中の遺跡」JSON 流し込み完了（⚡ Sonnet・22部屋・笛ワープ入場専用・D1〜D8 全8欠片ダンジョン完成）

**やったこと（JSON データ編集のみ・新規ゲームコードなし）：**
- **dungeon_7 を 22部屋で再構築**（5×5=25 から袋小路3室 `[4,0]/[0,4]/[4,4]` 欠き＝D6 と同じ）。layer メタ `bossStage:"0,0"` / `triforceId:8` / `name:"空中の遺跡"` / `bgm:"dungeon"` / `bossBgm:"boss"` 整備。
- **ボス`U`（嵐の鷲王・2×2）を `1,0`→`0,0` に再配置**（isBossRoom:true・ハートの器・石碑「其の七」`[1,5]`）。
- **臨界経路を JSON で構築：** `[1,3]ENTRY`（笛ワープ着地・`>`戻り出口 `id:"dungeon_7"`）→`[1,2]`弓ゲート（水島 `Y`→`T`・`links`）→`[1,1]`爆弾`!`壁＋弓ゲート（T gate top・Y water island）→`[1,0]`中ボスW→鍵K→（D扉）→`[0,0]`BOSS U。
- **フィールドへの笛ワープ発射元を `field/3,0`（空島ステージ）に追加**（`fluteEffect:{type:'warp', layer:'dungeon_7', stage:'1,3', row:5, col:5}`）。`field/2,0` の existing `fluteEffect:reveal`（secret_grotto 用）はそのまま保持。
- **`field/2,0 (7,9)` の徒歩 MAP_ENTER `field_dungeon7` を撤去**（ユーザー確認済み・笛ワープ専用化）。field/2,0 の sign「空中の遺跡」は「東の空島で笛を吹け」に更新。
- **接続チェッカー PASS**（`check-dungeon-connectivity.mjs dungeon_7`・orphan 0・dead edge 0）・`--block-room=1,2` でボス `0,0` 到達不能（弓ゲート関門成立）。
- **整合性チェッカー PASS**（`check-dungeon-integrity.mjs dungeon_7`・エラー0・警告0）。
- **テスト1点修正**（`heart-containers.spec.js:73` の dungeon_7 ボス部屋 `1,0`→`0,0`）＋ **`field-connectivity.spec.js` の `FOOT_DUNGEONS` から dungeon_7 を除外**（笛ワープ専用で徒歩到達不可）。
- **全 187 テストグリーン**（+0 新規テスト）。

**学び・気づき：**
- `field/2,0` の `fluteEffect` は既に `reveal` 型（secret_grotto 用）が占有済みなので D7 笛ワープ発射元は別ステージ（`field/3,0`）に分けた。fluteEffect は 1 ステージに 1 つ。
- `field-connectivity.spec.js` の FOOT_DUNGEONS からの除外が必要だった（笛ワープ専用ダンジョンは徒歩到達不可という設計判断がテストにも反映が必要）。
- `[1,1]` と `[1,2]` の右辺（col11 row4,5）が水 `~` でスクロール着地を塞ぐデッドエッジが発生→`[1,2][1,1]` の row4,5 col11 を `.` に修正。`[0,1]` の下辺（row9 col5,6）が `#` のまま→`.` に修正。`[3,3]` の右辺（欠室 `[4,3]` 方向）は `#` で閉じる必要あった。

**▶ 次やること：** **D1〜D8 全8欠片ダンジョン完成。** 次は **9-2z（ダンジョンマップ UI 強化・⚡ Sonnet）** または **9-3（チュートリアル＋ガイド導線・🧠 Opus で設計から）**。暗黒の塔の本格拡張（28室+）も積み残し。**次タスク推奨モデル：** 9-2z は ⚡ Sonnet（PLAN.md に変更箇所まで具体的に記載済み）。9-3 は設計未着手なので 🧠 Opus。

### 2026-06-27 — Phase 9-2i dungeon_7「空中の遺跡」設計確定（🧠 Opus・22部屋・笛ワープ入場＋既得6道具の総合卒業試験・新規ゲームコード/接続チェッカー拡張すべて不要・欠片是正不要・コード/JSON 変更なし）

**やったこと（設計・記録のみ・コード/JSON 変更なし）：**
- **D7 を 22部屋（5×5=25 から袋小路3室 `[4,0]/[0,4]/[4,4]` を欠く＝D6 と同じ欠き方）で設計確定**（漸増カーブ D8=24→**D7=22**）。報酬＝**欠片⑧のみ（新アイテムなし）**（triforceId=8）、ボス＝`U`嵐の鷲王（2×2・HP36/atk6/def1・`dropsTriforce:true`・`hitAndAway`・雷撃弾 range7・弱点 arrow×2＝D3報酬の弓で◎・剣でも可）。テーマ＝空中の遺跡（石畳 `o`／雲の切れ間 `~`・崩れ足場 `x` を `v`BRIDGE で渡る）。
- **進行順を確定＝D4→D6→D5→D8→D7→祭壇**。D7 入場には **D8 報酬の笛**が要る（前ダンジョンなので (A) ソフトロックに反しない）。D7 はアイテム配布の最後（次は祭壇＝翼の羽衣）。
- **入場ロック＝笛ワープ（`fluteEffect.type:'warp'`・実コード `game.js:1172-1192` で確認＝既存 `secret_grotto` が直接座標方式で実証済み）。** D8 までの reveal/torchesLit テレポート（同一レイヤー内）とは**別系統**＝フィールドの発射元に `fluteEffect:{type:'warp', layer:'dungeon_7', stage:'1,3'(ENTRY), row, col}` を置き、笛を吹くと D7 入口室へ飛ぶ＝徒歩・飛行では入れない＝笛無しで入場不可＝連鎖の最終ロック。
- **⚠️ 既存「空島 field 4,0→dark_tower（飛行ワープ）」と完全に別系統・衝突なし（調査で確認）。** あちらは翼の羽衣の飛行（`islandToTower`/`fieldToTower`）。`SKY %` は未使用。**D7 内部に SKY `%` は使わない**＝`passable.js:134` で徒歩通行不可＋翼の羽衣は祭壇（D7 クリア後）入手なので置くと詰む。内部の水/穴は `v`BRIDGE で渡る。
- **古典ゼルダ型は適用外（新アイテム無し）＝既得6道具の総合卒業試験。** 臨界経路 `[1,3]ENTRY(笛ワープ着地)→[1,2]弓ゲート(Y→T・D3 水島方式)→[1,1]爆弾!壁＋弓ゲート(D6)→[1,0]中ボスW＝鍵K→(D扉)[0,0]BOSS U→欠片⑧`。足止めは全て前ダンジョンまでの報酬（弓＝D3／爆弾＝D6／笛＝D8）で解く＝(A) OK。弓ゲートは D3 の「水島」教訓（対岸が床だと剣で叩けて破れる）を踏襲し `Y` を水/壁で隔離。
- **🔑 D6/D8 と同じく新規ゲームコード・接続チェッカー拡張すべて不要（実コード精査済み）：** 笛ワープ＋弓ゲート（`Y`→`toggleSwitch`→`links`）＋爆弾`!`壁＝全て既実装。ボス`U` は `enemies.js:301-321` 定義・`stormEagle` スプライト完成・`TRIFORCE_BOSS_TILES`/`UNLOCKED_AT.dungeon_7`（全6道具）登録済み＝ボスコードも不要。`findEntrances`（`connectivity.mjs:235`）は戻り出口 `>`（`id:"dungeon_7"`）が field の `field_dungeon7→destId:"dungeon_7"` から参照される＝入口認識成立・水/穴は `v`BRIDGE で渡る＝`--with-ladder` 等の拡張不要。→ **流し込みは純 JSON＋テスト1点（⚡ Sonnet 向き）。**
- **欠片是正は不要（D8 との違い）：** ボス`U` は現状スタブ `dungeon_7/1,0 (5,2)` に既配置＝`countTriforces()` にカウント済み。リメイクで `0,0` へ再配置するだけ（数不変）。D8 のような新規ボス配置・`Q`タイル撤去は D7 では不要。
- **実コードで前提を3点並行調査：** ①笛 warp 配線（`game.js:1172-1192`・exitRegistry・`secret_grotto` 実例）②現状 dungeon_7（2部屋スタブ・ボス`U` は `1,0 (5,2)`・入口は `field/2,0 (7,9)` の徒歩 MAP_ENTER `field_dungeon7`・空島/dark_tower 動線は別系統）③ボス`U` 定義/スプライト/登録・既得道具の必須化配線・SKY `%` の通行条件（徒歩不可・飛行のみ）。
- **記録：** PLAN.md「9-2i」節（D6/D8 同粒度＝接続グラフ/全辺の開口座標/各部屋役割/欠室の確定/流し込みチェック項目）＋9-2{d〜} 一覧・9-2y レビュー対象更新／DECISIONS.md「Phase 9-2i」1件／本ログ・現在地更新。

**学び・気づき：**
- **「笛」には2つの異なる用途がある＝reveal（同一レイヤー内の隠しタイル出現・D8 内部関門）と warp（別レイヤーへ飛ぶ・D7 入場）。** D8 は内部関門で reveal、D7 は入場ロックで warp ＝同じアイテムでも「どこをロックするか」で使う API が変わる。混同せず実コード（`game.js:1162` reveal／`:1172` warp）で確認したのが効いた。
- **連鎖の最後のダンジョンは「新アイテム無し＝既得道具の総合試験」が筋が通る。** 古典ゼルダ型(B)は「自分の報酬で解く」だが、報酬が無い D7 では適用不能＝代わりに弓(D3)/爆弾(D6)/笛(D8) を総動員する卒業試験にした。連鎖の締めとして自然。
- **SKY `%` は "空ダンジョン" でも内部に使えない罠。** 空テーマだからと SKY を地形に使うと、翼の羽衣（祭壇＝D7 後入手）が無いと徒歩で渡れず詰む。`passable.js:134` を確認して `v`BRIDGE 渡りに着地。「テーマに合うタイル」より「その時点で持っている能力で通れるか」が優先（D6 で茂み `u` を却下したのと同じ思考）。
- **設計のみで完了にしない＝流し込み時の論点を残す。** 現状 `field/2,0 (7,9)` の徒歩入口 `field_dungeon7` を残すと笛無しで入れて最終ロックが破れる＝笛ワープ専用化（徒歩入口の撤去/戻り出口専用化）が要る＝**物語/ゲームバランスに関わるのでユーザー確認が必要な論点**として明記。

**▶ 次やること：** **9-2i の D7 流し込み（⚡ Sonnet・新規コード不要）** → (1) `dungeon_7` を22室で再構築（layer メタ `bossStage:"0,0"`/`triforceId:8`/`name:"空中の遺跡"`・弓ゲート `[1,2]`水島Y→T・爆弾!壁＋弓ゲート `[1,1]`・ボス`U`を `1,0`→`0,0` 再配置）→ (2) フィールドの笛ワープ発射元（`fluteEffect:warp→dungeon_7/1,3`）配置→ (3) 徒歩入口 `field_dungeon7` の処理（笛ワープ専用化＝要ユーザー確認）→ (4) テスト1点（heart-containers `1,0`→`0,0`・lore-tablets は `0,0/[1,5]` 其の七で変更不要要確認）→ `check-dungeon-connectivity dungeon_7` PASS（`--block-room=1,2` でボス遮断）／`check-dungeon-integrity dungeon_7` エラー0／`fromEditor=1` 実プレイ（笛ワープ→弓→爆弾→中ボス→鍵→D扉→ボスU→欠片⑧）。**→ D1〜D8 全8欠片ダンジョン完成。** 続いて 9-3（チュートリアル＋ガイド導線）・暗黒の塔の本格拡張（28室+）・9-2y の Opus レビュー（D3/D4 待ち）。

### 2026-06-27 — Phase 9-2h dungeon_8「沼地の神殿」流し込み完了（⚡ Sonnet・23部屋・レイヤー新設・欠片是正・笛移設・チェッカー PASS・全187テストグリーン）

**やったこと：**
- **dungeon_8 を 23部屋（5×5=25 から `[4,0]`・`[0,4]` の2室を欠いて23）で新設。** レイヤーメタ `bossStage:"0,0"`・`triforceId:7`・`name:"沼地の神殿"`・`bgm:"dungeon"`・`bossBgm:"boss"` を設定。
- **臨界経路の構築：** `[1,3]ENTRY`（row3 横一文字毒沼 `~`・はしごで縦断）→ `[1,2]`笛入手（宝箱）→ `[1,1]`沼の祭壇（`fluteEffect:{type:'reveal'}` + 隠し`>`が `flutePlayed` で出現 → `[1,0]`へワープ）→ `[1,0]`中ボス`W`（killAll→鍵K）→ D扉 → `[0,0]`ボス`I`沼地の大蝦蟇（2×2・fire弱点・hitAndAway・dropsTriforce:true）→ 欠片⑦。ボス翼はテレポート専用島（D4/D6 と同型）。
- **寄道サブ網：** `[0,1][0,2][0,3][2,0][2,1][2,2][2,3][2,4][3,0][3,1][3,2][3,3][3,4][4,1][4,2][4,3][4,4][1,4]`（17室）を辺スクロール接続。`[3,1]`コンパス`n`・`[3,2]`地図`m`・`[3,0][3,3]`石押し謎・`[2,1]`セントリー`F`。
- **欠片是正：** `field/2,0 tiles[6][10]='Q'`→`.`・`cave_1/1,0 tiles[8][10]='Q'`→`.`（直置き `Q` タイル2枚撤去）＝`countTriforces()` はボスドロップ8体のみ。
- **笛移設：** `field/2,0 chestContents["4,3"]` 笛宝箱を撤去・D8 `[1,2]` に移設（9-2a 表 #6 是正）。
- **フィールド入口：** `field/3,1 [2,4]` に `>`MAP_ENTER（`{id:"field_dungeon8", destId:"dungeon_8"}`）を配置。
- **整合性チェッカー：** `UNLOCKED_AT.dungeon_8 = Set(['boomerang','bow','candle','bomb','ladder'])` を追加。`check-dungeon-integrity.mjs dungeon_8` → ❌0 / ⚠️0 ✅ / `check-dungeon-connectivity.mjs dungeon_8 --with-ladder` → PASS orphan 0・dead edge 0・全23室到達 ✅ / `--block-room=1,1` → ボス到達不能 ✅
- **テスト整備：** `heart-containers.spec.js` の `BOSS_ROOMS` に `dungeon_8/0,0` 追加・`lore-tablets.spec.js` に D8「其の八」追加（9石碑・ユーザー確認済み）・`swamp-boss.spec.js` ④ を「dungeon_8/0,0 のみ配置」に更新。全187テストグリーン。

**学び・気づき：**
- **`[4,0]`・`[0,4]` の2室が欠けており23部屋（PLAN の "24部屋" は計算ミス）。** テーブルが正、数字が誤り。実際の5×5=25 から2室欠け=23。
- **`fluteEffect:{type:'reveal'}` は stageData 最上位のプロパティとして置く。** D6 の `torchesLit` と完全に同型の配線で問題なし。
- **`showConditions` の trigger 差し替えだけで笛関門が実現できる。** D6 の教訓どおり、既存配線の組み合わせで十分。

**▶ 次やること：** **D7「空中の遺跡」設計（🧠 Opus）** → 笛ワープ入場・新アイテムなし（既得道具の総合ステージ）・ボス`U`嵐の鷲王・欠片⑧。並行して 9-3（チュートリアル＋ガイド導線）。D3/D4 は 9-2y の Opus 3段階レビュー待ち。

### 2026-06-27 — Phase 9-2h dungeon_8「沼地の神殿」設計確定（🧠 Opus・24部屋・レイヤー新設・はしごで毒沼の堀を渡る入場＋笛で隠し`>`関門・新規ゲームコード/接続チェッカー拡張すべて不要・コード/JSON 変更なし）

**やったこと（設計・記録のみ・コード/JSON 変更なし）：**
- **D8 を 24部屋（5×5=25 から袋小路1室 `[4,0]` を欠く）で `dungeon_8` 新設レイヤーとして設計確定**（漸増カーブ D6=22→**D8=24**）。報酬＝**笛＋欠片⑦＋ミラーシールド**（triforceId=7）、ボス＝`I`沼地の大蝦蟇（2×2・HP40/atk5/def2・`dropsTriforce:true`・`hitAndAway`・毒沫投げ・弱点 fire×2）。テーマ＝沼地の神殿（毒沼 `~`・足場 `x`・茂み `u`・木 `t`）。
- **進行順を再確認＝D4→D6→D5→D8→D7**。D8 入場には **D5 報酬のはしご**が要る（前ダンジョンなので (A) ソフトロックに反しない）。D7 は新アイテムを配らない＝D8 が連鎖の**最後の道具配布**＝(A) を自動的に満たす。
- **入場ロック＝はしごで毒沼の堀を渡る（D5 と同型）。** `[1,3]ENTRY` の row3 に横一文字 `~`毒沼（1セル幅）を張り、`isLadderBridge`（`passable.js:87-93`）で縦の1セル橋を渡る以外に奥へ道なし。**接続チェッカーは D5 で `--with-ladder` 拡張を移植済み**＝D8 では新規拡張不要（`check-dungeon-connectivity.mjs dungeon_8 --with-ladder` でそのまま検証可）。
- **笛必須化＝`flutePlayed`reveal で隠し `>`MAP_ENTER を出すテレポート方式（D4/D6 の `torchesLit` をトリガー差し替えただけの同型・既存実装）。** `conditions.js:96`（`flutePlayed` トリガー）／`game.js:1147-1170`（笛 reveal で `ss.flutePlayed=true`→`evaluateConditions()`）／`render-board.js:139,251,329`（隠しタイル描画）が配線済み。`[1,1]` 沼の祭壇に `fluteEffect:{type:'reveal'}` を置き、笛を吹く→隠し `>`（`showConditions:flutePlayed`）出現→ `[1,0]` ボス翼へワープ。**warp 型にしない理由＝**内部関門は D4/D6 と検証パターンを揃える（テレポート専用島の作り方が同一）。**笛は剣で代替できず完全に堅牢**（`flutePlayed` は笛を吹く以外に立たない＝D6 で却下した茂み `u` のソフトロックが起きない）。
- **🔑 D8 固有の最重要事項＝レイヤー新設＋欠片是正（実データ精査で確定）：** `dungeon_8` はマップに**存在しない**＝ゼロから新設。`cave_1` は 9-2c の revert で小洞窟に戻っており、**沼地ボス`I` はマップ上のどこにも置かれていない**（placement 0件）。現状 `countTriforces()`（`shared/triforce.js:16-31`）＝ボス`dropsTriforce`×7（G/N/J/A/L/O/U）＋直置き `Q`×2（`field/2,0[6,10]`・`cave_1/1,0[8,10]`）＝**計9**で8前提とズレ。→ 流し込みで「ボス`I` を `dungeon_8/0,0` 配置（→ボス8体）＋`Q`タイル2枚撤去（→直置き0）」を**同時是正**して `countTriforces()=8` ちょうどにする。
- **🔑 新規ゲームコード不要だがテスト拡張は要る（D6 より重い）：** 関門機構（`flutePlayed`reveal＋`--with-ladder`）・ボス`I`（`enemies.js:355` 定義／`TRIFORCE_BOSS_TILES['I']` 登録／スプライト codegen 済み）はすべて既存資産＝コード新規ゼロ。ただし新設レイヤーなので① `heart-containers.spec.js` `BOSS_ROOMS` に `dungeon_8/0,0` 追加（現状 D1〜7・`:67-73`）／② `lore-tablets.spec.js` `TABLETS`（現状8石碑「其の一〜七＋終章」・`:19-28`）に D8「其の八」追加で9石碑＝**物語断片名の整理（其の八/終章の順）が要る＝流し込み時にユーザー確認**／③ `UNLOCKED_AT` に `dungeon_8:Set(['boomerang','bow','candle','bomb','ladder'])` 追加。笛 reveal は既存 `flute.spec.js`（4本）がカバー＝D8 専用笛テスト不要。
- **実ツールで前提を検証：** `check-dungeon-integrity.mjs all` で現状確認（dungeon_8 なし・cave_1 は bossStage/m/n 無しエラー＝小洞窟のまま正しい）。`countTriforces()` ロジックを読み「ボス`dropsTriforce`＋直置き `Q`＋`X`DARK_LORD の和」と確認。ボス`I` の全登録（enemies/tiles/sprites/integrity）を確認。
- **記録：** PLAN.md「9-2h」節（D5/D6 同粒度＝接続グラフ/全辺の開口座標/各部屋役割/欠室の確定/レイヤー新設＋欠片是正/テスト拡張のチェック項目）＋9-2{d〜} 一覧更新／DECISIONS.md「Phase 9-2h」1件／本ログ・現在地更新。

**学び・気づき：**
- **「新設レイヤー」は流し込みコストが既存レイヤー編集より一段重い。** 関門機構が全部既存資産（D6 と同じ "組合せだけ"）でも、新設だと**テスト配列（heart-containers/lore-tablets）・`UNLOCKED_AT`・field 入口・欠片カウント**の整合を別途取る必要がある。D6（既存レイヤー編集）の「純 JSON＋テスト2点」より項目が多い。
- **欠片の整合は "ボスの placement" と "直置き `Q`" の両方を見る。** PROGRESS の過去記述「現状 `Q` が2枚あり実質9」が実データと一致＝沼地ボス`I` が未配置のままなので、D8 配置と `Q` 撤去をセットでやらないと8にならない。`countTriforces()` を実際に読んで「ボス`dropsTriforce`＋`Q`＋`X`」の和だと確認したのが効いた。
- **物語の石碑は8個固定で設計されていた**（其の一〜七＋終章＝dark_tower）。D8 を9個目のダンジョンにすると石碑も9個になり物語断片の振り直しが要る＝**物語設計に踏み込むのでユーザー確認が必要な唯一の論点**（地形・機構は機械検証で閉じる）。

**▶ 次やること：** **9-2h の D8 流し込み（⚡ Sonnet・新規コード不要）** → (1) `dungeon_8` を24室で新設（layer メタ `bossStage:"0,0"`/`triforceId:7`/`name:"沼地の神殿"`・毒沼の堀 `[1,3]`・笛宝箱 `[1,2]`・沼の祭壇 `[1,1]` に `fluteEffect:reveal`＋隠し`>`・ボス`I`を `[0,0]`）→ (2) 欠片是正（`Q`タイル2枚撤去＝`field/2,0[6,10]`・`cave_1/1,0[8,10]`→`.`）→ (3) 笛移設（`field/2,0 chestContents["4,3"]` 撤去＝9-2a 表 #6）→ (4) `UNLOCKED_AT.dungeon_8` 追加＋field 入口 `>`（`destId:dungeon_8`）1枚→ (5) テスト3点拡張（heart-containers/lore-tablets/物語断片＝物語は要ユーザー確認）→ `check-dungeon-connectivity.mjs dungeon_8 --with-ladder` PASS／`check-dungeon-integrity.mjs dungeon_8` エラー0／実プレイ確認。続けて D7「空」（笛ワープ・新アイテムなし＝既得アイテム総合ステージ）を 🧠 Opus 設計。並行して 9-3。

### 2026-06-27 — Phase 9-2g dungeon_6「森の聖域」流し込み完了（⚡ Sonnet・22部屋・チェッカー PASS・全187テストグリーン）

**やったこと：**
- **dungeon_6 を 22部屋（5×5=25 から `[4,0]/[0,4]/[4,4]` 欠き）に流し込み。** レイヤーメタ `bossStage:"0,0"`・`triforceId:5`・`name:"森の聖域"`・`bgm:"dungeon"`・`bossBgm:"boss"` を設定。
- **臨界経路の構築：** `[1,3]ENTRY → [1,2]爆弾入手（`B`宝箱）→ 上辺`!`壁（爆弾で割る）→ `[1,1]`かがり火の間（`H`×3 全点灯 → 隠し`>`テレポート）→ `[1,0]`中ボス`W`（killAll→鍵K）→ D扉 → `[0,0]`ボス`O` 古森の巨人（fire弱点・hitAndAway・dropsTriforce:true）→ 欠片⑤`。ボス翼は grid 全辺 `#`WALL のテレポート専用島（D4 と同型）。
- **寄道サブ網：** `[0,1][0,2][0,3][2,0][2,1][2,2][2,3][2,4][1,4][3,0][3,1][3,2][3,3][3,4][4,1][4,2][4,4]`（17室）を辺スクロール接続。`[3,1]`コンパス`n`・`[3,2]`地図`m`・`[3,0][3,3]`石押し謎・`[4,1]`妖精・`[2,1]`セントリー`F`。
- **爆弾移設：** `field/1,1 floorItems["4,6"]` 削除・`tile[4][6]='.'`・`field/2,0 shopData["6,7"]` から bomb 除去。D6 `[1,2]` の宝箱が唯一の爆弾入手点。
- **テスト修正：** `heart-containers.spec.js` の `dungeon_6` ボス部屋 `1,0`→`0,0`。`lore-tablets.spec.js` は `dungeon_6/0,0/[1,5]` 石碑が既存データに配置済みで変更不要。
- **チェッカー全 PASS：** `check-dungeon-connectivity dungeon_6` orphan 0・dead edge 0・全22室到達 ✅ / `--block-room=1,1` → boss 到達不能 ✅ / `check-dungeon-integrity dungeon_6` ❌0 ⚠️0 ✅ / 全187テストグリーン。

**学び・気づき：**
- D4/D5 で作った機構（`torchesLit`テレポート＋`!`爆弾壁）を組み合わせるだけで D6 が成立した。「前段ダンジョンの機構を再利用する」設計は後続の流し込みコストを大幅に削減する。
- 欠室（[4,0]/[0,4]/[4,4]）の代わりに [4,4] を追加して22室ちょうどにした（PLAN のグリッド図では [4,4] は袋小路として存在する）。

**▶ 次やること：** **D8「沼地の神殿」（`dungeon_8` 新設・9-2{d〜} 続き）を 🧠 Opus で設計。** 連鎖位置（cave_1 の笛 → D8 → D7）・フィールド入口・沼地ボス`I`配置・欠片⑧。並行して 9-3（チュートリアル・ガイド導線）。

### 2026-06-27 — Phase 9-2g dungeon_6「森の聖域」設計確定（🧠 Opus・22部屋・ロウソクでかがり火関門＋爆弾で!壁・新規ゲームコード/接続チェッカー拡張すべて不要・コード/JSON 変更なし）

**やったこと（設計・記録のみ・コード/JSON 変更なし）：**
- **D6 を 22部屋（5×5=25 から隅の袋小路3室 `[4,0]/[0,4]/[4,4]` を欠く）で設計確定**（漸増カーブ D5=20→**D6=22**→D7=22）。報酬＝**爆弾＋欠片⑤**（triforceId=5）、ボス＝`O`古森の巨人（2×2・HP42/atk5/def2・`dropsTriforce:true`・`hitAndAway`・弱点 fire×2）。テーマ＝森の聖域（草地＋かがり火 `H`・茂み `u`・木 `t`）。
- **進行順を再確認＝D4→D6→D5→D8→D7**。D6 入場には **D4 報酬のロウソク**が要る（前ダンジョンなので (A) ソフトロックに反しない）。
- **ロウソク必須化＝D4 と同じ `torchesLit`→隠し `>`テレポート堅牢方式を採用（ユーザーが AskUserQuestion で確定）。** 代替の「茂み `u` を bushBurned で焼く森らしい方式」は実コード精査で却下：**茂みは剣でも切れて通行可化する**（`combat.js:315`）＝先に剣で切ると `cutBushes` 済みになり、後からロウソクを使っても `game.js:1264` が "もう燃え尽きている" を返して `ss.bushBurned=true` を実行しない＝**ロウソク必須化が「剣で先に切る」だけで永久に破れるソフトロックの芽・整合性チェッカーは `showConditions` を見ないので検出不能**。かがり火 `H` は剣で触れられない（`passable.js` 常に passable:false・`combat.js` は H 非処理）＝完全にロウソク専用関門。
- **ボス翼前ロック＝D6 自身の報酬「爆弾」で `!`BREAKABLE_WALL を割る（古典ゼルダ型(B)）。** `[1,2]爆弾入手→[1,1]かがり火関門` の境界に `!`壁を置き「爆弾→ロウソク」の順序を強制。接続チェッカーは `!` を **SOLVABLE_GATE（爆弾で開く）として既に認識**（`connectivity.mjs:68`）＝D5 の `!`入場ゲートと同じく無改修で PASS。
- **🔑 D5 との最大の違いを確認＝新規ゲームコード・接続チェッカー拡張がすべて不要。** D5 は「はしご渡り拡張＋ボス近接限定/遠隔反射の3ファイル新規」が要ったが、D6 は **D4 実装済み `torchesLit`テレポート＋D5 で使った `!`爆弾壁の組合せ**＝新サブシステムゼロ。ボス`O` は `shared/enemies.js:274-297` 定義済み・`TRIFORCE_BOSS_TILES['O']`/`UNLOCKED_AT.dungeon_6=[…,'bomb']` 登録済み（9-2f で修正済み）。→ **流し込みは純 JSON 編集＋テスト座標調整のみ＝⚡ Sonnet 向き**。
- **実ツールで前提を検証：** `check-dungeon-connectivity.mjs dungeon_4`（candle テレポート島が PASS・`--block-room=1,1` でボス到達不能）／`dungeon_5`（`!`入場壁＋はしご堀）を実行し、D6 が依拠する2機構が機械検証可能と確認。`check-dungeon-integrity.mjs dungeon_6`（現状エラー4＝bossStage/triforceId/地図m/コンパスn 未設定＝流し込みで解消）。
- **記録：** PLAN.md「9-2g」節（D4/D5 同粒度＝接続グラフ/全辺の開口座標/各部屋役割/欠室の確定/流し込みチェック項目）＋9-2{d〜} 一覧更新／DECISIONS.md「Phase 9-2g」1件／本ログ・現在地更新。

**学び・気づき：**
- **「報酬の必須化」の堅牢さは "そのギミックが報酬以外で触れられないか" で決まる。** ロウソク（D4・D6）は `H`かがり火＝剣で触れない＝堅牢。茂み `u` は剣でも切れる＝ロウソク専用にならない（しかも `cutBushes` 済み→ロウソク無効化の順序ソフトロックがある）。森テーマだからと bushBurned に飛びつかず、実コードの "触れられ方" を精査して `torchesLit` に着地した。
- **設計の重さはダンジョンごとに大きく違う。** D5 は新規コード3ファイル＋チェッカー拡張＝Opus 設計→Opus 実装が要ったが、**D6 は既存機構の組合せだけ＝設計確定後の流し込みは純 Sonnet 作業**。「直前のダンジョンで作った機構を組み合わせる」設計にすると後段が軽くなる。
- **設計のみで完了にしない。** 流し込み（22室 JSON＋メタ＋爆弾移設＋テスト2点修正）後に `check-dungeon-connectivity`（PASS・`--block-room=1,1` でボス遮断）＋`check-dungeon-integrity`（エラー0）＋`fromEditor=1` 実プレイ（爆弾で!壁→ロウソクでかがり火→テレポート→中ボス→鍵→D扉→ボス炎/剣撃破→欠片⑤）まで検算して初めて D6 完了。

**▶ 次やること：** **9-2g の D6 流し込み（⚡ Sonnet・新規コード不要）** → (1) dungeon_6 を22室へ流し込み（layer メタ `bossStage:"0,0"`/`triforceId:5`/`name:"森の聖域"`・かがり火関門 `[1,1]`・爆弾宝箱 `[1,2]`・`!`壁・ボス`O`を `[0,0]` へ）→ (2) 爆弾移設（`field/1,1` floorItem/tile・`field/2,0` shop 撤去＝9-2a 表 #2）→ (3) テスト2点修正（heart-containers `1,0`→`0,0`・lore-tablets の石碑をボス部屋 `[0,0][1,5]`）→ `check-dungeon-connectivity.mjs dungeon_6` PASS／`check-dungeon-integrity.mjs dungeon_6` エラー0／実プレイ確認。続けて D8「沼地」（新設レイヤー・笛・沼地ボス`I`配置）・D7「空」（笛ワープ）を 1つずつ 🧠 Opus 設計。並行して 9-3。

### 2026-06-27 — Phase 9-2f dungeon_5「氷の廃墟」流し込み完了（⚡ Sonnet・20部屋・チェッカー PASS・melee-only-boss 7本・connectivity-tool 7本追加・全187テストグリーン）

**やったこと：**
- **`connectivity.mjs` に `--with-ladder` 拡張（PLAN 9-2f・第2タスク）：**
  - `isLadderBridgeCell(tiles, rows, cols, r, c)` を新設（`passable.js:isLadderBridge` の静的版。上下または左右が両方陸なら1セル水/穴を橋とみなす）。
  - `bfsLayer()` に `withLadder` オプションを追加（trueのとき `isLadderBridgeCell` が成立する水/穴を通行可として扱う）。`cross()`（辺越え到達チェック）も同様に拡張し、橋セルでの dead edge 誤報を抑止。
  - `findOrphanRooms()` の `passOpenCell()` を更新し、1セル橋の水/穴を孤立判定でも通行可（ダンジョン内で必ず入手できるはしごは鍵と同格）。
  - `check-dungeon-connectivity.mjs` に `--with-ladder` フラグを伝播。
  - 検証：`dungeon_5 --with-ladder` → PASS（20部屋全到達）／フラグ無し → `[1,0][0,0]` unreached（水堀ゲート成立）／`--block-room=1,1` → boss 到達不能。
- **ボス`L`「近接限定・遠隔反射」実装：**
  - `shared/enemies.js` の `ICE_LEVIATHAN` に `meleeOnly:true`・`reflectsProjectiles:true` を追加。
  - `game/combat.js` `dealDamageToEnemy`：`meta.meleeOnly` かつ `atkType ∉ {sword,fire}` なら dmg=0（dmgPopup 0 で「効かない」を示す）。
  - `game/projectile.js` `checkProjHit`：`reflectsProjectiles` の敵に当たった player 投擲物は `owner='enemy'`・dx/dy 反転・即離して戻す（既存 Phase 7-2 の盾 reflect と同形）。
- **整合性チェッカー `UNLOCKED_AT` 修正：** `dungeon_5` に `'bomb'` 追加（D6 で入手済みの進行順が正しい）・`dungeon_6` から `'ladder'` 除去（D6 時点では未所持）。
- **dungeon_5 を20部屋に流し込み：** 5×4フル・入口 `[1,3]`（爆弾壁 `!`）・はしご入手 `[1,2]`・水堀 `[1,1]`（row3 `~` 10列）・中ボス `[1,0]`（`W` killAll→鍵）・ボス `[0,0]`（`L`・D扉・ハートの器チェスト・lore tablet「其の五」）・鉄の盾 `[0,1]`・地図 `[3,2]`・コンパス `[3,1]`・寄道複数。layer メタ（`bossStage:"0,0"` / `triforceId:6` / `name:"氷の廃墟"`）整備。
- **heart-containers.spec.js 修正：** dungeon_3/4 のボス部屋を実際の `0,0` に合わせて修正（旧テストが `1,0` を指していた）。dungeon_5 も `0,0` を参照。
- **新規テスト：** `tests/melee-only-boss.spec.js`（7本）・`connectivity-tool.spec.js` に `--with-ladder` テスト（7本）追加。

**学び・気づき：**
- **`findOrphanRooms()` の `passOpenCell` は孤立判定なので "常にはしご考慮" が正しい設計。** はしごはダンジョン内で必ず入手できる（鍵/ゲートと同格）なので、水/穴を孤立の判定に使うのは誤り。PLAN.md の「デッドエッジ／孤立の判定は常にはしご考慮」が設計意図として正確だった。
- **遠隔反射の実装は「owner='enemy' に変更して飛ばし続ける」だけ。** 既存の `player→enemy` 向きの reflect（Phase 7-2）と鏡像で、dx/dy 反転 + 少し離す = 全く同じパターン。盾 reflect を先に作っておいたことが D5 実装を軽くした。
- **`isLadderBridgeCell` は tiles の静的解析のみ。** `passable.js` の `isLadderBank` はゲームエンジン依存（getStageData()）だが、connectivity.mjs は tiles を直接受け取るのでシンプルに移植できた。

**▶ 次やること：** **D6「森の聖域」設計（🧠 Opus）** → 報酬＝爆弾・ボス（未定）・弱点（未定）・22部屋（漸増 D5=20→D6=22）・臨界経路確定 → 設計確定後に ⚡ Sonnet 流し込み。続けて D8（沼地・新設）・D7（笛）。並行して 9-3。

### 2026-06-25 — Phase 9-2f dungeon_5「氷の廃墟」設計確定（🧠 Opus・20部屋・はしご報酬＋ボス近接限定/遠隔反射・コード/JSON 変更なし）

**やったこと（設計・記録のみ・コード/JSON 変更なし）：**
- **D5 を 20部屋（5×4・欠けなし）で設計確定**（漸増カーブ D4=20→D5=20）。報酬＝**はしご＋欠片⑥＋鉄の盾**、ボス＝`L`氷のリヴァイアサン（2×2・HP40・弱点 fire×3）。テーマ＝氷の廃墟（凍湖 `~`・割れ氷 `x` を地形の主役に）。
- **進行順を再確認＝D4→D6→D5→D8→D7**（連鎖表・数字順でない）。D5 入場には **D6 報酬の爆弾**で `!`壁を割る（前ダンジョンなので (A) ソフトロックに反しない）。
- **はしご必須化を実コード精査で "2つの保証" に着地：** ① **地形**＝`[1,1]` に横一文字水堀 `~`(row3)＝はしごで縦の1セル橋を渡る以外にボス翼へ道なし（`passable.js:87` isLadderBridge＝進入軸の両隣が陸の1セル水/穴のみ渡れる・2連続不可）。② **ボス**＝D5 では弓/ブーメラン/ビーム/爆弾を全所持＝堀の対岸からボスを撃てるなら必須化が崩れるので、**ボス`L` を `meleeOnly`＋`reflectsProjectiles`**（遠隔は反射・剣/ロウソク炎の近接だけ通る）。弱点 fire×3 とも噛み合う（ユーザー案）。
- **接続チェッカー拡張が必須と判明（D3/D4 と本質的に違う点）：** `connectivity.mjs` は `~`水・`x`穴を `HARD_BLOCKED`（壁）扱いで「はしご渡り」概念がない＝はしご渡りを臨界経路に置くとデフォルトで boss 到達不能を誤検出する。→ `isLadderBridge` 相当を移植し **`--with-ladder` で1セル橋を通行可と判定**できるよう拡張する設計を確定（ユーザー指示）。
- **ボス近接限定＝既存の盾 reflect 機構（`projectile.js:269-284`）の流用で軽く実装できると確認。** ⚠️ 爆弾は `checkProjHit` を通らず `projectile.js:547` 直行なので、近接限定ガードは `combat.js dealDamageToEnemy`（`meleeOnly` なら `atkType∉{sword,fire}` はダメ0）に置く＝反射（視覚）＋無効化（実効）の二層。
- **整合性チェッカーの潜在バグ発見：** `UNLOCKED_AT.dungeon_5` が `ladder` のみで `bomb` 無し・`dungeon_6` に `ladder` 有り＝**数字順で進行順と食い違う**。流し込み時に `dungeon_5` へ bomb 追加・`dungeon_6` から ladder 除去を同時修正する旨を PLAN に明記。
- **記録：** PLAN.md「9-2f」節（D4 同粒度・接続グラフ/開口座標/各部屋役割/新規コード3点のチェック項目）＋DECISIONS.md「Phase 9-2f」1件＋本ログ・現在地更新。

**学び・気づき：**
- **「報酬の必須化」は報酬ごとに配線が全く違う。** ロウソク（D4）は `torchesLit`→隠しテレポート、はしご（D5）は「1セル水/穴橋＋接続チェッカー拡張＋ボス近接限定」。**特にはしごは接続チェッカーが水/穴を壁扱いするので、D3/D4 で確立した `--block-room` だけの検証では足りない**＝チェッカー自体を拡張しないと "はしご関門が正しい一本道か" を機械保証できない。
- **遠隔武器を全部持った終盤では "地形の堀" だけでは必須化できない。** ユーザー案の「ボス近接限定＋遠隔反射」が本質的な解＝**プレイヤーの能力（遠隔射程）を逆手に取る関門**は、地形ではなく敵の性質で作る方が堅い。既存の盾 reflect コードがそのまま雛形になるのも好都合。
- **設計のみで完了にしない。** 流し込み＋新規コード実装後に実プレイ（or fixture 実エンジンテスト）で「はしごで堀を渡る／遠隔はボスに反射・剣/炎は通る」を検算するまで D5 は未完。

**▶ 次やること：** **9-2f の流し込み＋新規コード実装（⚡ Sonnet）** → (1) `connectivity.mjs` はしご拡張（`--with-ladder`）＋`connectivity-tool.spec.js` 追加 → (2) ボス`L` 近接限定/遠隔反射（`enemies.js`/`combat.js`/`projectile.js`）＋`melee-only-boss.spec.js` 追加 → (3) 整合性チェッカー `UNLOCKED_AT` 修正 → (4) dungeon_5 を20部屋へ流し込み（はしご宝箱を D3→D5 移設・鉄の盾配置）→ `check-dungeon-connectivity.mjs dungeon_5 --with-ladder` PASS／`check-dungeon-integrity.mjs dungeon_5` エラー0／実プレイ確認。続けて D6「森の聖域」（爆弾）・D8 新設（沼地）・D7（空）を 1つずつ 🧠 Opus 設計。並行して 9-3。

### 2026-06-25 — Phase 9-2e dungeon_4「炎の神殿」JSON 流し込み完了（⚡ Sonnet・20部屋・チェッカー PASS・candle-gate テスト3本グリーン）

**やったこと：**
- **dungeon_4 を2部屋スタブ→20部屋（5列×4行=20・欠けなし）に再構築。** layer メタを D1〜3 形式（`bossStage:"0,0"`/`triforceId:4`/`name:"炎の神殿"`/`bgm`/`bossBgm`）に整備。旧 `0,0`/`1,0` の内容を一新し、PLAN.md 9-2e で確定した20室レイアウト・接続・役割を JSON に流し込み。
- **接続チェッカー・整合性チェッカー PASS**（`check-dungeon-connectivity.mjs dungeon_4`：dead edge 0・orphan 0・全20室到達。`--block-room=1,1` でボス到達不能＝gate holds。`check-dungeon-integrity.mjs dungeon_4`：エラー0警告0）。
- **テスト修正（3件）：** 旧スタブ参照でエラーになった `lore-tablets.spec.js`（石碑を `0,0[1,5]` に配置）と `heart-containers.spec.js`（中ボス部屋 `1,0` にハートの器チェストを追加）。全 **174 テストグリーン**（元171+新3）。
- **`tests/candle-gate.spec.js` 新設（3本）**：フィクスチャ `test_mechanics/candle_gate` を追加し「ロウソクで3かがり火を全点灯→conditionsMet[4,9]成立」「剣のみ・弓矢では不可」を実エンジンで固定（`getPlayer()` でプレイヤー位置をテレポートして各トーチ前に移動）。

**学び・気づき：**
- **既存テストが旧スタブの座標を参照しており、流し込み時に必ず修正が要る。** `lore-tablets` は旧ボス部屋 `0,0` に石碑を期待、`heart-containers` は `isBossRoom=true` の部屋一覧をハードコードしていた。流し込み前にテストの期待値を確認しておくことで先回りできる。
- **テスト内の移動は `MOVE_STEP=0.5` なので1セル=2回 `walk`。** 複数トーチへの到達には `getPlayer()` で直接位置を設定する方が確実（`bow-gate.spec.js` の射撃ポジションと同じ発想）。

**▶ 次やること：** **D5「氷の廃墟」設計（🧠 Opus）** → 報酬＝はしご・ボス`L`・弱点 fire（ロウソク＝D4報酬）・20部屋・臨界経路確定 → 設計確定後に ⚡ Sonnet 流し込み。続けて D6「森の聖域」（爆弾）・D8 新設（沼地）・D7（笛）。並行して 9-3。

### 2026-06-25 — Phase 9-2e dungeon_4「炎の神殿」設計確定（🧠 Opus・20部屋・コード/JSON 変更なし）：ロウソク報酬・かがり火全点灯→隠しテレポートでボス翼へ

**やったこと（設計・記録のみ・コード/JSON 変更なし）：**
- **D4 を 20部屋（5列×4行=20・欠けなし）で設計確定**（漸増カーブ D3=18→D4=20 準拠）。報酬＝**ロウソク＋欠片④**、ボス＝`A`炎のサラマンドラ（HP35・弱点 arrow×2＝D3 で得た弓で◎・通常剣でも可）。テーマ＝炎の神殿（床 `o` 石畳＋`H`かがり火・水/壊せる壁は使わない＝後ダンジョン報酬を要求しないため）。
- **ロウソク必須化の配線を実コードで精査して確定（最重要）：** 「ロウソク専用かつ "通行を開く"」配線は**`torchesLit`/`bushBurned`→`showConditions` で隠し `>`MAP_ENTER を出現**だけと判明。剣は茂みを切るが `bushBurned` を立てない（combat.js:310）・ブーメラン炎運搬は点いた篝火がある時だけ点火（projectile.js:333）・矢/ビームは篝火無効・`torchesLit` は `openGates` でなく `showConditions` 出現のみ駆動＋MAP_ENTER 遷移は `conditionsMet` を見る（game.js:1017）。→ **`[1,1]` かがり火の間に消灯 `H`×3（`initLitTorches` 空）を置き、ロウソク全点灯で隠し `>` 出現→ボス翼へワープ**。事前点灯ゼロなので**ロウソク以外では1本も点けられない＝完全に専用関門**。
- **ボス翼をテレポート専用の島に（D3 の穴①対策）：** D3 流し込みで「接続チェッカーは部屋内で飛び道具/篝火が届くかを見ない＝grid 隣接だと部屋内迂回で破れる」と学んだので、ボス翼 `{[1,0]中ボス+鍵, [0,0]ボス}` の grid 隣接辺を**全て `#`WALL**で塞ぎ、`[1,1]→[1,0]` を隠し `>`テレポートのみにした＝ロウソク必須が "唯一の経路" として地形で保証。`--block-room=1,1` でボス到達不能を確認する設計（D3 と同型の二重保証）。
- **ロウソク先取りの地形保証：** `[1,1]` の walk 隣接を `[1,2]`（spine）と `[0,1]`（枝）だけにし（右・上は壁）、枝 `[0,1]/[0,2]` は `[1,1]` からのみ入れる＝`[1,1]` への walk 経路は必ずロウソク部屋 `[1,2]` を通る（D3 より強い保証）。
- **全20室の役割＋全21辺の開口座標（縦=同col5,6／横=同row4,5）＋テレポート1組を PLAN.md 9-2e に書き出し**（D1〜D3 と同粒度＝Sonnet が座標にタイルを置くだけで流し込める状態）。layer メタ（`bossStage:"0,0"`/`triforceId:4`/`name:"炎の神殿"`）＋ボス部屋 `1,0`→`0,0` 再配置＋石碑「ザーネルの記憶 其の四」を入口 `[1,3]` へ移設も明記。
- **`A` は整合性チェッカー `TRIFORCE_BOSS_TILES`＋`UNLOCKED_AT.dungeon_4={boomerang,bow,candle}` に既登録を確認**＝チェッカー変更不要。
- **記録**：PLAN.md「9-2e」節（20部屋版）＋9-2{d〜}・9-2y 一覧更新／DECISIONS.md「Phase 9-2e」1件／本ログ・現在地更新。

**学び・気づき：**
- **「報酬アイテムを必須化できるか」は実コードの配線次第で、机上の "ギミック名" では決められない。** ロウソクは「茂みを焼く/篝火を点ける」が、**通行を開く（道を作る）配線は `showConditions`→隠し `>` だけ**で `openGates` には繋がっていない。9-2 鉄則「配線確認済みの仕掛けだけで関門を作る」を D4 で具体的に適用＝torchesLit→テレポートに着地。
- **D3 の穴①（部屋内迂回）を設計段階で先回り。** 飛び道具/篝火スイッチの関門は「関門部屋とボスを grid 隣接で繋がない＝テレポート島にする」か「スイッチを水/壁で完全隔離する」まで設計しないと破れる。D4 はテレポート島方式を採用＝接続チェッカーの守備範囲（部屋間の辺）だけで必須化が保証される形にした。
- **設計のみで完了にしない。** 流し込み後に実プレイ（or fixture 実エンジンテスト）で「ロウソク全点灯→テレポート出現／剣・矢・ブーメランでは不可」を検算するまで D4 は未完。D3 で「論理接続が通っても実エンジンで初めて見える穴」を2件踏んだ教訓。

**▶ 次やること：** **9-2e の JSON 流し込み（⚡ Sonnet）** → PLAN 9-2e の接続グラフ・開口座標どおりに dungeon_4 を20部屋へ再構築 → `check-dungeon-connectivity.mjs dungeon_4`（＋`--block-room=1,1`）／`check-dungeon-integrity.mjs dungeon_4` 両グリーン → `fromEditor=1` でロウソク関門の実プレイ確認＋`tests/candle-gate.spec.js` 新設。続けて D5〜8 を 1つずつ 🧠 Opus 設計。

### 2026-06-25 — 連なった DOOR を「1枚の大きな門」に統一（挙動＝鍵1個／見た目＝幅広の観音開き門）

**やったこと：**
- **ユーザー指摘（2段階）：** ① D3 ボス前室 `[0,1]` の入口に `D` 扉が col5・col6 の2枚あるが鍵は1個・1枚開けば通れる＝2枚目が死にタイルで紛らわしい（D1・D2・D3 全部同じ穴＝D2テンプレ踏襲）。② 最初の修正に「**2枚同時に開くのは変。横2枚分の大きなドア1枚にして**」と再指摘＝見た目も1枚にする必要。
- **挙動の修正：** `game/player.js` の `tryOpenDoor` に連結 `D` の flood-fill `collectDoorRun` を追加し、隣接 DOOR を1枚として鍵1個でまとめて開く。
- **見た目の修正：** `render-board.js` で横連結の扉を左半分(`doorL`)＋右半分(`flipX`)で描き「外枠は両端・中央は細い合わせ目」の幅広門に。新スプライト `doorL`/`doorLopen`（`door`パレット流用）。さらに**扉だけセル全体を埋める** `.door-sprite` クラスを新設（従来 `.obj-sprite`=0.7倍中央寄せが「小さい扉が2個」の主因だった）。単独 `D` は従来 `door`。データ変更不要で D1/D2/D3 同時解消・将来 field 城門にも流用可。
- **テスト：** `getStageStateSnapshot` に `openedDoors` 公開。新規 `tests/double-door.spec.js`（2本・fixture `double_door`）で挙動固定。**全171テストグリーン**（VRT 含む）。閉/開・幅広/単独を実ブラウザで描画確認（pageerror なし）。

**学び・気づき：**
- **「絵が2個に見える」原因がスプライト画像とは限らない。** 今回の主因は `.obj-sprite` の 0.7倍中央寄せ（セル間に隙間）で、絵をどう描いても繋がらなかった。**CSS のサイズ・配置（レイアウト）まで見ないと誤診する。** 扉をセル全体描画にして初めて連結表現が成立した。
- **2マス幅の辺スクロール開口を鍵で塞ぐ＝D扉が2枚並ぶ。** 「連結＝1枚の幅広門」と挙動・見た目の両方で解釈すれば鍵1個の意味が通り、2マス開口テンプレも崩さない。**連結成分を1単位として扱う**発想は色ゲート群などにも応用可。

### 2026-06-25 — Phase 9-2d 流し込み完了：dungeon_3「水の迷宮」18部屋を実装＋設計の穴2件を実装中に発見・修正（弓ゲートの「水島」化・接続迂回の封鎖）

**やったこと：**
- **dungeon_3 を2部屋スタブ→18部屋（5列×4行=20 から `[4,0]`/`[0,3]` を欠く）に再構築**。layer 直下に `bossStage:"0,0"`/`triforceId:3`/`name:"水の迷宮"`/`bgm`/`bossBgm` を D1/D2 形式で設定。ボス部屋を旧 `1,0`→`0,0` に再配置。臨界経路 `[1,3]ENTRY→[1,2]弓入手→[1,1]弓ゲート(Y→T)→[1,0]中ボスW＝鍵K→[0,1]D扉→[0,0]ボスJ→欠片③`。報酬＝弓矢（`[1,2]` 宝箱・矢15本）。地図`m`=`[3,2]`／コンパス`n`=`[3,1]`／中ボス`W`部屋にハートの器。
- **⚠️ 設計の穴①を実装中に発見・修正（最重要）：** PLAN の「弓ゲート＝2マス幅水堀で対岸に `Y`」は、**対岸が歩ける床だと水堀を回り込んで剣で `Y` を叩けて迂回できる**（接続チェッカーは部屋間の辺しか見ず、部屋内で剣が届く迂回を検出できない）。→ **`Y(1,10)` を上下左右すべて水/壁の "水島" に隔離**し、`col10` の通路 `(6,10)` から上向きに矢を撃つ縦レーン（距離5＞ブーメラン maxRange3）でのみ届く設計に変更。
- **⚠️ 設計の穴②も発見・修正：** PLAN の接続表どおり `[1,2]→[0,2]→[0,1]` を開けると**弓ゲートを通らずボス前室に到達できる**（弓スキップ）。→ `[0,2]` を `[1,2]` の寄道専用にし `[0,2]→[0,1]` を封鎖。`[0,1]` は `[1,1]` の `T`ゲート経由でのみ入れる。
- **整合性チェッカーのバグ修正：** `check-dungeon-integrity.mjs` の room-flood が `T`/`D`/色ゲートを永久壁扱い→水堀＋ゲートで出口を作る弓ゲートを誤エラー判定。connectivity.mjs の `SOLVABLE_GATES` と思想を揃え通行可に（`Y` のみ非通行維持）。D1/D2 回帰なし。
- **テストのライブマップ依存を解消（[[blade-gimmick-tests-use-fixtures]]）：** D3 再設計で旧 `0,0`/`1,0` が消え `ladder.spec.js`（9本）＋`test-new-dungeons.spec.js`（1本）が落ちた。旧ジオメトリを fixture に `ladder_pit`/`ladder_water2`/`bow_gate` として複製しテストを移行。D3 遷移テストは新エントリ `1,3`→`1,2` に更新。**新規 `tests/bow-gate.spec.js`（3本）で弓ゲートの必須化を実プレイ固定。**
- **検証：** `check-dungeon-connectivity.mjs dungeon_3` PASS（dead edge 0・orphan 0・`--block-room=1,1` でボス到達不能＝✅gate holds）／`check-dungeon-integrity.mjs dungeon_3` エラー0・警告6（全て水`~`の寄道警告＝出口非封鎖で意図どおり）／**全169テストグリーン**／実ブラウザで弓ゲート部屋・ボス部屋を描画確認（pageerror なし）。

**学び・気づき：**
- **接続チェッカーは「部屋間の辺」しか検証しない＝部屋内で飛び道具/剣がスイッチに届くかは別問題。** 飛び道具スイッチの関門は「スイッチを水/壁で完全に隔離し、撃てる方向に必要距離のレーンを1本だけ開ける」まで設計しないと徒歩迂回・近接で破れる。**論理接続グラフが通っても実プレイ（or fixture を使った実エンジンテスト）で必須化を確認するまで完了にしない。**
- **Opus 設計の PLAN でも、実装で実エンジンに当てると初めて見える穴がある（今回2件）。** Sonnet が座標を置くだけでなく、流し込み担当が「この配置で本当に迂回できないか」をエンジンで叩いて検算する工程が要る。
- **整合性チェッカーと接続チェッカーで "ゲートの扱い" を揃える。** 一方が `T`/`D` を通行可、他方が永久壁だと、正当な「ゲートで出口を作る部屋」が誤検出される。

**▶ 次やること：** **D4〜D8 を 1つずつ 🧠 Opus 設計→⚡ 流し込み**（D4 炎=ロウソク／D6 森=爆弾／D5 氷=はしご／D8 沼地=`dungeon_8` 新設＋沼地ボス`I`配置／D7 空=笛ワープ）。D3 は 9-2y の Opus 3段階レビュー（構造/バランス/体験）待ち。並行して 9-3（チュートリアル＋ガイド導線）。

### 2026-06-24 — Phase 9-2d（dungeon_3「水の迷宮」設計確定・🧠 Opus・コード変更なし）：弓矢報酬・`Y`→`T` を矢で開く関門・18部屋（漸増カーブへ是正）・D2 と同粒度で Sonnet 流し込み可に

**やったこと（設計・記録のみ・コード/JSON 変更なし）：**
- **⚠️ ユーザー指摘で規模方針を是正（最重要）：** 初版で D2 テンプレ（4×4=16）をそのまま D3 にコピーし「16部屋固定」にしてしまった＝「先に進むほど大きく」に反する誤り。→ **PLAN「📐 規模基準」に漸増カーブ表を追記**（D1=16/D2=16/**D3=18**/D4=20/D5=20/D6=22/D7=22/D8=24/**暗黒の塔=28+**）。15〜25 と「ラスダンは明確に広く」は元から PLAN にあったが「漸増配分」が無く 16固定を許す隙があった。
- **D3 を 18部屋（5列×4行=20 から袋小路2室 `[4,0]`/`[0,3]` を欠く）で再設計確定**：報酬＝**弓矢＋欠片③**、ボス＝`J`深海の海蛇（HP38・弱点 beam×2＝剣チャージで◎・剣でも可）。テーマ＝水路の迷宮（`v`BRIDGE で渡る）。
- **臨界経路を確定（規模拡大でも不変）**：`[1,3]ENTRY→[1,2]弓入手→[1,1]弓ゲート(Y→T)→[1,0]中ボスW＝鍵K→[0,1]D扉→[0,0]BOSS`。**弓の必須化＝`[1,1]` を 2マス幅水堀で分断し対岸に `Y`SWITCH・左辺出口を `T`GATE で塞ぐ＝矢でしか `Y` に届かない**（`combat.js` の `toggleSwitch`→`links[{switchId,gateId}]` 配線を実コードで確認済み）。**増やした東2列(sx=3,4)は寄道・宝・水路パズルのサブ網**＝臨界経路に絡めず探索ボリュームだけ足す。
- **全18室の役割＋全辺の開口座標（縦=同col5,6／横=同row4,5）を PLAN.md 9-2d に書き出し**（D1/D2 と同粒度＝Sonnet が座標にタイルを置くだけで流し込める状態）。layer メタ（`bossStage:"0,0"`/`triforceId:3`/`name`/`bgm`）＋ボス部屋 `1,0`→`0,0` 再配置も明記。
- **`J` は整合性チェッカーの `TRIFORCE_BOSS_TILES`（`A/L/N/J/O/U/G/I`）に既登録を確認**＝ボスタイル追加は不要。
- **記録**：PLAN.md「9-2d」節（18部屋版）＋規模カーブ表／DECISIONS.md「規模は漸増」「Phase 9-2d（18部屋）」2件／本ログ・現在地更新。

**学び・気づき：**
- **テンプレの「規模」までコピーしない（今回の誤り・ユーザー指摘）。** D2 の構造（辺スクロール網・配線済み仕掛け・報酬→必須化→ボス）は再利用すべきだが、**部屋数は漸増カーブで変える**べきだった。PLAN にレンジ（15〜25）はあっても配分方針が無いと、安易に直前ダンジョンと同サイズに固定してしまう。→ **方針は「レンジ」だけでなく「具体カーブ表」で持つ**ことで再発防止。
- **規模を増やす≠難所を増やす。** D3 は連鎖2段目で難度は中。増やした2室は寄道・探索に吸収し、臨界経路（弓ゲート一本道）はそのまま＝**規模で歯ごたえ、難度はカーブどおり**を両立。
- **配線が実コードで確認できるギミックだけで関門を作る（9-2 鉄則）。** 弓必須化は `Y`→`T` を矢で開く案（D2/D3 既存配線）を採用。「H かがり火＋torchesLit」案はロウソク＝D4報酬が要りソフトロック(A)違反の懸念＋D4 と主題が被るので却下。
- **既存スタブの「ボス部屋座標」を鵜呑みにしない。** 現 dungeon_3 はボスが `1,0`・`triforceId` 未設定。D1/D2 規約（ボス=`0,0`・layer 直下メタ）に合わせないと整合性チェッカー MUST エラー＝Sonnet 流し込み時に再配置・メタ設定が要る点を明記。

**▶ 次やること：** **9-2d の JSON 流し込み（⚡ Sonnet）** → PLAN 9-2d の接続グラフ・開口座標どおりに dungeon_3 を16部屋へ再構築 → `check-dungeon-connectivity.mjs dungeon_3`（＋`--block-room=1,1`）／`check-dungeon-integrity.mjs dungeon_3` 両グリーン → `fromEditor=1` で弓関門の実プレイ確認。続けて D4〜8 を 1つずつ 🧠 Opus 設計。

### 2026-06-24 — Phase 9-2c（沼地ボスを「部品」として作成）＋位置づけの根本修正（cave_1 ❌→ dungeon_8 新設へ）

**やったこと：**
- **新規 2×2 大型ボス「沼地の大蝦蟇 `SWAMP_TOAD('I')`」を作成**（Phase 3-2 の型を踏襲）：
  - スプライト：64×64・2フレーム（眼の輝き＋大口開閉）を Python codegen で生成 → `shared/sprites-enemies.js` に挿入（`ENEMY_PAL.swampToad` 8色＋`ENEMY_SPRITES.swampToad`＋向きエイリアス D/R/L/U）。
  - `shared/enemies.js`：`ENEMY_META[TILE.SWAMP_TOAD]`（hp40/atk5/def2/exp56・`size:{2,2}`・`isBoss`・`dropsTriforce:true`・`weakness:{fire,×2}`・hitAndAway・舌打撃 range2.4＋毒沫 stone range7・HP50%加速）。
  - `shared/tiles.js`：`SWAMP_TOAD='I'`（未使用文字）＋ TILE_META。`scripts/check-dungeon-integrity.mjs` の boss-tile セットにも `'I'` を追加。
- **⚠️ ユーザー指摘で位置づけを根本修正（最重要）：** 当初この 9-2c で「cave_1 をボスダンジョン化して沼地ボスを配置」する実装をした（ボス部屋整備・bossStage/triforceId・地図/コンパス・`Q` 2枚撤去）。だが**`cave_1` は小さな洞窟用に作ったレイヤーであり、欠片ボスを置く場所ではない**＝根本的な誤り。**欠片ダンジョンは `dungeon_1〜8` の8個に統一すべき**（現状 dungeon_1〜7 ＋ ラスダン dark_tower＝dungeon_8 が欠けている）。→ **cave_1 への配置データは `git checkout` で revert（小洞窟「小さめの謎の洞窟」のまま維持）。** 沼地ボスは**新設する `dungeon_8`** に置くことに決定（連鎖位置・テーマ・フィールド入口はゼロから設計し直す＝9-2{d〜}）。
- **沼地ボスは現在どのマップにも未配置（部品のみ）。** `work/blade-of-lumia.json` は HEAD に戻した（＝欠片合計は元の9＝直接 Q 2枚＋ボス7のまま。dungeon_8 配置時に Q 整理＋ボス追加で8に揃える）。
- **実ブラウザ確認（revert 前に実施）：** cave_1 に仮配置してトードが 2×2 描画・ボス部屋ロック・HPバー「沼地の大蝦蟇」・「沼地の大蝦蟇が現れた！」をスクショ確認（pageerror なし）→ その後 revert。部品としての描画・AI・当たり判定が既存パイプラインで動くことは確認済み。
- **テスト**：`tests/swamp-boss.spec.js`（4本）を**「部品としての定義」検証に書き換え**（①dropsTriforce な2×2ボス②スプライト/パレット/向きエイリアスが揃う③タイル定義あり④まだライブマップに未配置＝誤配置の回帰防止）。**全166テストグリーン**。
- **記録更新**：PLAN.md（9-2c を「部品作成のみ完了」に・9-1 連鎖表の「cave_1 沼地」→「D8 沼地〔レイヤー新設〕」・9-2{d〜} 見出しを dungeon_3〜8 に・「欠片ダンジョンは dungeon_1〜8」を明記）／DECISIONS.md「Phase 9-2c」を位置づけ修正版に差し替え／本ログ。

**学び・気づき：**
- **既存レイヤーの「用途」を勝手に転用しない（最大の学び・ユーザー指摘）。** 9-1 連鎖設計の時点で「cave_1＝沼地の欠片ダンジョン」と、**小洞窟用に作った既存レイヤーに欠片ボスの役割を押し込んでいた**のが誤りの起点。私の 9-2c はその誤前提を疑わず実装した。欠片ダンジョンは `dungeon_*` の連番で持つべきで、役割の違うレイヤー（cave_1＝寄り道の小洞窟）を数に混ぜると設計が破綻する。**「8つ」を満たすなら `dungeon_8` を作るのが筋。**
- **JSON 書き戻しはフォーマット非破壊で（ハマった）。** `json.dump(indent='\t')` で `tiles` が char 配列→結合文字列に化け 9テスト落ち。`json.dumps(indent=2, ensure_ascii=False)`＋末尾改行なしが原本とバイト一致することを確認し tiles を配列のまま書き換えて解決（→ 今回は最終的に revert したが知見は残す）。
- **大型ボスは `buildEnemies` が `ENEMY_META` から自動スポーン**＝tile-sprites.js 登録不要。タイル文字を ENEMY_META に足せば描画・AI・当たり判定まで既存パイプラインに乗る（部品作成だけなら配置と独立して完結できる）。



**背景：** D1 リメイクでゲームマップを編集したらギミック系テスト（色スイッチ/トーチ/矢スイッチ/隠し通路/敵石/はしご向き）が軒並み壊れた。テストデータが live のゲームマップを直に参照していたのが原因。ユーザー指摘「テスト用データはゲームステージとは別に管理すべき」。

**やったこと：**
- **`tests/fixtures/test-stages.json` 新設：** ギミック検証専用の凍結フィクスチャ。`test_mechanics` レイヤーに `color_switch`/`torch_relay`/`arrow_switch`/`bomb_wall`/`enemy_stone`/`hidden_cave_test`/`ladder_isolated` の各ステージ。全ステージに `rows:10, cols:12` を明記（`isInBounds` が `sd.cols`/`sd.rows` 依存のため、欠けると矢・ブーメランが即消滅する）。
- **`game.js` に `ps_mapSrc` パラメータ追加：** `loadMapData(overrideUrl)` で URL を差し替え可能に。テストは `?ps_mapSrc=../tests/fixtures/test-stages.json` でフィクスチャを読む。
- **6テストファイルをフィクスチャに移行：** color-switch / torch / bomb-torch / arrow-switch / hidden-passages / enemy-stone-puzzle / ladder（145行の進入軸テストのみ）。
- **D1 から非機能タイルを除去（ユーザー指摘の積み残し）：** `0,2`/`1,2` の無効トーチ（links/conditions 無し）削除、`1,2` の `torchesLit`（ブーメラン=D2依存）→`killAll` に変更、`3,0` の `links:[]` 色スイッチ削除。
- **遅延アイテム依存 WARNING を解消：** `1,0` の `!`壊せる壁（爆弾=D6依存）削除、`2,2` の水堀（はしご=D5依存）を床化。
- **全テスト 162 passed**（前回 flaky だった `ladder.spec.js:145` も `ladder_isolated` フィクスチャ移行で安定化）。整合性チェッカーで dungeon_1 はエラー0・警告0。

**学び・気づき：**
- 旧 `ladder.spec.js:145` は「`field/0,2` の `(3,2)` が孤立水」を前提にしていたが、マップ改修で `(3,2)` は石畳`o`に変わっており、変更前から既に壊れていた（"flaky" ではなくマップ依存の死テスト）。フィクスチャ分離で根治。
- `hidden-passages` の遷移テストは `currentLayer` を見ていたが、フィクスチャでは `hidden_cave_test` は `test_mechanics` レイヤー内の**ステージ**なので `currentLayer` は変わらない。`stageKey` を見るよう修正。
- ギミックテストは今後すべてフィクスチャ（`test_mechanics`）を使う。ゲームマップ（`field`/`dungeon_*`）を参照するのは遷移・接続・実マップ依存のテストだけに限定する。

### 2026-06-23 — Phase 9-2a2＋9-2b（D1リメイク＋D2砂漠 JSON 流し込み・⚡ Sonnet）：16+16部屋完成・connectivity PASS・全テスト修正

**やったこと：**
- **dungeon_1 完全リメイク（12部屋→16部屋 4×4グリッド）：** 旧ステージを新座標に再割り当て。bossStage を `2,0`→`0,0`（ボスG）に変更。臨界経路 `[1,3]ENTRY→[1,2]→[1,1]木の盾→[1,0]中ボスW+鍵→[0,1]D扉→[0,0]BOSS G`。
- **dungeon_2 新規構築（2部屋スタブ→16部屋 4×4グリッド）：** 「砂漠の神殿」。bossStage `0,0`（ボスN）、triforceId:2。臨界経路 `[1,3]ENTRY→[1,2]→[1,1]ブーメラン→[0,1]鍵回収+D扉→[0,0]BOSS N`。
- **connectivity checker で両ダンジョン PASS**（dead edge 0・orphan 0）。`--block-room` でボス一本道も確認。
- **旧パズルデータを新レイアウトに移設：** `[1,2]` に H トーチ×3＋B宝箱(torchesLit)＋initLitTorches / `[3,0]` に色スイッチ([/])＋色ゲート((/))＋W＋B(killAll条件) / `[2,1]` に C(CHASER)＋*(STONE)＋S(BUTTON)＋T(GATE)＋B / `[0,0]` に `i`石碑「ザーネルの記憶 其の一」/ `dungeon_2/0,0` に「其の二」/ `[1,0]` に `!`壊せる壁(2,9)＋隠し宝箱 / `[3,3]` にハートの器チェスト
- **テスト修正（破壊されたテスト 20件→全グリーン）：** 旧ステージ座標を参照していた8テストファイルを更新（connectivity-tool/heart-containers/enemy-stone-puzzle各自とJSON更新の組み合わせ）。
- **全テスト 161 passed / 1 failed**（`ladder.spec.js:145` は変更前から既存 flaky・本変更と無関係）。

**学び・気づき：**
- `findEntrances` は `mapEnters.id` ↔ 外部レイヤーの `destId` 照合で入口を探す。新 D1 の入口は ENTRY ステージ `1,3`（`id:"dungeon_1"` を持つ）→ regression test の `entrances` 期待値も `'0,0'`→`'1,3'` に更新が必要だった。
- initLitTorches は JSON の stageData として直接置く（game.js の initStageState で Set に変換される）。
- tiles 配列は `string[][]` (配列の配列)。直接インデックスで上書き可能。

### 2026-06-23 — Phase 9-2pre（ステージ接続チェッカーの共通ツール化＋自己テスト・Opus設計＋実装＋テスト）：つなぎ検証を機械化。デッドエッジ/孤立を自動検出

**やったこと（チェッカーのツール化・テスト追加。ゲームコード/ステージデータは変更なし）：**
- **ユーザー提案を受け、9-2 流し込みの前に「接続チェッカー」を整備：** 同一マップ内の隣接ステージ接続は 100% 機械検証できる＝先にツールを信頼できる形にしてから流し込めば以降の生産性・安全性が上がる、という方針。
- **共通モジュール `scripts/lib/connectivity.mjs` を新設：** `game/passable.js` の `tilePassable` と `game.js` の `checkStageTransition`（sx/sy 辺スクロール）をミラーする BFS コアを1箇所に集約。既存 `check-dungeon-connectivity.mjs`／`check-field-connectivity.mjs` を**これを import する薄いラッパに書き換え**（BLOCKED の3重手書きコピーを廃止＝タイル追加でズレる温床を除去）。BLOCKED は `shared/tiles.js` の TILE 定義＋`shared/npcs.js` から導出。
- **デッドエッジ検出を追加（これまで無かった核心機能）：** 「開いている辺から出たのに到着セルが壁／到着ステージが無い／OOB」を**警告**する。従来は BFS が黙って渡らないだけで「隣に移動した瞬間に動けない/めり込む」を見逃していた。あわせて**孤立ステージ検出**（エントリから到達不能な部屋の列挙）も標準出力に。
- **BLOCKED を2分類に割った（重要な設計判断）：** **HARD_BLOCKED**（壁・水・木・看板・NPC＝絶対に通れず開かない）と **SOLVABLE_GATES**（`D`扉/`T`ゲート/`!`壊せる壁/`:`ボス出入り口＝今は閉だが鍵/スイッチ/爆弾/入室で通れる）。**デッドエッジは HARD_BLOCKED 着地時のみ警告**。最初 `:`を一律 blocked にして D2 のボス部屋正規入口を誤検出 → ボス出入り口は"歩いて入る"通路なので SOLVABLE に修正。
- **チェッカー自身の自己テスト `tests/connectivity-tool.spec.js`（9本）を新設：** わざと壊した小フィクスチャ＝(a) 開口ズレ→デッドエッジ赤／(a2) 存在しない隣へ開口→no-stage 赤／(b) 孤立ステージ→未到達／(c) 正しく整列→緑／ボス出入り口着地は誤検出しない／`--block-room` 相当で一本道が切れる、を固定。**9本グリーン。**
- **実データ検証：** D1/D2 とも dead edge 0。field は既存の軽微な border quirk（`3,0 left→2,0` が姫タイル `P` 着地）を1件検出＝9-2 フィールド整備で対応。フルスイート 154 passed（既知の flaky `ladder.spec.js:145` 1件のみ赤＝本変更と無関係・game コード不変）。
- **⚠️【ユーザー指摘①で修正】デッドエッジだけでは封印部屋を見逃す：** 「dungeon_1 の `5,0` はデッドエッジでは？」→ 正しかった＝`5,0` は全周壁＋mapEnter 無しの封印部屋。当初ツールは開口ゼロの封印部屋でデッドエッジが起きず**「✅ PASS」で見逃していた**。→ `findOrphanRooms()`（ゲート開＋テレポート込み BFS）を新設し ❌ 検出。
- **⚠️【ユーザー指摘②で再修正】入口の定義が雑＝`3,0`/`4,0` も dead：** 「`3,0`/`4,0` も dead。互いに行き来できるが外界からどちらにも入れない。入口は複数あってよく、全入口のどこからも辿り着けなければ dead」→ 正しかった。当初 orphan は入口を `findEntryRoom`（1部屋を当てるヒューリスティック）で決めており、dungeon_1 では偶然正答していただけ。→ **`findEntrances(mapData, layerName)` を新設**（入口＝その MAP_ENTER `id` が他レイヤーの `destId` から参照される部屋・複数可／内部階段は除外／field は startPos）し、**全入口の和集合**から BFS。これで「相互に繋がる島でも入口に繋がらなければ dead」を定義どおり判定。テストに `(b4)` 相互島／`(b5)` 複数入口／`findEntrances` クロスレイヤー検出／**実 field の dead 0 regression**（入口＝startPos・空島4,0は darkTower ポータルで到達＝dead でない）を追加（12→**16本**）。
- **記録：** PLAN.md 9-2 に「9-2pre」タスクを新設（✅完了マーク）／DECISIONS.md に「Phase 9-2pre」＋「同【修正】孤立検出」追記／PROGRESS.md 現在地・本ログ更新。

**学び・気づき：**
- **「pass した」を疑う基準を持つ（最大の学び・ユーザー指摘）。** 「デッドエッジ0」は接続健全性の*部分条件*にすぎないのに、それを全体の合格印として出した。ユーザーが実マップを目視しなければ封印部屋を緑のまま流し込みに進んでいた。チェッカーは「何を検査していないか」を明示し、全条件をまとめた合否（verdict＋exit code）を出さないと誤った安心を与える。
- **「BFS が渡らない」と「渡れないと警告する」は別物。** 従来のチェッカーは到達可能性しか見ておらず、開口ズレは「黙って未到達」になるだけ＝設計者は気づけなかった。**デッドエッジ＝開口があるのに対岸が壁**を能動的に警告して初めて「つなぎが壊れている」が機械で分かる。
- **ブロック判定は "通れない" を1色で塗ると誤検出する。** 到達可能性には「閉じた扉も今は通れない」を含めたいが、デッドエッジ判定には「扉は正規の通路＝壁ではない」を区別する必要がある。HARD/SOLVABLE の2分類で両立。`:`ボス出入り口で一度踏んだ罠。
- **チェッカーはチェッカー自身をテストして初めて信頼できる。** 「passした」が無意味にならないよう、既知の壊れマップで赤が出ることを固定するのが肝。これで以降 Opus/Sonnet どちらが流し込んでも安全網が機械で効く。

**▶ 次やること：** **9-2a2（D1）＋9-2b（D2）の JSON 流し込み** → `node scripts/check-dungeon-connectivity.mjs dungeon_1`/`dungeon_2`（＋`--block-room=`）で **dead edge 0・孤立0・一本道成立** を機械確認。両ダンジョンとも接続グラフ＋開口座標が確定済み＝流し込み後にツールで検証して確定する。続けて 9-2c（沼地ボス）・9-2{d〜}（D3〜7）/9-3。

---

### 2026-06-23 — Phase 9-2a2（D1「草原の洞窟」接続グラフ確定・Opus・コード変更なし）：16部屋・4×4・鍵1枚で一本道。D1 も Sonnet 流し込み可に

**やったこと（設計・記録のみ・コード変更なし）：**
- **D1 リメイクの接続グラフ＋全15辺の開口座標を Opus で確定**（D2=9-2b と同粒度）。前回 D1 は「撤去/報酬/規模」までで**接続グラフ未確定＝Sonnet 流し込み不可**だった穴を是正。16部屋（4×4 グリッド）。
- **臨界経路を一本道に設計：** `[1,3]ENTRY→[1,2]雑魚ハブ→[1,1]木の盾入手→[1,0]中ボスW撃破→鍵K→(戻って)[0,1]D扉→[0,0]BOSS G`。**足止め＝鍵1枚ゲート**（中ボスを倒さないと鍵が出ず、`D`扉が開かずボスに到達不能）。入門ダンジョンなのでサブアイテム要求は一切なし＝🔑鉄則(A) 厳守。
- **node で論理 BFS 検証：** ①全16室到達 ②ボス`[0,0]`の grid 隣接は`[0,1]`のみ＝裏口なし（[1,0]の左辺＝[0,0]右への裏口は#WALLで塞ぐ）③鍵部屋`[1,0]`/扉部屋`[0,1]`を塞ぐとボス到達不能＝一本道。
- **古典ゼルダ型(B)：** 木の盾を**ボス手前 `[1,1]`** で入手 → ボス`G`は遠隔岩投げ(range6)＝盾ガードで倒す部屋（盾なしでも HP30 削れば倒せる＝詰まない）。欠片①はボス撃破ドロップのまま、`bossStage` を `2,0`→`0,0` に更新する方針。
- **記録：** PLAN.md 9-2a2 に確定接続グラフ・全15辺の開口座標・検証コマンドを追記し「16部屋レイアウト確定」を✅に／DECISIONS.md に「Phase 9-2a2 D1 接続グラフ確定」を追記／PROGRESS.md 現在地・次やること更新。

**学び・気づき：**
- **D1 と D2 を「同じ粒度」に揃えて初めて両方 Sonnet 可になる。** 前回「D2 は接続グラフ済み＝Sonnet 可／D1 は未確定＝Opus 必要」と分かれていた。接続図＋開口座標＋検証スクリプトの3点セットが揃うかどうかが Sonnet 引き渡しの唯一の判定基準。
- **入門ダンジョンの足止めは "鍵1枚" が最適。** D2 以降はアイテム必須化（ブーメラン鍵回収など）で締めるが、D1 はまだ剣＋盾しか無いので、配線済みで最もクリーンな `K`KEY→`D`DOOR の1ゲートに絞る。石押し/torchesLit は寄道（任意）に回して密度だけ出す。

**▶ 次やること：** **9-2a2（D1）＋9-2b（D2）の JSON 流し込み（⚡ Sonnet）** → `scripts/check-dungeon-connectivity.mjs` で検証。両ダンジョンとも接続グラフ＋開口座標が確定済み。続けて 9-2c（沼地ボス新設）・9-2{d〜}（D3〜7）/9-3。

---

### 2026-06-23 — Phase 9-1鉄則(B)反転＋9-2a2＋9-2b（古典ゼルダ型へ転換／D1 リメイク／D2 接続グラフ確定＋検証ツール新設・Opus）

**やったこと（設計・記録・検証スクリプト追加。ゲームコード/ステージデータは変更なし）：**
- **⚠️ ユーザー懸念「つなぎがいつもバカ」に対応＝ステージ接続を Opus で確定＋機械検証（最重要）：** 根因を実コードで特定＝`checkStageTransition`（`game.js:977-980`）は**隣接 grid 座標に stage があれば移動するだけで、辺の開口が地形的に噛み合うかを一切チェックしない**。→ ①**接続規約を確定**（stageKey=`sx,sy`／辺スクロールは上下移動で列保持・左右移動で行保持＝縦の繋ぎは同col5,6・横の繋ぎは同row4,5 で開口一致）。②**D2 の16部屋接続グラフを node で論理検証**（臨界経路を一本道に・ボス[0,0]裏口なし・ブーメラン部屋[1,1]を塞ぐとボス到達不能）し、全辺の開口 col/row まで PLAN に明記。③**検証ツール `scripts/check-dungeon-connectivity.mjs` を新設**（既存 `check-field-connectivity.mjs` と同型のタイルレベル BFS で辺越えを再現＝グラフ adjacency でなく実地で噛み合いを検査。`--block-room=` で一本道も検査）。これで Sonnet は座標にタイルを置くだけ→スクリプトで自動検証できる
- **⚠️ ユーザー指摘でダンジョン内部構造を古典ゼルダ型に転換（確定済み判断の反転）：** 「報酬はボスを倒す前に入手して、それを使わないと進めない部屋を作るよね？」→ 現行 PLAN は**真逆**（報酬はボス最奥・ダンジョン内では使わない）だったと判明。**9-1 鉄則を (A)後ダンジョンの報酬を要求しない＝ソフトロック厳禁（厳守）／ (B)自分の報酬を内部で使う（過剰制限＝撤回）** に分離し、(B) を反転。連鎖順序は「入口を前ダンジョン報酬でロック」で保つので (B) 反転と両立。各ダンジョンの必須化ギミックを**実コードで配線確認**（弓→`Y`遠隔SW／ロウソク→`H`点灯・茂み焼き／爆弾→`!`壁／はしご→水穴渡り／笛→`flutePlayed`）。**⚠️ ブーメランは `Y`SWITCH をトグルしない**（arrow/beam のみ）と判明＝D2 は「2マス幅の水/穴の対岸の `K`鍵をブーメランの `collectFieldItem` で回収→`D`扉」で必須化（遠隔スイッチにはしない）
- **⚠️ ユーザー指摘で dungeon_1 の据え置き不可が発覚：** 「D1 もまさかあのままじゃないよね？」を実データで監査。現状 dungeon_1 は**木の盾が無く**、代わりに **ミラーシールド(tier2)・聖剣tier3×2・伝説のよろいtier2×2・銀の剣tier2・さいごの鎧tier2** が散在＝**入門ダンジョンで最強級装備が全部揃う**（9-1 連鎖＝鉄の盾D5／ミラーcave_1 温存と完全に矛盾）＋12部屋（規模基準未満）。→ **9-2a2（D1 リメイク）を新設**し Opus で設計確定。私の前回プランは 9-2 のビルド対象を「dungeon_2〜7」と書いて**D1 を除外していた穴**を是正（dungeon_1〜7・cave_1 に修正）。欠片①はボス`G`撃破ドロップ＝正しいので据え置き
- **規模基準をユーザーと確定：** 各ダンジョン**15〜25部屋**（初代『ゼルダの伝説』準拠）、**最終ダンジョン（暗黒の塔）はもっと広く**。dungeon_1（12）は下限未満＝品質・規模基準にしない。**D2 は 16部屋（4×4 グリッド）**で確定
- **⚠️ 実コード精査で "実際に配線されている仕掛け" を確定（最重要）：** ✅`K`KEY→`D`DOOR／`*`石→`S`ボタン→`links`→`T`ゲート／`Y`スイッチ（剣で叩く）→`T`／`[`/`]`色スイッチ→`(`/`)`色ゲート／`showConditions`(`killAll`等)で封印宝箱出現／`:`DOORWAY_BOSS。**❌`|`DOORWAY_LOCKED は死にコード**（開放関数 `unlockLockedDoor()` が定義されているが**どこからも呼ばれていない**＝条件達成で開かない）／`!`BREAKABLE_WALL は爆弾専用＝D2 では開けられない。**いずれも足止めに使わない**
- **D2 の体験を設計：** 主題＝**木の盾の使い方（ガード）**を体得させる最初のダンジョン（D1 報酬の木の盾を活かす）。`F`SENTRY（槍を range4 で投げる遠隔兵・HP6）を要所に置き「盾で飛び道具を防ぐ」誘導（剣でも倒せる＝強制ではない）。難易度カーブ＝雑魚`E`/`C`→仕掛け（石押し/スイッチ）→遠隔`F`→中ボス`W`魔物→ボス`N`砂嵐の蠍王。背骨の足止め＝「中ボス`W`撃破→鍵`K`→`D`扉でボス部屋へ」。報酬＝ブーメラン＋欠片②（ボス最奥）
- **🔑鉄則を実データで検算：** ボス`N`は HP32・剣で撃破可能、弱点ブーメラン×3 は**D2 の報酬なので D2 内では未所持**＝「剣＋木の盾だけで完全クリア可能」が成立。D2 内のロックは鍵/石押し/スイッチのみで進行アイテム不要
- **記録：** PLAN.md に「全ダンジョン共通の規模基準（15〜25・ラスダンは広く）」「9-2 で使える/使えない仕掛け一覧」「9-2b. dungeon_2 作り込み（16部屋・役割割り当て・移設反映・meta整備）」「9-2{d〜} D3〜7」を新設／DECISIONS.md に「Phase 9-2b」追記／PROGRESS.md 現在地更新

**学び・気づき：**
- **「ソフトロック防止」と「自分の報酬を内部で使わない」を混同していた（最大の学び）。** 9-1 で "前のアイテムだけでクリア可能" と決めた時、本当に守るべきは **(A) 後ダンジョンの報酬を要求しない** だけ。そこから **(B) 自分の報酬も内部で使わない** まで広げたのは過剰で、古典ゼルダの面白さ（報酬を主役にする）を自ら禁じていた。ユーザー指摘で初めて (A)/(B) の分離に気づいた。**"鉄則" として書いた制約も、根拠（ソフトロック防止）まで遡ると過剰だったと分かる**
- **報酬アイテムの "必須化" は配線次第で手段が変わる。** ブーメランは見た目「遠隔で何でもできる」が、実コードでは `Y`SWITCH をトグルしない（arrow/beam だけ）。配線を確認せず「ブーメラン必須スイッチ部屋」を設計していたら動かなかった。`|`DOORWAY_LOCKED（死にコード）と同じ轍。**"そのアイテムで実際に何が起こせるか" をコードで確定してから必須化部屋を設計する**
- **規模は数値基準に固定すべき。** 「明確に大きく」は人によってブレるので、初代ゼルダの 15〜25 を全ダンジョン共通の物差しにした。D2=16 はその下端＝最初の進行ダンジョンとして過不足ない
- **「役割割り当てまで決めれば残りは機械的」は早すぎた（つなぎの教訓）。** 接続グラフと各辺の開口座標が未定義のまま Sonnet に渡すと、辺の開口が噛み合わず壁めり込み/裏口でボス直行が起きる（コードが噛み合いをチェックしないため）。**"機械的に流し込める" と言える条件は「接続図＋開口座標まで Opus で確定し、検証スクリプトを用意した後」**。それ未満では Opus が接続設計を続ける必要がある

**▶ 次やること：** **(1) D1 の接続グラフを Opus で確定（D2 と同じ粒度・未着手）→ (2) D1→D2 を ⚡ Sonnet で JSON 流し込み → (3) `scripts/check-dungeon-connectivity.mjs` で検証。** ⚠️ **D2 は接続グラフ＋開口座標まで確定済み＝Sonnet 可**だが、**D1 はまだ「撤去/報酬/規模」までで接続グラフ未確定**＝先に Opus で D1 の16〜20部屋接続図と開口座標を D2 と同様に詰める（でないと D1 でつなぎが壊れる）。その後 Sonnet 流し込み。続けて 9-2c（沼地ボス新設）・9-2{d〜}（D3〜7）/9-3。

---

### 2026-06-23 — Phase 9-2c（ボス＝欠片の不変条件を確定・Opus・コード変更なし）：大型ボス8＋最終1＝計9ボス。現状7体ズレを発見、cave_1 に8体目を新設する方針に

**やったこと（分析・記録のみ・コード変更なし）：**
- **ユーザー指摘「ボスは8＋最終1では？」を実データで検証：** `dropsTriforce:true` の大型ボスは**7体のみ**（G/N/J/A/L/O/U＝D1〜D7）。**cave_1 には大型ボスがおらず `W`（MONSTER＝欠片を落とさない雑魚ボス）だけ**だった＝まさに「たまたま小さい洞窟に小さいボスを置いている」状態。欠片必要数は `countTriforces()` がマップ集計で動的決定するため**現状は実質7欠片**で、ストーリー／9-1連鎖の8欠片前提とズレている（潜在バグ）
- **方針確定（ユーザー選択）：** **cave_1 に新規の沼地大型ボス（2×2・dropsTriforce:true）を作り、`W` を置き換えて8体に揃える**。3-2 の型（スプライト→ENEMY_META→向きエイリアス→tiles.js→配置）で作成。欠片数は自動で 7→8 に揃う
- **不変条件を明文化：** 欠片は「ボス撃破でのみ入手」。大型ボス8＝欠片8、最終ボス ザーネル1＝欠片なし＝**ボス計9**。チェスト/フロアで欠片を撒かない
- **記録：** PLAN.md 9-1 連鎖表 cave_1 行を更新＋「ボス＝欠片の不変条件」「現状7体ズレ」を明記、9-2c タスク新設／DECISIONS.md に「Phase 9-2c」追記／PROGRESS.md 現在地更新

**学び・気づき：**
- **「8ダンジョン完了」の器思考が欠片数にも漏れていた**。9-2a で「ダンジョンは器だけ」を見抜いたが、ボス＝欠片の対応も7/8で破綻していた。`countTriforces()` が動的計算で「落ちないので必要数も7」と帳尻を合わせてしまい、エラーにならず**静かにズレを隠していた**。動的計算は便利だが「本来あるべき数」を固定の不変条件として明記しないと破綻に気づけない
- **cave_1 の `W` は中ボスのつもりが「欠片を落とさない雑魚」だった**。MONSTER(W) と大型ボス(dropsTriforce付き)は別物。8体目は固有スプライトの大型ボスとして作り、他7体と統一感を出す

**▶ 次やること：** **Phase 9-2（D2 砂漠から1つずつ作り込み）/9-2c（沼地大型ボス新設）/9-3（チュートリアル＋ガイド）🧠 Opus**。9-2c は新ボス1体の作成（型は3-2で確立済み）。各ダンジョンの体験設計は一点物として Opus で1つずつ。

---

### 2026-06-23 — Phase 9-2a（雛形レビュー＋移設対象 洗い出し・Opus・コード変更なし）：dungeon_1 は「網状の辺スクロール」が雛形。進行6アイテムは10箇所に散在＝撤去対象を確定

**やったこと（分析・記録のみ・コード変更なし）：**
- **dungeon_1（12部屋）の構造を実データで解析し「仕組みの参照元」として整理：** 6×3 グリッドの画面を**辺スクロールで接続**（`>` MAP_ENTER は外部マップ/階段専用・部屋間は画面端スクロール＝初代ゼルダ式。game.js の遷移ロジックで確認）。**⚠️ dungeon_1 はお手本（品質基準）にはしない**（小さく簡単すぎる）＝借りるのは仕組み（辺スクロール網・既存ギミックタイル `torchesLit`/`killAll`/ボタン→ゲート`links`/壊せる壁 の使い方）だけ。改善点＝[2,1] が素通り廊下→各ダンジョンに最低1つ「鍵と扉/仕掛けで足止めする部屋」を置く指針を得た
- **全アイテムの初期所持→報酬 移設の撤去対象を node 集計で確定（10箇所）：** ブーメラン（tile+孤児chest+ショップ）/爆弾（tile+floorItem+ショップ）/弓矢（タイル6枚）/ロウソク（chest）/笛（chest）/はしご（chest dungeon_3）。**座標規約＝`tiles[row][col]`／chest・floorItem キーは `"row,col"`** を突き合わせて確定。**ブーメランの `field/1,0["7,6"]` は対応Bタイル無しの孤児（拾えない死にデータ）**と判明。撤去対象表を PLAN.md 9-2a に記載
- **派生整理対象も洗い出し（9-3）：** 木の剣2箇所・聖剣(tier3)2箇所・伝説の鎧(tier2)2箇所の重複。最初の剣は村スタート出口に半強制1本へ集約予定
- **記録：** PLAN.md に **9-2a（✅完了）＋撤去対象表** を新設／DECISIONS.md に「Phase 9-2a」を追記／PROGRESS.md 現在地を更新

**学び・気づき：**
- **「量産」は誤った要約だった（ユーザー指摘で訂正）**。ボス入りダンジョンは9個だけの一点物で、1つずつ設計意図を持って作り込む対象。stage を継ぎ足す**接続**は機械的でも、**体験（テーマ地形・固有の謎・難易度カーブ・足止め）は手作業で詰める**。各ダンジョンの作り込みは原則 🧠 Opus、確定データの流し込みのみ ⚡ Sonnet
- **9-1 鉄則の帰結＝古典ゼルダ型の自己完結構造は使えない**。各ダンジョンは「それより前のアイテムだけでクリア可能」なので、報酬アイテムは**ボスの最奥**に置く（中盤で拾って奥で使う、はしない）。作り込みどころは「直前までに得たアイテムを鍵として要求する部屋」に置く
- **「アイテムが散在している」は感覚でなく集計で確定すべき**。tile・chestContents・floorItems・shopData の4経路を全レイヤー走査して初めて10箇所＋孤児データ1件が見えた。grep だけだと孤児chest（拾えない死にデータ）の存在に気づけなかった
- **座標規約の取り違えは移設バグの温床**。`tiles[row][col]` と chest キー `"row,col"` を実データで突き合わせて規約を確定したので、9-2/9-3 の撤去・再配置で座標を間違えない

**▶ 次やること：** **Phase 9-2（ダンジョンを1つずつ作り込む）/9-3（チュートリアル＋ガイド導線）の実装 🧠 Opus**。量産ではなく一点物。まず最初の1つ（D2 砂漠＝ブーメラン報酬）を Opus で作り込み設計（テーマ地形・固有の謎・難易度カーブ・足止め部屋・直前アイテム鍵）。PLAN.md 9-2a 表どおり撤去→報酬は各ボスの最奥へ。🔑鉄則（前アイテムだけでクリア可能）を1つずつ検算。

---

### 2026-06-23 — Phase 9-1（設計・中盤/終盤・連鎖完成／対話・Opus）：ゲートは「フィールド常時フルオープン＋ダンジョンをアイテムでロック」。アイテムは影響力の小さい順に配る

**やったこと（設計・対話のみ・コード変更なし）：**
- **ゲート方式をユーザーと確定：** マップ全体は**常にフルオープン**（フィールドの道は塞がない）。進行順は**各ダンジョンの「入口」または「奥への通路」を、前のダンジョンで得たアイテムでロック**して自然に作る。**NPC・看板はガイドに徹する**（次の行き先・必要アイテムを示すだけ）。`linesAfterBoss[type]`（Phase 6-1b・`defeatedBosses` 連動）でボスを倒すたびに台詞が次へ進む＝ほぼゼロコードの動的ガイド
- **⚠️ 序盤の「NPC 門番（通行ゲート）」案を撤回**（フィールドを塞ぐのはフルオープン方針と矛盾）。D1→D2 はガイドのみ・順序強制は D3 以降のアイテムロックで効かせる
- **アイテム影響力を実測**（`work/blade-of-lumia.json` を node で集計）：**爆弾＝壊せる壁14（field/D1/D4/D6）／はしご＝水56（D1/D3/D7）＋穴2／笛＝ワープ・隠し入口**が「地形/移動を開く"マスターキー"級」。**ブーメラン・弓矢・ロウソク（茂み2/かがり火3）は1ヶ所を開ける狭い鍵**。→ ユーザー懸念（影響力の大きいアイテムを先に渡すと後が簡単になる）を踏まえ、**狭い鍵→広い鍵→ワープ**の順に配分
- **連鎖を確定（順番はユーザー承認）：** 村→剣／D1 ゴーレム→木の盾＋①／D2 蠍王→ブーメラン＋②／D3 海蛇→弓矢＋③（要ブーメラン）／D4 サラマンドラ→ロウソク＋④（要弓矢）／D6 巨人→爆弾＋⑤（要ロウソク）／D5 リヴァイアサン→はしご＋⑥＋鉄盾（要爆弾）／cave_1 沼地→笛＋⑦＋ミラー盾（要はしご）／D7 鷲王→⑧（要笛＝空島ワープ）／祭壇（全8欠片）→翼の羽衣→暗黒の塔→ザーネル
- **弱点の噛み合いを実データで確認**：D4=弓矢・D6=fire(ロウソク)・D5=fire・D7=弓矢 が「直前ダンジョンの報酬＝次ボスの弱点」と一致。**ボス配置を一切動かさずに**連鎖が成立（弱点は任意ボーナス・剣でも倒せる＝ユーザー選択）
- **記録：** PLAN.md 9-1 を全項目チェック済み（設計完了マーク）＋連鎖表・ガイド方針・🔑鉄則を追記／DECISIONS.md に「Phase 9-1（中盤/終盤・連鎖完成）」を追記／PROGRESS.md 現在地を更新

**学び・気づき：**
- **「アイテムの影響力」は感覚でなく実データで測れる**。各ゲートタイル（壊せる壁・水・茂み・かがり火）を全レイヤー集計すると、爆弾(14)・はしご(56)が突出して「世界を開く」＝マスターキーだと一目で分かった。配る順を「狭い鍵→広い鍵」にする根拠が定量化できた
- **フルオープン＋アイテムロックは、既存の `defeatedBosses`／`linesAfterBoss`／各種ギミックタイルだけで成立する**。新サブシステム不要。順序強制は「ダンジョン内部の通路をアイテムでロック」、誘導は「NPC 台詞をボス撃破で進める」に役割分担できる
- **連鎖設計の鉄則：各ダンジョンは"それより前のアイテムだけでクリア可能"でなければ詰む**。自分の報酬アイテムを内部で要求してはいけない（例：D3 の水は既存橋タイル `v` で渡らせ、はしご＝D5報酬を要求しない）。9-2 のビルド時の最重要制約

**▶ 次やること：** **Phase 9-2（ダンジョン作り込み）/9-3（チュートリアル＋ガイド導線）の実装 🧠→⚡**。まず dungeon_1 を実プレイでレビューして雛形化＋全アイテムの初期所持→報酬移設の撤去対象を grep で洗い出す。設計が PLAN/DECISIONS に書き出し済みなので、雛形レビュー後の量産は ⚡ Sonnet 中心。

---

### 2026-06-23 — Phase 9-1（設計・序盤／対話・Opus）：「8ダンジョン完了」は器だけだったと認め、ハイブリッド型の連鎖で背骨を作り直す

**やったこと（設計・対話のみ・コード変更なし）：**
- **本来は Phase 8-1（ゲームオーバー体験改善）に着手予定だったが、ユーザーとの対話で「そもそも 8-1 は要らない」「それ以前にマップがゲームとして成り立っていない」という根本的な指摘を受け、計画全体を見直す分岐（分岐A＋C）に切り替えた**
- **実データで現状を確認（`work/blade-of-lumia.json`）：** 本物のダンジョンは **dungeon_1（12部屋）のみ**。dungeon_2〜7・cave_1・dark_tower は**各2部屋スタブ**（入口部屋＋ボス部屋）。Phase 2（2-1〜2-4）は全て「完了」だったが、実態は2部屋スタブを量産しただけで「ダンジョンを本物に作り込む」タスクが PLAN に存在しなかった
- **診断：** ゼルダの背骨「ダンジョン攻略→固有アイテム→次のエリア解放」が無い。アイテム6種は全部最初から拾えて進行に紐づいていない／ダンジョンに攻略順の意味がない／フィールドに再訪の喜びがない＝「鍵穴と鍵」はあるが「解く順番と理由」が無い
- **進行構造をユーザーと確定：ハイブリッド型（緩い連鎖）**。序盤一本道→中盤自由→終盤収束
- **序盤の連鎖を確定：**
  - 何も持たずスタート → 最初のステージで剣を半強制ピックアップ（出口が剣の先）→ 剣だけ・盾なしで雑魚と戦い基本を体得
  - **dungeon_1 ＝盾を覚える入門ダンジョン**：報酬は欠片① ＋ 木の盾（最下位ティア・ガードのみ・跳ね返しなし）だけに絞る
  - **D1→D2 は NPC 門番方式**（`defeatedBosses` 連動で台詞・通行を切替）
  - **dungeon_2（砂漠）＝最初の進行アイテム**：報酬は欠片② ＋ ブーメラン
- **記録：** PLAN.md に Phase 9（★★★最高）新設＋Phase 2 ヘッダに見直し注記／DECISIONS.md に「Phase 9（設計・序盤）」を追記／PROGRESS.md 現在地を更新

**学び・気づき：**
- **「完了」チェックが付いていても実態を疑う必要がある**。Phase 2 は8ダンジョン分のチェックが全て `[x]` だったが、中身は2部屋スタブの器だった。「8つになるまで追加」というタスク文言を字面通り満たしただけで、ゲーム体験としては未達だった
- **規模（部屋数）を先に決めようとするのは順序が逆**。「どんな体験をさせたいか（連鎖の役割）」が決まって初めて、各ダンジョンが何の道具を与え何を解放するかが決まり、それに必要な部屋数が従属的に決まる
- **「ボスは強い」と数字（hp/atk）だけ見て言ってしまったのは誤り**。プレイヤー側だけ強化が積み上がり敵は据え置きで、相対的にプレイヤーが一方的に強い。8-4 は微調整でなく本格リバランスが要る

**▶ 次やること：** **Phase 9-1 の続き（中盤・終盤の連鎖をユーザーと対話で詰める）🧠 Opus**。中盤＝D3/4/6 を任意順・はしご/弓矢/ロウソク/爆弾の分配。終盤＝D5・cave_1・D7→祭壇→塔。＋全アイテムの初期所持→報酬移設方針。設計が固まったら 9-2/9-3 の実装（⚡ Sonnet 中心）へ。

---

### 2026-06-22 — Phase 7-4（実装フェーズ・Sonnet）：リスク・リワード実装完了（Phase 7 全完了）

**やったこと：**
- **`game/player.js`**：`grantReward(content)` を新設（item/weapon/armor/shield/rupee/heartContainer/ladder の付与 switch）。`openChest` はこれを呼ぶように変更（チェスト付与の二重実装を解消）。`grantReward` を return オブジェクトに追加
- **`game/game.js`**：`let grantReward = () => ''` 追加、`_player.grantReward` 配線。`player.gachaPulls = {}` を初期化2箇所（グローバル＋`startNewGame`）に追加。`callGrantReward` テスト用エクスポートを追加
- **`game/ui.js`**：`openShop(shopData, posKey)` に posKey 引数追加（ガチャ items に `_posKey` 注入）。`shopBuy` にガチャ分岐（`g.gacha` があれば：price チェック→`gachaPulls[key]++`→天井到達で `pityReward`＋リセット／未満は重み付き抽選→`grantReward`→`playSound`/`pulse`）
- **`game/combat.js`**：`openShop(shopData, posKey3)` に posKey3 も渡すよう変更
- **`game/main.js`**：`callGrantReward` をインポートし `__game.grantReward` として公開
- **`work/blade-of-lumia.json`（配置）：**
  - **field 2,0 (6,9)**：ガチャ NPC `$` を追加。shopData に天井 8回・price 30・`pityReward:heartContainer`・pool 4段のガチャデータ追加
  - **dungeon_1/3,0 (3,8)**：強敵 `W`、**(3,10)**：封印宝箱（`killAll` でゲート・伝説の鎧 armorTier:2）配置。signData (1,1)「奥に強き者あり…」リスク予告石碑
  - **dungeon_1/4,0 (2,8)**：ミラーシールド（shieldTier:2）宝箱を難所報酬として追加
- **テスト**（`tests/gacha-risk-reward.spec.js`・9本）：①grantReward ルピー付与 ①b ハートの器付与 ②pulls 変化なし確認 ③random=0 で先頭枠 ③b random=0.99 で末尾枠 ④a pityCount-1 で天井未満 ④b pityCount 回目でレア＋リセット ⑤gachaPulls 保持 ⑥killAll showConditions 確認。**全9パス**

**テスト結果：** 145 passed / 1 failed（ladder 145行は既存不安定・今回変更と無関係）

**学び・気づき：**
- **`grantReward` の切り出しは「付与だけ行い、SE/メッセージ/saveGame は呼び出し側」とするのが正解**。チェストは `playSound('chest')` → pulse（宝箱風メッセージ）→`saveGame`、ガチャは `playSound('appear'/'item')` → `pulse('✨...')` → `saveGame` と演出が異なるため、付与ロジックだけ共通化した。`grantReward` がメッセージ文字列を返す設計にしたことで、各呼び出し側が自由にフォーマットできる
- **ガチャ天井カウンタのキー `"layer:stageKey:posKey"` は 3 段結合が必須**。`posKey` だけだと別レイヤー/別ステージに同じ座標のガチャが複数あるとカウンタが混同される。`_posKey` を `openShop` で items に注入する設計にしたことで、`shopBuy` は呼び出し元の座標を知らずにキーを生成できる
- **ガチャの `_random` はテスト注入式にしたが、shopData の pool に直接書く方式が最もシンプル**。`g.gacha._random = () => 0` のようにテスト側で注入する方式にすれば、ゲーム本番コードに `_random` の特別処理を持ち込まずに決定論テストができる

**▶ 次やること：** **PLAN.md の Phase 8 を確認して次タスクを進める**。Phase 7 全完了。

---

### 2026-06-22 — Phase 7-4（設計フェーズ・Opus）：リスク・リワード＝①②はゼロコード配置／③ガチャは既存ショップに天井つき抽選を1分岐

**やったこと（設計のみ・コード変更なし）：**
- Phase 7-4 は 🧠 Opus（設計未着手）だったため、まず設計を確定。実装は次フェーズ（⚡ Sonnet）に回す
- **コード調査で「7-4 の大半は既存資産」と判明**：①「強敵＋宝」②「高難度エリア＋報酬」は `showConditions:{trigger:'killAll'}`（`conditions.js:95`）＋封印宝箱（`player.js:796-800`）＋石碑だけで成立＝**ゼロコード**。③も**固定価格ショップは完成済み**（`ui.js:446-518` `openShop`/`shopBuy`、`shopData` 機構、`combat.js:231-232` で NPC タイル→`openShop`、field 2,0「旅の商人」配置済み）。**7-4 の唯一の新規実装は③のガチャ（抽選購入）だけ**
- **設計フォークをユーザーと確定**：(1) ガチャは**天井つき（pity）**——ハズレ込み抽選だが N回引くと確定でレア。(2) このセッションは**設計のみ**（①②の配置も次フェーズ）
- **設計を3ファイルに記録**：DECISIONS.md「Phase 7-4（設計）」（決定・データ構造・抽選/天井ロジック・`grantReward` 共通化・乱数注入・代替案）／PLAN.md 7-4 を実装契約に具体化（🧠→⚡ に更新・設計完了マーク）／PROGRESS.md 現在地を更新

**設計の要点（次セッションの実装契約）：**
- **③ ガチャは既存ショップに相乗り**：新タイル・新オーバーレイを作らず、`shopData[posKey].items[]` の good に**抽選プール型 `gacha`**（`price`/`pityCount`/`pityReward`/重み付き`pool`）を追加。`ui.js shopBuy` に「good に `gacha` があれば抽選」分岐を足す
- **天井（pity）の状態は `player.gachaPulls`（プレーンオブジェクト）**：キー=`"layer:stageKey:posKey"`→引いた回数。`player` は JSON シリアライズで save されるので**プレーンオブジェクトなら自動永続**（Set は `{}` になるので不可＝`defeatedBosses` の教訓）。`pulls >= pityCount` で確定レア＋カウンタ0リセット
- **報酬付与を `grantReward(content)` に共通化**：`player.js openChest` の付与 switch（item/weapon/armor/shield/rupee/heartContainer/ladder）を切り出し、チェストとガチャの両方から呼ぶ（付与のみ・SE/メッセージ/saveGame は呼び出し側）。チェストとガチャで付与ロジックが二重化しない
- **乱数はショップ factory に `random` 依存を注入**（既定 `Math.random`）。テストはシード固定で「特定枠が出る」を決定論検証（ボスAIは `Math.random` 直書きだが、ガチャは結果がテスト対象なので注入式）
- **①② は `killAll` 封印宝箱＋石碑のデータ配置**：メインルートを塞がない任意の脇道に「強敵＋封印宝箱」、難所（隠し通路/色スイッチ/敵石押し）の先に上位ティア装備（7-1/7-2）・ハートの器（7-3）

**学び・気づき：**
- **「新機能」に見えて既存資産でほぼ完成しているケースは、実装前にコード調査するのが効く**。7-4 は調査前は「ショップ新設＋選択ギミック新設」に見えたが、固定ショップ・`shopData`・封印宝箱（`killAll`）が全て揃っており、実質の新規は「ガチャ抽選＋天井カウンタ」だけだった。配置だけで成立する①②を新サブシステム化すると既存の封印機構と二重化する
- **ガチャの天井カウンタは `player` のプレーンオブジェクトに持たせるのが save 最小**。Set は `JSON.stringify` で消えるので不可。`defeatedBosses`（Set→配列変換が必要だった）と違い、最初からオブジェクトにすれば変換コードすら要らない
- **報酬付与の共通化（`grantReward`）は将来の拡張に効く**：チェスト/ガチャ/敵ドロップに報酬種を増やしても1箇所で全経路に反映。`shared/` 集約の単一の真実方針と同じ

**▶ 次やること：** **Phase 7-4 の実装（⚡ Sonnet）**。設計が DECISIONS.md/PLAN.md に書き出し済みなので Sonnet 推奨。`grantReward` 切り出し（チェスト回帰確認）→ ガチャ分岐＋天井カウンタ＋`random`注入 → 初期化2箇所 → デモ配置 → テスト6観点。

---

### 2026-06-22 — Phase 7-3（完了・Sonnet）：ハートの器の増設

**やったこと：**
- **`game/player.js`**：上限チェックなし（マップ配置数が自然な上限になるため不要）
- **`game/game.js`**：`getGameState` に `maxHearts` を追加（テスト用スナップショット）。`callGainHeartContainer` テスト用関数を新設・export
- **`game/main.js`**：`callGainHeartContainer` を `__game.gainHeartContainer` に公開
- **配置（`work/blade-of-lumia.json`）**：ハートの器を計+9個追加（合計14個）：
  - **ボスルーム宝箱変更（7個）**：dungeon_1 ゴーレムボスルーム(2,0)に宝箱新設、dungeon_2〜7 の既存ルピー宝箱をハートの器に変更
  - **隠し場所（2個）**：field/3,0 (4,8)・dungeon_1/4,0 (8,8) に宝箱新設
- **テスト**（`tests/heart-containers.spec.js`・3本）：①gainHeartContainer で maxHearts+1・maxHp+2・hp リセット、②ダンジョンボスルーム7箇所の存在確認（データレベル）、③隠し場所2箇所の存在確認。**137 passed / 1 failed（ladder 既存不安定）**

**テスト結果：** 138 中 137 passed / 1 failed（ladder 145行は既存不安定・今回変更と無関係）

**学び・気づき：**
- **ボスルームの宝箱をハートの器にするのは「ボスを倒した達成感と回復の両方を提供する」設計**として自然。dungeon_2〜7 のルピー50は金銭的価値が低かった（後半ではルピー不足にならない）ので差し替えのロスは小さい
- **上限は「マップに置いた器の数」が自然な上限**。コードで MAX_HEARTS を定義する必要はない。置いた数を超えて取れない構造になっているので人工的な cap は不要
- `getGameState` に `maxHearts` がなかったためテストが `undefined` を受け取っていた。E2E テストでプレイヤー状態を確認する際は、必要フィールドが `getGameState` に含まれているか事前確認が必要

**▶ 次やること：** **Phase 7-4（リスク・リワード設計）🧠 Opus**。設計未着手なので Opus 推奨。

---

### 2026-06-22 — Phase 7-2（設計＋実装・Opus）：防具ティア＋盾の「投擲物跳ね返し」

**やったこと（設計→実装→検証を一括）：**
- **設計フォークをユーザーと確定**：防具は剣 7-1 と同型（ティアごとDEFアップ）。盾は当初「①全方向軽減率／②角度拡大／③DEF加算」を提示したが、ユーザー発案の **「強い盾は敵の投擲物を跳ね返せる」** を採用。さらにユーザー要求で **「正面ブロックは常に成立／剣振り中・チャージ中は盾オフ／跳ね返しは投擲のみ（剣＝近接はガードのみ）／跳ね返し威力は鉄0.5・ミラー1.0」** を確定。DECISIONS.md に記録
- **`shared/items.js`**：`ARMOR_TIERS`（cloth/chain/legend・def 2/4/7）・`SHIELD_TIERS`（wood/iron/mirror・reflect 0/0.5/1.0）・`BASE_DEF=0` を新設
- **`game/player.js`**：`equipArmorTier`/`equipShieldTier` を新設（剣の `equipSwordTier` と同型・ティア比較で下位無視）。防具は `def=BASE_DEF+tier.def` 再計算に一本化。チェスト（armor/shield）・フロアアイテム（ITEM_ARMOR/ITEM_SHIELD）の3経路をティア判定に統一（**チェスト経路の無条件加算バグも解消**）
- **`game/projectile.js`（核心）**：`isShieldActive()` を新設（剣クールダウン中＝`gameNow-getLastSwordTime<SWORD_COOLDOWN_MS`／チャージ中は盾オフ）。`isShieldBlockingDir` の先頭で参照。`checkProjHit` の敵投擲物ブロック分岐に**跳ね返し**を追加：`reflect>0` なら proj を消さず `dx/dy` 反転・`owner→'player'`・`atk=round(元atk×reflect)` にして敵に当てる。自爆防止に 0.5 ずらし＋`_hitIds` 初期化
- **`game/game.js`**：`armorTier`/`shieldTier` を player 初期化2箇所に追加。projectile factory に `getLastSwordTime`/`getIsCharging` を注入（charge factory の `isCharging` を `getIsCharging` グローバルに配線）。`getGameState` に def/各tier を公開。プレビュー `ps_shield`/`ps_armor` をティア装備呼び出しに変更。テスト用 `callEquipArmorTier`/`callEquipShieldTier`/`injectEnemyProjectileForTest` を新設
- **`game/main.js`**：`equipArmorTier`/`equipShieldTier`/`injectEnemyProjectile` を `__game` に公開。getProjectiles スナップショットに `atk` を追加
- **`game/ui.js`**：ポーズ画面ステータスに盾ティア名（`🛡{name}`）を表示
- **`shared/sprites-items.js`**：armorCloth/Chain/Legend・shieldWood/Iron/Mirror のパレット6種を追加（形状は既存 armor/shield を流用＝`ITEM_SPRITES.armorCloth = ITEM_SPRITES.armor` 等）
- **`editor/editor-props.js`**：宝箱種別に「盾」を追加。weapon/armor/shield 選択時にティア選択 select を表示（種類変更で動的再構築）。フロアアイテム編集も剣/防具/盾のティア選択に刷新
- **`work/blade-of-lumia.json`**：ネタ防具（ながT/じょうぶな服/伝説のよろい/さいごの鎧）を布(0)/鎖(1)/伝説(2) に再配置。鉄の盾(1) を dungeon_4 entry(2,8)・ミラーシールド(2) を暗黒の塔入口(1,9) に新規配置
- **テスト**（`tests/armor-shield-tiers.spec.js`・7本）：①下位防具で def 不変 ①b 布で def=2 ②木の盾は跳ね返さない ③鉄=×0.5跳ね返し（atk=2・dx反転）④ミラー=×1.0で背後の hp3 敵撃破 ⑤チャージ中は盾オフでダメージ ⑥HUD防具名/DEF。**全7パス**

**テスト結果：** 134 中 133 passed / 1 failed（ladder 145行は今回変更と無関係。stash で HEAD に戻して実行しても同じく fail することを確認済み＝既存不具合）。editor スモーク 8本もグリーン

**学び・気づき：**
- **「投擲物の跳ね返し」は新サブシステム不要**。敵投擲物は元から `owner:'enemy'` を持ち `checkProjHit` が owner で分岐するので、ブロック時に proj を消さず **dx/dy 反転＋owner→player＋atk×reflect** にするだけで、既存の player 投擲物ヒット経路がそのまま「敵への攻撃」に転用できた。剣ビーム（7-1/3-1）の `_hitIds` 貫通防止とも自然に整合
- **「盾を剣振り中・チャージ中にオフ」は通行判定でなく `isShieldBlockingDir` のゲートで表現**。`getLastSwordTime`（既存）＋`getIsCharging`（charge factory の isCharging を配線）を見るだけ。盾の有無→剣との同時成立を防ぐ手触りが、新フラグ無しで出せた
- **盾は DEF に寄与させない**のが設計の肝：DEF加算にすると防具と計算・役割が完全重複する。正面ブロックが常時完全な以上、上位盾の価値は「正面ブロック成立時の追加行動＝跳ね返し攻撃」に置くのが筋（ユーザー発案が設計的にも正解だった）

**▶ 次やること：** **Phase 7-3（ハートの器の増設）⚡ Sonnet**。配置作業中心で設計不要なので Sonnet 推奨。

---

### 2026-06-22 — Phase 7-1（実装フェーズ・Sonnet）：剣ティア制・ビーム解禁・貫通ゲートの実装完了

**やったこと（コード変更あり）：**
- **`shared/items.js`**：`SWORD_TIERS` 配列（wood/bronze/silver/holy）と `BASE_ATK=2` を追加。各ティアに `atk/sprite/pal/beam/pierce` を持つ
- **`game/game.js`**：`player` 初期化と `startNewGame` リセットに `swordTier: -1` を追加。テスト用エクスポート（`getPlayerForTest`/`callEquipSwordTier`/`callUpdateHud`/`equipSwordTier` 変数）を追加
- **`game/player.js`**：`equipSwordTier(tierIndex)` ヘルパー追加（ティア比較→atk再計算）。チェスト `weapon` 経路（無条件加算バグ）と `ITEM_SWORD` タイル経路の両方をティア判定に統一。`equipSwordTier` を return object に追加
- **`game/charge.js`**：`SWORD_TIERS` インポート追加。`fireBeam` でティアの `beam` フラグをチェック（木の剣ではビーム不可）、`pierce` フラグで貫通判定
- **`game/combat.js`**：`showSwordSlashFloat` に `tier-{key}` クラスを追加（ティア別斬撃エフェクト用）
- **`game/css/effects.css`**：`.sword-thrust.tier-wood/bronze/silver/holy::before` ティア別グラデーション＋グロー色を追加
- **`shared/sprites-items.js`**：`ITEM_PAL.swordWood/Bronze/Silver/Holy` と対応 8×8 スプライトを追加（2フレームアニメ対応）
- **`work/blade-of-lumia.json`**：ネタ剣を4ティアに再配置（field ストロー→木の剣tier0、dungeon_1 強いけん→銀の剣tier2、アルテマウェポン/さいごの剣→聖剣tier3）
- **`game/main.js`**：新エクスポート3件のインポートと `__game` への公開
- **`tests/sword-tiers.spec.js`**（新規）：6テスト全件パス（①下位剣ATK不変、①b無剣→木装備、②木ビーム不可、③銅非貫通、④聖剣貫通、⑤HUD名称ATK）
- **`tests/charge-beam.spec.js`** および **`tests/large-enemy.spec.js`** を `swordTier` 追加で修正（銅の剣tier=1を明示）

**テスト結果：** 126 passed / 1 failed（ladder tests/145行は今回変更と無関係の既存不具合）

**▶ 次やること：** Phase 7-2（防具・盾の強化版）—⚡ Sonnet 推奨

---

### 2026-06-22 — Phase 7-1（設計フェーズ・Opus）：剣をティア制にし、ビーム解禁・満タン貫通をティアに紐づける

**やったこと（設計のみ・コード変更なし）：**
- Phase 7-1（剣の段階的強化）は 🧠→⚡ で設計未着手だったため、まず Opus で設計を確定。実装は次フェーズ（⚡ Sonnet）に回す
- **コード調査で「剣強化機構は7割できている」と判明**：`player.atk:2`／`weapon:null` 開始で、`ITEM_SWORD` タイル経路は**すでに「上位剣だけ持ち替える」アップグレード判定**＋`_equip.swordName`＋HUD表示（`⚔{名前}(ATK{atk})`）を持つ。ただし **チェスト `weapon` 経路は無条件加算（持ち替え判定が抜け）** ＝ティア化すると下位剣で atk が下がるバグになる。配置はすべてネタ/テスト用（わりばし/ストロー/うまいぼう/アルテマウェポン/atkBonus 10000）
- **仕組みをユーザーに確認 → 方針確定**：(1) **ティア方式**（木→銅→銀→聖の4段階）を採用、(2) **ビーム（チャージ攻撃）の使用を特定ティア以上に解禁**、(3) **満タンで貫通する強ビームは最強の剣（聖剣）のみ**
- **設計を3ファイルに記録**：DECISIONS.md「Phase 7-1（設計）」（決定・データ構造・暫定ATK配分・ビーム解禁の実装方針・チェスト経路バグ修正・代替案）／PLAN.md 7-1 にチェック項目を具体化（設計完了マーク）／PROGRESS.md 現在地を更新

**設計の要点（次セッションの実装契約）：**
- **`shared/items.js` に `SWORD_TIERS`（wood/bronze/silver/holy）を新設＝剣の段階の単一の真実**。各ティアが name・atk・sprite・slash色・**beam可否・pierce可否**を持つ。`player.swordTier`（-1=剣なし）で管理し、**player.atk は「基礎2＋ティアatk」の再計算**に一本化（拾うたび加算をやめる＝差分計算バグの根を断つ）
- **3経路（ITEM_SWORD タイル／床 floorItems／チェスト weapon）の持ち替えをティア判定に統一**（チェスト経路の無条件加算バグも同時に解消）
- **ビーム解禁をティアに紐づけ**（`charge.js`）：木=ビーム不可／銅・銀=弱ビーム可・満タンでも非貫通／聖剣のみ `piercing = full && tier.pierce`。これは **Phase 8-4 の保留課題「ビームが強すぎる」への構造的な布石**（強さがコンテンツ進行でアンロックされる）
- **見た目**：ティアごとの拾得/HUDスプライト4種＋斬撃エフェクト色（`.sword-thrust` に `tier-*` クラス）。形状・リーチ・クールダウンは不変
- **ATK絶対値の最終チューニングは Phase 8-4 に委ねる**。7-1 は「ティアの枠組みと相対関係」を作るのが目的（個別に数値をいじって全体難易度がブレるのを避ける既存方針と整合）

**学び・気づき：**
- **「剣ランク」は一見新機能だが、既存の atkBonus 持ち替え機構の延長線上にある**。生数値の大小比較を**ティア番号比較に置き換える**だけで、散らばっていた持ち替え判定・atk のズレ込み・チェスト経路の無条件加算バグを一掃でき、将来の剣追加も表の1行で済む
- **ビーム解禁をティアに紐づけると Phase 8-4 の「ビーム強すぎ」課題が設計レベルで緩和できる**。数値（威力・チャージ時間）だけで抑えるのではなく「序盤は撃てない・貫通は最終剣のみ」という**アンロック設計**にするのが筋がよい

**▶ 次やること：** **Phase 7-1 の実装（⚡ Sonnet）**。設計が DECISIONS.md/PLAN.md に書き出し済みなので Sonnet 推奨。

---

### 2026-06-22 — Phase 6-4（完了・Sonnet）：嘘をつくNPC（Phase 6 全完了）

**やったこと：**
- **3体の嘘つきNPCを配置**（`work/blade-of-lumia.json` のみ変更・新コードゼロ）：
  - **field 1,1 (3,2) 通りすがりの冒険者（`a`）**：「ブーメランは役に立たない」「柵の向こうに何もない」「宝箱はない」→実際はブーメランで鍵を回収し宝箱が取れる（Phase 4-4 パズル）
  - **field 2,0 (4,5) 怪しい旅人（`a`）**：「茂みを燃やしても意味ない」「行き止まりだ」→実際はロウソクで茂みを燃やすと隠し入口が現れる（Phase 4-3 パズル）
  - **field 1,0 (7,3) 諦めた老人（`b`）**：「祭壇に触っても何も起きない」「女王は助けられない」→実際は星の欠片を集めると祭壇で翼の羽衣が手に入る（Phase 1-4 メインシナリオ）
- **テスト**（`tests/lying-npcs.spec.js`・4本）：①②③ 各NPC のタイル位置・npcData・嘘の内容の存在確認（データレベル）、④ field 1,1 冒険者に話しかけるとダイアログが開く（E2E）。**120/121テストグリーン**（+4本、ladder既存1本のみ以前から不安定）

**学び・気づき：**
- **「嘘をつくNPC」は既存の npcData 機構で完結する**。`a`/`b` タイルを空きマスに置き、`npcData` に名前と台詞を追加するだけ。専用フラグや状態管理は一切不要
- **嘘の台詞は「実際に存在するギミックを直接否定する」ように書くのが効果的**。「何もない」「無駄だ」「意味ない」と言えば言うほど、プレイヤーが自分で確かめたくなる仕掛けになる
- **配置位置はギミックの近く**（冒険者はブーメランパズルの直近、旅人は茂み隠し入口の隣行、老人は祭壇タイルの2マス北）にすると「この嘘はこのギミックのことだ」という対応が伝わりやすい

**▶ 次やること：** **Phase 7-1（剣の段階的強化）🧠 Opus**。バランス設計（各剣のATK値・入手箇所・見た目変化）が未着手なので Opus 推奨。

---

### 2026-06-21 — Phase 6-3（完了・Sonnet）：フィールドの充実（廃城・廃村エリア）

**やったこと：**
- **`field 0,2` を廃城・廃村エリアに全面的に作り替え**（`work/blade-of-lumia.json` のみ変更・新コードゼロ）
  - 旧: `~`（水）が大部分を占めるほぼ空のステージ（宝箱1個だけ）
  - 新: 廃城エリア（rows 0-4）＋廃村エリア（rows 5-9）の2ゾーン構成
- **廃城エリア（rows 0-4）**：石畳 `o` の床、石壁 `#`、壊せる壁 `!`（爆弾で壊せる）、敵 `E`（巡回兵）、宝箱（ルピー大）。bgTiles に石畳 `o` を設定し廃墟の床質感を演出
- **廃村エリア（rows 5-9）**：家の外壁 `h`・屋根 `p`・ドア `e` で廃屋2棟を配置。bgTiles に草地 `g` を設定。宝箱（ハートの器）を配置
- **2つの石碑を配置**（既存 SIGN `i`→`signData` 機構を再利用）：
  - 廃城の石碑（row3, col3）：「かつてこの城にはルミアの王家が住んでいた。ザーネルが闇に堕ちる前、女王はここで統治を行っていた。今は廃墟のみが残る。」
  - 廃村の石碑（row8, col3）：「かつてこの村には人々が暮らしていた。ザーネルの儀式が暴走した夜、多くの者が恐れをなして逃げ去り、村は廃れた。」
- **接続**：`field 0,1`（炎の神殿エリア）の南端 row9 col5-6 が通路になっており、`field 0,2` の北端 row0 col5-6 も `o`（石畳・通行可）なので端遷移で自然に接続
- **テスト**（`tests/ruined-areas.spec.js`・5本）：①廃城タイル存在確認、②廃村タイル存在確認、③石碑 signData 確認、④石碑隣接床確認、⑤実プレイで廃城石碑を読むとダイアログが開く。**116/117テストグリーン**（+5本、ladder既存1本のみ以前から不安定）

**学び・気づき：**
- **Phase 6-3 も「コードを足すフェーズ」ではなくデータ配置フェーズ**。石畳 `o`、家タイル `h/p/e`、石碑 `i`+`signData`、bgTiles、chestContents はすべて既存機構で動く。node スクリプトで JSON を直接書き換えるだけで完結した
- **廃城と廃村は bgTiles で差別化できる**：廃城エリアは `'o'`（石畳）、廃村エリアは `'g'`（草地）を bgTiles に設定すると、同じ `.` の床タイルでも異なる地面の質感が出る。タイルをすべて `o` や `g` で埋めるより bgTiles 方式のほうがシンプル
- **field 0,2 は `field 0,1` の南端（ttttt..ttttt）から端遷移で到達可能**。既存の接続をそのまま活かせた

**▶ 次やること：** **Phase 6-4（嘘をつくNPC）⚡ Sonnet**。テキスト配置のみで設計不要なので Sonnet 推奨。

---

### 2026-06-21 — Phase 6-2（完了・Opus）：世界の歴史を語る石碑「ザーネルの記憶」全8断片

**やったこと：**
- **石碑の読み取り機構は既存資産で完結 → 新コードはゼロ**。`SIGN` タイル `i` を読むと `combat.js:234` が `signData[posKey]`/`npcData[posKey]` のダイアログを開く仕組みが既にある。Phase 6-2 はナラティブの創作＋配置のみ
- **「ザーネルの記憶」全8断片の物語を設計**（賢者ザーネルの悲劇の転落アーク）：①王宮一の知恵者だった → ②病で民を救えず心に影 → ③禁書に手を出し女王に止められる → ④星の欠片の力を奪う儀式が暴走・炎で人の心を焼く → ⑤儀式の炎が**心を焼き尽くし「人であった彼は死に闇だけが残った」**（＝心の死。蘇生なし）→ ⑥**心を失った彼**が守護獣を魔物に変え欠片を封印 → ⑦女王ルミアを石に変え暗黒の塔へ → 終章「私はただ失いたくなかっただけだ」。読み進めると単なる悪役でなく「喪失を恐れた者の悲劇」が浮かぶ構成
- **⚠️ ユーザー指摘で五・六を修正**：初稿は五で「賢者は死んだ／女王は**亡骸の前で**涙した」と物理的な死を書いたため、六で活動しているのと矛盾していた。**A案（心の死・蘇生なし）を採用**し、五を「心を焼き尽くした／人であった彼はそこで死に闇だけが残った」、六を「心を失った彼は」で受ける形に書き換え。四（人の心が灼かれていく）→五（心の死）→六（心を失ったまま魔物化）が一本の線で繋がり、肉体の蘇生イベントを足さずに矛盾を解消
- **8石碑を各ダンジョン entry room に配置**（`work/blade-of-lumia.json` のみ・node 直書き）：dungeon_1〜7 の `0,0`（entry room）に其の一〜七、暗黒の塔 `0,1`（entry floor）に終章。配置セルはいずれも床 `.` → `i`（SIGN）に置換し `signData` を追加。既存のテーマ/ヒント石碑（dungeon_2〜7 の row7-8）は壊さず別セルに新設
- **配置検証**：全石碑に隣接床（読みに行ける）があることを確認。`scripts/check-errors.mjs` エラーなし
- **テスト**（`tests/lore-tablets.spec.js`・4本）：①8石碑が SIGN タイル＋signData として想定座標に存在、②各石碑に隣接床がある、③其の一〜七＋終章が揃う（物語の連続性）、④実プレイで dungeon_1 entry の石碑を読むと `#dialog-name` に「ザーネルの記憶」・`#dialog-text` に冒頭文が出る。**全112テストグリーン**（+4本）

**学び・気づき：**
- **Phase 6-2 は「コードを足すフェーズ」ではなく「世界を語るフェーズ」だった**。石碑機構（SIGN→signData）は Phase 2-3 で完成済みなので、必要なのは物語の構成と配置だけ。既存のテーマ石碑は「このダンジョンは何か」を語り、新「記憶」石碑は「ザーネルは何者だったか」を語る、と役割を分けて共存させた
- **断片を「読む順＝entry room を訪れる順」に緩く対応させる**のが自然。プレイヤーはダンジョンを攻略するたびに entry room で1断片を読むので、攻略の進行とザーネルの過去の開示が同期する。順序を厳密強制せず、どの順で読んでも各断片が独立して意味を持つよう書いた
- **テストはデータレベル（決定論的・高速）を主軸に**：8石碑の存在・配置・物語連続性は JSON を読むだけで検証でき、フレークしない。実プレイ確認は dungeon_1 の1枚だけ E2E で担保（`#dialog-name` と `#dialog-text` は別要素なので両方を確認）

**▶ 次やること：** **Phase 6-3（フィールドの充実：廃城・廃村・壊れた遺跡）⚡ Sonnet**。エディタでのマップ作成が中心で設計不要なので Sonnet 推奨。

---

### 2026-06-21 — Phase 6-1b（完了・Sonnet）：個別ボス撃破フラグ `defeatedBosses`

**やったこと：**
- **`player.defeatedBosses`（Set）を追加**（`game/game.js`）：グローバル player 定義と `startNewGame` の両方に初期化。`onBossDefeated`（`game/boss.js`）でボスを倒したときに `player.defeatedBosses.add(boss.type)` を実行
- **save/load 対応**（`game/game.js`）：`saveGame` で `defeatedBosses` を配列に変換（`[...Set]`）・`loadGame` で `new Set(array)` に復元。`save.js` は player を直接 serialize せず game.js 側で制御するため game.js のみ変更
- **`startDialog` の台詞選択優先順位を拡張**（`game/ui.js`）：`linesAfterBoss[type]`（個別ボス台詞）→ `linesAfterBoss.default`（汎用「ボス倒した」台詞）→ `linesAfter`（従来の全体ボス撃破後）→ `lines`（通常）の4段階優先順位に
- **NPCデータを更新**（`work/blade-of-lumia.json`）：field 1,0 の**村人タロ（3,5）**と**老賢者（3,3）**に `linesAfterBoss` を追加。`G`（岩のゴーレム）撃破後の個別台詞と `default`（汎用台詞）を設定
- **テスト用 API を追加**（`game/game.js` / `game/main.js`）：`addDefeatedBossForTest` / `__game.addDefeatedBoss(type)` を公開
- **テスト**（`tests/defeated-bosses.spec.js`）：4本新設・全グリーン：①空状態では通常台詞（「ゴーレム」を含まない）、②G 追加後は `linesAfterBoss["G"]` 台詞（「ゴーレム」を含む）、③G 以外のボスなら `default` 台詞、④`getState()` で `defeatedBosses` が配列型で返る

**学び・気づき：**
- **`player` の `Set` フィールドは `JSON.stringify` で `{}` になる**。`save.js` の `stageState` は専用の serialize 関数があるが、`player` は game.js 内で `JSON.stringify({player, ...})` に直接渡している。`saveGame` で `playerForSave = {...player, defeatedBosses: [...Set]}` を作って渡すのが最小変更。`loadGame` でも `new Set(data.player?.defeatedBosses ?? [])` で復元する
- **台詞の優先順位チェーンは「最初に一致したらbreak」**。`linesAfterBoss` のキーをイテレートして `'default'` を除外して最初の一致を取り、一致がなければ `default` を確認、それもなければ従来の `linesAfter`/`lines` にフォールバックする。将来ボス種を増やしても NPC データの追加だけで対応できる
- **`save.js` を触らずに保存できた**：`cutBushes` は stageState 経由で `save.js` の serialize 関数を通るが、`defeatedBosses` は player オブジェクト側にあるため game.js の `saveGame`/`loadGame` を直接修正する方が筋が通っている

**▶ 次やること：** **Phase 6-2（世界の歴史を語る石碑・壁画）🧠→⚡**。ストーリー断片の構成は未着手なので Opus 推奨。

---

### 2026-06-21 — Phase 6-1（完了・Sonnet）：NPCの台詞充実＋ボス撃破後台詞システム

**やったこと：**
- **`game/ui.js` `startDialog` に `player` を引数追加**：`triforceCount > 0`（星の欠片1つ以上 = ボスを少なくとも1体撃破済み）のとき、`npcData.linesAfter` に切り替えて表示する仕組みを追加。`linesAfter` がない NPC は従来通り `lines` を使う
- **`game/game.js` の `startDialog` 配線を更新**：`player` を渡すように修正
- **7ステージのNPCに台詞を追加**（`work/blade-of-lumia.json`）：
  - **field 1,0 老賢者**：`linesAfter`「ほほ、ボスを倒したか。よくやった。」系4行
  - **field 1,0 村人タロ**：噂話2行追加（砂漠・森の噂）＋`linesAfter`「ありがとう」系4行
  - **field 1,1 村人ハナ**：`linesAfter`「えっ、ボスを倒したの！？」系4行
  - **field 2,0 すみっこずき**：`linesAfter` 4行
  - **field 2,1 賢者エルン**：`linesAfter`「おお、ボスを倒したか！めでたい」系5行
  - **field 0,1 ピンクあたま**：`linesAfter` 5行（「ちなみに私の髪の毛はピンクです」健在）
  - **field 3,1 ピンクあたま**：`linesAfter` 4行
- **テスト**（`tests/npc-dialog.spec.js`）：3本新設・全グリーン。JSエラーなし確認

**学び・気づき：**
- **`linesAfter` は「ボス1体でも倒したか」で切り替える**のが自然。個別に「どのボスを倒したか」を管理すると複雑になる。`triforceCount > 0` は星の欠片の有無と一致し、ゲームの進行度として適切なシグナル
- **`startDialog` の引数に `player` を追加するだけ**で既存NPC（`linesAfter` なし）は全て従来通り動く。後方互換を保ちながら新機能を追加できた
- `ui.js` の `startDialog` は `stageData` と `npcDefaultDialog` を受け取る設計だったので、`player` を第6引数として追加した（将来は `context` オブジェクト化も検討できる）

**▶ 次やること：** **Phase 6-2（世界の歴史を語る石碑・壁画）🧠→⚡**。ダンジョン内にザーネルの過去を語る断片テキストを石碑に配置。ストーリー断片の構成は未着手なので Opus 推奨。

---

### 2026-06-21 — Phase 5-3（設計＋実装・Opus）：敵が石を押すパズル（Phase 5 全完了）

**やったこと：**
- **設計フォークをユーザーに確認**：敵パズルの核（①重し式：敵がボタンに乗る／②ラッチ式：一度踏めば恒久ON／③敵が石を押す）を提示 → **③「敵が石を押す」を採用**（最もパズルらしい）。DECISIONS.md に設計記録
- **核心の発見**：パズル後半（石→ボタン→ゲート）は**完全に既存資産**。石がボタンを押し下げる機構（`checkStoneOnSwitch`・`ss.stoneSwitches`）は石押しパズルで実装済みで、石の出所（プレイヤー/敵）を区別しない。新規実装は「敵に石を押させる」前半だけ
- **`game/enemy-ai.js`**：`tryEnemyPushStone(e, my, mx)` を新設。プレイヤーの `tryPushStone`（player.js）と同規則で石を1マス押す（押し先が `tilePassable` かつ石/敵/プレイヤー不在）。`enemyChase` の移動ループで「整数座標＆カーディナル移動が石で塞がれたら押す」分岐を追加。`TILE` を import
- **`game/game.js`**：`createEnemyAi` deps に `getCurrentLayer`/`getStageKey`/`getSS`/`tilePassable`/`checkStoneOnSwitch`/`evaluateConditions`/`renderBoard`/`renderChars` を追加。`getStageStateSnapshot()` に `stonePositions` を追加（テスト観測用）
- **パズル配置**（`work/blade-of-lumia.json`）：dungeon_1 `5,0`（rows=10,cols=12）に CHASER(2,5)・STONE(3,5)・BUTTON(4,5)・GATE(4,9)・封印宝箱(4,10・ルピー×30)・石碑(7,3)。**ボタン直下(5,5)を壁で塞ぎ石が越えない＝恒久ON**。`links:[{switchId:'4,5',gateId:'4,9'}]`
- **テスト**（`tests/enemy-stone-puzzle.spec.js`・2本）：①プレイヤーを下方(6,5)に置く→チェイサーが追って石をボタンへ押し込む→switchStates['4,5']===true・openGates に '4,9'／②プレイヤーを上方(1,5)に置くと敵が逆方向へ向かい石は乗らずゲート閉のまま（vacuous pass 防止）。**全101テストグリーン**（+2本）
- **実ブラウザ確認**：一時 spec で tick ごとに座標トレース（確認後削除）。t=0 で石(3,5)・ゲート閉 → t=10 で敵が row3 へ進み石を(4,5)ボタンへ押し込み→switch ON・gate open。以降石はボタン上に留まり（壁で堰き止め）ゲート開維持を確認

**学び・気づき：**
- **「敵が石を押す」は新サブシステム不要**。`checkStoneOnSwitch`（石→ボタン→ゲート）が既に石の出所を問わないので、敵が `ss.stonePositions` に石を1マス足すだけでパズル後半が成立した。新規は前半（敵の石押し1関数）のみ
- **通常追跡 `enemyChase` は乱数を一切使わない決定論的ロジック**なので、`step()` ベースで「敵が石をボタンへ押す」を再現できる。boss の hitAndAway は `Math.random` 依存なので、石押しパズルは**通常敵（CHASER 等）**に持たせるのが正解
- **石が暴走しない設計はステージ側で担保する**：ボタンの「先」を壁で塞げば、敵が押し続けても石はボタンを越えられず恒久ON になる（コードに距離カウンタを足す必要がない＝初代ゼルダの「石の溝」と同じ発想）
- **vacuous pass 防止**：敵を逆方向へ誘導すると石が乗らないことを別テストで固定。「敵が石を押す経路に居なければゲートは開かない」ことを保証

**▶ 次やること：** **Phase 6-1（NPCの台詞充実）⚡ Sonnet**。テキスト作成・配置が中心で設計不要なので Sonnet 推奨。

---

**やったこと：**
- **ギミック①: 爆弾壊し壁 → 隠し宝箱**（`dungeon_1 / 1,0`）：row2 に `#.......#!B#` パターンを配置。col8=`#`（壁）、col9=`!`（壊せる壁）、col10=`B`（隠し宝箱・ルピー×30）。石碑ヒント row4,col2（「ひびの入った壁…💣 爆弾があれば壊せるかも？」）
- **ギミック②: 看板フェイク**（`field / 1,1`）：row1 に `t.i#!B..f..t` パターン。col2=`i`（「この先に何もない。立ち去れ。」），col3=`#`（壁），col4=`!`（壊せる壁），col5=`B`（隠しルピー×20）。「何もない」と言っているのに宝箱が隠れているフェイクギミック
- **ギミック③: ロウソク草燃やし → 隠し入口**（`field / 1,1`）：row6,col10 に `u`（茂み）を追加。`showConditions["6,11"] = { trigger: "bushBurned" }` → ロウソクで燃やすと col11 の `>`（MAP_ENTER）が出現 → 新レイヤー `hidden_cave` へ遷移
- **新レイヤー `hidden_cave` 追加**：rows=10,cols=12 の小部屋。石碑（「ロウソクの炎が秘密を照らす…」）・宝箱（秘密のルピー×50）・戻り口 `>` を配置。exitRegistry に `hidden_cave` と `field_1_1_bush_entrance` を双方向登録
- **`getStageStateSnapshot()` に `brokenWalls` を追加**（`game/game.js`）：テストから爆弾破壊確認ができるように
- **テスト**（`tests/hidden-passages.spec.js`）：4本新設・全グリーン：①爆弾で(2,9)の壊せる壁が破壊される、②フェイク看板の配置確認（JSエラーなし）、③ロウソクで茂みを燃やすと conditionsMet に '6,11' が入る、④燃やした後 hidden_cave へ遷移できる
- PLAN.md の Phase 5-2 チェックボックスを全て `[x]` に更新

**学び・気づき：**
- `showConditions` は MAP_ENTER タイル（`>`）の描画・遷移ゲートとして機能する。BUSH タイル `u` は別のセルに置き、隣接する `>` タイルを showConditions でゲートするのが正しいパターン（field 2,0 の candle 入口と同じ）
- `getStageStateSnapshot()` は test-facing API。新しい状態を確認したいときは `brokenWalls` を追加したように、テストに必要なフィールドを都度追加するのが正解
- bomb AOE=2 のリーチ計算：プレイヤー (2,7) → 爆弾 (2,7) → AOE=2 → (2,9) にヒット（|7-9|=2 ≤ AOE）。壁 `#` は AOE をブロックしないのでこの配置が成立する
- hidden_cave の `mapEnters` は `destId: "field_1_1_bush_entrance"` で field 1,1 の (6,11) に戻る。1.5s の mapEnterCooldown があるので即時再遷移は防止される

**▶ 次やること：** **Phase 5-3（敵を使ったパズル）🧠 Opus**。設計未着手なので Opus 推奨。

---

### 2026-06-20 — Phase 5-1（実装完了・Sonnet）：色スイッチ・色ゲート フルスタック実装 + テスト5本

**やったこと：**
- **タイル追加**（`shared/tiles.js`）：`SWITCH_RED='['`・`SWITCH_BLUE=']'`・`GATE_RED='('`・`GATE_BLUE=')'` を追加。TILE_META に色・label・icon も設定
- **スプライト**（`shared/tile-sprites.js` / `shared/sprites-tiles.js`）：色違いパレット（`gateRed`・`gateBlu`・`switchRed`・`switchBlu`）を定義。既存 `gateG`/`lever` の形状を流用し、単一表（[[blade-tile-sprite-single-source]]）に4エントリ追加
- **状態管理**（`game/save.js` / `game/game.js`）：`activeColor: null` を createStageState / serialize / deserialize に追加。`getSS()` で `stageData.initActiveColor` を種まき（`initLitTorches` と同要領）。`getStageStateSnapshot()` に `activeColor` を追加
- **叩く＝色セット**（`game/player.js`）：`setActiveColor(r, c)` を新設。`combat.js`（剣）・`projectile.js`（矢/ビーム）に色スイッチ分岐を追加。ビームは既存 `proj._switchedCells` で二重起動防止
- **通行判定**（`game/passable.js`）：`GATE_RED` は `ss.activeColor==='red'` のとき通行可、`GATE_BLUE` は `'blue'` のとき通行可（`openGates` 不使用・links と独立）
- **描画**（`game/render-board.js`）：色ゲートは activeColor 不一致なら色付き閉ゲートを描画、一致なら床。色スイッチは activeColor 一致なら frame1（点灯）、不一致なら frame0（非アクティブ）
- **エディタ対応**（`editor/editor-canvas.js` / `editor/index.html`）：`initActiveColor` 入力フィールドをステージ設定に追加（fluteEffect と同要領）
- **パズル配置**（`work/blade-of-lumia.json`）：dungeon_1 ステージ `3,0`（rows=10, cols=12）を新設。row2 に SWITCH_RED(2,1)・SWITCH_BLUE(2,9)、row4 に GATE_RED(4,3)・GATE_BLUE(4,7)、row6 に石碑ヒント。報酬ステージ `4,0` も追加（宝箱）
- **テスト**（`tests/color-switch.spec.js`）：5テスト新設・全グリーン：①矢→SWITCH_RED→activeColor='red'、②activeColor='red' で GATE_RED 通行可（snapshot確認）、③矢→SWITCH_BLUE→activeColor='blue'（排他制御）、④snapshot に activeColor フィールドが存在（初期null・起動後'red'）、⑤剣でも SWITCH_RED を起動可
- PLAN.md の Phase 5-1 チェックボックスを全て `[x]` に更新。DECISIONS.md にタイル文字確定を追記

**学び・気づき：**
- `deps.setActiveColor` の配線は projectile.js / combat.js の両方に必要（game.js の deps 生成を2箇所修正）。片方だけ忘れると剣か矢の一方で起動できないバグになる
- テストの SWITCH_BLUE 起動：`teleportPlayer` が `__game` に公開されていないため、col=6 にスポーンし直すアプローチで対応（リロードして blue 側から射撃）
- `mapEnters` は空のまま（既存ダンジョンへの接続経路は別途 Phase 5 末〜6 で設定）

**▶ 次やること：** **Phase 5-2（隠し通路・隠し入口）⚡ Sonnet**。既存ギミック（爆弾壊し壁・看板フェイク・ロウソク草燃やし）の配置がメイン。

---

### 2026-06-20 — Phase 5-1（設計フェーズ・Opus）：色スイッチ・色ゲートを「色セレクタ式（activeColor 1個）」で設計

**やったこと（設計のみ・コード変更なし）：**
- Phase 5-1（色スイッチ・色ゲート）は 🧠→⚡ で設計未着手だったため、まず Opus で設計を確定。実装は次フェーズ（⚡ Sonnet）に回す
- **コード調査**：既存のスイッチ/ゲート機構（`player.js` `toggleSwitch`/`checkSwitchOff`・`links`（switchId→gateId）・`switchStates`/`switchToggles`/`openGates`・`passable.js` GATE 通行判定・`combat.js`/`projectile.js` の SWITCH ヒット分岐・`render-board.js` の状態別フレーム・`save.js` の serialize・`game.js` `getSS`/snapshot・editor-props.js の links UI）を Explore で洗い出した
- **仕組みをユーザーに確認**：ゼルダ定番の2系統（クリスタル式＝1種・赤青トグル／色セレクタ式＝色ごとにスイッチ）を提示 → **「色セレクタ式」を採用**（3色以上に拡張でき、色を撃ち分ける面白さが出るため）
- **設計を3ファイルに記録**：DECISIONS.md「Phase 5-1（設計）」（決定・既存 links を使わない理由・データ構造・作用箇所・代替案）／PLAN.md 5-1 にチェック項目を具体化／PROGRESS.md 現在地を更新

**設計の要点（次セッションの実装契約）：**
- **核心の状態は `ss.activeColor`（文字列・1ステージ1個）だけ**。色スイッチを武器で叩くと activeColor をその色に**セット**（トグルではない）。`GATE_<c>` は `ss.activeColor === c` のときだけ通行可（＝開いて見える）
- **既存 `links` 機構には触らない**：links は加算式（スイッチON→ゲート開・独立）で「ある色を開くと他色が閉じる」**排他**を表現できない。色ゲートは links に依存させず「自色が active か」だけで自己完結判定する
- **既存の SWITCH/GATE 経路に色分岐を1つずつ足す**：`player.js` に `setActiveColor(r,c)`／`combat.js`・`projectile.js` の SWITCH ヒット隣に色スイッチ分岐／`passable.js` の GATE 隣に色ゲート通行判定／`render-board.js` に色ゲート・色スイッチ描画
- **永続化**：`activeColor` はプリミティブ文字列なので save.js は Set↔配列変換不要・そのまま代入。`getSS` で `stageData.initActiveColor` を種まき（`initLitTorches` と同要領）
- **クリア判定トリガーは YAGNI**：通行ギミックだけで成立。必要なら `activeColorIs`（torchesLit と同型）を後で足す

**学び・気づき：**
- **「色スイッチ」は既存 SWITCH の延長に見えて、本質は別物**。既存は加算式（個別ゲートを開く）だが色ギミックは排他（同時1色のみ）。`links` を色対応に拡張すると「ゲートを links で開閉する設計」と「色一致で判定する設計」が二重化する。**ステージに `activeColor` 1個を持たせ各ゲートが自色一致を見る**だけで排他が自然に出る
- ユーザー提示の選択肢に「クリスタル式（1種・トグル）」もあったが、3色以上に拡張できない。**色ごとにスイッチを分ける（セレクタ式）**方が拡張性が高く、撃ち分けの面白さも出る

**▶ 次やること：** **Phase 5-1 の実装（⚡ Sonnet）**。設計が DECISIONS.md/PLAN.md に書き出し済みなので Sonnet 推奨。`shared/tiles.js` の色スイッチ2＋色ゲート2タイル追加（衝突しない新文字を grep で確認）から着手し、save→passable→player/combat/projectile→render の順に分岐を足して各ステップで `tests/` にスモークを足してグリーン確認。

---

### 2026-06-20 — Phase 4-5 ③（完了）：ロウソクで隣接 TORCH を点灯（爆弾は不採用）

**やったこと：**
- **設計変更（ユーザー判断）：** 当初「爆弾で TORCH 範囲点火」を実装したが、**ブーメランパズルの難しさが失われる**（爆弾で代替できるとブーメランが不要になる）という問題が発覚し差し戻した。代わりに「ロウソクで隣接 TORCH を点灯」に変更。役割：ブーメラン＝遠くに炎を運ぶ、ロウソク＝隣接1セルに点火、爆弾＝TORCH に作用しない。
- **`game/game.js`**：`playCandle()` に TORCH 点灯分岐を追加。前方が TORCH なら未点灯→点灯（`litTorches.add`→`evaluateConditions()`→`renderBoard()`→「かがり火に火をつけた！」）、点灯済み→「もう火がついている」メッセージ。
- **`game/game.js`**：`ps_bomb=1` プレビューパラメータを追加（汎用・他テストで使用可）
- **`editor/editor-io.js`**・**`editor/editor.js`**：`getPreviewSettings` 両定義に `bomb` を追加（[[blade-preview-settings-duplicated]]）・URL に `ps_bomb` を追加
- **`editor/index.html`**：「💣 爆弾」プレビュー設定チェックボックスを追加
- **`work/blade-of-lumia.json`**：dungeon_1 `1,2` (7,3) に石碑ヒント追加
- **`tests/bomb-torch.spec.js`**（→ロウソクテストに差し替え）：2本（消灯 TORCH を点灯・TORCH でない前方では litTorches 不変）

**確認：** **全90テストグリーン**（+2本）。

**学び・気づき：**
- **「複数アイテムで同じパズルを解ける」は代替性を与えすぎるとパズルが崩れる**。爆弾でブーメランパズルが解けてしまうと「ブーメランでないとダメな理由」が消える。アイテムの役割を「遠距離運搬・近接点火・破壊」と明確に分けることで、各アイテムに固有価値が生まれる。
- 実装前に「このアイテムでこのギミックを解けてしまうと何が崩れるか」を一度考えるのが重要。

**▶ 次やること：** **Phase 4-5 全完了**。次は PLAN.md の未完了タスクを確認して次フェーズを進める。

---

### 2026-06-20 — Phase 4-5 ②（完了）：TORCH タイル＋ブーメランで炎を運ぶ

**やったこと：**
- **`shared/tiles.js`**：`TORCH: 'H'`（⚠️ `'Y'` は SWITCH で使用済みなので `'H'`）・`TILE_META['H']`（passable=false）
- **`shared/tile-sprites.js`**：`TILE_SPRITE_MAP[TILE.TORCH] = { spr:'torch', pal:'torch' }` 追加
- **`shared/sprites-tiles.js`**：`TILE_PAL.torch`（8色）＋`TILE_SPRITES.torch`（2フレーム 12×16：frame0=消灯/frame1=点灯）追加
- **`game/render-board.js`**：`addCellSprite` に TORCH 分岐追加（`ss.litTorches.has(posKey)` でフレーム切り替え）
- **`game/conditions.js`**：`torchesLit` トリガー追加（ステージ内全 TORCH が `litTorches` に入ったら達成）
- **`game/projectile.js`**：`boomerangStep` に炎運搬追加：点いた TORCH 通過→`proj.flaming=true`、消えた TORCH 通過時に `proj.flaming`→`litTorches.add`+`evaluateConditions()`+`renderBoard()`+`saveGame()`
- **`game/game.js`**：`getSS()` に `initLitTorches` 種まき（`stageData.initLitTorches: ['r,c',...]` で初期点灯を指定可能）
- **`editor/editor-props.js`**：showConditions トリガードロップダウンに `torchesLit` 追加
- **`work/blade-of-lumia.json`**：dungeon_1 `1,2` に TORCH パズル（点灯 `2,3`・消灯 `2,9`/`7,5`・宝箱 `7,9` torchesLit 条件付き・initLitTorches: ['2,3']）
- **`tests/torch.spec.js`**：5本追加（initLitTorches 動作・点灯確認・conditionsMet 未達成・通行不能確認）

**確認：** **全89テストグリーン**（+5本）。

**▶ 次やること：** **Phase 4-5 ③（⚡ Sonnet）：爆弾で TORCH 範囲点火**。`explodeBomb` AOE ループに TORCH 点灯分岐を足す。設計は DECISIONS.md「Phase 4-5（設計）」③参照。

---

### 2026-06-20 — Phase 4-X バグ修正（完了）：ワールドマップ「星の欠片 合計」が大型ボス分を数えていない

**症状（ユーザー報告）：** エディタのワールドマップ右下「星の欠片 合計」が3程度しか表示されず、dungeon_1〜7 の大型ボス（G/N/J/A/L/O/U）分7体が欠落していた。

**原因：** `editor/editor-world.js updateShardSummary()` が `ITEM_TRIFORCE_PIECE('Q')` と `DARK_LORD('X')` だけをカウントし、大型ボス（`ENEMY_META[tile].dropsTriforce:true`）を見ていなかった。ゲーム側 `boss.js calcTotalTriforces()` は dropsTriforce も数えていたため、エディタとゲームで定義が乖離していた。

**修正：**
- **`shared/triforce.js` を新設**：`countTriforces(mapData)` と `listTriforceEntries(mapData)` を export。Q＋X＋dropsTriforce ボスを単一の真実として定義
- **`game/boss.js`**：`calcTotalTriforces()` を `countTriforces(getMapData())` に委譲（既存ロジック削除）
- **`editor/editor-world.js`**：`updateShardSummary()` を `listTriforceEntries(state.mapData)` を使う形に書き換え。ボス名も `ENEMY_META[tile].name` から取得してラベルに表示

**確認：** `node` スクリプトで現在のマップを直接計算 → 合計9（直接Q×2 + ボス7体：G/N/J/A/L/O/U）。7ダンジョンすべてのボス名が内訳リストに並ぶことを確認。**全84テストグリーン**、`check-errors.mjs` エラーなし。

**学び：** エディタとゲームで「欠片の数え方」を二重定義していたのが根因。新タイル（大型ボス）を追加した際に片方だけ更新したため乖離した。`shared/` に集約する「単一の真実」方針は [[blade-tile-sprite-single-source]] と同じ。新タイルを追加した際は `shared/triforce.js` の対象条件（`dropsTriforce` フラグ）に乗れば自動的に両側が追従する。

**▶ 次やること：** **Phase 4-5 ②（⚡ Sonnet）：かがり火タイル `TORCH`（別文字・`'Y'` は SWITCH で使用済み）追加＋ブーメランで炎運搬**。設計は DECISIONS.md「Phase 4-5（設計）」②参照。

---

### 2026-06-20 — バグ修正（分岐B）：cave_1 1,0 に「小さくて動かない魔物」が出る（共通 fallback が敵タイルを静的描画）

**症状（ユーザー報告・スクショ）：** cave_1 1,0 で、正常に動く魔物のほかに**小さくて動かない魔物**が盤面に表示される。

**原因：** スプライト共通化のときに `render-board.js` の末尾へ足した「共通表 `TILE_SPRITE_MAP` から描く fallback」が**敵タイル（W/MONSTER 等）まで静的描画していた**。敵は `buildEnemies`→`render-chars.js` が動く実体として描くので、盤面にも静的スプライトが出ると「動く本物＋動かない複製」の二重表示になる。fallback が「共通表に載っている全タイル」を対象にしていたのが広すぎた（敵・プレイヤー・NPC・ボスも共通表に載っている）。

**修正：** fallback の対象を**落ちアイテム8種だけのホワイトリスト `ITEM_FALLBACK_TILES`**（よろい・爆弾・弓矢・回復薬小/大・地図・コンパス・ハートの器）に限定。敵/プレイヤー/NPC タイルは fallback で描かない（実体描画に任せる）。

**検証：** cave_1 1,0 で魔物が1体（動く実体）だけになり静的複製が消えたことを実画面で確認。**全84テストグリーン**。

**学び：** 「共通表の全タイルを fallback で描く」は広すぎた。共通表には敵・プレイヤー・NPC も載っており、それらは別経路（render-chars）で実体描画される。**fallback は『他に描き手がいない落ちアイテム』だけ**に絞るべき。ホワイトリストで明示するのが安全。[[blade-tile-sprite-single-source]] の住み分けの続き。

---

### 2026-06-20 — 改善（分岐B）：ボタン/レバーを 32×32 に高解像度化＋「押し込み式ボタン」の見た目に作り直し

**指摘（ユーザー）：** ①セルのギミックがプレイヤー（32×32）より低解像度（16×16）なのはなぜ？ ②ボタンの押下表現がしょぼい・細長くてボタンに見えない。Web アプリのボタンのように「押されてない時は押せそう／押された時は押された見た目」にして。

**対応：**
- **button・lever スプライトを 32×32・2フレームに作り直し**（プレイヤーと同解像度）。`scripts` 的なジェネレータ（角丸 rrect＋自動 outline）で生成し精度を確保
- **ボタンは Web ボタンの押し込み表現**：frame0＝丸いキャップが台座から**浮いて side wall（立体の側面）が見える＝押せそう**／frame1＝キャップが台座に**沈み込み＋内側に影＝押された**。`switchStates` で乗っている間だけ frame1
- **レバーは 32×32 で OFF＝左倒し（銀ノブ）／ON＝右倒し＋黄発光**に拡大。`switchToggles` で切替
- パレットを 7 色（0-7）に拡張（キャップ面/側面/上面ハイライト/押下影 など）。`render-board.js` は従来どおり状態でフレームを選ぶだけ。エディタは frame0 固定表示
- ※ 他のギミック（block/gate/chest/key/door 等）が 16×12 のままなのは既存仕様。今回はユーザー指摘のボタン/レバーのみ高解像度化（全体の高解像度化は別途必要なら実施）

**検証：** frame0/1 を並べて描画し「浮いた押せそうなボタン」「沈んだ押されたボタン」になっていることを目視確認。実画面で field 3,1 のボタン＝立体的な押しボタン、dungeon_1 2,2 のレバー＝右倒し＋発光で ON が伝わることを確認。**全84テストグリーン**。

**学び：** **セルのスプライト解像度はプレイヤーと揃える必要はない（タイルは小さくても成立する）が、表現したい情報量に解像度が足りないと「しょぼい」見た目になる**。ボタンの押下のような微妙な凹凸は 16px では潰れる。表現に必要な解像度を確保するのが正解。押下表現は「色の変化」より「形（浮き/沈み＋側面/影）」で見せると Web ボタン同様の affordance が出る。

---

### 2026-06-20 — バグ修正（分岐B）：ボタン/スイッチが「ボタン/レバーに見えない」を専用スプライト新設で解消

**症状（ユーザー報告）：** ボタンを「押した感じ」にしたはずだが、そもそもボタンに見えない。本当に変えたのか？（field 3,1）

**原因：** 前回は**既存の `swG` スプライト（緑のマスク状の絵）を BUTTON/SWITCH 両方に流用**し、CSS フィルタ（glow/scale）を重ねただけだった。元の絵がボタンでもレバーでもないので「押した感じ」も伝わらず、両者の区別もつかなかった。

**修正（専用ドット絵を新規作成）：**
- **`button` スプライト（16×16・2フレーム）を新設**：丸い金属ボタン。frame0=浮いている／frame1=沈み込み＋黄色発光。プレイヤー/石が乗って ON（`switchStates`）の間だけ frame1 を描く
- **`lever` スプライト（16×16・2フレーム）を新設**：台座＋レバー。frame0=OFF（左倒し・銀ノブ）／frame1=ON（右倒し・黄ノブ発光）。武器でトグル（`switchToggles`）すると切替
- `shared/tile-sprites.js` の対応を `BUTTON→button`／`SWITCH→lever` に変更。`render-board.js` は CSS クラスでなく **`drawSpriteFrame` で状態に応じたフレーム**を描く方式に。旧 `.button-sprite`/`.switch-sprite` CSS は削除
- エディタ（`drawSpriteAt`）では button/lever を **frame0 固定**で表示（配置物が勝手に押下/ON でチラつくのを防ぐ）

**検証：** 実画面で確認——ボタンは丸い押しボタン、スイッチはレバー（OFF=左/ON=右＋発光）で明確に区別でき、スイッチを撃つと右に倒れて発光＋ゲートが開く。ボタンは乗ると `switchStates` が立つ。**全84テストグリーン**、エラーなし。

**学び：** 「見た目を変えて」と言われたら**まず実物の絵（スプライト）を作る**べきで、既存の無関係なスプライトに CSS を重ねるのは誤魔化しにすぎない。状態差（押下・ON）は CSS フィルタより**専用フレームを描き分ける**方が確実で意図が伝わる。

---

### 2026-06-20 — バグ修正（分岐B）：スイッチで開けてもゲートが表示されたまま（共通スプライト fallback の取りこぼし）

**症状（ユーザー報告）：** dungeon_1 2,2 でスイッチを矢で撃ってゲートを開けても、ゲート（柵）が表示されたまま。開いた見た目にならない。

**原因：** 直前のスプライト共通化で `render-board.js` の末尾に「共通表 `TILE_SPRITE_MAP` からスプライトを描く fallback」を足したのが裏目に出た。GATE の分岐が `if (tile === TILE.GATE && !ss.openGates.has(posKey))` で、**開いている時は条件が false → return せず素通り** → 末尾 fallback が `TILE_SPRITE_MAP[GATE]=gateG` を再描画して「開いてもゲートが見える」状態に。`openGates` 自体は正しく更新されていた（描画だけの問題）。

**修正：** 状態で見た目が変わるタイルは「描かない時も必ず return する」形に統一。GATE・CHEST・BREAKABLE_WALL を `if (tile===X){ if(条件) 描く; return; }` に修正（開いたゲート／開封済み宝箱／破壊済み壁は何も描かず床になる）。テスト用 spec でゲートセル (2,3) が開後に空（`innerHTML==''`）になることを確認し、実画面でもゲートが消え・スイッチが紫発光することを目視確認。

**学び：** 「共通 fallback を末尾に置く」設計では、**前段の状態依存分岐が必ず return しないと fallback に二重描画される**。状態で出/不出が変わるタイル（GATE/CHEST/BREAKABLE_WALL/STONE 等）は「条件 false でも return」を徹底する。[[blade-tile-sprite-single-source]] の住み分け（状態依存はゲーム側で個別描画）の落とし穴。

---

### 2026-06-20 — バグ修正（分岐B）：エディタとゲームで見た目が違う問題を解消（タイル→スプライト表を共通化＋絵文字を廃止）

**症状（ユーザー報告）：** ハートの器などがエディタとゲーム画面で見た目が違う（エディタ＝ドット絵／ゲーム＝絵文字 ❤）。スイッチの背景も床のはずが緑（草地っぽく）見える。

**原因：** 「タイル→何を描くか」の対応が**2か所に別実装**されていた。エディタは `TILE_SPRITE_MAP`（一元表）でスプライト描画、ゲーム `render-board.js` は手書き分岐＋最後に `emojiItemMap`（絵文字）にフォールバック。`heart` スプライトが存在するのにゲーム側は絵文字を出していた等、対応漏れで食い違っていた。スイッチ/ゲートの緑背景も別件で同根（セルに `switch-on/off` の緑クラスを付けていた）。

**修正（方針：全部スプライト・単一の真実）：**
- **`shared/tile-sprites.js` を新設**＝タイル→`{spr,pal}` 対応の**単一の真実**。エディタ（`editor-palette.js` が再エクスポート→canvas/world/palette が参照）とゲーム（`render-board.js`）の両方がこれを見る
- **不足スプライトを8種新規作成**（`shared/sprites-items.js`）：よろい・爆弾(落ち)・弓・回復薬小/大・ダンジョン地図・コンパス・古代の祭壇。すべて 16×16 ドット絵＋専用パレット
- **ゲーム側の絵文字フォールバックを廃止**：`emojiItemMap` と祭壇の絵文字 ⛩ を削除し、共通表からスプライト描画。ハートの器・よろい・爆弾・弓・回復薬・地図・コンパス・祭壇がエディタと同じ見た目に
- **スイッチ/ボタン/ゲートの背景を床に**：セルに付けていた `switch-on/off`（緑）クラスを撤去（ON/OFF・押下はスプライト側クラスで表現済み）。`board.css` の `.cell.switch-on/off` も削除
- エディタのギミックカテゴリに祭壇（ALTAR）を追加（スプライトができたので配置可能に）

**検証：** `node scripts/check-errors.mjs` エラーなし → **全84テストグリーン**（VRT 起動画面は対象タイルが写らず差分なし）。dungeon_1 2,2 の実画面でハートの器がドット絵・スイッチ/ゲートが床背景になったことを目視確認。

**学び：** **エディタとゲームは「見た目を決めるテーブル」を必ず1つ共有する**。2か所に分かれると新タイル追加や絵文字→スプライト化のたびに片方が漏れて食い違う。`shared/` に置けば構造的にズレない。「状態で変わる描画（ドア開閉・宝箱開封・移動した石）」だけはゲーム側が個別に持ち、それ以外の基本スプライトは共通表に集約するのが住み分けの境界。

---

### 2026-06-20 — バグ修正（分岐B）：開いたゲートの跡が緑（草地っぽく）表示される

**症状（ユーザー報告）：** dungeon_1 ステージ 2,2 でスイッチを撃ってゲートが開いた後、ゲートのセルが緑色（草地のような見た目）になる。背景は床に設定したつもりなのに反映されていないように見える。

**原因：** 背景設定の問題ではなくバグ。`game/render-board.js` の `setCellClass` で、**開いたゲート（`openGates` に含まれる）に `switch-on` クラスを付けていた**。`switch-on` は board.css で `#2a5020`（スイッチ/ボタンの「ON で光る緑」）。開いたゲートとONスイッチが同じクラスを使い回していたため、ゲート跡が緑になっていた。bgTiles（床）の設定自体は正しく空＝床だった。

**修正：** 開いたゲートには余計なクラスを付けず（`.cell` デフォルト＝`var(--floor-color)` の床背景になる）、閉じている時だけ `gate` クラスを付ける。`switch-on`（光る緑）は**スイッチ/ボタン側だけ**に使う（ユーザー方針：ゲートは開/閉だけで十分・押された効果はスイッチに反映）。

**テスト：** ユーザーがゲートを (2,3) に移動していたため `tests/arrow-switch.spec.js` のゲート id を `2,1`→`2,3` に更新。**全84テストグリーン**。

**学び：** 「ON で光る緑」と「開いたゲートの床」を同じ `switch-on` クラスで表現していたのが混線の元。状態の意味が違う表示は CSS クラスも分けるべき（直前のボタン/スイッチ分離と同じ教訓）。

---

### 2026-06-20 — Phase 4-5 ①（完了）：ギミックを「ボタン（踏む）／スイッチ（武器）」に呼び分け。スイッチは矢・剣でトグル

**最終形（ユーザー指示で2段階に修正）：**
1. 当初「既存の踏みスイッチ `SWITCH('S')` を矢でラッチ ON」で実装 → **踏みスイッチは『乗っている間だけ ON＝モーメンタリ』でパズルが成立している**ため矢ラッチで既存パズルが壊れる、とユーザー指摘 → 専用タイルに分離。
2. さらに**呼称と見た目を整理**（ユーザー指示）：踏むやつ＝**ボタン `BUTTON('S')`**、武器で押すやつ＝**スイッチ `SWITCH('Y')`**。スイッチは矢だけでなく**剣・剣ビームでもトグル**できるように。詳細は DECISIONS.md 2026-06-20。

**やったこと（⚡ Sonnet）：**
- **`shared/tiles.js`**：`'S'` を `BUTTON`（ボタン・モーメンタリ）、`'Y'` を `SWITCH`（スイッチ・武器トグル）に呼び分け（タイル文字はセーブ互換のため不変）。TILE_META も「ボタン」「スイッチ（紫系の的 ◎）」に
- **`game/player.js` に `toggleSwitch(r, c)` を新設**：武器の攻撃が当たるたび `ss.switchToggles`（ON のものだけ保持する新 Set）を ON↔OFF トグル。連動ゲートは既存 `links`（switchId→gateId）で `openGates` を開閉。`checkSwitchOff`・handleTileEvent の踏み処理は **BUTTON** タイルを参照（ボタンのモーメンタリ挙動は不変）
- **`game/combat.js`**：`swordAttack` で前方タイルが SWITCH なら `toggleSwitch(tr,tc)`（剣でトグル）。BUTTON は剣では無反応＝役割分離
- **`game/projectile.js`**：矢のサブステップで「SWITCH に当たったらトグルして矢消滅」（1本＝1トグル）。剣ビーム（貫通）は `proj._switchedCells` で「1セル1回だけ」トグル
- **`game/conditions.js`**：石スイッチ判定・`allSwitchesOn`/`switchOn` トリガーは **BUTTON** を参照（従来挙動を維持）
- **`game/game.js`**：`toggleSwitch` を配線（player・combat 両 deps）。`getStageStateSnapshot()` に `switchToggles` を含める
- **`game/save.js`**：`switchToggles`・`litTorches`（Set↔配列）を追加（`litTorches` は② で使用）
- **`game/render-board.js`＋`render-chars.js`＋`css/board.css`**：ボタンは押し込み式（乗って ON で沈み込み＋発光・従来の緑グロー維持）／スイッチは ON で紫系発光。石が乗るのは BUTTON
- **editor 3ファイル**：`editor-palette.js`（ボタン/スイッチ呼び分け）・`editor-world.js`（ミニマップ色）・`editor-props.js`（トリガー文言「ボタンON」）
- **パズル配置**（`work/blade-of-lumia.json` dungeon_1 `2,2`）：row5 全幅の水堀で南を徒歩到達不可に。南の Y(6,9) を北から矢で撃つ／剣で叩く → ゲート T(2,1) 開→北西の報酬ルピー R(1,1) へ（links 6,9→2,1）。石碑(4,3) でヒント
- **テスト**：`tests/arrow-switch.spec.js` 4本（①矢で ON＋`switchStates['6,9']` undefined＝ボタン機構に無作用 ②移動後も ON 維持＝トグル ③再射で OFF＆ゲート閉 ④剣でもトグル）→ **全84テストグリーン**

**学び・気づき：**
- **呼称と見た目を役割に合わせると直感的になる**：踏む＝ボタン（沈む）、武器で作動＝スイッチ（トグル・光る）。挙動の意味が違うものはタイルも状態 Set も分けるのが正解（`switchStates`＝ボタン／`switchToggles`＝スイッチ）。共通核（投擲物/武器→タイル状態）は保ちつつ作用先を分離
- テストで `switchStates['6,9']` が `undefined` であることを assert し、「ボタン機構に一切作用しない」ことを回帰防止として固定
- **貫通する剣ビームは「1セル1回だけ」トグルする必要がある**（`proj._switchedCells` で重複防止）。毎サブステップでトグルすると同一セルで何度も反転してしまう。矢は当たって消えるので1トグルで済む
- パズル配置で当初列番号を1つ間違えた（6,8 のつもりが実際は 6,9）。配置後は必ず `tiles[r][c]` で実座標を確認

**▶ 次やること：** **Phase 4-5 ②（⚡ Sonnet）：かがり火タイル（`TORCH`・⚠️`'Y'` は SWITCH で使用済みなので別文字）を追加**＋ブーメランで炎運搬。`shared/tiles.js` に TORCH（別文字）追加 → `boomerangStep` の通過セル処理に炎運搬（点いた TORCH で `proj.flaming=true`／消えた TORCH へ点火→`litTorches.add`→`evaluateConditions()`）→ 描画。永続化（`litTorches`）は① で済み。設計は DECISIONS.md「Phase 4-5（設計）」②。

---

### 2026-06-19 — Phase 4-5（設計フェーズ・Opus）：組み合わせギミックを「投擲物/爆弾→タイル状態」の共通核に集約

**やったこと（設計のみ・コード変更なし）：**
- Phase 4-5（アイテムの組み合わせギミック）は 🧠→⚡ で設計未着手だったため、まず Opus で設計を確定。実装は次フェーズ（⚡ Sonnet）に回す
- **コード調査**：既存の `projectile.js`（`projectileTick`/`boomerangStep`/`explodeBomb`/`collectFieldItem` 呼び出し箇所）・`conditions.js`（`evaluateConditions`・トリガー一覧）・`player.js`（`SWITCH` 踏み処理・`checkSwitchOff`）・`passable.js`・`save.js`・`tiles.js`・`render-board.js` を読み、再利用できる土台を洗い出した
- **共通核を定義**：3サブギミック（①弓矢でスイッチ ②ブーメランで炎 ③爆弾＋特定タイル）は全て **「投擲物・爆弾が『敵/壁』だけでなく『タイルの状態』に作用する」** という1点に集約できると判断。新しい飛翔体ループや火管理機構は作らず、既存ループに最小分岐を足す方針にした
- **設計を3ファイルに記録**：DECISIONS.md に「Phase 4-5（設計）」を追記（決定・代替案・実装順）、PLAN.md 4-5 にチェック項目を具体化、PROGRESS.md 現在地を更新

**設計の要点（次セッションの実装契約）：**
- **① 弓矢でスイッチ**：既存 `SWITCH('S')` を再利用。矢のサブステップ補間ループで SWITCH 通過を検出して ON。**ラッチ式**（`ss.shotSwitches` に記録し `checkSwitchOff` の OFF 対象から除外＝離れても ON 維持）。ON 処理は既存 switch 踏みロジックを共通ヘルパー化して矢から呼ぶ。当面は弓矢のみ
- **② ブーメランで炎**：新タイル **`TORCH('Y')`**（点灯/消灯を `ss.litTorches` で永続管理）。`boomerangStep` の通過セル処理に「点いた TORCH で `proj.flaming=true`／消えた TORCH に火を運んで点火」を足す。主軸は点火方向
- **③ 爆弾＋特定タイル**：`explodeBomb` の AOE ループに「TORCH を範囲点火」分岐を足す（壊せる壁破壊・敵ダメージに並ぶ新作用）
- **クリア判定**：`showConditions` に新トリガー **`torchesLit`**（全 TORCH 点灯）を追加（笛 `flutePlayed`・ロウソク `bushBurned` と同型）
- **永続化**：`save.js` に `litTorches`・`shotSwitches` の Set↔配列変換を追加（`cutBushes`/`brokenWalls` と同じ要領）

**学び・気づき：**
- 一見バラバラな3ギミックも「**投擲物/爆弾がタイルの状態を変える**」という核で統一すると、新規実装は「既存ループに分岐を足す＋ss に Set を1〜2個＋トリガー1個」で済む。核を見抜かずに別々に作ると重複実装になる
- スイッチの「撃ったら ON 維持」は**タイマー式でなくラッチ式（Set 記録）**が単純で決定論的。`checkSwitchOff` から除外するだけで「踏みっぱなし不要のスイッチ」が作れる
- 炎の状態は**投擲物オブジェクトの `proj.flaming`**（火種＝投擲物と一緒に消える）と **TORCH の `ss.litTorches`**（永続）に役割分担するのが自然。player フラグや別管理ループは不要

**▶ 次やること：** **Phase 4-5 の実装（⚡ Sonnet）**。設計が DECISIONS.md/PLAN.md に書き出し済みなので Sonnet 推奨。①弓矢スイッチ（既存資産で完結・最低リスク）から1サブギミックずつ実装し、各ステップで `tests/` にスモークを足してグリーン確認。

---

### 2026-06-19 — Phase 4-4（完了）：ブーメランでフィールドアイテム回収（鍵・ルピー）

**やったこと：**
- **`collectFieldItem(r, c)` を `game/player.js` に新設**：指定セルの鍵（K）・ルピー小（r）・ルピー大（R）を `handleTileEvent` と同じ `pickedKeys` ガードで回収。SE/メッセージ/HUD更新/saveGame まで一括処理。
- **`game/projectile.js` の `boomerangStep` に呼び出し追加**：往路・復路どちらでも毎ステップ `collectFieldItem(toTileRow(proj.y), toTileCol(proj.x))` を呼ぶ。deps に `collectFieldItem` を追加
- **`game/game.js` を配線**：`let collectFieldItem = () => {}` 宣言→`createProjectile` deps に追加→`_player.collectFieldItem` を代入
- **`getGameState` に `player.keys` / `player.rupees` を追加**（テストで確認に必要）
- **ブーメランパズルを配置**（`work/blade-of-lumia.json` field `1,1`）：
  - 鍵 `K` を (2,8) に配置し、周囲を柵 `f` で囲む（直接アクセス不可・ブーメランは柵を通過可）
  - 施錠扉 `D` を (5,8)、宝箱 `B` を (6,8)（ルピー×5）に配置
  - 石碑 `i` を (4,8) にヒント：「🪃 ブーメランなら届くかも？」
- **tiles の format 修正**：node スクリプトが string 行と array 行を混在させた (`rowArr.forEach is not a function` エラー発生)。全行を array に統一して修正
- **テスト**：`tests/boomerang-item.spec.js` 3本（鍵回収で keys+1・再収集ガード・ルピー回収）→ **全81テストグリーン**

**学び・気づき：**
- **`isTilePassableForProj` は WALL と未破壊 BREAKABLE_WALL だけをブロック**。柵 `f`・扉 `D` は通過できる。これを利用すると「歩いては届かないが投擲物なら届く」パズルを柵/扉で自然に作れる
- **JSON の tiles 行は array で統一する**。元ファイルは全ステージ array 形式だが、node スクリプトで string を代入すると混在状態になる。書き込み前に `row.split ? row.split('') : row` で必ず array に変換する（`rowArr.forEach is not a function` の典型的な原因）
- **`boomerangStep` への `collectFieldItem` 追加は往路・復路の後ろで1行呼ぶだけ**で済む。ブーメランの座標更新後にセルを確認するだけなので既存ロジック（敵ヒット・壁判定・帰還）への影響ゼロ

**▶ 次やること：** **Phase 4-5（アイテムの組み合わせギミック）🧠→⚡**（弓矢でスイッチを撃つ・ブーメランで炎を操作など）。設計が未着手なので Opus で先に設計することを推奨。

---

### 2026-06-18 — Phase 4-3b（完了）：ロウソクの炎で前方の敵にダメージ（攻撃種別 `fire` 新設・弱点連動）

**やったこと：**
- **`playCandle()` に炎ダメージ判定を追加**：前方セル (`tr`, `tc`) に敵がいれば `dealDamageToEnemy(e, CANDLE_FIRE_DMG, 'fire')` を呼ぶ。茂みの有無・燃焼済みに関わらず毎回実行（先に敵判定→その後茂み分岐）
- **`CANDLE_FIRE_DMG = 3` を `game/constants.js` に追加**（控えめ値・ロウソクは発見用途の補助攻撃）
- **攻撃種別 `'fire'` を新設**：Phase 3-3 の弱点属性システムに乗るだけ（新規実装不要）
- **弱点設定**：`shared/enemies.js` の氷のリヴァイアサン(L) → `weakness:{type:'fire',multiplier:3}`（炎で氷が溶ける）、古森の巨人(O) → `weakness:{type:'fire',multiplier:2}`（炎で樹皮を焼く）。旧設定（bomb）から変更
- **メッセージ分岐**：前方に敵がいて茂みなし → `'🔥 炎が敵を焼いた！'`、茂みなし・敵なし → `'🕯 炎が揺らめいた…'`、茂み燃焼成功 → `'🔥 茂みが燃え上がった！'`（敵同時ヒットは両方処理）
- **検証**：`node scripts/check-errors.mjs` エラーなし → `tests/candle.spec.js` 6本（既存4＋新2：「前方に敵がいるとき炎ダメージが入る」「氷のリヴァイアサンは fire で×3倍ダメージ」）→ **全77テストグリーン**

**学び・気づき：**
- **「前方セルに敵がいるか」は `enemies.find(e => toTileRow(e.y) === tr && toTileCol(e.x) === tc)` で1行**。剣の複雑な射影/距離判定（大型敵AABB対応）は不要だった。ロウソクは「前方1セルに炎を当てる」という直感的な操作なのでセル一致判定で十分
- **茂みセルに敵がいる場合も正常動作**：炎ダメージ → 茂み燃焼の両方が発動する（`playCandle` の先頭で敵判定してから茂み分岐に入る構造）
- **弱点設定の変更（bomb→fire）は既存テストのデグレをチェック要**：`weakness.spec.js` は `FIRE_SALAMANDER(A) → arrow` と `ROCK_GOLEM(G) → bomb` をテストしており、今回変更した L・O は対象外 → 全テストグリーンで確認済み

**▶ 次やること：** **Phase 4-4（ブーメランでアイテム回収）⚡ Sonnet**（ブーメランが通過したマスの鍵・ルピーを回収できる機能）。設計は PLAN.md 4-4 参照。

---

### 2026-06-18 — Phase 4-3（完了）：ロウソクを実装（笛と同型・前方の茂みを燃やす→bushBurned で隠し入口出現）

**やったこと：**
- **ロウソクを笛と同型の active magic サブアイテムとして実装**（`subItems.candle`・`type:'magic'`・`uses:Infinity`・`hasCandle` フラグは作らない）。使うと **前方セル（heroDir 由来）の茂み（BUSH）を燃やす**
- **燃焼は既存の `cutBushes`（剣切りと共有の Set）を再利用**して通行可化（描画も render-board の既存 cutBushes 分岐に乗る・save 永続）。さらに `ss.bushBurned=true` を立て `evaluateConditions()` を呼び、`showConditions` の**新トリガー `bushBurned`** で gate された隠し通路/入口/アイテムを出現させる（笛の `flutePlayed` と1対1で同型）
- **剣で切っても `bushBurned` は立たない**＝「剣＝通行可化のみ／ロウソク＝通行可化＋発見」の役割分離。combat.js の茂み切りは無改修
- 実装ファイル：`shared/items.js`（candle 定義）・`game/conditions.js`（bushBurned トリガー）・`game/game.js`（`playCandle()`＋`showCandleFireEffect()`・useSubItem 分岐・ps_candle 適用・getState に hasCandle）・`shared/sounds.js`（`fire` SE「ボッ…ゴォォ」）・`game/css/effects.css`（`.candle-fire` 炎演出＋keyframes）・エディタ4ファイル（index.html の ps-candle トグル／editor-io.js・editor.js の両 getPreviewSettings／editor-props.js の宝箱「ロウソク」＋トリガー「bushBurned」）・`work/blade-of-lumia.json`（field 2,0：(2,6) ロウソク宝箱・(4,7) 茂み・(4,8) bushBurned 隠し入口→既存 secret_grotto を再利用）
- **検証**：`node scripts/check-errors.mjs` エラーなし → `tests/candle.spec.js` 4本（所持&active／燃やすまで遷移しない／燃やすと出現して secret_grotto へ遷移／前方が茂みでなければ出現しない）→ **全75テストグリーン**（既存71＋candle 4・VRT 含む＝起動画面 field 1,0 は不変）。一時 spec で炎演出 div の生成・JSエラーなしを実ブラウザ確認（確認後削除）

**学び・気づき：**
- **「茂みを燃やす」は新サブシステム不要**。剣切りの `cutBushes`（通行可化＋描画消去＋永続）と笛の `showConditions`（hide/reveal＋永続）という**既存の2機構を組み合わせる**だけで「燃やす→隠し通路出現」が成立した。新規は `playCandle()` と `bushBurned` トリガー1個のみ
- **炎演出 div は再描画の後に出す**：`showCandleFireEffect` を `renderBoard()/renderChars()` の**前**に append すると、再描画が charLayerEl を作り直して消えてしまう（functional テストは通るが視覚要素が出ない）。笛の warp 演出は遷移するので顕在化しなかったが、その場に留まる演出では再描画後に出すのが必須。前方が茂みでない（再描画しない）分岐では先に出してよい
- **「剣でも切れるが発見はロウソクだけ」は bushBurned を立てる主体を分けるだけ**で表現できた。同じ `cutBushes` を共有しつつ、発見トリガーをロウソク専用にすることで、剣で雑に茂みを退けても隠し通路は現れない＝ロウソクに固有価値を持たせられた

**▶ 次やること：** **Phase 4-3b（ロウソクの炎で敵にダメージ）⚡ Sonnet**（`playCandle()` で前方の敵に `dealDamageToEnemy(e, dmg, 'fire')`＋Phase 3-3 弱点属性と連動）。設計は PLAN.md 4-3b 参照。※ 暗闇照らしは重いので IDEA.md に記録（Phase 5 で別タスク化）。その後 4-4（ブーメラン回収）。

---

### 2026-06-17 — Phase 4-2（完了）：笛の仕上げ。未確定だったテスト3はテスト側のナビバグと判明、実装は正しかった

**やったこと：**
- 前セッションで「未確定」だった flute テスト3（reveal→隠し入口→secret_grotto 遷移）を切り分け。一時 spec で `pageerror` を監視しつつプレイヤー座標を1ステップずつトレースした結果、**実装は完全に正しく、テストの移動ナビゲーションが隠し入口 (5,3) を通り越していた**のが原因と判明
  - スポーン (4,4) から `down 6`（y→7）→`left 6`（x→1）では終点 (7,1) で、隠し入口 (5,3) を一切踏んでいなかった（だから遷移しなかった）
  - 正しい経路 `down 2`（y→5）→`left 2`（x→3）で (5,3) に乗ると、reveal→`enterStage('secret_grotto')` が `pageerror` ゼロで完走することを確認
- テスト2（笛なしでは遷移しない）も同じく入口を踏んでおらず**vacuously pass していた**ため、両テストの移動を `down 2/left 2` に修正。テスト2には遷移 setTimeout(100ms) を確実に見送るため `waitForTimeout(300)` を追加
- **flute テスト4本グリーン → 全71テストグリーン**（既存67＋flute 4）。一時 spec は削除済み
- VRT 起動画面（field 1,0）はデモが field 2,0 にあるため不変＝基準画像更新不要（全体テストで確認済み）
- PLAN.md 4-2 を `[x]`・実装状態表（笛行）を完了に更新。DECISIONS.md は前セッションで記録済み（追記不要）

**学び・気づき：**
- **「テストが落ちる＝実装バグ」とは限らない**。前セッションのデバッグログで「遷移分岐まで到達・ゲート通過」まで確認できていたのに遷移しなかったのは、そもそも**プレイヤーが入口セルに乗っていなかった**から。座標を1ステップずつトレースすれば一目で分かった
- **E2E の「○○しても遷移しない」系テストは、対象セルを実際に踏むナビゲーションになっているか必ず確認する**。今回テスト2は入口を踏まずに通っていた（偽の安心）。reveal 前は通り越し・reveal 後は乗る、で同じ経路を使うと両者を正しく対比できる
- 0.5 セル刻み移動では「目的セル row,col に乗る」= 整数座標 (col, row) に到達すること。`down N`/`left N` の N は (目標−現在)×2 で計算する

**▶ 次やること：** **Phase 4-3（ロウソク）⚡ Sonnet**（既存の茂み切り拡張：前方の草・茂みを燃やす→隠し通路/入口出現）。設計は PLAN.md 4-3 参照。

---

### 2026-06-17 — Phase 4-2（途中・引き継ぎ）：笛（フルート）を実装。reveal/warp の仕組みは完成、テスト1本だけ未確定

**⚠️ このエントリは「未完了タスクの引き継ぎ」。次セッションは下記【残作業】から再開する。**

**設計（DECISIONS.md に詳細記録済み）：**
- 笛は **active サブアイテム**（ブーメラン/弓矢と同型・`useSubItem` で発動・`type:'magic'`・`uses:Infinity`）。`player.hasFlute` は作らず `subItems.flute` で管理（save/load 自動）。
- 効果は **ステージ単位の `stageData.fluteEffect`** で決まる：
  - `{type:'reveal', message?}` → `ss.flutePlayed=true` にして `evaluateConditions()`。`showConditions` の**新トリガー `flutePlayed`** で gate された隠しタイル（隠し入口 `>`・隠しアイテム）が出現。`conditionsMet` がセーブされるので一度出した入口は永続。
  - `{type:'warp', destId, message?}` → `exitRegistry[destId]` へ `enterStage`（MAP_ENTER と同じ仕組みを再利用）。
  - 無し → 「何も起きない」メッセージのみ。
- **隠し入口の遷移ゲートも追加**：`checkStageTransition` の MAP_ENTER 分岐に「`showConditions` があり `conditionsMet` 未達なら遷移しない」を追加（笛で出すまで描画も遷移もされない）。

**実装済みファイル（投入済み・`node scripts/check-errors.mjs` エラーなし）：**
- `shared/items.js`：`flute` 定義（icon 🎵・type magic・uses Infinity・専用スプライト未作成＝pause/HUD は icon フォールバック）
- `game/conditions.js`：`evaluateConditions` に `flutePlayed` トリガー追加
- `game/game.js`：`playFlute()`＋`showFluteWarpEffect()` 新設・`useSubItem` に flute 分岐・`init` の `ps_flute` 適用・`getState` に `hasFlute`/`activeSubItem` 追加・MAP_ENTER 遷移の showConditions ゲート追加
- `game/css/effects.css`：`.flute-warp`（竜巻の渦巻き演出）＋ `@keyframes flute-warp-spin`
- エディタ：`editor/index.html`（プレビュー設定に `ps-flute` トグル＋ステージ設定に `stage-flute-effect` JSON 入力）・`editor/editor-io.js`（getPreviewSettings＋URL に ps_flute）・`editor/editor.js`（**重複定義の getPreviewSettings にも flute 追加**＝[[blade-preview-settings-duplicated]] 教訓）・`editor/editor-props.js`（宝箱内容に「笛」・トリガーに「flutePlayed」）・`editor/editor-canvas.js`（fluteEffect の読み書き：JSON パース・保存）
- `work/blade-of-lumia.json`：**デモ配置**（node 直書き）
  - field 2,0：`fluteEffect={type:'reveal',...}`／(4,3) に笛の宝箱 `B`（chestContents type:item item:flute）／(5,3) に隠し入口 `>`（id:`field_secret_entrance` destId:`secret_grotto`・showConditions `flutePlayed`）／(4,5) にワープ戻り先 mapEnter（id:`field_secret_back` destId:`field_2`・`>` タイル無しで exitRegistry 登録のみ）
  - 新レイヤー `secret_grotto`（0,0 のみ）：到着 mapEnter(5,2)・報酬ルピー宝箱(5,9)・石碑(5,5)・`fluteEffect={type:'warp', destId:'field_secret_back'}`

**テスト（`tests/flute.spec.js` 新設・4本）：**
1. ✅ `ps_flute=1` で笛所持＆active
2. ✅ 隠し入口は笛を吹くまで遷移しない
3. 🟡 **未確定**：笛を吹く（reveal）→隠し入口へ歩く→`secret_grotto` へ遷移
4. ✅ secret_grotto で笛を吹く（warp）→`field` へ戻る

**3 の調査でわかっていること（重要・次セッションはここから）：**
- デバッグログで **遷移分岐まで到達し、ゲートも通過している**ことを確認済み：プレイヤーが (5,3) に乗ると `tile='>' enter={id:field_secret_entrance,destId:secret_grotto} reg=true cond={trigger:flutePlayed} met=true` まで出る（＝reveal も exitRegistry も正常、遷移条件は満たしている）。
- それでも `currentLayer` が `secret_grotto` に変わらなかった。**遷移は 100ms の `setTimeout` 経由**なので、テストの待ち方（`step` だけでは実時間が進まない）か、`enterStage('secret_grotto',...)` 着地後の状態取得タイミングの問題の可能性が高い。`enterStage` が例外を投げているかは未確認（その確認の spec 実行直前にセッションを中断した）。

**【残作業】（次セッションでこの順に）：**
1. テスト3の切り分け：一時 spec で `page.on('pageerror')` を見つつ、(5,3) 到達後に `await page.waitForTimeout(300)` を入れて `enterStage('secret_grotto')` が例外なく完走するか確認。例外があれば secret_grotto ステージ定義の不足フィールドを補う。例外が無ければテストを「`movePlayer`+`step` の後に十分な実時間待ち（遷移 setTimeout 100ms 以上）を入れる」形に直す。
2. テスト4本グリーン＋`npx playwright test` 全体グリーンを確認（既存 67 + flute 4 = 71 本想定）。
3. 実機（一時 spec・確認後削除）で「笛吹く→隠し入口出現→入る／secret_grotto で笛→戻る」を目視確認。VRT 起動画面（field 1,0）はデモを field 2,0 に置いたので不変のはず（要確認）。
4. PLAN.md の 4-2 チェックボックスを `[x]`、進捗サマリ表・実装状態表（笛・ロウソク行）を更新。
5. DECISIONS.md は記録済み（追記不要）。

**学び（暫定）：** 隠し入口は「描画を hide するだけ」では不十分で、`checkStageTransition` 側にも同じ条件ゲートを足さないと「見えないのに踏むと入れる」抜けが出る。reveal は showConditions、warp は exitRegistry と、**既存パイプラインの再利用だけで新サブシステム無しに実装できた**。

**▶ 次やること：** 上記【残作業】1〜4。完了後 Phase 4-3（ロウソク）⚡。

---

### 2026-06-17 — バグ修正（分岐B・追補2）：はしごの向きを「進入軸」で決める（進入軸をラッチ）

**症状（ユーザー報告・スクショ）：** 水に向かって**上から下へ移動**したのに、はしごが**横向き**で表示される。ユーザー指摘どおり「プレイヤーがどの向きで入ってきたか」を向き判定に使っておらず、**セルの左右が陸か（横優先）**だけで決めていた（前回の「向きで消える」バグを避けるため軸非依存にした副作用）。

**原因と修正：**
- 前回 4-1c で「向きを変えると消える」不具合を避けるため、向きを**セルの地形（横優先）**で決めていた。しかしこれだと孤立水（上下左右すべて陸）で**下移動でも横向き**になってしまう
- **正しくは「進入軸（プレイヤーがそのセルへ入ってきた方向の軸）」で向きを決める**。ただし live な `heroDir` を使うと「上で向きを変える＝消える/変わる」が再発するため、**進入軸をラッチする**方式にした：
  - `game/player.js`：移動が**実際に成功したときだけ** `player._ladderAxis = moveAxis` を更新（向きだけ変えて移動できなかったときは前回の軸を保持）
  - `game/render-chars.js`：`updateLadderOverlay` で `player._ladderAxis` を使い `ladderOrientationAt(r,c,axis)` で向き決定。未設定時はセル地形フォールバック。デバッグ/テスト用に `.char-ladder` に `data-orient`（'h'/'v'）属性を付与
- これで「下/上移動→縦向き・左右移動→横向き」かつ「はしごの上で向きを変えても向き・表示は変わらない」を両立

**テスト：** `tests/ladder.spec.js` に1本追加（field 0,2 r3c2 の孤立水へ、下移動で入ると `data-orient='v'`・右移動で入ると `data-orient='h'`）。**全67テストグリーン**。

**学び：** 「向きで消える」を避けて軸非依存にしたら「向きが反映されない」逆の不具合を生んだ。正解は**進入軸を移動成功時だけラッチ**——live な向き（毎フレーム変わる heroDir）でも完全な軸非依存でもなく、その中間（最後に実移動した軸を保持）が「向きは正しく・上で回頭しても消えない」を同時に満たす。

**▶ 次やること：** 下記 Phase 4-1c エントリと同じ（Phase 4-2 笛 🧠 / 4-3 ロウソク ⚡）。

---

### 2026-06-17 — バグ修正（分岐B・追補）：上下に均等に跨るときは下側セルにはしごを出す

**症状（ユーザー報告・スクショ）：** 横移動で上下2セルが同時に橋になりうるとき、はしごが**上側セル**に出てしまい、キャラが**水の上に浮いて見える**。下側セルに出すべき（足元にはしごが来るように）。

**修正：** `game/render-chars.js` の `updateLadderOverlay` で「プレイヤー中心に最も近い橋セル1枚」を選ぶ際、**距離がほぼ同じ（上下に均等に跨る）場合は `r` が大きい方＝下側セルを優先**するタイブレークを追加（EPS 比較）。横移動の二重表示修正（最近傍1枚）はそのまま、縦の均等跨りだけ下側に倒す。

**テスト：** `tests/ladder.spec.js` に1本追加（col4 row5,6 の水に y=5.5 で跨り、はしごが下側 row6 に出ることを `.char-ladder` の top 位置で確認）。**全66テストグリーン**。

**学び：** 「最近傍1枚」だけでは上下均等跨りでループ順（上が先）に依存して上側が選ばれる。プレイヤースプライトの足元は下寄りなので、**均等なら下側を選ぶ**のが見た目に合う。タイブレークを明示しないと描画がループ順に暗黙依存する。

**▶ 次やること：** 下記 Phase 4-1c エントリと同じ（Phase 4-2 笛 🧠 / 4-3 ロウソク ⚡）。

---

### 2026-06-17 — バグ修正（分岐B）：はしご描画の二重表示・向き変更で消える・プレビュー設定の取りこぼし

**症状（ユーザー報告・スクショ付き）：**
1. 半セル位置（2セルに跨る）から水に入ろうとすると、はしごが**2枚**表示される
2. はしごの上で**橋の向きと違う方向を向く**と、はしごが**消えてしまう**（向きは変えてもはしごは残すべき）
3. **プレビュー設定の「はしご所持」が実機で効かない**（宝箱から取り直さないと機能しない）

**原因と修正：**
- **①②の原因は `game/render-chars.js` の `updateLadderOverlay`**：
  - ① 跨っている全セル（`r0..r1, c0..c1`）の橋セルにそれぞれ1枚ずつ敷いていた → 半セル位置で水＋陸（または水＋水隣接）に跨ると複数枚出る。**プレイヤー中心に最も近い橋セル1枚だけ**に限定して修正
  - ② 向きを `heroDir`（進入軸）由来で決め、軸が橋でなければ `ladderOrientationAt(...,axis)` が null を返して**はしごが消えていた**。**向きを「セルの地形（橋の向き）」で決める**よう戻した（軸なし呼び出し）。通行判定の軸チェックは `isPassable` 側が担うので、描画を軸に依存させる必要はない。これで橋の上で向きを変えてもはしごは残る
- **③の原因は `editor/editor.js`**：canvas クリック経由のプレビュー起動ハンドラがインラインで `ps` オブジェクトを組み立てており、そこに **`ladder` と `wingrobe` が欠落**していた（`editor-io.js` の `getPreviewSettings` には有るが、こちらの重複定義には無かった）。そのため `ps.ladder` が `undefined` → `openPreview` で `ps_ladder=0` になり、プレビューでは常にはしご無しだった。**`ladder`/`wingrobe` を追加**して修正
- **前回「機能は正常」と結論づけたのは誤り**：直接 URL（`?ps_ladder=1`）では効いていたが、**実際のプレビュー起動経路（canvas クリック → editor.js ハンドラ）では ps が欠落**していた。URL を直叩きするテストでは経路の違いを見逃していた

**テスト追加（回帰防止・計 ladder 11本 / editor 8本）：**
- `tests/ladder.spec.js`：「半セル位置で跨ってもはしごは1枚だけ」「はしごの上で向きを変えても消えない（上/下を向いても残る）」の2本
- `tests/editor.spec.js`：「canvas クリック経由のプレビュー起動で `ps_ladder=1` が iframe URL に渡る」1本（③の経路を実際に踏む回帰テスト）
- `node scripts/check-errors.mjs` エラーなし → `npx playwright test` **全65テストグリーン**

**学び・気づき：**
- **「直接 URL では効く」≠「実際の操作経路で効く」**。プレビュー設定のように UI 経由で組み立てるパラメータは、**実際の起動ハンドラを踏むテスト**でないと取りこぼしを検出できない。設定オブジェクトの組み立てが2箇所（`editor-io.js` と `editor.js`）に重複していたのが温床——将来は `getPreviewSettings` を1箇所に集約して両経路で共有すべき
- **描画の向きは「セルの地形」、通行可否は「進入軸」と役割を分ける**のが正解だった。向きまで軸に連動させると「向きを変える＝はしごが消える」副作用が出る。橋の向きはセル固有の性質なので地形で決め、軸チェックは通行判定だけに閉じ込める
- 半セル straddle の描画は「跨る全セルに描く」と二重化しやすい。**プレイヤー中心に最も近い1セルだけ**を選ぶと、初代ゼルダの「足元に1枚」の見た目に一致する

**▶ 次やること：**
- [ ] **Phase 4-2（笛）** 🧠 Opus：ワープ・隠しダンジョン出現の仕組み設計（未着手なので Opus 推奨）。または **Phase 4-3（ロウソク）** ⚡ Sonnet（既存の茂み切り機能の拡張）

---

### 2026-06-17 — Phase 4-1c（完了）：はしごの「進入軸」通行判定（縦連続水を縦に渡れないように修正）

**やったこと：**
- **核心バグ①の修正（`game/passable.js`）：** 旧 `isLadderBridge` は「横橋 OR 縦橋」のどちらか成立で通す**方向非依存**判定だったため、左右が陸の縦連続水（dungeon_3 0,0 col4）を**縦方向にスルスル渡れて**いた。`isLadderBridge` を `isHorizBridge`/`isVertBridge` に分離し、**進入軸 axis を受ける `isLadderCrossable(r,c,axis)`** を新設（`'h'`→横橋のみ・`'v'`→縦橋のみ・未指定→従来どおり両軸 OR）
- **`isPassable(nx,ny,axis)` に進入軸を追加：** 水/穴への新規進入は「進入軸の橋」のときだけ許可。ただし**プレイヤーが既に乗っている水/穴セルは進入軸チェック対象外**（橋の上から陸へ・どの軸でも抜けられる＝軸ロックなしの手触り）。`game/player.js` の `movePlayer` で `dir` から `moveAxis`（左右='h'/上下='v'）を出して `isPassable` の2箇所に渡す。`game.js` の deps wrapper も `axis` を透過
- **描画も同条件に（`game/render-chars.js`）：** `updateLadderOverlay` で `heroDir` から進入軸を出し、`ladderOrientationAt(r,c,axis)`（軸対応に拡張）で「進入軸の橋のときだけ・向きも軸で」はしごを出す。通行判定と描画の向き定義を一致させた
- **バグ②（プレビュー「はしご保持」が効かない）を再現調査 → 機能は正常と判明：** 実機の editor-preview URL パス（`fromEditor=1&ps_ladder=1&t=Date.now()`）を模した一時 spec で「`hasLadder=true` になる・渡れる」を確認（確認後削除）。原因は**仮説(c)**：4-1b で「渡り中だけ表示」に変えたため、陸に立っている間ははしごが見えず「効いていない」ように見えただけ。コード配線・キャッシュバスタともに問題なし
- **回帰テスト4本追加（`tests/ladder.spec.js`、計9本）：** (1) 縦連続水は縦に渡れない（col4 を下へ進んでも止まる）、(2) 同じ col4 を横には1セル渡れる、(3) 横橋の上から上の陸へ抜けられる（軸ロックなし）、(4) 橋の上から交差軸のさらに先の水へは渡れない（多段はしご不変条件）。`node scripts/check-errors.mjs` エラーなし → `npx playwright test` **全62テストグリーン**

**学び・気づき：**
- **「方向を問わない橋判定」は1セル幅の孤立水では正しく動くが、連続水では破綻する**。左右が陸でも上下が水なら「横橋」としては成立してしまい、縦移動を止められない。**進入軸を判定に持ち込むのが唯一の正解**で、これで「孤立水は両軸 OK・連続水は連続方向だけ NG」が自然に表現できた
- **軸ロック（橋方向のみ移動可）を入れずに『既に乗っているセルは進入軸チェック対象外』とするのが手触りの鍵**。0.5刻みの straddle 移動だと、橋の上から上の陸へ抜ける動きは「今乗っている水セル」を縦軸で再評価してしまい誤ブロックする。既乗りセルを除外することで、新規進入だけに軸条件がかかり「陸へはいつでも出られる／水へは軸一致が必要」が両立した
- **「効かない」報告がコードバグとは限らない**。4-1b の仕様変更（渡り中だけ表示）で見た目が変わったことが「効いていない」という体感を生んでいた。再現可能な spec で機能の生死を切り分けてから原因を結論づけるべき（キャッシュを疑う前に機能テスト）

**▶ 次やること：**
- [ ] **Phase 4-2（笛）** 🧠 Opus：ワープ・隠しダンジョン出現の仕組み設計（未着手なので Opus 推奨）。または **Phase 4-3（ロウソク）** ⚡ Sonnet（既存の茂み切り機能の拡張）

---

### 2026-06-16 — Phase 4-1b（完了）：はしご描画を「初代ゼルダ式」に修正（渡っている最中だけ足元に出る）

**やったこと：**
- **背景（ユーザー指摘で2回直した）：** v1 はプレイヤー要素内にはしごを入れ「張り付いて一緒に動く」誤り。**v2（私の誤実装）は「架かりうる橋セル全部に常設描画」してしまった**——スクショで水路に縦にはしごが並び続けるのは明確に誤り。正しい初代ゼルダの挙動は「**リンクが1マス幅の水/穴に踏み込んだ瞬間に、その足元の1セルだけにはしごが架かり、渡り切ると消える**」
- **正しい仕様を言語化してから実装**：(1) 常設しない、(2) プレイヤーが今重なっている水/穴の橋セルにだけ1枚出す、(3) 渡り切って陸セルだけになれば消える、(4) 向きはセルの地形（左右地上＝横／上下地上＝縦）、(5) z-index はセル描画より上・**プレイヤー/敵（char-abs）より下**（ユーザー指定）
- **`game/passable.js`**：`ladderOrientationAt(r, c)` を新設・export（水/穴かつ橋が成立すれば向き `'h'`/`'v'` を返す。架からなければ null）。`isLadderBridge`/`isLadderBank` を流用し判定の二重定義を回避
- **`game/render-chars.js`**：`updateLadderOverlay()` を新設。char-layer 内に `.char-ladder`（**セル固定**・`transition:none`・`z-index:-1`）要素として、プレイヤーが重なるセル範囲（0.5刻みで最大2セル）の中の水/穴橋セルにだけ `ladderH`/`ladderV` を1枚敷く。**毎回 querySelectorAll で全 `.char-ladder` を消してから敷き直す**ので、渡り切れば自然に消える。`updatePlayerCharEl`（移動毎）と `renderChars`（全再描画）の両方から呼ぶ
- **誤実装（v2）の撤去**：`render-board.js` の `addLadderToCell`・WATER/PIT セルへの常設描画分岐を削除。`game.js` の `createRenderBoard` に渡していた `getPlayer`/`ladderOrientationAt` も撤去し、代わりに `createRenderChars` に `ladderOrientationAt` を注入
- **`game/css/tiles.css`**：`canvas.sprite.ladder-sprite`（セル全面）。z-index は親 `.char-ladder`（`-1`）で制御
- **検証**：`node scripts/check-errors.mjs` エラーなし → `npx playwright test` **全58テストグリーン**。スクショで「踏み込んだ穴セルにだけはしご・隣の穴は空のまま・水路に常設されない」を目視確認。`tests/ladder.spec.js` の回帰テストを「陸にいる間は出ない／橋セルに踏み込むと出る（プレイヤー要素の外）／渡り切ると消える」に書き直し

**学び・気づき：**
- **「初代ゼルダのはしごを知っている」と口で言う前に、挙動を1ステップずつ言語化してユーザー確認すべきだった**。v1（張り付き）→v2（常設）と2回外したのは、私が「セル固定」を「常設」と取り違えたため。正しくは「**一時的な橋**＝渡っている間だけ足元に出る」。テストも v2 では「常設」を緑にしてしまい誤りを通した（テストが仕様の取り違えを追認した悪い例）
- **「渡り中だけ出して渡り切れば消える」は『毎回全削除→今の足元セルだけ敷き直す』が最も素直**。状態保持（どのセルに出したか）を持たず、プレイヤー座標が重なる橋セルを毎フレーム再計算するだけで「出現→追従せずセル固定→消滅」が成立する
- **z-index の階層：char-layer 全体が `z-index:5`（セルの上）。その中で `.char-ladder` を `-1` にするとセル描画より上・プレイヤー（auto≒0）/敵（`6`）より下**になる。ユーザー指定の「セルより上・キャラより下」を1要素の負 z-index で満たせた

**▶ 次やること：**
- [ ] **Phase 4-2（笛）** 🧠 Opus：ワープ・隠しダンジョン出現の仕組み設計（未着手なので Opus 推奨）。または **Phase 4-3（ロウソク）** ⚡ Sonnet（既存の茂み切り機能の拡張）

---

### 2026-06-16 — Phase 4-1（完了）：はしご（自動わたり方式・水/穴を1セル渡れる）

**やったこと（設計→実装→検証を一括）：**
- **設計確認（ユーザー方針）**：操作方式は **自動わたり（初代ゼルダ式）**／対象は **水＋新「穴」タイル**。穴は徒歩通行不可なので「落下」は構造的に発生しない＝落下ダメージ処理は不要（安全）と判明
- **`shared/tiles.js`**：新タイル `PIT='x'`（穴・落とし穴）を追加。TILE_META に暗い穴の見た目を定義
- **`shared/items.js`**：`ladder`（はしご）定義を追加。type は `passive`（サブアイテムスロットでは使わず `player.hasLadder` フラグで管理＝`hasWingRobe` と同型）
- **`game/passable.js`（核心ロジック）**：
  - `LADDER_OVER = {WATER, PIT}`（はしごで越えられる障害物）を新設
  - `isPassable` に「はしご所持時、`LADDER_OVER` かつ `isLadderBridge(r,c)` なら越えられる」分岐を追加（飛行分岐の隣）
  - `isLadderBridge(r,c)`：**交差軸の両隣が地上**（横＝左右が地上 or 縦＝上下が地上）なら true。`isLadderBank` は「水/穴でない通行可セル」を地上と判定。**2連続の水/穴は橋脚が片側で成立せず＝自然にブロック**される設計
  - `tilePassable` で `PIT` を地上ブロックに追加（はしごは `isPassable` 側で許可）
- **`game/game.js`**：`player.hasLadder` を初期化2箇所（グローバル＋`startNewGame`）に追加。プレビュー `ps_ladder=1` 対応。`getState`（getGameState）に `hasLadder` を公開
- **`game/player.js`**：宝箱 `content.type==='ladder'` で `hasLadder=true`＋取得メッセージ。`giveSubItem` の passive 分岐にも ladder を追加
- **描画**：`render-board.js`＋`css/tiles.css` で `PIT` を暗い穴として描画。`render-chars.js` の `addLadderOverlay`（水/穴の上に乗っている間だけ表示）
- **はしごの見た目を絵文字→2Dドット絵に修正**（ユーザー指摘）：当初 🪜 絵文字をプレイヤーに重ねていたが3D風で不自然だった。**初代ゼルダ式の2Dドット絵ハシゴ**を `shared/sprites-items.js` に新規作成（`ladderV`＝縦向き・側レール上下／`ladderH`＝横向き・レール左右、ともに 16×16・`ladder` パレット）。移動方向（縦/横）に合わせてスプライトを選び、`z-index:-1` でプレイヤーの背面にセル全面で敷く。`renderChars` と `updatePlayerCharEl` の両方で呼ぶ
- **⚠️ 描画位置がまだ誤り（次セッションで Phase 4-1b として修正）**：上記の修正でも **はしごをプレイヤー要素内に入れている**ため、はしごがプレイヤーに張り付いて一緒に動いてしまう。正しくは「**はしごが架かる水/穴セルにセル固定で描画**」（プレイヤーは追従させない・向きはセルの地形で決める）。詳細は PLAN.md「4-1b」に記載。**今回はここまでで context 上限のため次セッションへ持ち越し。**
- **`editor/editor-props.js`**：宝箱内容の選択肢に「はしご」を追加（オーサリング対応）
- **パズル設置**：`work/blade-of-lumia.json` dungeon_3 0,0 最下段 row8 を `#B..x...x.B#` に（col1=はしご宝箱／col4,col8=単セルの穴／col10=報酬ルピー宝箱）
- **検証**：`node scripts/check-errors.mjs` エラーなし → `tests/ladder.spec.js` 4本（はしご無しは穴で止まる／所持で単セルの穴2つを渡れる／**2連続水は渡れない**／宝箱で hasLadder 取得）→ `npx playwright test` **全57テストグリーン**（既存53＋新4・VRT 含む＝起動画面差分なし）

**学び・気づき：**
- **「自動わたり」は per-tile データを足さず、`isPassable` に種別＋形状（交差軸の両隣が地上か）の判定を1つ足すだけで実装できた**。`hasWingRobe`/`flying` と同じ「player 専用の passable 緩和」パターン。敵は `isPassableForEnemy`（別関数）を通るので影響しない
- **「1セルだけ渡れる／2連続は渡れない」は橋脚判定（交差軸の両隣が地上）で自然に表現できる**。「N セルまで」のような距離カウントは不要だった。2連続水は隣もまた水なので `isLadderBank` が false を返し、橋が架からない
- **穴（PIT）を徒歩通行不可にすると「落下」が構造的に起きない**。プレイヤーははしごが無ければ穴に踏み込めず、はしごがあれば渡り切るので、落下ダメージ・復帰処理という重い実装を回避できた（手触りもシンプル）
- **取得は passive サブアイテムではなくブールフラグが正解**。`hasWingRobe` と同様 `player` 全体が JSON シリアライズされるので save/load が自動対応。サブアイテムスロット（使用回数・active 切替）の概念に乗せると「使う」操作が無いアイテムで混乱する

**▶ 次やること：**
- [ ] **Phase 4-2（笛）** 🧠：ワープ・隠しダンジョン出現の仕組み設計。または **Phase 4-3（ロウソク）** ⚡（既存の茂み切り機能の拡張）。4-2 は仕組み設計が未着手なので 🧠 Opus 推奨、4-3 は既存拡張なので ⚡ Sonnet 可

---

### 2026-06-16 — Phase 3-4（完了）：ブーメランスタン（Phase 3 全完了）

**やったこと：**
- **`game/constants.js`**：`BOOMERANG_STUN_MS = 1500`（スタン持続時間）を追加
- **`game/projectile.js`**：`enemyCenter` を `hitbox.js` から import。ブーメランヒット時（`proj.type === 'boomerang'` ブランチ）に `e.stunUntil = gameNow() + BOOMERANG_STUN_MS` をセット＋`showStunEffect(e)` を呼ぶ。`showStunEffect` は敵中心に ⭐ テキストを `BOOMERANG_STUN_MS` ms 間表示
- **`game/enemy-ai.js`**：`enemyTick` に `if (e.stunUntil && now < e.stunUntil) continue;` を追加。スタン中は移動・攻撃を完全スキップ
- **`game/css/effects.css`**：`.stun-burst`（⭐ がフロート上昇するアニメ、duration はスタン継続中）を追加
- **`game/game.js`**：`getEnemiesSnapshot` に `stunUntil` フィールドを追加。`stunEnemyById(id, durationMs)` を新設・export（テスト用）
- **`game/main.js`**：`stunEnemyById` を import し `__game.stunEnemy(id, ms)` として公開
- **`tests/boomerang-stun.spec.js`** 3本：(1) スタン後 `stunUntil` が gameTime より大きい、(2) スタン中は step を踏んでも座標変化なし、(3) スタン期間過ぎると `stunUntil < gameTime`（期限切れ確認）

**学び・気づき：**
- **`injectTestEnemy` の敵は `speed:0` 固定**のため「スタン切れ後に実際に動く」はAI由来のspeedが適用されない。スタン期限切れの確認は「`stunUntil < gameTime`」というロジック状態で確認する方が決定論的
- **`getEnemiesSnapshot` はテスト用APIであり、必要な新フィールドを都度追加する**のが正しい運用（スナップショットに `stunUntil` がなかったため最初のテストが `null` を返した）
- **スタン中に ⭐ を表示するエフェクトは `showWeaknessBurst` と同じ「charLayerEl にdiv追加→setTimeout削除」パターンで実装**できる。duration=BOOMERANG_STUN_MS に合わせると「⭐が消えたらスタン切れ」という視覚フィードバックになる

**▶ 次やること：**
- [ ] **Phase 4（サブアイテム拡充）** へ。Phase 3 は 3-1〜3-4 全完了。推奨モデルは PLAN.md の Phase 4 タスク見出しを参照

---

### 2026-06-16 — Phase 3-3（完了）：ボスの弱点属性（攻撃種別ごとの倍率ダメージ）

**やったこと（設計→実装→検証を一括）：**
- **設計：弱点判定はダメージの単一窓口 `dealDamageToEnemy` に集約**。既存の攻撃種別（剣=`'sword'`・投擲物の `proj.type`='arrow'/'boomerang'/'beam'・爆弾=`'bomb'`）をそのまま「攻撃種別」として流用し、新フィールドや種別マッピングを増やさない最小設計にした
- **`shared/enemies.js`**：`ENEMY_META[*].weakness = { type, multiplier }`（任意）を新設。冒頭にフィールド仕様コメントを追加。8大型ボスに弱点を設定（武器をバラけさせて使い分けを促す）：
  - 岩のゴーレム G→爆弾(×3) / 氷のリヴァイアサン L→爆弾(×3) / 古森の巨人 O→爆弾(×2)
  - 炎のサラマンドラ A→弓矢(×2) / 嵐の鷲王 U→弓矢(×2)
  - 深海の海蛇 J→剣ビーム(×2) / 砂嵐の蠍王 N→ブーメラン(×3)
- **`game/combat.js`**：`dealDamageToEnemy(e, dmg, atkType)` に第3引数を追加。`weakness.type === atkType` なら **def 適用前の素ダメージに multiplier を乗算**（`Math.round`）。`atkType` 省略時は弱点判定なし＝従来挙動（後方互換）。剣ヒットは `'sword'` を渡すよう変更。弱点時は SE を `key`（高めの「キンッ」）に切替＋`showWeaknessBurst(e)`（敵中心の放射状フラッシュ）＋ポップアップを「WEAK! -N」表示（`isWeak` フラグ）
- **`game/projectile.js`**：投擲物ヒット（貫通含む）で `dealDamageToEnemy(e, proj.atk, proj.type)`、爆弾爆発で `dealDamageToEnemy(e, ..., 'bomb')` を渡すよう変更
- **`game/game.js`**：deps wrapper 2箇所（combat・enemy-ai 用）に `atkType` を通す。テスト用 `injectTestEnemy(x,y,hp,w,h,type='E')` に type 引数を追加（実ボスタイプを注入可能に）。`dealDamageToEnemyById(id,dmg,atkType)` を新設・export
- **`game/main.js`**：`__game.injectEnemy(...,type)` と `__game.dealDamage(id,dmg,atkType)` を公開
- **`game/css/effects.css`**：`.weak-dmg`（オレンジ大文字＋発光）・`.weak-burst`（放射状グラデの閃光・0.5sでスケールアウト）を追加
- **検証**：`tests/weakness.spec.js` 4本（炎ボス arrow ×2・岩ボス bomb ×3・弱点でない種別は等倍・弱点未定義のダミー敵は等倍）。`node scripts/check-errors.mjs` エラーなし → `npx playwright test` **全50テストグリーン**（既存46＋新4・VRT 含む）

**学び・気づき：**
- **「攻撃種別」は既存データ（`proj.type` と剣/爆弾の呼び出し元）にすでに存在していた**ので、新たな種別 enum やマッピングは不要だった。弱点機構は「単一ダメージ窓口に1引数足す＋ENEMY_META に1フィールド足す」で全攻撃経路（剣・矢・ブーメラン・ビーム・爆弾）をカバーできた
- **倍率は def 適用前の素ダメージに掛けるのが直感的**（`max(1, dmg*mult - def)`）。def 適用後に掛けると高防御ボスで倍率効果が頭打ちになる。「弱点を突けば硬い敵もごっそり削れる」という狙いに合う
- **テストは `injectTestEnemy` に type 引数を足し、`dealDamageToEnemyById` で直接ダメージを与える**のが決定論的で確実。実際の矢/爆弾を飛ばす E2E より、弱点倍率という純粋なダメージ計算を hp 差分で測る方がフレークしない。注入敵は def=0 なので素ダメージ＝倍率がそのまま hp 減少に出る

**▶ 次やること：**
- [ ] **Phase 3-4（ブーメランスタン）** ⚡：ブーメラン命中で敵を一定時間スタン（動けなくする）。スタン中は特別ダメージ。`enemy-ai.js` の `enemyTick` にスタン状態（`e.stunUntil` を `gameNow()` 基準で）を追加し、スタン中は移動・攻撃をスキップ。`checkProjHit` のブーメランヒット時に `e.stunUntil` をセット。これで Phase 3 完了
- ※ Phase 3-3 で `weakness:{type:'boomerang'}` を砂嵐の蠍王に設定済み。3-4 のスタンと組み合わせると蠍王戦に戦略性が出る

---

### 2026-06-16 — Phase 3-2（完了）：残り4体の大型ボスを一括追加（砂嵐の蠍王・深海の海蛇・古森の巨人・嵐の鷲王）

**やったこと：**
- **砂嵐の蠍王 `SAND_SCORPION('N')`**（dungeon_2 砂漠の神殿）：
  - 64×64スプライト（2フレーム）。8本鉗肢・尾の毒針・鋼甲殻。パレット（1=輪郭 2=暗茶 3=中茶 4=明茶 5=砂黄 6=赤眼 7=橙眼輝）
  - hp32/atk5/def1・speed×1.1・鉗肢なぎ払い2.3・毒針7射程・HP50%→speed×1.6/CD×0.75。向きエイリアス4つ
  - dungeon_2 boss room (1,0) row2,col5 の `X` → `N`
- **深海の海蛇 `SEA_SERPENT('J')`**（dungeon_3 水の迷宮）：
  - 64×64スプライト（2フレーム）。大型頭部・鱗模様・側鰭。パレット（1=輪郭 2=深青 3=中青 4=明青 5=水面 6=シアン 7=白輝）
  - hp38/atk4/def3・鉗肢なぎ払い2.6・水球8射程・HP50%→speed×1.4/CD×0.7。向きエイリアス4つ
  - dungeon_3 boss room (1,0) row4,col5 の `X` → `J`
- **古森の巨人 `FOREST_GIANT('O')`**（dungeon_6 森の聖域）：
  - 64×64スプライト（2フレーム）。樹皮の鎧・苔パッチ・光る木の実コア。パレット（1=輪郭 2=暗緑 3=中緑 4=明緑 5=苔 6=橙コア 7=黄コア輝）
  - hp42/atk5/def2・speed×0.9・枝腕なぎ払い2.4・木の実投げ6射程・HP50%→speed×1.3/CD×0.8。向きエイリアス4つ
  - dungeon_6 boss room (1,0) row2,col5 の `X` → `O`
- **嵐の鷲王 `STORM_EAGLE('U')`**（dungeon_7 空中の遺跡）：
  - 64×64スプライト（2フレーム）。広翼展開・雷弧エフェクト・鉤爪。パレット（1=輪郭 2=暗灰 3=中灰 4=明灰 5=鋼青 6=電黄 7=白電輝）
  - hp36/atk6/def1・speed×1.3（鷲らしく速め）・鉤爪2.1・雷撃弾7射程（speed1.4）・HP50%→speed×1.7/CD×0.65。向きエイリアス4つ
  - dungeon_7 boss room (1,0) row2,col5 の `X` → `U`
- **共通対応**：`shared/tiles.js` に4タイル文字（N/J/O/U）＋TILE_META追加。`shared/sprites-enemies.js` にパレット＋スプライト＋向きエイリアス追加。`shared/enemies.js` にENEMY_META追加。各ボス部屋の2×2占有スペースが全て床 `.` であることを確認してから配置
- **検証**：`npx playwright test` → **全46テストグリーン**（既存46本・変更なし）

**学び・気づき：**
- **64×64スプライトは描画関数（Node.js）で自動生成するとコード量を大幅削減できる**。`fill(g,r1,c1,r2,c2,v)` / `outline(...)` / `hline(...)` ヘルパーだけで複雑な形状を描け、2フレームのアニメは眼・コアのパレットインデックスを 6/7 で切り替えるだけで「脈動」に見える
- **boss_room の 2×2 確認は「row,col・row,col+1・row+1,col・row+1,col+1」全4マスが `.` か確認する**。今回は全ダンジョンでクリアだったが、dungeon_5（iceleviathan）で過去に石が邪魔した前例があるので毎回確認する
- **スプライトのバリエーションは「テーマカラー＋シルエット形状」で差別化できる**。蠍（横広・ハサミ）/海蛇（縦長・鱗）/樹人（幹+枝腕）/鷲（大翼展開）と、シルエットが全く異なるためパレット色がやや似ていても識別できる

**▶ 次やること：**
- [ ] **Phase 3-3（ボスの弱点属性）**：`ENEMY_META` に `weakness` フィールドを新設。特定アイテムで倍率ダメージ（炎ボス→弓矢・岩ボス→爆弾など）。`dealDamageToEnemy()` で攻撃種別を受け取り弱点なら倍率適用。弱点ヒット時に特別エフェクト
- または
- [ ] **Phase 3-4（ブーメランスタン）**：ブーメラン命中でスタン効果・スタン中は特別ダメージ

---

### 2026-06-16 — Phase 3-2（量産）：大型ボス2体目・3体目を追加（炎のサラマンドラ・氷のリヴァイアサン）

**やったこと（セッション再開後の作業）：**
- **セッション再開時の状況確認**：マシン強制終了後の再開。PROGRESS.md は最新（Phase 3-2 採用まで記録済み）、コードの変更（Phase 3-1・3-2 の全実装）はコミット未済のまま。全45テストグリーンを確認してから着手
- **炎のサラマンドラ `FIRE_SALAMANDER('A')` を追加**（dungeon_4 炎の神殿のボス）：
  - `shared/tiles.js`：タイル文字 `'A'`・ラベル「炎のサラマンドラ」・icon `Ａ` を追加
  - `shared/sprites-enemies.js`：24×24・2フレーム スプライト（溶岩色の巨大トカゲ。角ヘルム・鱗アーマー・炎眼コアが脈動）＋パレット（1=輪郭 2=暗炎 3=中炎 4=明炎 5=ハイライト 6=眼コア 7=眼輝）。向きエイリアス4つ（`fireSalamanderD/R/L/U`）
  - `shared/enemies.js`：`ENEMY_META[TILE.FIRE_SALAMANDER]`（hp35/atk5/def2・size{2,2}・dropsTriforce:true・hitAndAway・尻尾2.2＋炎石7射程・HP50%加速+攻撃頻度0.8倍）
  - `work/blade-of-lumia.json`：dungeon_4 ボス部屋 `1,0` の `X`(row2,col5) → `A` に置換
- **氷のリヴァイアサン `ICE_LEVIATHAN('L')` を追加**（dungeon_5 氷の廃墟のボス）：
  - `shared/tiles.js`：タイル文字 `'L'`・ラベル「氷のリヴァイアサン」・icon `Ｌ` を追加
  - `shared/sprites-enemies.js`：24×24・2フレーム スプライト（霜に覆われた海竜。白角・氷鱗・白眼核が脈動）＋パレット（1=輪郭 2=深氷 3=中氷 4=明氷 5=ハイライト 6=眼コア 7=眼輝）。向きエイリアス4つ（`iceLeviathanD/R/L/U`）
  - `shared/enemies.js`：`ENEMY_META[TILE.ICE_LEVIATHAN]`（hp40/atk4/def3・size{2,2}・dropsTriforce:true・hitAndAway・咬みつき2.5＋氷礫8射程・HP50%加速+攻撃頻度0.75倍）
  - `work/blade-of-lumia.json`：dungeon_5 ボス部屋 `1,0` の `X`(row2,col5) → `L`・(row3,col5) の `*`（石）を `.` に変更（2×2占有スペースを確保）
- **検証**：`tests/large-enemy.spec.js` に「2×2 注入でエラーなし・wrapper 大型サイズ」テスト追加（1本）。`npx playwright test` → **全46テストグリーン**（既存45＋新1）。VRT 基準画像は起動画面に差分なしでそのまま
- **欠片整合**：`calcTotalTriforces` は `dropsTriforce` フラグで汎用カウント。dungeon_4/5 の X→A/L の置換でも欠片総数は変わらない（X=5体 + G=1体 + A=1体 + L=1体 + Q×2 = 10個。前回9個から1増だが JSON を書き換えた count に依存）。実際の数は `calcTotalTriforces()` 実行時動的に再計算されるので整合は自動維持

**学び・気づき：**
- **大型ボスの量産は「スプライト24×24→ENEMY_META→向きエイリアス→配置」の4ステップで回せる**。hitAndAway AI・applyEnemySize・golem-lumber アニメ・dropsTriforce はすでに汎用化済みなので、各ボス固有のスプライトと stats だけ決めれば動く
- **dungeon_5 の石押しパズルタイル（`*`）がボスの占有スペースに重なっていた**。置換時は top-left から w×h の全セルが床（`.`）であることを確認してから配置する。今回は (row3,col5) の `*` を `.` に変更して解消した
- **向きエイリアスは `${base}${dirSuffix}` の規則で自動参照される**（`enemy-ai.js` の `baseName.replace(/[DRLU]$/, '')` + `dirSuffix` 付与）。`SPRITES` に `fireSalamanderD/R/L/U` が存在すれば AI の向き切替が自動で効く。1体ごとに AI コードを変更する必要はない

**▶ 次やること：**
- [ ] **大型ボスの残り4体を固有化する**（dungeon_2 砂漠/dungeon_3 水/dungeon_6 森/dungeon_7 空中）。同じ手順で各テーマに合ったスプライト・stats を定義して配置。
- または
- [ ] **Phase 3-3（ボスの弱点属性）**：`ENEMY_META` に `weakness` フィールドを新設し、特定アイテムで倍率ダメージが入るように。炎ボスは弓（冷却）・氷ボスは爆弾（熱）など。


### 2026-06-16 — Phase 3-2（採用）：岩のゴーレムを dungeon_1 ボスに正式採用＋「動いてる感じ」を付与

**やったこと（ユーザー方針：(1)正式採用→最初のダンジョンのボスに／(3)大型敵は複数種つくる／「もっと動いてる感じがほしい」）：**
- **岩のゴーレムを dungeon_1（最初のダンジョン）のボスに昇格**（`isBoss:true`・hp30/atk4/def2・hitAndAway AI・近接剣 range2.2＋岩投げ stone・HP50%で加速フェーズ）。`work/blade-of-lumia.json` の dungeon_1 ボス部屋 `2,0` の魔王 `X`(row1,col5) を `G` に置換（占有 rows1-2 cols5-6 は床）。**開始村 field 1,0 の試作 `G` は撤去**（検証用だったので床に戻した）
- **「動いてる感じ」を3点で付与**（正面固定で止まって見える不満の解消）：
  1. **向き切替**：hitAndAway AI が毎tick プレイヤー方向に `e.sprite` を `rockGolem{D/R/L/U}` へ切替（左は flipX）。向きエイリアスを `sprites-enemies.js` に追加（正面対称なので全向き同じ絵を流用）。**AI のスプライト差し替え時も canvas を 100% に再設定**して大型サイズを維持（enemy-ai.js）
  2. **重々しい揺れアニメ**：`board.css` に `@keyframes golem-lumber`（上下＋左右ロール＋わずかな伸縮）を追加し、`.char-abs.large-enemy canvas.sprite` に適用。`render-chars.js` の `applyEnemySize` で wrapper に `large-enemy` クラスを付与
  3. コア・眼の脈動（既存の2フレーム）
- **欠片整合性を汎用フラグ `dropsTriforce` で担保**（複数種ボスに効く設計）：`ENEMY_META.dropsTriforce:true` のボスは撃破で星の欠片を落とす。`boss.js` の `onBossDefeated`（ドロップ判定）と `calcTotalTriforces`（必要数カウント）を `DARK_LORD` 決め打ちから `dropsTriforce` 参照に拡張。**合計必要数は9個のまま不変**（直接2＋魔王6＋ゴーレム1。X×7 のうち1体を G に置換しただけ）
- **検証**：一時 spec（確認後削除）で dungeon_1 ボス部屋 `2,0` を実ブラウザ確認——ゴーレムが 2×2（144px）・`large-enemy` クラス付与・24×24 canvas、ボス部屋ロック演出（「扉が閉じた！」）発火、AI を 40 step 動かしてもエラーなし、プレイヤーを右に置くと `rockGolemR` に向き切替＋大型サイズ維持。`node scripts/check-errors.mjs` エラーなし → **全45テストグリーン**（VRT 基準画像は試作ゴーレム撤去で起動画面が元に戻ったため更新）

**学び・気づき：**
- **「ボスの種類を DARK_LORD 決め打ち」だった箇所（ドロップ・欠片カウント）は `dropsTriforce` フラグに一般化すると、大型ボスを複数種足しても欠片整合が崩れない**。タイル文字を増やすたびに boss.js を条件分岐で汚さずに済む
- **hitAndAway AI のスプライト差し替えは大型サイズ設定（canvas 100%）を吹き飛ばす**。差し替え箇所でも `e.w/e.h>1` なら 100% を再適用しないと、向きが変わった瞬間に 1セルに縮む。揺れアニメは wrapper の `large-enemy` クラス経由なので差し替えても効き続ける（クラスは wrapper、アニメ対象は内側 canvas）
- **「動いてる感じ」は (a) 向き切替 (b) 待機揺れアニメ の2本立てが効く**。向き切替だけだと遠距離で止まって見え、揺れだけだと方向感がない。両方入れると重量級の存在感が出る

**▶ 次やること（方針(3)：大型敵を複数種つくる）：**
- [ ] **2種類目以降の大型敵を作る** 🧠→⚡。size 機構・dropsTriforce・揺れアニメ・向きエイリアスの“型”は確立したので、2体目以降は (a) 24×24 スプライト＋パレットを描く → (b) ENEMY_META に size/attacks/phases を定義 → (c) 向きエイリアス4つ → (d) 配置、の手順を繰り返すだけ。テーマ別（炎/氷/森…）のダンジョンボスに展開する案。設計は固まっているので Sonnet でも可だが、スプライト1体目の手描き品質を見て判断
- ※ 残ボス（魔王 X×5／ラスボス ザーネル Z）に size を付けて大型化するかは、複数種の手応えを見てから決める


### 2026-06-16 — Phase 3-2（追補）：岩のゴーレムのスプライトを「鎧風の石の戦士」に作り直し＋テストのフレーク修正

**やったこと：**
- **ユーザーフィードバック「見た目が気持ち悪い（動きはOK）」を受けてスプライトのみ差し替え**。旧デザイン（溶けた顔っぽい岩塊＋オレンジの眼）→ **鋼鉄質の石アーマーをまとった重厚な戦士**へ：角ばったヘルム＋光るシアンの眼・広い肩アーマー・両腕の拳・胸の発光コア（脈動）・ガッシリした脚。24×24・2フレーム（コア/眼が脈動）
- パレットも青系コア＋鋼グレーに変更（`rockGolem`：1=輪郭 2=暗石 3=中石 4=明石 5=ハイライト 6=コア青 7=コア輝）。**当たり判定・移動ロジックは一切変更なし**（スプライトデータとパレットのみ）
- 実機（一時 spec・確認後削除）で新スプライトの描画・JSエラーなしを確認 → グロさが消えてかっこいい大型敵になった
- **テストのフレーク修正**：`tests/large-enemy.spec.js` の剣テスト2本が「剣クールダウン（`gameNow()` 基準・`SWORD_COOLDOWN_MS=100`／`lastSwordTime` 初期0）」に弾かれて hp が減らず落ちることがあった。**`swordAttack()` の前に `step(2)` で論理時間を進めてクールダウンを越える**決定論的な書き方に修正（Phase 0-1 の決定論ループの作法に合わせた）。VRT 基準画像 `game-start.png` も新スプライトで更新
- `node scripts/check-errors.mjs` エラーなし → **全45テストグリーン**

**学び・気づき：**
- **「動きはOK・見た目だけ直す」はスプライトデータ（`SPRITES_*`）とパレット（`*_PAL`）だけ触れば完結する**。当たり判定・サイズ機構（size:{w,h}）はスプライトと独立しているので、絵を何度でも安全に描き直せる
- **`gameNow()` 基準のクールダウンを持つ操作（剣・石押し）をテストする時は、必ず `step()` で論理時間を進めてから実行する**。開始直後は `gameNow()` が小さく `now - lastTime < COOLDOWN` に弾かれてフレークになる。実時間 `waitForTimeout` 頼みではなく決定論ループで時間を進めるのが正解

**▶ 次やること：**
- [ ] **Phase 3-2 の適用先をユーザーと決める**（前エントリと同じ）：試作ゴーレムを正式採用/撤去するか・既存ボスに size 付与するか・大型敵を複数種作るか


### 2026-06-16 — Phase 3-2（機構完成）：ボス大型化＝汎用 size:{w,h} 機構＋試作 2×2 敵「岩のゴーレム」

**やったこと（設計→実装→検証）：**
- **ユーザー方針を確認**：既存ボスをいきなり大型化せず、(1) 汎用の size 機構を入れる → (2) 新規 2×2 敵を1体だけ作って動作確認 → (3) うまく作れたら「どのボスに適用するか・複数種作るか」を改めて決める、という段取りに。試作は**当面 isBoss:false の大型通常敵**にしてボス演出を絡めず size 機構だけ純粋検証
- **`ENEMY_META.size:{w,h}`（省略時1×1）を新設**：`buildEnemies`（game.js）が `e.w = m.size?.w ?? 1` / `e.h = ...` を付与。既存敵は全て size 未指定＝自動的に 1×1 で挙動不変
- **当たり判定を占有範囲(AABB)ベースに一般化**（共通ヘルパー `game/hitbox.js` 新設）：
  - `enemyPointHit(e, px, py, margin)`：**1×1 のとき `|px-e.x|<margin && |py-e.y|<margin` と完全一致**（halfX=halfY=0）。w/h が大きいほど箱が body 中心へ移り半幅が `(w-1)/2+margin` に広がる。`projectile.js` の `checkProjHit`（margin 0.6・ビーム/弓矢/ブーメラン）と `enemy-ai.js` の `checkEnemyContact`（margin 0.9・接触ダメージ）に適用
  - `combat.js` の剣 `swordAttack`：敵中心 `enemyCenter(e)` への射影/直交距離判定に、**攻撃方向の body 半サイズ `halfFwd` を届く距離に・直交の `halfSide` を横の許容幅に加算**。1×1 では 0 で従来一致、大型は「中心が遠くても手前の面で当たる」
- **移動・衝突を w×h 占有に拡張**（`passable.js`）：`isPassableForEnemy` は占有セル全部の壁チェック＋石/他敵/プレイヤーとの AABB 重なり判定。プレイヤー `isPassable` の「敵セルに入れない」も大型敵の占有 w×h 全セルをブロック（めり込み防止）
- **描画対応**（`render-chars.js`）：`applyEnemySize(wrapper, e, cellPx)` を追加。wrapper を `w*cellPx × h*cellPx`・`z-index:6` にし、CSS の `canvas.sprite {…!important}` を `100% !important` で上書きして canvas を全面追従。24×24（2×2 セル相当）の `rockGolem` スプライト（眼が光る岩塊・2フレーム）を `sprites-enemies.js` に新規作成
- **試作敵を定義・配置**：新タイル `ROCK_GOLEM('G')`・`ENEMY_META`（hp24/atk4/def3・速度SLOW・size{2,2}・charge＝体当たり）。`work/blade-of-lumia.json` の開始村 field 1,0 row3,col8（占有 rows3-4 cols8-9＝全床）に1体配置
- **検証**：`tests/large-enemy.spec.js` 3本（2×2 のビーム手前面ヒット・剣リーチが body 半分ぶん拡大・1×1 デグレなし）。`injectTestEnemy`/`__game.injectEnemy` を w/h 受け取りに拡張。`node scripts/check-errors.mjs` エラーなし → **全46テストグリーン**。一時 spec（確認後削除）で field 1,0 の `G` が wrapper 144×144px（=2×72セル）・24×24 canvas・JSエラーなしで描画されることを実ブラウザ確認。VRT 起動画面にゴーレムが映るため基準画像 `game-start.png` を更新

**学び・気づき：**
- **「占有範囲ヘルパーで一本化し、1×1 を箱が潰れた特殊ケースとして従来挙動に一致させる」のが安全**。大型ボス専用の判定関数を別建てすると 1×1 と分岐が二重化してデグレ源になる。`(w-1)/2` が 0 に潰れる設計にしたことで、既存の 0.6/0.9 マージンや剣の射影判定をそのまま温存できた（全42→46テストでデグレゼロ）
- **剣の「中心への射影距離」判定は、大型敵だと中心が遠すぎて当たらない**。攻撃方向に沿った body 半サイズを「届く距離」に足し、直交方向の半サイズを「横の許容幅」に足すことで、向いている面の手前で当たるようになった
- **大型敵の canvas サイズは CSS の `!important` と戦う必要がある**。`.char-abs canvas.sprite` が `width/height: var(--cell) !important` で 1セル固定なので、JS から `setProperty('width','100%','important')` で上書きして wrapper（w×h）追従にした
- スプライトを 24×24 で作ると 12×16 の通常敵と同じ「1セル=12px 相当」の密度になり、2×2 セルにちょうど収まる

**▶ 次やること：**
- [ ] **Phase 3-2 の適用先をユーザーと決める**：(a) 試作ゴーレムを正式な敵/ボスとして残すか撤去するか、(b) 既存ボス（魔王 X×7 / ラスボス ザーネル Z）に size を付けるか、(c) 大型敵を複数種作るか。決まったら meta に `size` を足す＋ボス部屋レイアウトが 2×2 を収容できるか確認（ボス部屋は cols12×rows10 程度あるので余裕はある見込み）


### 2026-06-16 — Phase 3-1（完了）：チャージ攻撃／剣ビーム（押下で剣・長押しでチャージ・離してビーム）

**やったこと（設計→実装を一括）：**
- **ユーザーと仕様を確定**：(1) 攻撃ボタンを**押した瞬間に必ず通常の剣**が出る、(2) **押しっぱなしでチャージ開始**、(3) **離した時のチャージ量で発射判定**＝1/4未満はビームなし・1/4以上は弱ビーム（剣ATK・非貫通）・**満タンは強ビーム（剣ATK×2・貫通）**、(4) **チャージ中は移動速度0.5倍**（移動はできる）
- **`game/charge.js`（`createCharge` factory）を新設**：`_chargeStart`（押下した論理時刻）を持ち、`gameNow()` 基準でチャージ割合を計算（テストで決定的）。`startCharge`/`releaseCharge`/`cancelCharge`/`tickCharge`/`getMoveSpeedFactor` を公開。キーリピート対策として `startCharge` は既にチャージ中なら何もしない
- **ビームは既存の投擲物システムに相乗り**：`fireBeam()` が `addProjectile({type:'beam', piercing:full, strong:full, atk})` を呼ぶ。`projectile.js` の `createProjEl` に beam の CSS 描画分岐（横/縦で width/height 入替・strong は太く明るく）を追加。`checkProjHit` に**貫通対応**を追加（`proj.piercing` のときは `_hitIds` Set で同一敵の二重ヒットを防ぎつつ消えずに飛び続ける）
- **入力配線（`input.js`）**：攻撃キー（Space/z）keydown で `e.repeat` を除外して「剣＋startCharge」、keyup で `releaseCharge`。モバイル剣ボタンは `click` をやめ touchstart/end・mousedown/up/leave で press/release（`_swordHeld` で多重発火防止）。`processHeldKeys` の移動量に `getChargeMoveSpeedFactor()` を乗算してチャージ中減速
- **`game.js` 配線**：addProjectile 設定後に charge factory を生成。`gameTick` に `tickCharge()`（オーラ更新）、`enterStage` に `cancelCharge()`（遷移でチャージ中断）を追加。テストフック（`callStartCharge`/`callReleaseCharge` → `__game.startCharge/releaseCharge`）も公開
- **演出（`css/effects.css`）**：チャージオーラ `.charge-aura`（ready=青・full=金、pulse アニメ）＋ビーム本体 `.sword-beam`/`.beam-strong`。専用ドット絵スプライトは後回し（SPRITES_NEEDED.md の P3/FX1/FX2、既存スラッシュ演出と同じ CSS 代用方針）
- **`game/constants.js`**：`CHARGE_FULL_MS=720`（6フレーム）・`CHARGE_MIN_RATIO=0.25`・`BEAM_SPEED=4.0`・`BEAM_STRONG_MULT=2`
- **検証**：`tests/charge-beam.spec.js` 3本（1/4未満で離してもビーム無し・十分溜めて離すと beam が前方生成・満タンビームは貫通で hp=1 の敵2体を同時撃破）。`node scripts/check-errors.mjs` エラーなし → `npx playwright test` **全42テストグリーン**（VRT 含む＝起動画面に差分なし）。一時 spec で実機の aura ready/full クラス・beam DOM 生成・離脱後のオーラ消去・JSエラーなしを確認（確認後削除）

**学び・気づき：**
- **チャージ機構は論理時間 `gameNow()` に乗せると Phase 0-1 の `step()` で決定的にテストできる**。「押下時刻を記録し、離した時に経過時間で判定」だけなので状態は `_chargeStart` 1個で済む。飛行（`player.flying`）と同じく "1フラグ＋トグル" パターンの再利用
- **ビームは新しい飛翔体を作らず既存 `addProjectile` に `type:'beam'` で乗せるのが最小差分**。トンネリング防止の区間補間・境界/壁判定・当たり判定がそのまま効く。新規対応は「描画分岐」と「貫通（`piercing`+`_hitIds`）」の2点だけ
- **`piercing` フラグは弓矢で宣言済みだが checkProjHit で未実装だった**（矢は実際は最初の敵で消えていた）。今回ビームのために貫通を正しく実装したので、矢の `piercing:true` も今後活かせる（ただし矢の挙動は据え置き＝当面ビームのみ貫通）
- **モバイルの「押しっぱなし」は `click` では取れない**。touchstart/touchend + mousedown/up/leave に変えて `_swordHeld` フラグで多重発火を防ぐ。touchcancel/mouseleave での release 漏れ対策も入れた

**▶ 次やること：**
- [ ] **Phase 3-2**：ボスの大型化（2×2セル以上・複数セル当たり判定）🧠 Opus。当たり判定を複数セルにまたがるよう拡張するのが難所のため設計から（現在の `dealDamageToEnemy`/剣・投擲物の当たり判定は単一セル中心前提）


### 2026-06-16 — Phase 2-4（完了）：フィールド接続の仕上げ＋到達性回帰テスト固定（Phase 2 全完了）

**やったこと：**
- **ワールドマップ全体の徒歩到達性をBFSで機械検証**（`scripts/check-field-connectivity.mjs` を新設）。`game.js` の `checkStageTransition`（sx/sy のステージ端遷移）と同じ規則で field レイヤー全ステージを横断し、各ダンジョン入口 `>` の到達可否を判定
  - 判明：**8ダンジョンへの道はほぼ整備済み**。6つ（dungeon_1,2,3,5,6,7）は徒歩で直接到達。残り2つは**既存のギミック付きの道**：
    - dungeon_4（炎の神殿, field 0,1）：field 1,1 西端の**爆弾で壊せる壁** `!` 経由（NPCハナが明示済み）
    - cave_1（沼地の洞窟, field 3,1）：field 3,0 の**鍵扉** `D`（鍵 `K`@4,10 は東の魔物 `W` が守る）＋石押しパズル経由
    - field 4,0（空島）は飛行のみ＝終盤仕様で到達不可は意図どおり
  - ギミック解放を許可したBFS（`!`・`D` を通行可扱い）で全8ダンジョンが到達可能、かつ鍵が扉より先に取れる・石碑隣接可を確認
- **唯一の穴（field 3,0 のヒント皆無）を解消**（`work/blade-of-lumia.json` のみ変更・node直書き）：
  - field 3,0 (8,5) に石碑 `i` を追加：「沼地の洞窟へ続く鍵扉・鍵は東の魔物が守る・扉は南の柵の切れ目」を案内
  - field 2,0 すみっこずきの台詞に「東の先に沼地の道・鍵扉の先に古い洞窟」の噂を1行追加（cave_1 方向を示すNPCが従来いなかった）
  - 石碑配置後にBFS再検証＝必須経路（鍵・扉・石碑隣接）を塞いでいないことを確認
- **接続回帰テスト固定**：`tests/field-connectivity.spec.js`（データレベルBFS）を新設。全8ダンジョン入口の徒歩到達性（ギミック解放込み）＋沼地ヒント石碑の存在を保証。**全39テストグリーン**（既存37＋新2）
- 実機検証（一時spec・確認後削除）：field 3,0 (8,4) スポーン→右向き→剣で石碑「【沼地への道】」表示・JSエラーなしを確認

**学び・気づき：**
- **「フィールド拡張」の前にまず到達性をBFSで測ると、実は道はほぼ完成していて穴は1点（ヒント欠落）だけだった**。新ステージを足す前に現状を機械検証することで、過剰実装を避けられた
- **ダンジョン接続の回帰テストはブラウザでギミックを解かせるより、JSONを読むデータレベルBFSが決定的かつ高速**（39本でも8.7秒）。`checkStageTransition` の端遷移規則をテスト側に複製し、爆弾壁 `!`・鍵扉 `D` は「解ける関門」として通行可扱い、壁/水/木/山/柵/看板は不可、で現実のプレイ可能性を表現できる
- 看板 `i` は `passable.js` で通行不可。配置時はBFSで「必須経路を塞がない・かつ隣接して読める」を必ず確認する（前フェーズの教訓を踏襲）

**▶ 次やること：**
- [ ] **Phase 3-1**：チャージ攻撃（剣ビーム）🧠→⚡。攻撃ボタン長押しでゲージ→満タンで正面貫通の光剣を発射。チャージ機構の設計が未着手なので **🧠 Opus でまず設計**（その後の実装は Sonnet へ切替提案）


### 2026-06-16 — Phase 2-3（完了）：ダンジョン進行のヒント（石碑・NPCの噂話）

**やったこと：**
- **各ダンジョン入口付近に石碑（看板 `i`）を8枚配置**（`work/blade-of-lumia.json` のみ変更）
  - dungeon_1（草原の洞窟）：field 1,1 row7,col8
  - dungeon_2（砂漠の神殿）：field 1,2 row6,col4
  - dungeon_6（森の聖域）：field 1,2 row4,col3
  - dungeon_3（水の迷宮）：field 2,0 row3,col9
  - dungeon_7（空中の遺跡）：field 2,0 row7,col8
  - dungeon_4（炎の神殿）：field 0,1 row7,col5
  - dungeon_5（氷の廃墟）：field 2,1 row7,col6
  - cave_1（沼地の洞窟）：field 3,1 row8,col9
  - 各石碑の台詞：ダンジョン名・テーマの説明・入口方向案内（↑↓←→）
- **フィールドNPCに噂話台詞を追加**：
  - 村人 タロ（field 1,0）：「南の洞窟だけじゃなく各地にダンジョンがある」
  - すみっこずき（field 2,0）：「水の迷宮・空中の遺跡の方向案内」
  - 賢者 エルン（field 2,1）：「南に氷の廃墟がある」
  - ピンクあたま（field 0,1）：「南に炎の神殿への入口がある」
- **不具合修正**：最初に field 2,1 row8,col4 に石碑を置いたところ、dungeon_5 接続テストのスポーン位置と被り `passable=false` でテスト失敗。row7,col6 に移動して解決
- **全37テストグリーン**（既存37本、変更なし）

**学び・気づき：**
- 看板 `i`（SIGN）は `tiles.js` の `passable: true` だが、**`passable.js` では `false`**（隣接して剣で読む仕様）。テストのスポーン位置や通路に置くと詰まるので注意
- 石碑配置はマップのタイルを確認してから「敵・宝箱・NPCがなく、入口から1〜2マス離れた空きマス」に置くのが安全

**▶ 次やること：**
- [ ] **Phase 2-4**：フィールドマップ拡張（8ダンジョンへの道を整備・ギミック配置）🧠→⚡


### 2026-06-16 — Phase 2-2（完了）：ダンジョンボス固有化・各ダンジョン内容の充実

**やったこと：**
- **dungeon_2〜7 を完全リデザイン**（前セッションの最小構成からテーマ付き構成へ）
  - **ステージ構成を統一**：entry room `0,0` + boss room `1,0`
    - entry `0,0`：フィールドからの入口（MAP_ENTER `>`・id='dungeon_X'・destId='field_dungeon_X'）＋テーマ地形＋敵（E/C/F）＋宝箱＋石碑。右端 col11 rows 4-5 が開口して boss `1,0` へ（x >= cols 端遷移）
    - boss `1,0`：`isBossRoom=true`・DARK_LORD `X`・DOORWAY_BOSS `:` 左端 col0 rows 4-5（x < 0 端遷移で entry `0,0` に戻る）・宝箱
  - **各ダンジョンのテーマ地形**：砂漠(石柱 `*`)・水の迷宮(水路 `~`＋橋 `v`)・炎の神殿(壁＋破壊可能壁 `!`)・氷の廃墟(石柱)・森の聖域(木壁＋破壊可能壁)・空中の遺跡(水路＋橋)
  - **フィールド接続は entry `0,0` の MAP_ENTER に集約**：exitRegistry['dungeon_X'] → entry room のスポーン位置。field → entry → boss と正しい順序で入れる
- **ステージ navigation の根本修正**：前実装は stage key `1,0` からの `y < 0` 遷移（→ `1,-1` で存在しない）を誤って使っていた。`x >= cols` 遷移（entry→boss）・`x < 0` 遷移（boss→entry）が正しいと確認して再実装
- **テスト更新**：boss room 遷移テストを「entry 0,0 右端 → boss 1,0」の正しい動作に差し替え
- **デバッグ用一時ファイル削除**：debug-boss.spec.js / debug-boss2.spec.js / debug-boss3.spec.js を削除
- **全37テストグリーン**（test-new-dungeons.spec.js 3本含む）

**学び・気づき：**
- stage key `sx,sy` のナビゲーション：`x < 0` → `sx-1,sy`（左）、`x >= cols` → `sx+1,sy`（右）、`y < 0` → `sx,sy-1`（上）、`y >= rows` → `sx,sy+1`（下）。entry が `1,0`・boss が `0,0` の場合、entry から boss へは「左遷移（`x < 0`）」になる。「entry から boss へは上遷移（`y < 0`）」は誤り（`1,-1` で存在しないステージに向かう）
- **exit の向きとステージ key の関係を図に書いて確認するのが最も確実**。BFS で到達可能性を確認してもステージ座標の方向を間違えると繋がらない
- dungeon_1 パターン（`0,0` が entry・`1,0` が inner など）に合わせて entry/boss を `0,0`/`1,0` にすると、`x >= cols` 遷移で entry→boss、`x < 0` 遷移で boss→entry と直感的に対応できる

**▶ 次やること：**
- [ ] **Phase 2-3**：ダンジョン進行のヒント（石碑・フィールドNPCの噂話）⚡
  - 各ダンジョン入口付近に「何があるか」を匂わせる石碑を配置（entry room にすでに石碑を入れたので、フィールド側の入口付近へも追加検討）
  - フィールドの村NPCに「次のダンジョンの方向」を示す噂話台詞を追加


### 2026-06-15 — Phase 2-1（完了）：8ダンジョン体制への拡張

**やったこと：**
- **dungeon_2〜dungeon_7 の6ダンジョンを新規作成**し、既存の dungeon_1 / cave_1 / dark_tower と合わせて計8ダンジョン体制を達成
  - 各ダンジョンは最小構成：入口部屋 `1,0`（敵1〜2体・上部に通路でボス部屋へ端遷移）＋ボス部屋 `0,0`（DARK_LORD `X`・宝箱・ドアウェイ `:`）
  - テーマ：砂漠の神殿(dungeon_2)・水の迷宮(dungeon_3)・炎の神殿(dungeon_4)・氷の廃墟(dungeon_5)・森の聖域(dungeon_6)・空中の遺跡(dungeon_7)
- **フィールド入口（`>` タイル + mapEnter）を6箇所配置**：field 1,2(row7/row4)・field 2,0(row3/row7)・field 0,1(row8)・field 2,1(row8)
  - BFS で全6入口の到達可能性を検証（`check-dungeon-connections2.mjs`）
  - dungeon_5 の初期位置が家の壁内で到達不可だったため修正（row4→row8）
  - dungeon_2 の入口がフィールド敵（騎士 F）の配置行と重なっていたため row8→row7 に移動
- **`calcTotalTriforces()` が動的カウント**するため、DARK_LORD `X` 7体 + 直接 `Q` 2個 = 星の欠片9個が祭壇の必要数として自動反映される
- **遷移テスト2本追加**（`tests/test-new-dungeons.spec.js`）：dungeon_2/dungeon_5 の入口から `movePlayer+step` で実遷移を確認。**全35テストグリーン**
- **バグ修正**：スクリプト生成ステージに `rows`/`cols`/`bgTiles`/`links` 等のフィールドが欠けており、`renderBoard` が描画できなかった（cells=0、壁・敵が一切表示されない）。dungeon_1 の正規構造に揃えて修正

**学び・気づき：**
- `calcTotalTriforces()` は全レイヤー横断で `X`（DARK_LORD）と `Q`（ITEM_TRIFORCE_PIECE）を数えるため、**ダンジョンと DARK_LORD を追加するだけで星の欠片の必要数が自動的に増える**。祭壇の「あと○つ足りない」メッセージも自動更新される
- フィールド敵（`E`/`F`/`C` タイル）は起動時に enemies 配列に登録されて消えるが、**タイル文字が残っているため BFS の passable 判定では邪魔しない**。ただしプレイヤーが隣接すると接触ダメージが発生するので、入口の隣に強い敵がいる場合はテスト側か配置側で対応が必要
- **入口の '>' がフィールド縁の `t`（木）や `M`（山）と隣接していても問題ない**（プレイヤーは `>` に乗った瞬間に遷移するので、タイルの隣接は関係ない）
- ステージ端遷移と MAP_ENTER 遷移の双方向接続：`exitRegistry[id]` に戻り口を登録し `destId` で遷移先を参照する仕組みは既存の dungeon_1 と同パターン

**▶ 次やること：**
- [ ] **Phase 2-2**：各ダンジョンボスの固有化・内容の充実 ⚡
  - 現状は全新規ダンジョンが同じ最小構成（DARK_LORD 1体・宝箱1個）。各テーマに合わせた敵配置・地形（水タイル・溶岩・氷床）・石碑/看板による雰囲気づくりを行う
  - ボスを固有種（炎ボスはMONSTER `W` + パレット差し替えなど）に差別化することも検討


### 2026-06-15 — Phase 1-5（追補）：飛行で自然物（木・茂み・柵）も飛び越え可に拡張

**やったこと：**
- ユーザー相談「飛べるなら障害物も越えられていいのでは？山は越えられない等にしては？」を受け、**飛び越え範囲を `SKY`/`WATER` のみ → 自然物（`SKY`/`WATER`/`TREE`/`BUSH`/`FENCE`）に拡張。山・壁・家・閉じた門/扉は引き続き不可**
- 実装は `passable.js` の `FLYABLE_OVER` Set 1個で**タイル種別分類**（per-instance のデータ追加なし）。`isPassable` の飛行分岐を `t===SKY||WATER` → `FLYABLE_OVER.has(t)` に変更
- **場外クランプの安全ネット**を `checkStageTransition` に追加：木境界を飛び越えて遷移先ステージの無い端に出たら、`player.x/y` をマップ内（margin 0.5）に戻して詰みを防ぐ
- テスト追加（`tests/dark-tower.spec.js`）：「飛行中は木を飛び越えられるが山は越えられない」。**全33テストグリーン**。実ブラウザで開始村の木の上を飛ぶ様子も確認（山は境界として機能）

**学び・気づき：**
- 飛行緩和は **`isPassable`（プレイヤー）側だけ**に置けば敵（`isPassableForEnemy`）に漏れない。`tilePassable`（地上の真実）は据え置きで `SKY` を明示ブロックに加えるだけ。「越えられる障害物の種類」は per-tile フラグでなく**コード側 Set で十分分類できる**（データを増やさない）
- フィールド境界が「木」で作られている箇所は飛行で抜けられてしまう。**境界として確実に閉じたいなら山(M)を使う**のが設計上の指針。木境界の場所は場外クランプで保険をかけた
- 翼の羽衣は最終盤入手なので、木越え解禁は実質「クリア前の自由探索（victory lap）」。序盤マップ破壊リスクは構造的に小さい

**▶ 次やること：**
- [ ] **Phase 2-1**：8ダンジョンへの拡張（前エントリ参照）


### 2026-06-15 — Phase 1-5（完了）：飛行移動システム＋暗黒の塔＋ラスボス ザーネル

**やったこと：**
- **終盤フローを一本に接続**：全収集→祭壇で翼の羽衣→**飛行で空島の虚空を越える**→暗黒の塔→ザーネル撃破→エンディング。これで Phase 1（ストーリー基盤）が完結
- **飛行移動システムをフル実装**（ユーザー選択。1-3 の元方針どおり）：
  - `shared/tiles.js`：新タイル `SKY='%'`（空・虚空。通行不可・飛行中のみ越えられる）追加
  - `game/passable.js`：`tilePassable` で `SKY` を地上ブロックに。`isPassable` に「`player.flying` なら `SKY`/`WATER` を越える」分岐を追加（敵は `isPassableForEnemy` 経由なので地上判定のまま＝飛べない）
  - `game/player.js`：`toggleFlight()` を実装。羽衣がなければ拒否／離陸は即可／着陸は現在セルが地上(`tilePassable`)のときだけ（空・水の上では降りられない）
  - `game/game.js`：`player.flying` 初期化（グローバル＋`startNewGame`）。`enterStage` で**自動着陸**（到着セルが地上なら着陸、空/水なら飛行維持で立ち往生を防ぐ）。`checkStageTransition` に **`destId==='darkTower'` の `hasWingRobe` ゲート**を追加。`getGameState`/プレビュー `ps_wingrobe` も対応
  - `game/input.js`：**F キー**＋モバイル **🪽ボタン**で `toggleFlight`
  - `game/ui.js`：羽衣取得後に飛行ボタンを表示・飛行中はハイライト
  - 描画：`render-board.js`＋`css/tiles.css`（星空グラデの空セル）、`render-chars.js`＋`css/board.css`（飛行中プレイヤーの浮遊アニメ `wing-float`＋発光）
- **暗黒の塔をマップに追加**（`scripts/add-dark-tower.mjs` でディスク直書き・冪等）：
  - field `3,0` の到達可能な床(2,2)に塔への portal MAP_ENTER（`id:'fieldToTower'`/`destId:'darkTower'`）→ ゲートが効く
  - field `4,0`（空島）：到着足場(3,2) → 虚空 `SKY` の谷(cols4-7) → 浮島の塔入口(3,9)。谷を全行 SKY で分断し、飛行でしか渡れない構造に。石碑(看板)で操作ヒント
  - `dark_tower` レイヤー：入口フロア `0,1`（追跡者×2・騎士×2）→ 上の開口 → ボス部屋 `0,0`（ボス用ドアウェイ `:` で入室ロック・最奥にザーネル `Z`）
  - **接続は全て MAP_ENTER テレポート方式**（field の閉じた縁＝木/柵に阻まれる問題を回避）。BFS で「portal は徒歩到達可・塔入口は徒歩では不可・飛行で到達可・ボス到達可」を機械検証
- **検証**：`node scripts/check-errors.mjs` エラーなし → `npx playwright test` **全32テストグリーン**（既存29＋新3 `tests/dark-tower.spec.js`：飛行で虚空越え／羽衣なしは越えられない／塔入口で遷移）。加えて一時 spec で実ブラウザ確認（確認後削除）：ザーネル撃破→エンディングオーバーレイ発火／空島の描画（星空・浮遊プレイヤー・🪽ボタン）

**学び・気づき：**
- **飛行は「player 専用の passable 緩和」で実装するのが軽量**。敵は `isPassableForEnemy`（別関数）を通るので、`isPassable` 側にだけ `flying` 分岐を足せば敵に影響しない。`tilePassable`（地上の真実）は据え置き、`SKY` を明示ブロックに加えるだけで「徒歩は不可・飛行で越える」が両立する
- **field の縁遷移は信用しない**。field 3,0 の右端 `.` は内部から木・柵で隔離されており、ステージ端遷移では 4,0 に到達できなかった。MAP_ENTER テレポート（ダンジョン入口と同じ仕組み）に切り替えて解決。新エリアを足すときは「到達可能な床に portal を置く」のが確実
- **自動着陸は「到着セルが空/水なら飛行維持」が必須**。空島入口に着地すると即着陸→虚空の上で身動き取れず詰む。到着タイルを見て分岐することで、塔の空島でも安全に立ち往生を防げた
- **接続検証は BFS スクリプトが効く**。「徒歩で portal 到達可・飛行必須・ボス到達可」を3条件まとめて機械確認でき、手作業のプレイ確認より確実だった

**▶ 次やること：**
- [ ] **Phase 2-1**：ダンジョン数を8つに拡張（現状 dungeon_1 / cave_1 / dark_tower）。`calcTotalTriforces()` は動的カウントなので、ダンジョンと欠片を増やせば祭壇の必要数も自動的に増える
- ※ Phase 1（ストーリー基盤）は 1-1〜1-5 全完了。名称変更・老賢者・終盤フロー・祭壇/翼の羽衣・暗黒の塔/ザーネル/飛行までが一本に繋がった


### 2026-06-15 — エディタ拡張：ワールドマップに星の欠片サマリ（総数＋内訳リスト）を追加

**やったこと：**
- ユーザー要望「マップ上に星の欠片が全部でいくつあるか、どのレイヤー/ステージ/座標にあるかをワールドマップに表示したい（祭壇/エンディング確認のため）」に対応
- **ワールドビュー右下に「星の欠片サマリ」パネルを新設**。ゲーム側 `boss.js` の `calcTotalTriforces()` と**同じ定義**で全レイヤー横断で数える：星の欠片タイル `Q`（ITEM_TRIFORCE_PIECE）＋魔王タイル `X`（DARK_LORD・撃破で欠片を落とす）
  - 表示：**合計個数**（大きく）＋「直接拾える欠片◭ N ＋ 魔王撃破で出現 X N」の内訳＋**全件リスト**（`レイヤー / ステージ / (row R, col C)` ＋ 種別ラベル）
  - 「この個数を集めると終盤フローが進む。プレビューの『星の欠片』初期値をこの数にすると祭壇の挙動を確認できる」というヒントも併記
- 実装：
  - `editor/index.html`：`world-main` 末尾に `<div id="world-shard-summary">` を追加
  - `editor/editor-state.js`：DOM参照 `worldShardSummaryEl` を追加
  - `editor/editor-world.js`：`updateShardSummary()` を新設し `renderWorldGrid()` 末尾で呼ぶ（読込・レイヤー切替・ステージ追加/削除のたびに自動更新）
  - `editor/editor.css`：サマリパネル＋リストのスタイル（既存の `--accent2` 金色をアクセントに）
- **検証**：`npx playwright test` **全29テストグリーン**（既存エディタスモーク7本含む）。加えて一時 spec で実マップ（blade-of-lumia.json）を localStorage 注入してエディタを開き、**総数=3・内訳3件（field/2,0 row6col10＝欠片／dungeon_1/2,0 row1col5＝魔王／cave_1/1,0 row8col10＝欠片）** が表示されることを実ブラウザ確認（確認後削除）

**学び・気づき：**
- 星の欠片の「総数」はゲーム側 `calcTotalTriforces()` が唯一の正典。エディタ表示も**同じ定義（Q＋X）を複製**することで「エディタの数字＝ゲームのエンディング条件」を一致させた。将来 Phase 2 でダンジョンを8つに増やすと、このサマリが自動的に8（以上）を示すようになる
- `renderWorldGrid()` がワールド表示の単一更新フックなので、そこに `updateShardSummary()` を足すだけで全更新タイミング（読込・レイヤー切替・ステージ操作）をカバーできた
- **次の確認手順**：このサマリで総数（現状3）を確認 → ゲームをエディタプレビューで開く際に「星の欠片」初期値を3にする（`?ps_triforce=3`）→ 祭壇(field 1,0 row8col6)へ移動して「翼の羽衣」付与を確認できる

**▶ 次やること：**
- [ ] ユーザーが上記手順でプレビューから祭壇の挙動を確認
- [ ] その後 **Phase 1-5**（暗黒の塔・ザーネル配置）で終盤フローを一本に繋ぐ


### 2026-06-15 — Phase 1-4（完了）：古代の祭壇を実装（最小版・タイル方式A案）

**やったこと：**
- ユーザーの「エンディングは今プレイで確認できる？／祭壇を今作ってしまえば？」という問いを受けて実装。**現状マップは星の欠片2+魔王1=計3個で `calcTotalTriforces()`=3 → 3個集めれば従来フォールバックで即エンディングに入れる**ことを確認（`window._debugEnding()` でも演出だけ即確認可）
- **祭壇は専用ステージを作らず「フィールドに祭壇タイル1個」方式（A案）で実装**（軽量・1セッション完結）：
  - `shared/tiles.js`：`ALTAR='^'`（古代の祭壇・通行可・絵文字 `⛩`）を追加
  - `game/render-board.js`：祭壇タイルの描画（専用スプライト未作成のため `⛩` 絵文字フォールバック）
  - `game/boss.js`：`altarExists()` を「祭壇タイル `^` の存在検出」に変更（旧 MAP_ENTER id 方式は併用フォールバックに）。`offerAtAltar()` を新設（全収集なら `hasWingRobe=true`＋光柱フラッシュ `showAltarLightPillar()`＋fanfare＋メッセージ＋saveGame、不足なら「あとNつ足りない」、取得済なら「すでに授かった」）
  - `game/game.js`：createPlayer deps に `offerAtAltar` を注入
  - `game/player.js`：`handleTileEvent` に祭壇分岐を追加（`_lastAltarPosKey` ガードで乗りっぱなし連打を防止・祭壇から離れたら解除）
  - `work/blade-of-lumia.json`：最初の村 field `1,0` の row8,col6 に祭壇タイルを配置（開始位置から到達可能なことを BFS で確認）
- **検証**：`node scripts/check-errors.mjs` エラーなし → `npx playwright test` **全29テストグリーン**。加えて一時 spec で実ブラウザ検証（確認後削除）：
  - 祭壇 `⛩` が field 1,0 に描画される
  - エディタプレビュー（`?fromEditor=1&...&row=8&col=5&ps_triforce=3`）で全収集状態で祭壇に乗る → 「翼の羽衣を授かった！」表示＋**`hasWingRobe=true` がセーブに永続化**

**学び・気づき：**
- **「祭壇」は専用ステージ（B案・MAP_ENTERで往復）でなくフィールドにタイル1個（A案）で十分**。1-3 で `altarExists()` をハードコードせず抽象化しておいたおかげで、判定を「タイル検出」に差し替えるだけで誘導フローが繋がった
- **乗りっぱなしタイル（祭壇・将来のワープ等）は `_lastAltarPosKey` 方式で連続発火を抑止**：同じセルに居る間は1回だけ発火し、別タイルに移ったらガード解除。`pickedKeys`（拾うと消えるアイテム）とは別の「再訪できるが連打させない」パターン
- **テストでフィールドの特定タイルまで歩かせるのは壁干渉で不安定**。エディタプレビューモード（`?fromEditor=1` で `row`/`col`/`ps_triforce` 指定）で**目的地の隣に必要な状態でスポーンさせる**のが確実。BFS で経路を求めて queueInput する方式は壁の角で詰まりやすかった
- 祭壇は画面外（row8）なので VRT（起動直後画面 game-start.png）に影響せず基準画像更新不要だった

**▶ 次やること：**
- [ ] **Phase 1-5**：暗黒の塔（ラストダンジョン）🧠→⚡
  - 暗黒の塔ステージを作成。入り口は `hasWingRobe=true` でないと通れない（飛行移動方式 ← 1-3 決定）
  - 最奥にラスボス `ZARNEL`（`Z`）を配置 → 撃破で `onBossDefeated` の `isFinalBoss` 分岐がエンディング発火（実装済み）
  - ※ 現状は祭壇で羽衣を得た後、塔が無いとエンディングに繋がらない（羽衣取得後は `checkTriforceClear` が何もしないため）。1-5 完成で「収集→祭壇→塔→ザーネル→エンディング」が一本に繋がる


### 2026-06-15 — Phase 1-3（完了）：ゲーム終盤フローの変更（設計＋安全な基盤実装）

**やったこと：**
- **設計方針：終盤を「後方互換フォールバック付きの状態機械」にする**（DECISIONS.md に詳細記録）。祭壇・塔・ザーネルが未配置の現状でも従来どおり「全収集→即エンディング」で完走できることを担保しつつ、1-4/1-5 が乗るだけの基盤を敷いた
- **`game/constants.js`**：終盤フローの MAP_ENTER 予約 id を定義（`ALTAR_EXIT_ID='altar'` / `DARK_TOWER_EXIT_ID='darkTower'`）。1-4/1-5 はこの id を入り口に設定する“契約”
- **`shared/tiles.js`**：ラスボス `ZARNEL`（タイル `Z`）を追加。`DARK_LORD`（`X`）のコメントを「ダンジョンボス・星の欠片を落とす」に明確化（最終ボスと分離）
- **`shared/enemies.js`**：`ZARNEL` の ENEMY_META 追加（HP80・atk8・`isBoss:true`・**`isFinalBoss:true`**・aura・hitAndAway・stone+sword の2攻撃・2フェーズ）。専用スプライト未作成のため当面 `darklord` を流用
- **`game/game.js`**：`player.hasWingRobe` フラグを2箇所の player 初期化（グローバル定義・`startNewGame`）に追加。`saveGame` が player 全体を JSON 化するため save/load は自動対応。createBoss deps に `getExitRegistry: () => exitRegistry` を注入
- **`game/boss.js`**：
  - `checkTriforceClear()` を分岐化：(a) `hasWingRobe=true` → 何もしない（ザーネル撃破待ち）、(b) 祭壇 id が exitRegistry にある → **エンディングを出さず祭壇へ誘導メッセージ**、(c) どちらでもない → **従来どおり即エンディング**（フォールバック）。`altarExists()` ヘルパー追加
  - `onBossDefeated()` に **`isFinalBoss` 撃破でエンディング発火**を追加（欠片演出より前に分岐）。ザーネルは欠片を落とさず直接エンディングへ
- **検証**：`node scripts/check-errors.mjs` ページエラーなし → `npx playwright test` **全29テストグリーン**（デグレなし）。加えて `createBoss` をモック deps で生成する一時 Node テストで分岐ロジック5ケース（祭壇なし全収集→エンディング／祭壇あり→誘導のみ／羽衣取得済→何もしない／収集未完→何もしない）を実コードで確認（確認後削除）

**学び・気づき：**
- **終端処理が逆のボスは別タイルに分けるのが正解**：`DARK_LORD`(X) は「撃破で星の欠片を落とす」、`ZARNEL`(Z) は「撃破でエンディング」。同一タイルにフラグで両義を持たせると `onBossDefeated` と `calcTotalTriforces`（X を欠片数にカウント）の両方が条件分岐だらけになる。タイルを分ければ `calcTotalTriforces` は X を数え続け（ザーネルは欠片源に含めない）、`isFinalBoss` 判定もタイプ一致で済む
- **`player` 全体を JSON シリアライズしている設計のおかげで、進行フラグ追加が save/load 自動対応になる**（`save.js` の純粋関数は player を触らないため変更不要）。フラグは初期化2箇所（グローバル `let player` と `startNewGame`）に足すのを忘れない
- **ステージ名のハードコードを避け、MAP_ENTER の予約 id の有無でフェーズ判定する**ことで、1-4/1-5 がステージをどこに作るか（どの layer/stage か）に依存せず分岐できる。エディタでステージを置く位置の自由度を保てる
- 敵は `buildEnemies`→`renderChars` 経由でスプライト名から描画されるため、`ZARNEL` の `sprite:'darklord'` 流用は render-board.js のタイル描画分岐を増やさずに済む（タイル `Z` 自体は敵スポーン専用で常設スプライト描画は不要）

**▶ 次やること：**
- [ ] **Phase 1-4**：古代の祭壇の実装 🧠→⚡
  - 祭壇ステージをマップに追加し、入り口 MAP_ENTER の `id` に `'altar'`（=`ALTAR_EXIT_ID`）を設定（これで boss.js が誘導モードに切替）
  - 祭壇インタラクション：星の欠片を全部（`calcTotalTriforces()` 個）持っていれば捧げられる → `player.hasWingRobe = true`
  - 捧げた演出：光柱エフェクト・「翼の羽衣を授かった」メッセージ
  - ※ 祭壇タイル/スプライトは新規。Phase 0-4 SPRITES_NEEDED.md の最優先5点（古代の祭壇・翼の羽衣）を参照


### 2026-06-15 — Phase 1-2（完了）：序盤の老賢者NPCを配置・ストーリー導入

**やったこと：**
- **最初の村（field `1,0`）に老賢者NPCを配置**：プレイヤー開始位置（row2,col2）の斜め右下 row3,col3 に NPC タイル `b` を置いた
  - ⚠️ **当初 row3,col2（開始位置の真南）に置いたが、NPCタイルは通行不可（`passable.js` で `NPC_SPRITE_MAP[tile]` は false）のためプレイヤーが南に進めなくなる不具合があった**。斜め隣（3,3）に移すことで上下左右4方向の移動を一切塞がず、かつ隣接対話できる配置に修正
  - 専用の老賢者スプライトは未作成（新規スプライトは Phase 0-4 の管轄）のため、既存 NPC タイル `b`（茶系パレット＝老人風）を流用。名前・台詞で「老賢者」と分かるようにする方針
- **老賢者の台詞でストーリー導入**（`work/blade-of-lumia.json` の `field.stages["1,0"].npcData["3,2"]` に5行）：
  - 「目を覚ましたか若き勇者よ」→「女王ルミアがザーネルの呪いで石になった」→「星の欠片を8つ集め古代の祭壇に捧げれば呪いが解ける」→「ザーネルがダンジョンに魔物を放ち欠片を封じている」→「旅立つのじゃ」
  - IDEA.md のストーリー導入3点（①呪い ②欠片8つ＋祭壇 ③激励）を反映
- **役割分担**：既存の村人タロ（row3,col5）は方向案内（東＝森・南＝洞窟）に専念、老賢者がストーリーを語る構成にしたため台詞の重複・矛盾なし
- 編集は `node`（`fs.writeFileSync` + `JSON.stringify(d,null,2)`）でディスク直書き（過去のエディタ系ツールでの大ファイル書き込み失敗の教訓に従う）。JSON は2スペースインデントでラウンドトリップ一致を事前確認
- `npx playwright test` → **全29テストグリーン**（デグレなし）。加えて一時 spec で「**開始位置から上下左右4方向すべて移動可能**＋老賢者(3,3)に隣接して対話開始（台詞表示・JSエラーなし）」を実挙動で確認（確認後 spec は削除）

**学び（重要・ハマりどころ）：**
- **NPCタイルは `tiles.js` のメタでは `passable: true` だが、実際の通行判定 `passable.js` では `NPC_SPRITE_MAP[tile]` があれば通行不可（壁扱い）**。プレイヤーがNPCに重ならないための仕様。NPCを配置する際は「プレイヤーの移動を塞がない位置」に置くこと。特に開始位置の隣接マスを塞ぐと初手で詰む
- **エディタの表示は `localStorage`（`bladeOfLumiaMapData`）から読む**ため、`work/blade-of-lumia.json` をファイル編集しただけではエディタに反映されない。エディタで「読込」してファイルを取り込むと反映される（プレビューは実JSONを読むので即反映）。これは仕様どおり
- **実機で「表示されない／動かない」を見たら、まずブラウザ／Viteのキャッシュを疑う**（DECISIONS 2026-06-13 の教訓どおり）。今回も Vite 再起動で NPC が表示された
- **反省：最初の確認テストが緩すぎた**。「いずれか1方向動けばOK」「対話が出ればOK」では、真南が塞がれた不具合を検出できなかった。配置変更時は「全方向の移動可否」を実挙動で確認すべきだった

**あわせて対応（用語統一・ユーザー指摘）：**
- **ピンク髪NPC台詞の退行を修正**：作業ツリーに「星の欠片」→「トライフォースのかけら」へ逆戻りした台詞があった（ユーザーが編集途中だったもの）。Phase 1-1 の確定方針に合わせ「星の欠片」へ戻した
- **「ハートコンテナ」表示を「ハートの器」に全統一**（表示テキスト・ラベル・コメント）：`shared/tiles.js`（コメント＋label）・`shared/sprites-items.js`（コメント）・`game/player.js`（コメント＋フィールド取得 pulse）・`game/boss.js`（コメント）・`editor/editor-props.js`（chest/item ラベル2箇所）・`work/blade-of-lumia.json`（chest name）。内部識別子 `heartContainer`（JSON item値・`content.type` 比較・editor の value）は**セーブ/JSON互換のため維持**（星の欠片と同じ方針）
- ※ 宝箱への `"type": "heartContainer"` 追加（ユーザー編集分）はそのまま保持

**学び・気づき：**
- NPC は「タイル文字（`a`/`b`/`P`/`$`）＋ `npcData["row,col"]`（name・lines）」で配置・台詞設定する。`npcData` 未設定なら `npcs.js` の `NPC_DEFAULT_DIALOG` が使われる。`$`（ショップ）以外のNPCは攻撃ボタンで `startDialog` が発火する（combat.js）
- VRT（`game-start.png`）が通ったのは、開始画面のスクリーンショット範囲に変化が出ない描画（老賢者は画面内だが基準画像更新不要の差分閾値内）か、開始時点のビューでカバーされたため。NPC追加の機能確認は VRT ではなく対話スモークで担保した

**▶ 次やること：**
- [ ] **Phase 1-3**：ゲーム終盤フローの変更 🧠（状態遷移の設計）
  - 現在：星の欠片8個ですぐエンディング
  - 変更後：8個集まったら「古代の祭壇」ステージへ誘導 → 捧げると「翼の羽衣」入手 → 暗黒の塔解放 → ラスボス → エンディング
  - まず現状の `checkTriforceClear`（boss.js）の8個集めたらエンディング発火ロジックを読み、どこで分岐させるか設計する


### 2026-06-15 — Phase 1-1（完了）：テキスト・名称変更「トライフォースのかけら」→「星の欠片」＋エンディング書き直し

**やったこと：**
- **表示名のみ変更（内部変数名は互換維持）**：grep でトライフォース関連テキストを網羅確認し、表示に出る箇所のみを「星の欠片」に変更
  - `shared/tiles.js`：ラベル `'トライフォースのカケラ'` → `'星の欠片'`
  - `game/boss.js`：pulse メッセージ2箇所（ボス撃破後の出現メッセージ・収集メッセージ）を「星の欠片」に変更
  - `game/player.js`：フィールドでの拾得メッセージを「星の欠片を手に入れた！」に変更
  - `work/blade-of-lumia.json`：NPC（ピンク髪）の台詞を「星の欠片」に変更
- **エンディングメッセージを新ストーリーに書き直し**（`game/boss.js`）：
  - 旧：`魔王を倒し、すべてのトライフォースのカケラを集めた！<br>ルミアの平和は守られた……`
  - 新：`ザーネルを倒し、すべての星の欠片を集め、女王ルミアの呪いを解いた。<br>光が世界に戻り、ルミアの地に平和が訪れた……`
- **内部変数名は維持**（DECISIONS 2026-06-13 の方針通り）：`ITEM_TRIFORCE_PIECE`・`triforceCount`・タイル文字 `Q` はセーブ互換のため変更なし
- `npx playwright test` → **全29テストグリーン**（デグレなし）

**学び・気づき：**
- 表示テキストの影響範囲は4ファイル（tiles.js・boss.js・player.js・JSON）。editor 側（editor-palette.js の TILE_SPRITE_MAP など）はラベル非表示のため変更不要だった
- 内部変数名維持により「セーブデータとの後方互換」と「表示名の変更」を安全に分離できた

**▶ 次やること：**
- [ ] **Phase 1-2**：序盤の老賢者NPCを配置・台詞でストーリー導入 🧠→⚡
  - 開始直後の村に老賢者NPCを配置
  - 台詞：「女王ルミアが魔術師ザーネルの呪いで石になった」「星の欠片を8つ集めて古代の祭壇に捧げれば呪いは解ける」「旅に出よ、若き勇者よ」


### 2026-06-15 — Phase 0-6（完了）：TS・ビルドは見送り、計測の上で Phase 0 をクローズ

**やったこと：**
- **採用判断タスク（コード変更なし）。** Phase 0-6「TypeScript・ビルドの検討」を、現状の実コードと公開構成を確認した上で判断
- **公開構成を確認**：本番は Vercel。`vercel.json` は `{ outputDirectory: "outputs", trailingSlash: true }` のみで**ビルドステップなし**。`outputs/` 配下に blade-of-lumia 以外に3ゲーム（one-step-dungeon / dungeon-world / sword-duel）＋共通ランディング `index.html` が同居し、全体が素ファイルのまま静的配信される
- **esbuild と Vite の関係を整理**（ユーザーの疑問に回答）：esbuild は Vite 内部で使われる。dev サーバ（`vite`）は素ファイルを個別配信し minify しない。本番最適化は `vite build`（内部で esbuild minify + Rollup bundle）が担う。よって esbuild 単独導入は不要で、最適化するなら `vite build` を足すだけ
- **起動時間を実測**（Playwright + CDP・一時 spec を作成→計測後に削除）：本番同等の未バンドル構成で、転送量律速を排除するため**帯域無制限・RTT のみ注入**し import 連鎖の段数の影響を切り出した
  - リクエスト30（JS 27 + CSS/JSON/他）。素ファイル合計297KB / gzip後70KB
  - time-to-board：RTT 0ms=269 / 30ms=482 / 75ms=817ms
  - ボトルネックは**転送量ではなく JS27本の import 連鎖×RTT**
- **判断：plain ESModule 維持（TS も `vite build` も見送り）→ Phase 0 クローズ**。理由：70KBと軽量・改善幅は数百msで致命的でない・無ビルド運用の手軽さ喪失・4ゲーム同居の Vercel 設定整合コスト・Phase 1+ はコンテンツ中心で型化の費用対効果が低い
- DECISIONS.md に判断＋計測値＋再判断トリガーを記録。PLAN.md の 0-6 完了化・ファイルサイズ表（game.js 4075→1268行に実測更新）・実装状態表を更新

**学び・気づき：**
- **Vite dev サーバでの帯域スロットル計測は誤診を生む**。dev は素ファイル(297KB)をソースマップ＋HMR注入で約2MBに水増しするため、帯域を絞ると「転送量律速」で起動3.2秒という本番と無関係な数字が出る。本番（素297KB配信）に近づけるには**帯域無制限＋RTTのみ注入**し、import 連鎖の段数だけを切り出すのが正しい
- 「esbuild を入れる vs Vite を使う」は対立ではない。Vite が内部で esbuild を呼ぶので、最適化したいなら新ツールを足さず `vite build` を使うのが正解
- 本番が「ビルドなしで `outputs/` を素配信」という構成だと、1ゲームだけ `dist/` 化するのは Vercel 設定（`outputDirectory`）と噛み合わせる整合作業が要る。同居構成では「全部素 or 全部ビルド」の方が管理が楽

**▶ 次やること：**
- [ ] **Phase 1-1**：テキスト・名称変更 ⚡
  - 「トライフォースのかけら」→「星の欠片（せいのかけら）」を**表示名のみ**変更（内部変数 `Q`/`ITEM_TRIFORCE_PIECE`/`triforceCount` はセーブ/JSON互換のため維持＝DECISIONS 2026-06-13 の方針）
  - エンディングメッセージを新ストーリーに書き直し（「魔王を倒し…」→「ザーネルを倒し、女王ルミアの呪いを解いた…」）
  - 影響範囲：表示テキスト（game/ 配下の boss.js/ui.js 等）・`shared/tiles.js`・`shared/items.js`。grep で表示文字列を洗い出してから着手
- ※ Phase 0 完了により**新ゲーム機能の実装が解禁**（WORKFLOW 共通ルール）

### 2026-06-15 — Phase 0-5（完了）：タイルバリエーション設計支援を実装

**やったこと：**
- **🗺 タイルタブ＋`#view-tile` を `editor/index.html` に追加**（3ペイン：左=テーマ選択+タイルグリッド、中央=パレットエディタ、右=エクスポートコード）
- **`editor/editor-tile.js` を新設**（自己完結モジュール・`initTileEditor()` / `onLeaveTileEditor()` を export）：
  - テーマプリセット8種（デフォルト/炎/水/氷/森/砂漠/空中/闇）：各テーマで主要ダンジョンタイル（block/door/swG/gateG/water/breakableWall/mapEnter/doorway/doorwayBoss/doorwayLocked）の色パレットを上書き定義
  - タイルプレビューグリッド：テーマ別にキャンバスで各タイルスプライトを描画・クリックで選択
  - パレットエディタ：選択タイルの大型プレビューキャンバス + 色スウォッチ（クリックで color input）→ 色変更時にプレビューとグリッドをリアルタイム更新
  - テーマ切り替え：セレクトボックスで変更 → 全タイルグリッド再描画
  - リセット：「テーマ初期値に戻す」ボタンでそのテーマのプリセット値に戻す
  - エクスポート：デフォルト（TILE_PAL）から変更されたタイルのみを `TILE_PAL_OVERRIDE` 形式で JS コード生成 → クリップボードコピー対応
- **`editor/editor-tile.css` を新設**（3ペインレイアウト・タイルプレビューグリッド・パレットスウォッチ・色スウォッチ・大型プレビューキャンバス）
- **`editor/editor.js` を統合**：`initTileEditor()`/`onLeaveTileEditor()` を import・`showView('tile')` 分岐追加・タブクリックリスナー追加
- **editor スモークテスト3本追加**（`tests/editor-tile.spec.js`）：(1) タイルタブ表示＋テーマセレクト＋タイルグリッドが複数アイテム表示＋エクスポートtextarea存在＋JSエラーなし、(2) タイル選択→active＋パレットスウォッチ表示＋大型プレビュー表示＋リセットボタン存在、(3) 炎テーマに切り替え→エクスポートコードに「炎」を含む
- `npx playwright test` → **全29テストグリーン**（既存26＋新3、デグレなし）

**学び・気づき：**
- テーマデータは `themeData[themeKey][tileKey] = palette` の2重オブジェクトに保持し、編集は `themeData` への直接代入とすることで「プリセット初期値」とは別に管理。リセット時は `THEME_PRESETS` の `overrides` に戻す設計にした
- パレット編集は `<input type="color">` を非表示で配置し、スウォッチクリック → `.click()` で呼び出す方式。スウォッチ再描画なしでインプレース更新できるため再描画コストが低い
- エクスポートは「デフォルト TILE_PAL と全く同じパレットの場合は変更なし扱い」（JSON.stringify で比較）にしたため、デフォルトテーマ選択時はコードが簡潔になる

**▶ 次やること：**
- [ ] **Phase 1-1**：テキスト・名称変更（「トライフォースのかけら」→「星の欠片」）⚡  ← Phase 0-5 全完了につき Phase 1 に移行可能
- または
- [ ] **Phase 0-6**：TypeScript・ビルドの検討 🧠

### 2026-06-15 — Phase 0-5（後半）：アイテム定義エディタを実装

**やったこと：**
- **📦 アイテムタブ＋`#view-item` を `editor/index.html` に追加**（3ペイン：左=アイテムリスト、中央=エディタフォーム、右=エクスポートコード）
- **`editor/editor-item.js` を新設**（`initItemEditor()` / `onLeaveItemEditor()` を export）：
  - アイテムリスト描画：`ITEM_META`（サブアイテム）と `EQUIP_META`（装備）を2セクションに分けて列挙・クリックで選択・active ハイライト
  - ITEM_META 用フォーム：名前/アイコン/スプライト名/パレット名・タイプ選択（throwable/placeable/consumable/passive）・uses形式切替（Infinity/null/数値）・タイプ別追加フィールド（breakPower/piercing/aoeRadius/damage/healAmount）の動的表示切替
  - EQUIP_META 用フォーム：名前/アイコン・スロット選択（weapon/shield/armor）・スロット別ステータス（atkBonus/damageReduction/defBonus）の動的表示切替
  - 変更適用：フォーム値を itemBuffer/equipBuffer に反映 → JS コード生成
  - エクスポート：選択中エントリ + Export All（ITEM_META または EQUIP_META 全体）を textarea に表示 → クリップボードコピー
- **`editor/editor-item.css` を新設**（item-layout 3ペイン・アイテムリストスタイル・セクションタイトル・エクスポートパネル）
- **`editor/editor.js` を統合**：`initItemEditor()`/`onLeaveItemEditor()` を import・`showView('item')` 分岐追加・タブクリックリスナー追加
- **editor スモークテスト3本追加**（`tests/editor-item.spec.js`）：(1) アイテムタブ表示＋アイテムリスト＋エクスポートtextarea存在＋JSエラーなし、(2) アイテム選択→active＋フォーム表示＋基本入力欄存在、(3) 名前編集→変更適用→エクスポートコードに `ITEM_META` と編集後名前を含む
- `npx playwright test` → **全26テストグリーン**（既存23＋新3、デグレなし）

**学び・気づき：**
- items.js の `uses` フィールドは `Infinity`・`null`・数値の3パターンがある。`structuredClone` は `Infinity` を正しくコピーする（`NaN` や `undefined` と異なり問題なし）。エクスポートコード生成時も `valueToJs()` ヘルパーで `Infinity`→文字列 `'Infinity'`（引用符なし）に変換する必要がある
- タイプ別追加フィールド（`throwable`/`placeable`/`consumable`/`passive`）の表示切替は、タイプ選択セレクトの `change` イベントで `renderTypeFields()` を再描画する方式にした。フォーム全体を再描画するとフォーカスが飛ぶため、フィールド部分のみを差し替えるのがポイント
- キャラクターエディタと同じ `.char-form` / `.form-row` CSS クラスを流用することで、中央パネルのレイアウトを統一できた（editor-character.css に定義済みのスタイルがそのまま適用される）

**▶ 次やること：**
- [ ] Phase 0-5 残り：タイルバリエーション設計支援（テーマ別パレットプレビュー）⚡ ← 必要性低め、Phase 1 優先でもよい
- または
- [ ] Phase 1 ストーリー基盤：「トライフォースのかけら」→「星の欠片」テキスト変更・老賢者NPC・エンディング書き直し⚡

### 2026-06-15 — Phase 0-5（中盤）：キャラクター定義エディタを実装

**やったこと：**
- **🧙 キャラクタータブ＋`#view-character` を `editor/index.html` に追加**（3ペイン：左=敵リスト、中央=エディタフォーム、右=エクスポートコード）
- **`editor/editor-character.js` を新設**（492行・`initCharacterEditor()` / `onLeaveCharacterEditor()` を export）：
  - 敵リスト描画：`ENEMY_META` 全エントリを列挙・クリックで選択・active ハイライト
  - エディタフォーム：基本情報（名前/HP/ATK/DEF/EXP）・スプライト/パレット名・速度プリセット（低速/通常/高速）＋カスタム値入力
  - ボスフラグ・ヒット&アウェイ・オーラエフェクトのチェックボックス
  - 攻撃設定：単一攻撃（attack）vs 複数攻撃（attacks配列）の切替チェックボックス
    - 単一：タイプ選択（charge/sword/spear/stone）・charge以外は射程/CD/飛翔速度を表示
    - 複数：動的リスト・各エントリごとにタイプ/射程/CD/飛翔速度・追加/削除ボタン
  - フェーズ設定：動的リスト・HP閾値/速度倍率/攻撃CD倍率・追加/削除ボタン
  - 変更適用：フォーム値を editBuffer に反映 → `ENEMY_META[key]` を更新 → JSコード生成
  - エクスポート：`[TILE.XXX]: {...}` 形式でコード生成 → textarea 表示 → クリップボードコピー
- **`editor/editor-character.css` を新設**（117行）：3ペインレイアウト・敵リストアイテム・攻撃/フェーズアイテムのスタイル
- **`editor/editor.js` を統合**：`initCharacterEditor()` を `init()` で呼び出し・`showView('character')` 分岐追加・タブクリックリスナー追加・ビュー離脱時 `onLeaveCharacterEditor()` を呼ぶ
- **editor スモークテスト3本追加**（`tests/editor-character.spec.js`）：(1) キャラクタータブ表示＋敵リスト＋エディタパネル＋エクスポートtextarea存在＋JSエラーなし、(2) 敵選択→active＋フォーム表示＋基本入力欄存在、(3) HP編集→変更適用→エクスポートコードに `"hp": 99` 含む
- `npx playwright test` → **全23テストグリーン**（既存20＋新3、デグレなし）

**学び・気づき：**
- enemies.js の `ENEMY_META` 構造は単一攻撃（attack）と複数攻撃（attacks配列）の二重構造・フェーズ設定（phases配列）・initialModeWeights等の多様なフィールドを持つため、フォームのUI設計で「初見で分かりやすい」と「全機能を網羅」のバランスを取った。複数攻撃は「チェックボックスで切替」形式にし、使わない場合は隠すことでフォームの複雑さを軽減
- 攻撃タイプ（charge/sword/spear/stone）ごとに表示するパラメータが異なる（chargeは射程/CDなし、spear/stoneは飛翔速度あり）ため、タイプ変更イベントで表示/非表示を切り替える動的UIが必須だった
- `structuredClone()` で `ENEMY_META` をディープコピーして編集バッファに入れることで、「変更適用」まで元データを壊さない安全な編集フローを実現
- エクスポートコードは `TILE.` 定数名を含むため、`getTileConstantName()` で逆引き処理を実装（TILE.PATROL → "PATROL"）

**▶ 次やること：**
- [ ] Phase 0-5 残り：アイテム定義エディタ（items.js）/ タイルバリエーション設計支援（テーマ別パレットプレビュー）⚡。同じ自己完結タブ方式で追加

### 2026-06-14 — Phase 0-5（前半）：スプライトエディタを実装

**やったこと：**
- **🎨 スプライトタブ＋`#view-sprite` を `editor/index.html` に追加**（3ペイン：左=グリッド/ツール/パレット、中央=描画キャンバス、右=フレーム/読込/エクスポート）
- **`editor/editor-sprite.js` を新設**（`initSpriteEditor()` / `onLeaveSpriteEditor()` を export・状態は内部に閉じ込め）：
  - グリッド可変（32²/16²/12×16/8²/6²/64² プリセット）／パレット編集（クリック選択・カラーピッカー変更・色追加・index0=透明チェッカー）
  - ドット描画（ペン/消しゴム・ドラッグ連続）／複数フレーム（追加・複製・削除・サムネ選択・再生プレビュー）／左右反転・クリア
  - 既存スプライト読込（`SPRITES`/`PAL` から deep copy）→ heroD 等の派生作成に有効
  - エクスポート：`sprites-*.js` の配列形式コード生成（パレット行 + `SPRITES.xxx = [...]`）→ textarea 表示 → クリップボードコピー
- **`editor/editor.js` を配線**：showView を全ビュー/全タブ一旦リセット方式に拡張し `sprite` 分岐を追加・`init()` で `initSpriteEditor()` を呼ぶ・離脱時に再生停止
- **`editor/editor-sprite.css` を新設**（editor.css と分離してタブ干渉を回避）。index.html から `<link>` 追加
- **editor スモークテスト2本追加**（`tests/editor.spec.js`、計6本）：(5) スプライトタブ表示＋キャンバス/パレット＋JSエラーなし、(6) heroD 読込→32×32反映→エクスポートコードに `SPRITES.` を含む
- `npx playwright test` → **全19テストグリーン**（既存17＋新2、デグレなし）

**学び・気づき：**
- スプライトエディタは既存エディタ状態（mapData/state）と独立しているため editor-state.js に相乗りせず editor-sprite.js 内に専用 state を閉じ込めるのが綺麗。editor.js とは showView と init だけで疎結合に保てた
- データ形式（`SPRITES.x=[frame][row][col]=palIdx`、`PAL.x=[transparent,...]`）を sprites.js の `drawSpriteFrame` と完全一致させたので読込→編集→エクスポートのラウンドトリップが自然に成立
- **Cline の `replace_in_file`/`write_to_file` は PROGRESS.md/PLAN.md のような大きいファイルで「成功と返すが実ファイルが変わらない」事象が発生**。独立した小ファイルは問題なし。→ ドキュメント更新は Node スクリプト（fs.writeFileSync）でディスク直書きする回避策を採用

**▶ 次やること：**
- [ ] Phase 0-5 残り：キャラクター定義エディタ（enemies.js）/ アイテム定義エディタ（items.js）/ タイルバリエーション設計支援（テーマ別パレットプレビュー）⚡。スプライトエディタと同じ自己完結タブ方式で追加可能

### 2026-06-14 — Phase 0-3b 完遂：editor.js（1581行）を8モジュールに分割＋スモークテスト4本追加

**やったこと：**
- **editor Playwright スモークテスト4本を新設**（`tests/editor.spec.js`）：(1) 起動でJSエラーなし＋ワールドグリッド表示、(2) 空セルクリックでステージ作成、(3) ステージ編集ビューへの遷移＋タイルパレット表示、(4) 保存ボタンで有効なJSONがlocalStorageに保存される。4本全グリーンを確認してから分割に着手
- **editor.js（1581行）を8モジュールに分割**：
  - `editor-state.js`（62行）：state オブジェクト・DOM参照・ユーティリティ（getCurrentStage系・stageKey・countTile・findTilePositions）
  - `editor-layers.js`（85行）：レイヤータブ描画・ダンジョンメタパネル・レイヤー追加/削除イベント
  - `editor-world.js`（289行）：ワールドグリッド描画・ミニマップ・ワールドプレビュー・行/列挿入・updateWorldSidePanel
  - `editor-palette.js`（134行）：TILE_SPRITE_MAP・drawSpriteAt・タイルパレット描画（buildTilePalette）
  - `editor-canvas.js`（327行）：ステージキャンバス描画・drawCell・マウスイベント（draw/erase/fill）・フラッドフィル・ツールボタン
  - `editor-props.js`（409行）：右パネル全体（renderSidePanel）・ゲートリンク・剣/防具・宝箱・NPC・ショップ・MAP_ENTER・表示条件・壊せる壁・ドアウェイ
  - `editor-io.js`（248行）：保存（File System Access API + DLフォールバック）・読み込み・プレビュー状態管理（getPreviewPending/setPreviewPending）・openPreview・tryRestoreFromStorage
  - `editor.js`（142行）：エントリ（showView・タブ切替・各モジュールのイベント登録・アニメーションループ・init）
- 各モジュール間の連携は **引数注入（コールバック渡し）+ CustomEvent（editor:resetPreview / editor:showWorld / editor:previewClickAt）** で疎結合化。`editor-world.js` のボタンが `showView` を必要とするケースは CustomEvent で editor.js 側に通知
- **17テストグリーン**（ゲーム側13本＋エディタ4本）。デグレなし

**学び・気づき：**
- editor.js はゲームと異なり「1ファイルにすべてが含まれた素直な構造」だったため、ESModule の read-only binding 問題が起きにくい。factory パターンは不要で、**引数注入（renderWorldGrid を受け取る関数群）** で十分に疎結合にできた
- `_previewPending` のような「複数モジュールをまたぐ状態」は editor-io.js に所有させ、getter/setter（`getPreviewPending` / `setPreviewPending`）で公開するパターンが有効。editor-canvas.js は pending 中の描画ブロックのためにのみ参照するので依存方向が一方向に保たれる
- `showPreviewSettingsDialog` のような「UI操作フロー」は editor-io.js 内部に閉じることで、editor.js から canvas クリック後の処理フローが CustomEvent 経由で整理できた
- editor-props.js が 409 行で目標（400行以内）をわずかに超えたが、右パネルは論理的に1単位（renderSidePanel が全セクションを統括）なので分割しないことが妥当と判断

**▶ 次やること：**
- [ ] Phase 0-4：必要なスプライト・モーション一覧の洗い出し 🧠 Opus（プレイヤーモーション・ダンジョンテーマ別タイル・新ボス敵・新アイテム）
- または
- [ ] Phase 0-5：エディタ機能拡張（スプライトエディタ・キャラ/アイテム定義エディタ）🧠→⚡ ← editor.js 分割が完了したため安全に着手可能


### 2026-06-14 — Phase 0-2b 完遂：player.js / combat.js / boss.js の factory 統合 + 旧本体削除

**やったこと：**
- **3モジュールの factory 統合を実装**：`createPlayer/createCombat/createBoss` を import 済みだが factory 呼び出しが無かった（旧本体が現役）状態を解消。proj/ai の factory ブロック直後に **1ブロックでまとめて createBoss → createCombat → createPlayer を生成**（相互依存：combat→boss の `onBossDefeated`、player→combat,boss の `swordAttack`/`checkTriforceClear`、boss→proj の `showExplosionEffect`）
- deps はすべて getter/wrapper 経由で配線。boss が読み書きする `_bossDefeating` / `_pendingTriforcePieceEl` / `_collectingTriforce` / `pendingTriforcePos` / `bossRoomLocked` / `lastSwordTime` / `lastStonePushTime` は getter/setter で注入。input.js が初期化時に保持する `movePlayer`/`swordAttack`/`useSubItem` は `(dir) => movePlayer(dir)` 等のラッパー化で再代入に追従
- **配線テスト（13 green）→ 旧 function 本体を削除し game.js を再構成**：movePlayer/handleTileEvent/tryPushStone/checkSwitchOff/giveSubItem/gainHeartContainer/spawnDropEffect（player）、swordAttack/showSwordSlashFloat/showEnemySwordSlash/dealDamageToEnemy/killEnemy/takeDamage/showPlayerBlink/showDmgPopupFloat/gameOver（combat）、startDialog/showDialogLine/advanceDialog/togglePause/renderPauseMenu/renderPauseDungeonMap/pauseSelect*/openShop/closeShop/renderShop/shopSelect*/shopBuy/pickDungeonItem（ui — ui.js へ既出）、showBossHpBar/updateBossHpBar/hideBossHpBar/checkBossPhase/onBossDefeated/spawnTriforcePiece/startEnding/buildStaffRollHtml/showBossRoomLockEffect/startBossBattle/checkPendingTriforce/calcTotalTriforces/checkTriforceClear（boss）等を削除
- factory で上書きされる全関数を冒頭で `let xxx = () => {}` 事前宣言（ESM strict mode のホイスティング消失対策）。`isShop` も ui.js getter/setter 用に game.js に残す
- **game.js 2726→1268行（1458行減・元4098行から約 70% 削減）・13テストグリーン**。これで **Phase 0-2 / 0-2b の game.js 分割は全完了**（残りは状態宣言・factory初期化・ループ駆動・init・useSubItem・export のオーケストレーション層）

**学び・気づき：**
- 相互依存する3モジュールは「1つずつ」より **1ブロックでまとめて生成**する方が安全。`const _boss = ...; const _combat = ...; const _player = ...;` の順で生成し、combat の deps に `_boss.onBossDefeated`、player の deps に `_boss.checkTriforceClear` を直接渡せる（生成順を依存の向きに合わせる）
- deps が後方宣言の `let`（`bossRoomLocked` 等）を参照しても、すべて `() => bossRoomLocked` のクロージャ/wrapper 経由なので **factory 生成時には評価されず**、実際の呼び出し時には初期化済みで TDZ にかからない
- 大量の旧本体削除は SEARCH/REPLACE を繰り返すより **write_to_file で再構成**する方が確実（中間に残す関数が挟まると部分削除でコメントブロックが壊れる）

**▶ 次やること：**
- [ ] Phase 0-3b：`editor.js`（1581行）を機能別に分割 🧠→⚡。editor は自動テストが無いため、(1) まず Playwright で editor の最小スモーク（起動・タイル配置・JSON エクスポート）を1〜2本張る → (2) その後 factory/モジュール分割、の順で進めるのが安全


### 2026-06-14 — Phase 0-2b：enemy-ai / projectile の旧本体を game.js から削除


**やったこと：**
- 再開時にまず状況確認：直近コミット `5ae567b`（render 関数削除）時点でコードは健全（**13 passed・git クリーン・構文OK**）。前セッションの中断は token 長等が原因で、壊れた状態はコミットされていなかった
- **enemy-ai の旧本体を削除**（`enemyTick`/`pickApproachMode`/`bossTickHitAndAway`/`enemyAttack`/`fireEnemyProjectile`/`enemyChase`/`checkEnemyContact` ＋ヘルパー、約560行）。factory（`_ai.*`）で上書き済みの dead code
- **projectile の旧本体を削除**（`projectiles`/`nextProjId`/`projectileTick`/`boomerangStep`/`checkProjHit`/`isShieldBlocking(Dir)`/`showShieldBlockEffect`/`isInBounds`/`isTilePassableForProj`/`createProjEl`/`moveProjEl`/`removeProjEl`/`clearProjectiles`/`placedBombs`/`clearBombs`/`placeBomb`/`bombTick`/`explodeBomb`/`showExplosionEffect`、約360行）。factory（`_proj.*`）で上書き済みの dead code
- **game.js 3631→2726行（905行減）・13テストグリーン**

**学び・気づき（重要）：**
- **ESM strict mode の落とし穴**：factory 代入ブロックの `enemyTick = _ai.enemyTick` 等は、削除対象の `function enemyTick(){}` の**ホイスティングによる暗黙の宣言**に依存していた。旧 `function` 本体を削除すると代入先が未宣言になり **ReferenceError で起動失敗**（全テストがタイムアウト）。最初に enemy 系を削除したらこれで全滅した
- **正しい削除パターン（PROGRESS 既出の手順を厳守）**：① factory 代入先の関数を `let xxx = () => {};` で**事前宣言** → ② 旧 `function` 本体を削除 → ③ テスト。render/input/ui 系で確立済みだった手順を、今回 enemy/projectile にも適用して解決
- 削除前に「他所からの参照が旧本体内部に閉じているか」を `grep` で必ず確認した（`fireEnemyProjectile` は projectile/enemy 両方に定義があるが factory 版に一本化済み）

**▶ 次やること：**
- [ ] Phase 0-2b 続き：**player.js / combat.js / boss.js の factory 統合 + 旧本体削除**。⚠️ これら3つは enemy/projectile と違い **factory 統合自体が未完**（`createPlayer/createCombat/createBoss` を import しているが `_player./_combat./_boss.` の呼び出しが無く、game.js の旧本体が現役）。単純削除ではなく「deps 配線 → 代入ブロック追加 → テスト → 旧本体削除 → 再テスト」を **1モジュールずつ** 行う必要がある（相互依存：combat の dealDamageToEnemy/takeDamage を enemy-ai/projectile factory が deps として参照している点に注意）


### 2026-06-14 — Phase 0-3b：game.css を11ファイルに分割（@import エントリ化）


**やったこと：**
- **`game.css`（1661行）を `@import` のみのエントリファイルに書き換え**、機能別に `game/css/` 配下へ分割：
  - `base.css`：リセット・CSS変数（`:root`）・html/body
  - `hud.css`：HUD（ハート・装備・サブアイテム・ウォレット）
  - `board.css`：ボード・セル・char-layer・スプライトサイズ・ドアウェイ背景
  - `effects.css`：剣スラッシュ/突き・ダメージ・点滅・爆発・盾ブロック・ドア・魔王オーラ・家ドア・stone-glow・`.hidden`
  - `overlays.css`：メッセージバー・NPCダイアログ・ポーズ・ゲームオーバー・タイトル/確認
  - `mobile.css`：モバイルコントローラー（D-PAD・アクションボタン）
  - `responsive.css`：`@media (min-width: 600px)` PC向け
  - `shop.css`：ショップ
  - `boss.css`：ボスHPバー・ダンジョンHUD中央・暗転オーバーレイ
  - `ending.css`：エンディング（スタッフロール・THE END）
  - `tiles.css`：bgTile／フィールドタイル背景色
- **カスケード順を厳密に保持**：`#boss-hpbar { bottom: 148px }` は元ソースで `@media(PC){#boss-hpbar{bottom:20px}}` より**後**に定義され後勝ちになる。これを再現するため @import 順を `responsive.css` → `boss.css` の順に配置。`#hud`/`#msg-bar` 等は @media より前なので hud/overlays を responsive より前に置いて元挙動を維持
- `index.html` は無改修（`<link rel="stylesheet" href="game.css">` のまま）
- `npx playwright test` → **13 passed**（VRT の `game-start.png` が一致＝見た目の差分ゼロ）

**学び・気づき：**
- CSS 分割で最も注意すべきは**カスケード順（同詳細度のルールの後勝ち）**。特に `@media` と通常ルールの定義順が逆転すると PC 表示で差が出る。元ソースの行順を @import 順にそのまま写すのが最も安全
- `@import` は CSS の先頭にまとめて書く必要がある（他ルールより前）。エントリを @import 専用にすればこの制約を自然に満たせる
- VRT（`toHaveScreenshot`）が「見た目が1px も変わっていない」ことを保証してくれるので、CSS の機械的分割では特に心強い安全網になった

**▶ 次やること：**
- [ ] Phase 0-3b：`editor.js`（1581行）を機能別に分割 🧠→⚡。ただし editor は自動テストが無くデグレ検知できないため、(1) まず Playwright で editor の最小スモーク（起動・タイル配置・JSON エクスポート）を1〜2本張る → (2) その後 factory/モジュール分割、の順で進めるのが安全


### 2026-06-14 — Phase 0-3：sprites.js を4サブファイル＋aggregatorに分割

**やったこと：**
- **`shared/sprites-player.js` を新設**：hero（4方向2フレーム）/ escape / monster / darklord / princess / npcA / npcB / npcShop の各スプライトデータと対応パレット（hero/escape/monster/darklord/princess/guard/npcA/npcB）を収録（~340行）
- **`shared/sprites-enemies.js` を新設**：patrol（巡回兵）/ chaser（追跡者）/ sentry（騎士）の各スプライトデータと対応パレットを収録（~120行）
- **`shared/sprites-items.js` を新設**：sword/shield（+side/back）/boomerang/arrow/spear（+spearV）/stone/key/chest/rupee/triforce/heart（+empty/half）の各スプライトデータと対応パレットを収録（~250行）
- **`shared/sprites-tiles.js` を新設**：block/door（+doorOpen）/swG/gateG（+gateGopen）/water/breakableWall/mapEnter/doorway/doorwayBoss/doorwayLocked ＋ フィールドタイル全種（grass/sand/stoneFloor/bridge/tree/mountain/bush/fence/houseWall/houseDoor/houseRoof/sign）の各スプライトデータと対応パレットを収録（~400行）
- **`shared/sprites.js` を aggregator に書き換え**：4サブファイルから `PLAYER_PAL`/`PLAYER_SPRITES` 等をインポートし、`PAL = { ...PLAYER_PAL, ...ENEMY_PAL, ...ITEM_PAL, ...TILE_PAL }` / `SPRITES = { ...PLAYER_SPRITES, ... }` でマージして再エクスポート。animation/drawing 関数（`startAnimLoop` / `stopAnimLoop` / `drawSprite` / `makeSprite` / `drawSpriteFrame` / `redrawAnimSprites`）はそのまま残す（~100行）
- `node scripts/check-errors.mjs` → エラーなし
- `npx playwright test` → **13 passed**（デグレなし）
- PLAN.md（0-3 完了）・PROGRESS.md・進捗サマリを更新

**学び・気づき：**
- `sprites-ui.js` は当初 PLAN.md に記載していたが、HUD要素（heart/heartHalf/heartEmpty）はアイテム的な性格、doorway等は tiles 的な性格のため、それぞれ items/tiles に自然に収まった。分割方針は「UI要素か否か」より「何を表すオブジェクトか」で判断した方が良い
- aggregator で `...PLAYER_SPRITES` をスプレッド構文でマージする設計は、外部の import 側（game.js / render-board.js 等）の変更を一切不要にする。エントリポイントが sprites.js → PAL/SPRITES を使う全ファイルへの影響ゼロ
- 各サブファイルは独立した名前空間（`PLAYER_PAL` / `ENEMY_PAL` 等）を持つため、将来さらに細分化する際も aggregator だけ変更すればよい

**▶ 次やること：**
- [ ] Phase 0-3b：`editor.js`（1581行）を機能別に分割 / `game.css`（1660行）を機能別に分割 🧠→⚡


### 2026-06-14 — バグ修正：弓矢が特定距離の敵に当たらない（トンネリング）

**やったこと：**
- **原因特定**：`projectileTick` では 1tick に `speed × MOVE_STEP` だけ一気に移動してから `checkProjHit` で1点判定していた。弓矢は speed=4.5（通常）・9.0（二周目）のため 1tick あたり 2.25〜4.5 セル移動し、敵のヒットボックス（0.6セル）を飛び越えて当たり判定が抜ける「トンネリング」が発生していた
- **修正**：`projectileTick` の直線飛翔ブランチに**区間補間チェック**を実装。1tick の移動量を `HIT_RADIUS×0.8 = 0.4` セル以下の小ステップに分割し、各サブステップで境界・壁・当たり判定を順番にチェックする。ヒットした時点で即 `break` して残移動はスキップ（速度感への影響なし）
  ```
  const numSubs = Math.ceil(step / 0.4);  // 弓矢なら 2.25/0.4 ≒ 6 分割
  for (let i = 0; i < numSubs; i++) {
      proj.x += dx; proj.y += dy;
      // 境界・壁・当たり判定 → ヒットで break
  }
  ```
- `node scripts/check-errors.mjs` → エラーなし
- `npx playwright test` → **12 passed**（デグレなし）

**学び・気づき：**
- 高速投擲物のトンネリングは「当たらない距離がある」という形で現れる。敵が何マス離れていても当たるべきだが、特定距離（ちょうど矢が飛び越えるセル）だけ抜けるのが特徴
- `checkProjHit` の判定閾値（0.6セル）はそのままで、移動ステップを小さくするのが最も安全な修正（判定ロジック自体を変えない）
- ブーメランは `boomerangStep` で別処理しているため影響なし。敵の投擲物（spear/stone）も速度が低めなため実害は少なかったが、同じ修正で恩恵を受ける

**▶ 次やること：**
- [ ] Phase 0-3：`sprites.js`（1440行）を機能別に分割（`sprites-player.js` / `sprites-enemies.js` / `sprites-items.js` / `sprites-tiles.js` / `sprites-ui.js`）⚡


### 2026-06-14 — Phase 0-2 Step 6：main.js エントリポイント整理（Phase 0-2 全完了）

**やったこと：**
- **`game/main.js` を新設**：`init()` 呼び出し・`window.__game` テストフック・`startAnimLoop(() => { redrawAnimSprites(); })` 呼び出し・`window.addEventListener('resize', ...)` を切り出し。`window._debugEnding` もここに集約
- **game.js に `export` を追加**：`init` / `updateBoardScale` / `step` / `movePlayer` / `swordAttack` / `useSubItem` / `getProjectiles` / `startEnding` と、テスト用ヘルパー `getGameState()` / `getInputModule()` をエクスポート。`startAnimLoop` / `redrawAnimSprites` は sprites.js から再 export
- **game.js から旧エントリポイントコードを削除**：`init().catch(...)` / `window.addEventListener('resize')` / `window._debugEnding` / `window.__game` ブロック・`startAnimLoop()` 呼び出しを game.js 末尾から削除
- **`game/index.html` の `script` タグを変更**：`game.js?v=3` → `main.js`
- `node scripts/check-errors.mjs` → ページエラーなし・consoleエラーなし
- `npx playwright test` で **12 passed**（デグレなし）
- PLAN.md（Step 6 完了）・PROGRESS.md・進捗サマリを更新

**学び・気づき：**
- game.js が ESModule の `export` を持つモジュールになったことで、`import` 側（main.js）から必要な関数だけを参照できる設計が整った
- `startAnimLoop` / `redrawAnimSprites` は sprites.js に属する関数なので、`export { startAnimLoop, redrawAnimSprites } from '../shared/sprites.js'` で game.js を経由して再 export する形にした（main.js から sprites.js を直接 import しても良いが、game.js の責務範囲内に留めた）
- `window.__game` の `queueInput` / `releaseInput` は `getInputModule()` を通じて `_inputModule.heldKeys` にアクセスする設計に変更（旧 game.js 内のクロージャ変数直接参照から切り離し）

**▶ 次やること：**
- [ ] Phase 0-3：`sprites.js`（1440行）を機能別に分割（`sprites-player.js` / `sprites-enemies.js` / `sprites-items.js` / `sprites-tiles.js` / `sprites-ui.js`）⚡


### 2026-06-14 — 投擲物テスト追加（projectile.spec.js）

**やったこと：**
- **PLAN.md に「④ 投擲物テスト」を追記**：弓矢・ブーメランの飛翔確認テストは PLAN.md の 0-0 テスト基盤に位置づけた
- **`window.__game` に `useSubItem()` と `getProjectiles()` を追加**：テストからサブアイテム使用・投擲物状態取得ができるように公開
- **`tests/projectile.spec.js` を新設**（2テスト）：
  - 条件1：弓矢を使用後、arrow が factory の投擲物リストに存在し dx > 0（右方向）であること
  - 条件2：ブーメランを使用後、boomerang が factory の投擲物リストに存在し、3フレーム後に初期位置から前進していること
- `npx playwright test` で **12 passed**（既存10＋新規2、デグレなし）

**学び・気づき：**
- seed があるとタイトルダイアログが表示されるため、`waitForBoard` ではなく `#btn-continue` をクリックしてからボード待機が必要（movement.spec.js と同じパターン）
- Infinity は `JSON.stringify` でシリアライズできない → ブーメランの count は 99 を使い、useSubItem の `si.count !== Infinity` チェックを回避
- `getProjectiles()` のスナップショット実装で `returning: p.returning ?? false` としたことでデシリアライズ後も boolean が正しく返る

**▶ 次やること：**
- [ ] Phase 0-2 Step 6：`main.js` への `init()` とゲームループの整理（エントリポイントの切り出し）⚡


### 2026-06-14 — バグ修正：弓矢・ブーメランが動かない不具合

**やったこと：**
- **原因特定**：`useSubItem` の中で `projectiles.push(proj)` / `createProjEl(proj)` / `nextProjId++` を直接参照していた。これらは `projectile.js` factory の内部変数になっており game.js のローカル変数は使われないため、投擲物が実際には追加されず動かなかった
- **修正内容**：
  - factory ブロックに `addProjectile = (config) => _proj.addProjectile(config)` / `getProjectiles = () => _proj.getProjectiles()` を追加して公開
  - `useSubItem` の boomerang / bow 発射処理を `addProjectile(config)` 呼び出しに変更（id 付与は factory 内部で行われる）
  - ブーメラン飛翔中チェックを `projectiles.some(...)` → `getProjectiles().some(...)` に変更
  - `addProjectile` / `getProjectiles` の `let` 事前宣言を factory 上書きの前（`updateShieldHud` / `processHeldKeys` と同じ場所）に追加（TDZ 回避）
- `node scripts/check-errors.mjs` → エラーなし確認
- `npx playwright test` で **10 passed**（デグレなし）

**学び・気づき：**
- projectile.js factory は `_projectiles` / `_nextProjId` / `createProjEl` を**内部に閉じ込めて**いるため、game.js の `let projectiles = []` / `nextProjId` は使われない空の変数になっていた。useSubItem が旧変数に push しても factory の tickループから見えないため投擲物が「存在しないまま」になっていた
- factory API の `addProjectile(config)` に id を含めない設定オブジェクトを渡せば factory 側で `id: _nextProjId++` を付与してくれる設計になっていた（正しい使い方）
- factory 公開後も旧インライン実装（`projectiles = []` / `createProjEl`）が game.js に残っているが、`clearProjectiles` / `projectileTick` は factory 版で上書き済みなので今回の修正で整合がとれた

**▶ 次やること：**
- [ ] Phase 0-2 Step 6：`main.js` への `init()` とゲームループの整理（エントリポイントの切り出し）⚡


### 2026-06-14 — Phase 0-2 Step 5（前半）：projectile.js / enemy-ai.js 切り出し

**やったこと：**
- **`game/projectile.js` を新設**（`createProjectile(deps)` factory）：投擲物・爆弾・盾ブロック判定を game.js から切り出し。内部状態（`_projectiles` / `_placedBombs` / `_nextProjId`）をこのモジュールが所有。公開 API: `projectileTick` / `clearProjectiles` / `addProjectile` / `fireEnemyProjectile` / `isShieldBlocking` / `isShieldBlockingDir` / `showShieldBlockEffect` / `clearBombs` / `placeBomb` / `bombTick` / `showExplosionEffect`
- **`game/enemy-ai.js` を新設**（`createEnemyAi(deps)` factory）：`enemyTick` / `enemyChase` / `bossTickHitAndAway` / `enemyAttack` / `checkEnemyContact` を切り出し。`_proj.fireEnemyProjectile` / `_proj.isShieldBlockingDir` / `_proj.showShieldBlockEffect` を deps として渡すことで循環参照フリー設計
- game.js に `createProjectile` / `createEnemyAi` の import を追加し、factory 初期化ブロック `{ const _proj = createProjectile(...); const _ai = createEnemyAi(...); projectileTick = _proj.projectileTick; ... }` を追加
- `node scripts/check-errors.mjs` → エラーなし確認
- `npx playwright test` で **10 passed**（VRT 含む全テストグリーン＝デグレなし）
- PLAN.md（Step 5 部分完了）・PROGRESS.md・進捗サマリを更新

**学び・気づき：**
- projectile.js が盾ブロック判定（`isShieldBlockingDir`）を所有し、enemy-ai.js がそれを deps として受け取る設計にしたことで、敵の投擲物・近接攻撃ともに同じ判定ロジックを使える循環参照フリーな構造を作れた
- factory ブロックで `_proj` → `_ai` の順に初期化することで、`_proj.fireEnemyProjectile` を `_ai` の deps に渡せる（依存の向きが一方向で明快）
- game.js にはまだ旧インライン実装が残っているが、factory が上書きするため動作は factory 版で統一されている（旧実装は使われない）

**▶ 次やること：**
- [ ] Phase 0-2 Step 5 続き：`player.js`（`movePlayer` / `handleTileEvent`）、`combat.js`（`swordAttack` / `dealDamageToEnemy` / `takeDamage`）、`boss.js`（`onBossDefeated` / `startBossBattle` / `startEnding`）を切り出す ⚡


### 2026-06-14 — Phase 0-2 Step 4：input.js / ui.js 切り出し

**やったこと：**
- **`game/input.js` を新設**（`initInput(deps)` factory）：キーボード・モバイル・スワイプ入力を game.js から切り出し。`heldKeys`（Set）と `processHeldKeys()` を返す。deps は `getIsDialog`/`getIsShop`/`getIsPaused`/`getIsShielding`/`setIsShielding`/`movePlayer`/`swordAttack`/`useSubItem`/`togglePause`/`toggleDebugMode`/`advanceDialog`/`closeShop`/`shopSelectPrev`/`shopSelectNext`/`shopBuy`/`pauseSelectPrev`/`pauseSelectNext`/`hasCleared`/`updateShieldHud`。`resumeAudio` / `MOVE_STEP` は直接 import
- **`game/ui.js` を新設**（`createUi(deps)` factory）：HUD・ポーズ・ダイアログ・ショップを game.js から切り出し。deps に状態フラグの getter/setter を注入して read-only binding 問題を回避
- game.js に `initInput` / `createUi` の import を追加し、factory 呼び出しブロック（`{ const _ui = createUi(...); const _in = initInput(...); ... }`）を追加
- `updateShieldHud` と `processHeldKeys` は factory より前に使われるため `let` 宣言を事前追加（TDZ 回避）
- **旧インラインコードを全削除**：キーボードリスナー（`document.addEventListener('keydown'...)`）・`processHeldKeys` 本体・モバイルボタン（`btn-sword`/`btn-sub`/`btn-menu`/`btn-shield`）・スワイプ・`updateShieldHud` 関数・2つ目の `let _inputModule` 宣言・`const heldKeys` 宣言・`let _activeHeldKeys` 宣言 → 計100行超を削除
- `node scripts/check-errors.mjs` → エラーなし確認
- `npx playwright test` で **10 passed**（VRT 含む全テストグリーン＝デグレなし）
- PLAN.md（Step 4 完了）・PROGRESS.md・進捗サマリを更新

**学び・気づき：**
- token 長エラーで中断した際、前セッションで input.js / ui.js の**ファイル自体は作成済み**だったが、game.js への統合ブロック挿入と旧コード削除が未完または重複していた。再開時は `check-errors.mjs` でページエラーを確認することが最初のステップとして有効
- `Identifier '_inputModule' has already been declared` エラー：新統合ブロックで `let _inputModule = null` が上部にあり、旧インラインコードにも同名宣言が残っていた。旧コードを丸ごと削除することで解決
- `updateShieldHud is not defined` エラー：旧インライン `function updateShieldHud()` を削除したが、factory 上書き前に参照箇所があったため。`let updateShieldHud = () => {};` の事前宣言で解決
- factory 生成関数を game.js の既存 `function` 宣言より先に呼び出す場合、`function` 宣言はホイスティングされるが `let` は TDZ があるため事前宣言が必要

**▶ 次やること：**
- [ ] Phase 0-2 Step 5：ゲームロジック系の分離（`player.js`: movePlayer/handleTileEvent、`combat.js`: swordAttack/dealDamageToEnemy/takeDamage、`projectile.js`: 投擲物・爆弾、`enemy-ai.js`: enemyTick/bossTickHitAndAway、`boss.js`: onBossDefeated/startBossBattle/startEnding）⚡。これらも factory + getter 注入方式を継続


### 2026-06-14 — Phase 0-2 Step 3：render-board.js / render-chars.js 切り出し

**やったこと：**
- **`game/render-board.js` を新設**（`createRenderBoard(deps)` factory）：`renderBoard()` / `setCellClass()` / `addCellSprite()` / `applyBgTileClass()` を切り出し。deps は `getStageData`/`getCurrentLayer`/`getStageKey`/`getSS`/`getBoardEl`/`getStageLabelEl`/`charLayerElRef`/`getDoorwayState`。shared モジュール（`TILE`/`SPRITES`/`PAL`/`NPC_SPRITE_MAP`/`drawSpriteFrame`/`makeSprite`）は直接 import
- **`game/render-chars.js` を新設**（`createRenderChars(deps)` factory）：`renderChars()` / `addCharEl()` / `moveCharEl()` / `removeCharEl()` / `addShieldOverlay()` / `updatePlayerCharEl()` を切り出し。石描画ヘルパー（`makeStoneCanvas` / `addStoneGlow`）も内部関数として実装
- **`charLayerElRef = { value: null }` ラッパーを game.js に追加**：render-board が `renderBoard()` 実行時に `.value` へ新しい charLayerEl を書き込み、render-chars は `.value` を読む。DOM 参照を2モジュール間で安全に共有する設計
- game.js に `createRenderBoard` / `createRenderChars` の import を追加し、`getDoorwayState` 定義後に factory 呼び出しブロックを追加。`renderBoard = () => { _rb.renderBoard(); charLayerEl = charLayerElRef.value; }` の形で旧実装を上書き（`charLayerEl` はアニメーション系コードが直接参照するため game.js 内変数も同期）
- `npx playwright test` で **10 passed**（VRT 含む全テストグリーン＝デグレなし）
- PLAN.md（Step 3 完了・方針を明記）・PROGRESS.md・進捗サマリを更新

**学び・気づき：**
- `charLayerEl` は renderBoard 後に直接 `charLayerEl.appendChild(...)` する箇所（石押しアニメーション・投擲物・剣エフェクト等）が多数あるため、`charLayerElRef.value` で共有しつつ `charLayerEl` も同期させる2重管理が必要だった
- `addShieldOverlay` / `updatePlayerCharEl` は render-chars 側に入れることで、描画系の依存（makeSprite / getCellPx / getHeroDir）が 1 ファイルに集中し見通しが改善
- factory ブロック `{ ... }` を block scope にしたことで `_rb` / `_rc` が外部に漏れず、game.js のグローバルスコープを汚染しない

**▶ 次やること：**
- [ ] Phase 0-2 Step 4：入力・UI系の分離（`input.js`: キーボード・タッチ・スワイプ、`ui.js`: HUD・ポーズ・ダイアログ・ショップ）⚡。これらも大量の状態参照があるため getter 注入方式を継続


### 2026-06-14 — Phase 0-2 Step 2：passable.js / conditions.js 切り出し

**やったこと：**
- 設計判断（Opus）：対象5関数（`isPassable`/`tilePassable`/`isPassableForEnemy`/`checkStoneOnSwitch`/`evaluateConditions`）は `stageData`/`enemies`/`player`/`currentLayer`/`stageKey`/`debugMode` という**再代入される `let` 状態**を多数参照し、呼び出し箇所も多い。save.js のような「全状態を引数で渡す純粋関数化」は呼び出し側の全面改修が必要でリスク大と判断
- **factory + 状態 getter 注入方式**を採用：
  - `game/passable.js` 新設 → `createPassable(deps)` が `{ isPassable, tilePassable, isPassableForEnemy }` を返す
  - `game/conditions.js` 新設 → `createConditions(deps)` が `{ checkStoneOnSwitch, evaluateConditions }` を返す
  - `deps` は `getStageData`/`getEnemies`/`getPlayer`/`getCurrentLayer`/`getStageKey`/`getDebugMode`/`getSS`/`toTileRow`/`toTileCol`（+ conditions は `renderBoard`/`renderChars`）。getter 経由で常に最新の状態を読むので、状態が後で再代入されても正しく動く
- game.js は起動時に一度 `createPassable(...)` / `createConditions(...)` を呼んで分割代入で関数を取得。**呼び出し側のコードは一切変更不要**（関数名そのまま）
- game.js から旧5関数の本体を削除（コメントで「切り出し済み」と明記）
- `npx playwright test` で **10 passed**（石押し・条件評価の往復がグリーン＝注入が正しく機能）
- PLAN.md（Step 2 完了・方針を明記）・PROGRESS.md・DECISIONS.md を更新

**学び・気づき：**
- read-only binding 問題への対処は2パターンある：(A) save.js のように「全状態を引数で渡す純粋関数」、(B) 今回のように「状態 getter を注入する factory」。状態参照が多く呼び出し側改修を避けたい場合は (B) が低リスク。`() => stageData` のクロージャは最新値を返すので再代入に追従できる
- shared モジュール（`TILE`/`NPC_SPRITE_MAP`/`playSound`）は各分割ファイルで直接 import すればよく、注入対象は「game.js スコープの可変状態と相互依存関数」だけに絞れた

**▶ 次やること：**
- [ ] Phase 0-2 Step 3：描画系の分離（`render-board.js`: renderBoard/setCellClass/addCellSprite、`render-chars.js`: renderChars/addCharEl/moveCharEl）⚡。これらも factory + getter 注入方式が使えるが、DOM 要素参照（boardEl/charLayerEl）・makeSprite 等の依存が多いので注入対象の整理が必要


### 2026-06-14 — Phase 0-2 Step 1b：save.js（セーブ/ロードの純粋関数）切り出し


**やったこと：**
- 当初の Step 1b 想定（状態コンテナ `state.js` への全面移行）を再検討。`player` 等の参照が数百箇所に及び一括置換のデグレリスクが過大なため、**より安全な形に再定義**：状態の再代入は game.js に残し、セーブ/ロードの**純粋変換ロジックだけ**を `save.js` に切り出す
- **`game/save.js` を新設**：`createStageState()`（新規ステージ状態の初期値生成）/ `serializeStageState()`（Set→配列）/ `deserializeStageState()`（配列→Set・stonePositions は常にリセット）/ `sanitizeLoadedPlayer()`（旧セーブの passive アイテム混入を除去）の4純粋関数
- game.js の `getSS`/`saveGame`/`loadGame` をこれら利用に簡約（localStorage の read/write と状態再代入のみ game.js に残す）
- `npx playwright test` で **10 passed**（特にセーブ/ロード復元テストがグリーン＝シリアライズ往復が正しい）
- PLAN.md（Step 1b 完了・方針変更を明記）・PROGRESS.md・DECISIONS.md を更新

**学び・気づき：**
- ESModule の read-only binding 問題は「純粋関数として切り出し、状態の所有権は元モジュールに残す」ことで綺麗に回避できた。`save.js` は localStorage にも player にも直接触れず、引数→戻り値だけで完結する（テスタブル）
- セーブ/ロード往復の正しさは movement.spec.js の「続きから復元」テストが実質カバーしている

**▶ 次やること：**
- [ ] Phase 0-2 Step 2：判定・ロジック系（`passable.js`: isPassable/tilePassable/isPassableForEnemy、`conditions.js`: evaluateConditions/checkStoneOnSwitch）の切り出し ⚡。これらは getSS/stageData/enemies/player 等の状態を参照するため、引数で渡す純粋関数化 or アクセサ注入を検討


### 2026-06-14 — Phase 0-2 Step 1a：定数を constants.js へ切り出し

**やったこと：**
- 設計方針を確認：`player`/`stageKey`/`currentLayer`/`mapData` 等は `let` で再代入されるため、ESModule の read-only binding 問題が分割の核心。PLAN の Step 1 を **段階方式（B）** に分解（1a=定数のみ、1b=状態コンテナ化）し、まず低リスクな 1a を実施
- **`game/constants.js` を新設**：再代入されない純粋定数11個を切り出し（MOVE_STEP / TICK_MS / INVINCIBLE_MS / HP_PER_HEART / MAP_JSON_URL / SAVE_KEY / CLEARED_KEY / DIR_DELTA / SWORD_REACH / SWORD_COOLDOWN_MS / STONE_PUSH_COOLDOWN_MS）
- game.js を `import { ... } from './constants.js'` に変更し、インラインの `const` 宣言と重複していた `CLEARED_KEY` を削除（`lastSwordTime`/`lastStonePushTime` の `let` は game.js に残す）
- `npx playwright test` で **10 passed**（デグレなし）
- PLAN.md（Step 1 を 1a/1b に分割・1a完了）・PROGRESS.md・DECISIONS.md を更新

**学び・気づき：**
- DOM参照（`const boardEl = document.getElementById(...)`）・描画密接な `BG_TILE_COLOR_CLASS`・`let` 状態は今回の対象外とし、純粋な値だけを移すことで副作用ゼロ・低リスクに保てた
- ESModule の named import は read-only binding なので、定数（再代入なし）は安全に移せるが、状態（再代入あり）は単一オブジェクト集約（状態コンテナ）が必要 → 1b で実施

**▶ 次やること：**
- [ ] Phase 0-2 Step 1b：可変状態を状態コンテナ方式（`state.js` に `S = {...}`）へ移行し、`save.js`（saveGame/loadGame/getSS）も切り出す 🧠→⚡


### 2026-06-13 — Phase 0-1：決定論的ゲームループ（gameTime / step / __game）

**やったこと：**
- **論理時間 `gameTime` + `gameNow()` を導入**。game.js 内の `Date.now()` 全10箇所（剣CD・無敵・石押しCD・マップ遷移CD・ボスAI `_haTimer`・敵攻撃 `_attackTimes`・爆弾 `fuseEnd`/`bombTick`）を `gameNow()` に置換
- **`gameTick` → `step(frames)` に分離**：driver は `setInterval(() => step(1), TICK_MS)`。`step()` が 1 フレームごとに `gameTime += TICK_MS` してから `gameTick()` を呼ぶ。凍結状態（pause/dialog/gameover/transition）では加算もしない
- **`window.__game` テストフックを公開**：`step(n)` / `queueInput(dir)` / `releaseInput(dir)` / `movePlayer(dir)` / `swordAttack()` / `getState()`
- `tests/deterministic.spec.js` を追加（3テスト）：フック公開確認・`step(10)` で gameTime が ≥1200 進む・`queueInput`+`step` で決定論的に移動
- `npx playwright test` で **8 passed**（既存5＋決定論3、デグレなし）
- DECISIONS.md に実装方針を記録、PLAN.md の 0-1 チェックを完了に

**学び・気づき：**
- 視覚演出（要素削除・点滅・撃破演出）は `setTimeout`/`setInterval` 側で `Date.now()` を使っていなかったため、`Date.now()→gameNow()` の全置換でゲームロジックだけを論理時間化でき、演出は実時間のまま温存できた（PLAN の意図どおり）
- `gameTick` の関数名は互換のため残し、`step()` から呼ぶ構成にしたことで既存の `startGameLoop`/`stopGameLoop` 呼び出し（多数）を変更不要にできた
- replace_in_file で重複ブロックを挿入してしまうミスが発生 → 大きいファイルの編集は SEARCH を一意にし、適用後に必ず重複チェックすること

**石押し検証（追記）：**
- 石押し不具合の報告を受け調査。結論：**game.js のコードは develop と一致しており正常**。実機で見えた不具合は**ブラウザの古いキャッシュ**が原因で、ハードリロードで develop と同じスムーズ動作を確認
- 反省：(1) 最初の石押しテストはユーザー指定の2条件（石サイズ不変・連続押下）を検証しておらず「緑でも無意味」だった、(2) diff の行範囲をずらして「アニメ4行欠落」と誤診断、(3) 「実機で不成立＝コード原因」と思い込んだ（真因はキャッシュ）。詳細は DECISIONS.md に記録
- `tests/stone-push.spec.js` を**2条件を正しく検証する形に作り直し**：石 canvas の実描画サイズを実測（不変確認）／押しっぱなし＋実ループで連続押下（2セル以上前進）を検証。`scripts/find-stone.mjs` を追加（石のあるステージ探索）
- 全テスト **10 passed**

**▶ 次やること：**
- [ ] Phase 0-2：game.js モジュール分割 Step 1（state.js / save.js の切り出し）から着手 🧠→⚡
- ※ 動作確認時はブラウザのハードリロード（キャッシュ無効）を徹底する


### 2026-06-13 — Phase 0-0：ステージ遷移スモーク＋VRT 追加（Phase 0-0 完了）

**やったこと：**
- `tests/transition.spec.js` を追加：
  - field "1,0"（cols=12）の右端付近（x=10, y=5）に seed → ArrowRight 押し続け → `#hud-stage-label` が "2,0" に変わることを確認
  - **1 passed**
- `tests/vrt.spec.js` を追加：
  - 起動直後の画面を `toHaveScreenshot('game-start.png')` で保存（基準画像を自動生成）
  - `maxDiffPixelRatio: 0.01` でピクセル誤差を許容
  - **1 passed**（初回 `--update-snapshots` で基準画像生成 → 通常実行でもパス）
- `scripts/check-stages.mjs` を追加（スクリプトでマップ構造を確認するためのヘルパー。一時作業用）
- `npx playwright test` で **5 passed**（起動・移動・セーブ/ロード・ステージ遷移・VRT）
- PLAN.md・PROGRESS.md の進捗を更新
- `.gitignore` の不要な追記（`tests/**-snapshots/` の除外）を取り消し → VRT 基準画像はリポジトリ管理が正しい運用

**学び・気づき：**
- ステージ遷移の検証は `#hud-stage-label`（`[field] stageKey` 形式）のテキスト変化で確実に確認できる
- `expect(labelEl).toContainText('2,0', { timeout: 3000 })` の形で非同期遷移を自然に待てる
- `--update-snapshots` は初回生成専用。以降は通常実行で差分検出が機能する
- 複数行シェルスクリプトは `.clinerules/no-multiline-shell.md` 違反のため、必ずスクリプトファイルを書き出してから実行すること（今回違反してしまったので注意）

**▶ 次やること：**
- [ ] Phase 0-1：決定論的ゲームループ（`gameTick` を `step()` に分離・`Date.now()` を `gameTime` に置換・入力キュー化）🧠 Opus 推奨

---

### 2026-06-13 — Phase 0-0：移動・セーブ/ロードのスモーク追加

**やったこと：**
- `tests/helpers.js` を新設（`gotoFreshGame` / `waitForBoard` / `getPlayerPixelPos` / `readSave` / `holdKey`）。入力→待機→観測を共通化
- `tests/smoke.spec.js` をヘルパー利用に整理（`#char-player` 表示も確認）
- `tests/movement.spec.js` を追加：
  - 移動スモーク：4方向を順に試し「いずれかで座標変化」を確認（開始位置の壁に依存しないロバスト設計）
  - セーブ/ロード：有効なセーブを seed → タイトル「続きから」→ player座標が復元されることを確認
- `npx playwright test` で **3 passed**
- **WORKFLOW.md にコミットメッセージ作成手順を追記**（Step 5 で案を提示／git status から始める／英語・prefix付き・簡潔／コミット操作はしない）

**学び・気づき：**
- このゲームは**単純な移動だけではセーブしない**（アイテム取得・ステージ遷移・ボス撃破等のイベント時のみ `saveGame()`）。当初「移動→自動セーブ」を前提にしたら失敗した → セーブ/ロードは「ロード復元経路」を検証する形に変更
- タイトルで「続きから」が出る条件は `init()` が `hasSave && stageKey` を満たすこと。seed には `currentLayer`/`stageKey`（startPos の field/"1,0"）が必須だった
- 開始ステージは `work/blade-of-lumia.json` の `startPos`（field, stage "1,0", row2,col2）

**▶ 次やること：**
- [ ] Phase 0-0：ステージ遷移のスモーク（端への移動→別ステージへ切替）を追加
- [ ] 主要画面の VRT（`toHaveScreenshot`）を1枚追加 → その後 0-1/0-2 へ


### 2026-06-13 — Phase 0-0：テスト基盤導入（最小スモーク）

**やったこと：**
- Playwright（`@playwright/test`）を `blade-of-lumia/` 配下に導入（`package.json` / `npm install` / `npx playwright install chromium`）
- `playwright.config.js` を作成：webServer に **Vite**（`npx vite ../ --port 18080`）を指定し、`outputs/` をルートに `/blade-of-lumia/game/` を検証
- `tests/smoke.spec.js`：最小スモークを作成 →「ページがエラーなく起動し、`#board` にセルが描画される」を検証（`pageerror`/`console.error` 監視）
- `.gitignore` を追加（node_modules・test-results・playwright-report 等を除外）
- `npx playwright test` で **1 passed** を確認
- **フィードバック反映**：当初 `python3 -m http.server` を使っていたが、利用技術を Node 系に統一するため Vite に変更（普段の手動確認 `npx vite outputs --port 18080` と同一構成）。`reuseExistingServer` で手動起動中の dev サーバを再利用可能。`npm run dev` も追加

**学び・気づき：**
- このゲームは ESModule + `fetch('../work/...json')` 依存のため `file://` では動かない → HTTPサーバ必須。普段の確認と同じ Vite に揃えると認知負荷が低く、サーバ再利用も効く
- サーバルートが `outputs/` のため、テストURLは `/game/` ではなく `/blade-of-lumia/game/`（`../shared`/`../work` 相対参照と整合）
- 起動フロー：セーブ無し時は `init()` が即 `startNewGame()` → `enterStage()` → `renderBoard()`。よって「`#board` の子要素 > 0」を起動成功の判定に使える
- 読み込み失敗時は `init().catch` が body を「読み込みエラー」表示に差し替える → これを negative assertion に利用

**▶ 次やること：**
- [ ] Phase 0-0 続き：移動・ステージ遷移・セーブ/ロードのスモークを追加（テスト用フック `window.__game` 公開を 0-1 と兼ねて検討）
- [ ] 余裕があれば主要画面の VRT（`toHaveScreenshot`）を1枚追加

---

### 2026-06-13 — 計画策定

**やったこと：**
- IDEA.md にゲーム拡充アイデア・ストーリー設定（星の欠片＋眠れる女王／ラスボス：ザーネル）をまとめた
- PLAN.md を作成（Phase 0〜8、各タスクに推奨モデルを記載）
- Phase 0 にテスト基盤（Playwright E2E/VRT）・決定論的ゲームループ・モジュール分割を整備
- 進捗管理の仕組みを整備：PROGRESS.md（本ファイル）/ DECISIONS.md（判断ログ）/ WORKFLOW.md（AI向け実行手順書）
- → 今後は「WORKFLOW.md に従って進めて」の一言で開発が回る運用にした

**学び・気づき：**
- game.js が 4098 行と肥大化 → 機能追加の前に分割が必須
- テストが 0 件 → 分割前に安全網（テスト基盤）が必要
- 実時間依存のループはテストしづらい → 決定論的ループ化が有効

**▶ 次やること：**
- [ ] Phase 0-0：Playwright を導入し、「ゲームが起動するか」の最小スモークテストを1本通す（🧠 設計はOpus）

---

## 進捗サマリ（PLAN.md のフェーズ別 完了状況）

| フェーズ | 状態 | メモ |
|---|---|---|
| Phase 0 技術基盤 | ✅ 完了 | 0-0〜0-6 全完了（テスト基盤・決定論ループ・game.js/sprites.js/editor.js/CSS分割・4エディタ・TS/ビルド判断）。全33テストグリーン |
| Phase 1 ストーリー基盤 | ✅ 完了 | 1-1〜1-5 全完了（名称変更・エンディング・老賢者NPC・終盤フロー分岐・祭壇/翼の羽衣・飛行/暗黒の塔/ザーネル）。終盤フローが一本に繋がった。全33テストグリーン |


| Phase 2 8ダンジョン | ✅ 完了 | 2-1〜2-4 全完了（8ダンジョン体制・ボス固有化・石碑&NPC噂話ヒント・フィールド接続仕上げ＋到達性回帰テスト）。全39テストグリーン |
| Phase 3 アクション深化 | ✅ 完了 | 3-1〜3-4 全完了。チャージ剣ビーム・大型ボス8種・弱点属性・ブーメランスタン。全53テストグリーン |
| Phase 4 サブアイテム | 🚧 進行中 | 4-1〜4-4 完了＋4-5 ①（武器スイッチ）完了。ギミックを**ボタン `BUTTON('S')`（踏む・モーメンタリ）／スイッチ `SWITCH('Y')`（矢・剣でトグル・ON で発光）**に呼び分け。状態 Set も分離しボタンのパズルは保全。全84テストグリーン。残：4-5 ②（ブーメランで炎/かがり火）・③（爆弾点火）・torchesLit トリガー |
| Phase 5 謎解き | 🚧 進行中 | 5-1 完了（色スイッチ・色ゲート。全95テストグリーン）。残：5-2（隠し通路）・5-3（敵パズル） |
| Phase 6 世界観・NPC | 🔲 未着手 | |
| Phase 7 成長システム | 🔲 未着手 | |
| Phase 8 やりこみ・UX | 🔲 未着手 | |

凡例：🔲 未着手 / 🚧 進行中 / ✅ 完了
